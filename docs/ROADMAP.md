# ROADMAP(自動ループのバックログ。上から順に1つずつ)

## 品質・基盤
- [x] 2026-09-16 **R79 公共施設が R35 の除外ルールを通過して観光候補に残る**: R40 の別府観察で、上位30件に「別府市総合体育館」「別府市公会堂」「野口病院管理棟」「別府市野口原総合運動場陸上競技場」「京都大学…地球熱学研究施設」「ビーコンプラザ」「トキハ別府店」「別府郵便電話局電話分室」「別府駅 (大分県)」の9件が混入(`dump-rank beppu` で実測)。原因は R35 の OSM 適用漏れではなく、**`TITLE_SUFFIX_NG` が末尾一致のみのため「病院管理棟」「〇〇大学…施設」「〇〇駅 (大分県)」のように語の後ろに何かが付くと素通りする**点で、落ちていない候補の多くは source=wiki。対処は (a)`TITLE_KEYWORD_NG`(部分一致)に体育館・公会堂・市民会館・庁舎・管理棟・事務所・研究所・競技場・運動場・分室 等を追加、(b)曖昧さ回避の括弧を剥がす正規化を `isExcludedName` 冒頭に追加、(c)`NAME_PROTECT_SUFFIX` に観光案内センター・ビジターセンター・タワー・展望・地獄・砂湯 等を先に追加して誤爆を防ぐ。**`センター` 単体・`店` 単体は禁止**(観光案内センター/〇〇本店を巻き込む)。4 fixture で before/after を比較し落ちた候補を全件目視、観光対象の誤爆0が条件。rank の重み・閾値・geo.js・fixtures は触らない。`check-engine.mjs` にケース追加。詳細は docs/NEXT.md
- [x] 2026-09-16 R80 **(最優先)** 構造化データによる除外判定への移行 — ただし **R79 が推奨した「OSM タグのホワイトリスト化」は計画役の実測で不要と判明**: (a)`fixtures/*.json` は `overpass.elements` に生タグを完全保持しており fixture 再生成は不要だが、(b)`geo.js:527 buildOverpassQuery()` の6 clause が既にホワイトリストそのもので `amenity=school|hospital|townhall`・`office=*`・`building=*` 単独の要素は応答に入ってこない、(c)4 fixture で `CATEGORY_RULES` にどれも当たらない名前付き OSM 候補は **全エリア0件**、(d)R79 で落ちた13件は全件 source=wiki。よって engine.js にホワイトリストを足しても差分0。振り替え後の本題は **wiki 単独候補の非対称の解消**で、`isExcludedArticle`(engine.js:427)が名前と冒頭文の語しか見ていない一方 OSM 側は Overpass で構造化判定済みという点。実装は「wiki のフィルタを `osmItems` 確定後に回し、除外語に当たった記事でも `isSamePlace` で OSM の観光タグ付き要素に一致するなら通す(タグを語より優先)」。rank の重み・閾値、fixtures の再生成、Overpass クエリは変更禁止。4エリアの dump-rank before/after を全件目視・誤爆0・check-engine にケース追加・check-all 25本全緑。09_研究ノート R79 段落の事実誤認も訂正する。詳細は docs/NEXT.md
- [ ] R2-1 検索候補のドロップダウンがエリアチップ行の上に重なって出る(チップ「草津」が候補の背後から半分はみ出して見える)。候補を開いている間はチップを隠す/暗くする等、どちらを見せるか方針を決めて直す。デザイン判断が要るので朝の相談向き

## 機能(計画書08 v3.1/v3.2)

## 研究(09ノート)— コードではなく文書

## 小さな改善候補(手が空いたとき・コスト0円)
- [x] 2026-09-16 R11 小地図の高さがmobileで固定値なので、密集時だけ少し高くする(または縦横比を調整して詰まりを緩和する)余地がないか検討(kusatsu/hakone/dogoの180px/220px比較の結果、採用。`.feedmap--tall`(220px)を`cards.length>=25`で付与)
- [x] 2026-09-16 R14 fixtures/hakone.json の軽量化: 904KB のうち提案生成に使っていない OSM タグ(建物属性・住所細目など)を落とす。**(2026-09-16 計画役が実測して着手可能と判断: (a)再生成は不要で、既存 JSON を加工する `scripts/slim-fixtures.mjs` を新設する方式にする。(b)geo.js を全部読み、消費されるタグキーは name/name:ja/tourism/historic/leisure/amenity/natural/man_made/wikidata/wikipedia*/website/contact:website/opening_hours の14種だけと確定したので、除去リストではなく安全側の keep-list で落とす。engine.js は el.tags を一切読まない。(c)hakone は全838KB中444KBがタグで、localwiki 46KB・name:en 22KB・addr:* 約47KB・source 8.8KB 等が丸ごと不要。keep-list 適用の実測は 900KB→586KB(−35%)、kusatsu 65→56KB・dogo 117→94KB・beppu 173→123KB。(d)fixtures は minify 済みなので `JSON.stringify(j)` で書くこと(pretty print すると逆に増える)。完了条件は4エリアの dump-rank 差分ゼロ。詳細は docs/NEXT.md)** 実施結果: `scripts/slim-fixtures.mjs` 新設・`make-fixture.mjs` に同じ keep-list を import して保存直前適用。実測 kusatsu 65.3→55.5KB / hakone 900.1→585.7KB / dogo 117.1→93.7KB / beppu 172.7→122.7KB、4エリアとも dump-rank 差分ゼロ確認済み。詳細は docs/FIXTURES.md。
- [x] 2026-09-16 R19 hakone fixture の far 集中の是正: 収集半径(make-fixture の 30km)と far 判定の閾値(車1時間以上)が噛み合っておらず、far が 30km 境界付近に不自然に固まっている。半径と閾値の整合を取り、`?fixture=hakone&demo=far` で分布が自然になることを dump-rank で確認する。rank の重みは触らない **(調査完了・閾値変更は見送り。R76 の4エリア実測で「far の件数は osmRadiusM としきい値30kmの大小関係だけで決まる」ことを確認したが、是正3案(FAR_DRIVE_MINをエリア別に下げる/osmRadiusMを揃える/far定義を相対基準にする)はいずれも副作用があり、どれを採るかはみのるんの判断が必要なため NIGHTLOG「朝の相談」に起票して今回は実装しない。詳細は09研究ノート「R76+R19 far の4エリア実測」節参照)**
- [x] 2026-09-16 R28 状態Aで宿ピンが0件のときの案内バナー: 地図を動かしても宿が1件も取れない範囲(山間部・海上など)では現在なにも出ず「壊れている?」と見える。`.mapnote` の既存の仕組みを流用して「この範囲には宿のデータがありません。エリアチップか検索から選べます。」を1行出す(混雑トーストとは文言を分ける)。撮影は `?demo=` に0件状態を再現するフラグを足して外部APIなしで行う。**(2026-09-16 計画役の調査: 案内文自体は既に app.js:396 `setMapNote(hotels.length ? '' : 'この範囲には宿が見つかりませんでした')` で実装済み。残作業は撮影用フラグ(`?demo=nohotels`)と `scripts/check-nohotels.mjs` による機械検査、および混雑トーストとの優先順位の確認のみ)**
- [x] 2026-09-16 R37 状態Bの小地図で宿ピン(♨)をタップすると宿の位置へ `panTo` する: 番号ピンは R10 でカード側から辿れるようになったが、宿ピン自体は押しても何も起きない。`renderFeedMap()` の宿マーカーに `click` ハンドラを足し、`feedMap.panTo([hotel.lat, hotel.lon])` するだけ(ズームは変えない)。入力ゼロ原則に反しない(タップ1回で戻るだけ)
- [x] 2026-09-16 R39 `?fixture=` のときヘッダーに小さな「固定データ」バッジ: 現在は見出し文字列に `(固定データ)` を括弧書きしているだけで、本番URLを見た人には何のことか分からない。`.topbar` に淡色の小さなバッジ要素を出し、見出しからは括弧書きを外す。表示のみでロジック変更なし
- [x] 2026-09-16 R40 fixture 4エリア目「別府」の追加: 草津(内陸・密集)・箱根(谷・分散)・道後(市街地隣接)に続く4つ目として、広域に温泉地が散る別府を追加する。`node scripts/make-fixture.mjs beppu`(Overpass 1回)。R19(far 分布)の材料にもなる
- [x] 2026-09-16 R42 カード要約の「…」で文の途中が切れる問題: `engine.js` の `truncate()` が 120字で機械的に切るため「山号は草津山。」の直後など、切れ目が語の途中に落ちることがある。上限の手前にある**最後の句点(。)で切る**(句点が上限の 60% より手前にしか無いときだけ従来どおり字数で切って「…」を付ける)方式に変える。rank の重み・閾値・除外ルールは触らない。`scripts/check-engine.mjs` にケースを追加し、3エリアの1位カードを撮影して切れ方を目視比較する
- [x] 2026-09-16 R43 状態Aの宿ピンに宿名のツールチップ(タップで表示): 状態Aの地図では宿ピンが♨アイコンだけで、押すまでどの宿か分からない。R13 で `aria-label` は付いているので、Leaflet の `bindTooltip`(ライブラリ追加なし)で宿名を出す。ホバーではなく**タップで開く**設定にして入力ゼロ原則を守る(タップ1回で名前が見えるだけ、宿の選択は従来どおり)。密集時にツールチップ同士が重ならないか `?demo=` で撮影確認する
- [x] 2026-09-16 R44 `?q=` ジャンプ後に宿ピンが0件なら自動でズームを1段引く: R28 で「この範囲には宿が見つかりませんでした」は出るようになったが、ユーザーは自分でピンチアウトする必要がある。`?q=` や検索でエリアへ飛んだ直後に限り、宿0件なら**1回だけ**ズームを1段下げて再取得する(無限に引かないよう1回で打ち止め、通常の地図ドラッグでは発動しない)。Overpass 呼び出しが1回増えるので、混雑(429/504)時は再取得せず現状のバナーのままにする
- [x] 2026-09-16 R45 fixture のヘッダーバッジに生成日時を表示: R39 で付いた「固定データ」バッジに、`json.meta` の生成日時(`generatedAt` が無ければ make-fixture.mjs の保存時に追加する)を `title` 属性かバッジ内の小さな文字で出す。いつ取ったデータを見ているのかが分かり、fixture の鮮度が古くなったことに気づける。表示のみでロジック変更なし・Overpass は叩かない
- [x] 2026-09-16 R46 `docs/check.mjs` にレスポンスサイズの記録を追加: R33 で応答時間は記録できるようになったが、バイト数は見ていない。各GETの `content-length`(無ければ本文長)をKB単位で行に併記し、末尾に合計KBと最大項目を出す。閾値での失敗判定は付けない(GitHub Actions のログに毎日の数字が残り、fixture 肥大やアセット追加で重くなった日が後から分かるのがゴール)。R14(fixture 軽量化)の効果測定の物差しにもなる
- [x] 2026-09-16 R47 状態Bのカード一覧の末尾に「この提案の作り方」1行を置く: 現在カードが30枚並んで終わるだけで、順位が何で決まっているのか(暫定の rank・Wikipedia/OSMの2ソース・カテゴリ多様性の減点)が一切書かれていない。「順位は試作中の指標で決めています。有名な場所が下に来ることがあります。」程度の正直な注記1行と、`docs/09_研究ノート` へのリンクを淡色で出す。表示のみ・ロジック変更なし。`?fixture=kusatsu` と `&embed=1` の両方で位置が崩れないか撮影で確認する(埋め込みでは出すか消すかを実装時に決めて NEXT.md に理由を書く)
- [x] 2026-09-16 R48 `?embed=1` で高さを親に通知する postMessage: 現在 `demo/hotel-page.html` の iframe は高さ固定で、中身が長いと iframe 内に二重スクロールが出る。埋め込み時だけ `ResizeObserver` で `document.body.scrollHeight` を監視し、変化したら `parent.postMessage({ type: 'yadotabi:height', height }, '*')` を送る(送信のみ・受信しない・他の情報は載せない)。受信側の数行のサンプルを `demo/hotel-page.html` に実装して、iframe が中身の高さに自動で伸びることを撮影で確認する。埋め込みでないときは一切送らない
- [x] 2026-09-16 R49 README に「判断待ちの設計課題」節を新設: NIGHTLOG の「朝の相談」4件(検索候補とエリアチップの重なり / カテゴリ多様性減点が有名どころを締め出す / 実APIとfixtureのWikipedia件数の食い違い / 小地図ピンのずらし幅)を、外から読んでも分かる形に書き直して README に公開する。「作りかけを隠さない」姿勢そのものが個人開発の見どころになる。文書のみ・コード変更なし
- [x] 2026-09-16 R50 fixtures の再生成手順を `docs/` に明文化: 現在 `scripts/make-fixture.mjs` の使い方がコード内コメントにしかなく、いつ・どの条件で再生成してよいか(Overpass は各エリア1回まで、429/504 が出たら日を改める、再生成後は `?fixture=<area>` の撮影とカード枚数の比較が必須)が残っていない。`docs/fixtures.md` を新設し、対象4エリア・座標・半径・generatedAt の意味・再生成の判断基準を表で書く。R14(軽量化)や R40(別府追加)の前提資料になる。文書のみ・コード変更なし
- [x] 2026-09-16 R51 カードのリンクチップの並び順を根拠のある順にする: 現在「Googleマップ / 公式 / Instagram / TikTok / YouTube」の固定順だが、公式サイトがあるスポットでは公式が先に来る方が役に立つ。`Googleマップ(行き方) → 公式 → SNS3種` の順に並べ替えるだけ(チップの見た目・リンク先・タップ領域44pxは変更しない)。`scripts/check-a11y.mjs` のタップ領域検査が引き続き全件OKであることと、`?fixture=kusatsu` 1位カードの撮影で折り返しが増えていないことを確認する
- [x] 2026-09-16 R52 README に `?fixture=` / `?demo=` / `?simulate=` / `?slow=` / `?embed=1` / `?perf=1` の**全パラメータ一覧表**を追加: 現在これらは app.js のコード内コメントと NIGHTLOG に散らばっており、外から読んだ人がどの画面を再現できるのか分からない。「パラメータ / 値の例 / 何が起きるか / 外部APIを叩くか」の4列の表にまとめ、`docs/FIXTURES.md`(R50)からもリンクする。app.js を grep して**実在するものだけ**書く(実装されていないものを書かない)。文書のみ・コード変更なし
- [x] 2026-09-16 R53 `demo/hotel-page.html` に「埋め込みコードのコピー説明」を1行足す: 現在 `<pre class="tag-example">` にタグは載っているが、コピーして貼る手順(width/height の目安、`?hotel=<lat>,<lon>,<宿名>` の宿名部分は URL エンコードが要ること、R48 の受信スクリプトの要否)が書かれていない。営業資料として読める2〜3行を `.sales-notes` に追記する。表示のみ・やどたび本体は無変更
- [x] 2026-09-16 R54 状態Bのカードに「宿から◯km」表記を足すか検討: 現在カードには「徒歩10分 · 車2分」しか出ておらず、距離そのものが見えない。既に `engine.js` が距離を持っているので**表示だけ**で足せる。ただし1行が長くなって mobile で折り返す恐れがあるので、`?fixture=kusatsu`/`hakone`/`dogo` の mobile 撮影で折り返しが増えないことを先に確認し、増えるなら「徒歩/車のどちらか片方を落として距離に差し替える」案も撮り比べて NIGHTLOG に理由を残す。rank・engine のロジックは触らない
- [x] 2026-09-16 R56 リンクチップの見た目調整(mobile 375px で5個を1行に収める): R51 の撮り比べで「ラベル短縮・順序変更では 4個+1個の折り返しが解消せず、チップの**合計幅**が主因」と判明した。残る手段は幅そのものを詰めることなので、`style.css:430` の `.feedcard__link` の `padding: 5px 10px` と `font-size: var(--fs-xs)`(12px)、`.feedcard__links` の `gap: 10px 8px` を1段小さくする3案(A:paddingのみ / B:padding+font-size 11px / C:横gapと横paddingのみ)を撮り比べ、1行に収まる中で最も文字が大きい案を採る。**SNS のラベル文字列は変更不可**(`scripts/check-passive.mjs:94` が `hasText: 'Instagram'` に依存)。`.feedcard__link::after { height:44px }`(R13 のタップ領域)は維持する。完了条件は 375px で全 `.feedcard__link` の `offsetTop` が同値・check-a11y 全OK・check-all 18本全緑。詳細は docs/NEXT.md
- [x] 2026-09-16 R55 `scripts/check-all.mjs` の実行時間短縮: 現在17本で約1分、うち Playwright を使う本が毎回ブラウザを起動し直している(サーバは同一ポート3000のため共有不可)。各 check 本の中身は編集しない制約があるので、**`check-all.mjs` 側で `PLAYWRIGHT_*` 系の環境変数や `--only <名前>` 引数(既定は全本)を足す**方向で、開発中に一部だけ回せるようにする。まず各本の所要msを表から読んで**遅い上位3本を特定し NIGHTLOG に記録する**ところまでを1サイクルの成果とする(最適化は測ってから)
- [x] 2026-09-16 R57 カード画像の `alt` をスポット名にする: 現在カード画像の `alt` が空(または汎用)で、読み上げ・画像読み込み失敗時に何の写真か分からない。`app.js` の `feedCardHtml()` の `<img>` に `alt="<スポット名>の写真"` を入れるだけ。`check-a11y.mjs` に「全 `.feedcard__media img` の alt が空でない」検査を足して機械確認する。表示は変わらないので撮影は `?fixture=kusatsu` mobile 1枚でデグレ確認のみ
- [x] 2026-09-16 R58 状態Bでブラウザの戻るでも状態Aに戻れるようにする: 現在は画面内の「←」ボタンでしか戻れず、スマホで端末の戻るを押すとサイト自体を離脱してしまう。状態Bへ遷移するときに `history.pushState` し、`popstate` で状態Aへ戻す。`?hotel=` で直接開いた場合(履歴に状態Aが無い)は従来どおり離脱でよい。二重 push しないこと。Playwright の `page.goBack()` で機械検査する
- [x] 2026-09-16 R59 検索欄にクリアボタン(×)を足す: 入力後に全消しするのが面倒で、候補を閉じる手段も曖昧。入力が1文字以上のときだけ右端に `×` を出し、押したら値を空にして候補を閉じフォーカスを戻す。タップ領域44pxを満たすこと(`check-a11y.mjs` の対象に追加)。入力ゼロ原則には反しない(入力を**減らす**方向の改善)
- [x] 2026-09-16 R60 「もっと見る」展開後に31〜60番の小地図ピンが増えない件の注記: R16 で31件目以降をカードに出せるようになったが、小地図のピンは1〜30のままで、展開後のカードの番号バッジを押しても光るピンが無い。ピンを足すと密集分離(`nudgeOverlaps`)が壊れる恐れがあるので、まずは**現状を明記する**方向で対処する(展開ボタン押下後に「31番以降は地図に出していません」の淡色1行を `#feed-note` の近くに出す、番号バッジのタップ対象から31以降を外す等)。ピンを実際に足すかは別タスクとして切り出す
- [x] 2026-09-16 R61 状態Aの地図の初期位置を「最近見た宿」があればそこにする: 現在は毎回同じ既定位置(または現在地なし)から始まるので、一度使った人がまた同じ操作をやり直すことになる。`localStorage` の受動ログ/最近の宿(`yado.passive.v1` 系)に宿の座標があれば、状態Aの `map.setView()` の初期中心をその宿にする(ズームは既定のまま)。`?hotel=`/`?q=`/`?fixture=` が指定されているときは従来優先。入力ゼロ原則に反しない(勝手に前回の続きから始まるだけ)。`?demo=recent` で機械検査できるようにする
- [x] 2026-09-16 R62 カードの写真が縦長のときの見切れ確認: `.feedcard__media` は固定の高さ/比率で切っているはずなので、縦長写真(人物・滝・五重塔など)で被写体の頭が切れていないかを3エリアの上位30枚で目視する。`object-position` を `center` から `top` 寄りにするだけで改善するなら変更し、変わらないなら「確認した」事実だけ NIGHTLOG に残して閉じる。撮影は fixture のみ・外部API 0回。engine/geo は触らない
- [x] 2026-09-16 R63 状態Aの下に「サンプルを見る」デモ導線を置く: 初見の人は宿を選ばないと何も起きないので、`?fixture=kusatsu|hakone|dogo` を小さなチップ3つ(「草津の例」「箱根の例」「道後の例」)としてエリアチップ行の下に淡色で出す。押すと固定データの状態Bへ直行して「このサイトは何を見せるのか」が1タップで分かる。埋め込み(`?embed=1`)と `?fixture=` 中は出さない。表示のみ・ロジック変更なし。`scripts/check-a11y.mjs` のタップ領域44px対象に追加する
- [ ] R64 `scripts/check-all.mjs` を GitHub Actions でも回す: 現在 Actions は `docs/check.mjs`(本番URLへのGETのみ)しか動かしておらず、ロジック検査20本はローカル限定。Playwright が要るので `.github/workflows/check.yml` とは別ワークフローで `npx playwright install --with-deps chromium` を入れて回す。**先に無料枠内に収まるか確認すること**(publicリポジトリなら Actions は無制限のはずだが、実際の設定を確認してから。有料化の恐れがあるなら実装せず NIGHTLOG の「朝の相談」に回す)。check 本体は無編集、Playwright の import パス(`C:\workspace\tools\shot\node_modules` 直書き)が Linux で解決できないため、環境変数でパスを切り替える小改修が各本に要る点も見積もること
- [x] 2026-09-16 R65 状態Bから「戻る」で状態Aに戻ったとき、直前の地図の中心とズームを厳密に復元する: 現在は `yado.mapview.v3` の保存タイミング(`saveMapView()` は `moveend` 経由のみ)次第で、宿を選ぶ直前の位置と微妙にずれる可能性がある。`selectHotel()` の時点で明示的に `saveMapView()` を呼び、`goBack()` で `map.setView()` し直す。Playwright で「地図を動かす→宿を選ぶ→戻る」の前後で center/zoom が一致することを機械検査する(外部API 0回・`?demo=` 系フラグで宿取得は止める)。engine/geo/fixtures は触らない。**(2026-09-16 計画役が Playwright で実測: 状態Aの地図インスタンスは破棄されず `goBack()` も地図に触らないため、同一セッション内の「戻る」では center/zoom は完全一致しズレない。`goBack()` への `setView()` 追加は不要かつ有害(flyTo 飛行中の選択で飛行先に着地できなくなる)。真のバグは `saveMapView()` が `moveend` の debounce(250ms)経由でしか呼ばれず、ドラッグ直後にピンをタップすると `yado.mapview.v3` が古いまま残り、**次回訪問・リロード時の初期位置**がずれる点のみ。対処は `selectHotel()` に `saveMapView()` を1回足すだけ。詳細と回帰テストの作り方は docs/NEXT.md)** — **同一セッションの戻りではズレないことを実測で確認。真因は selectHotel 時の未保存で、次回訪問時の初期位置に影響していた**
- [x] 2026-09-16 R66 カードの写真タップで大きく表示(ライトボックス): 現在カードの写真は小さく切り抜かれており、縦長写真(R62)でも全体像が分からない。写真をタップしたら全画面の暗幕に元の比率で1枚表示し、どこを押しても閉じる簡易ライトボックス(ライブラリ追加なし・CSSとJSで数十行)。閲覧のみで入力ゼロ原則に反しない。`Esc` と端末の戻るでも閉じられること、`prefers-reduced-motion` でフェードを無効にすること、暗幕の下のページがスクロールしないこと(`body` 固定)を条件にする。`?fixture=kusatsu` mobile/desktop の撮影で確認し、タップ領域44pxは `check-a11y.mjs` の対象に追加する
- [x] 2026-09-16 R67 `scripts/check-all.mjs` の失敗時に失敗画面を `screenshots/` に残す: 現在1本でも FAIL すると exit 1 になるだけで、何が崩れていたのかは再実行しないと分からない。Playwright を使う本が落ちたときにその場のスクリーンショットを `screenshots/fail_<本名>_<日時>.png` に保存する。**各 check 本の中身は編集しない制約があるので**、まず環境変数(例 `YADO_SHOT_ON_FAIL=1`)を `check-all.mjs` から渡す方式が各本の無編集で実現できるかを調べ、できないなら「失敗した本の名前と再現コマンドを `check-all.mjs` の末尾にまとめて出す」だけに留める(調査結果を NIGHTLOG に残すところまでを成果とする)
- [x] 2026-09-16 R68 `?embed=1` に背景色パラメータ `&bg=` を足す: 埋め込み先の予約サイトの地色が白以外だと、iframe の白背景だけが浮いて見える。`&bg=<6桁または3桁のHEX>`(`#` なし・`/^[0-9a-fA-F]{3}|[0-9a-fA-F]{6}$/` で厳格に検証し、不正なら黙って無視)を読んで、埋め込み時だけ `body` の背景色に適用する。文字色は変えない(コントラスト事故を避けるため、暗色指定は今回の対象外とし NIGHTLOG に理由を残す)。`demo/hotel-page.html` の iframe URL にも1例足して撮影で確認する。`?embed=1` 以外では一切効かないこと
- [x] 2026-09-16 R69 キーボードだけでカードを辿れるようにする: 現在マウス/タップ前提で、Tab キーでカードのリンクチップに順に到達できるか・「もっと見る」に Enter で届くかを未確認。Playwright の `page.keyboard.press('Tab')` を繰り返して `document.activeElement` の遷移順を記録し、(a) 1枚目のカードのリンクチップ群 → 2枚目 → … と自然な順に進むか (b) 番号バッジがフォーカス可能か (c) 「もっと見る」に Enter で展開できるか を機械検査する。壊れている点だけを `tabindex` を増やさない範囲(DOM 順の見直し・`button`/`a` の使い分け)で直す。フォーカスリングは R13 で既に強調済みなので触らない。`scripts/check-keyboard.mjs` 新規
- [x] 2026-09-16 R70 `?fixture=` に「ランダム」を足す: `?fixture=random` と指定されたら kusatsu/hakone/dogo(R40 完了後は beppu も)から1つを実行時に選んで読み込む。R63 で状態Aに置いたサンプル導線チップにも「おまかせ」を1つ足し、押すたびに違うエリアの例が出るようにする(初見の人が何度か押して「土地によって出るものが違う」ことに気づけるのが狙い)。選択は `Math.random()` の1行・外部API 0回・入力ゼロ原則に反しない(タップ1回)。不正値や対象0件のときは従来どおり黙って通常動作にフォールバックする。`scripts/check-sample.mjs` に「random 指定で3エリアのいずれかのラベルが出る」検査を追加
- [x] 2026-09-16 R71 「写真が無いカード」の割合を README に記録: 現在カードの何割が Wikipedia 画像を持たず絵文字プレースホルダになっているのかが誰にも見えていない。`scripts/dump-rank.mjs` の仕組みを流用した使い捨て計測で、3エリア(R40 後は4エリア)それぞれの上位30件・上位60件について `imageUrl` の有無を数え、README の「判断待ちの設計課題」節の近くに「草津 30件中◯件に写真あり」の小さな表として載せる。文書のみ・コード変更なし・外部API 0回。R23(画像失敗フォールバック)や R66(ライトボックス)が実際どれだけの割合のカードで効くのかの物差しになる
- [x] 2026-09-16 R74 サンプル導線チップが増えたときの折り返し確認と横スクロール化: R40 で `SAMPLE_LINKS` が4件+「おまかせ」の5本になると、375px 幅で `.samples` が1行に収まらず改行して他要素に被る恐れがある。`?demo=zoomout` mobile で実測し、崩れていれば `.chips` と同じ横スクロール(`overflow-x:auto` + 右端 `mask-image` フェード)に寄せる。R40 の撮影で問題が出なかった場合は「確認した」事実だけ NIGHTLOG に残して閉じる。`scripts/check-a11y.mjs` のタップ領域44px検査が引き続き全件OKであることが条件。style.css のみの変更・ロジック無変更
- [x] 2026-09-16 R75 `fixtures/*.json` のサイズを README の開発者向け節に表として記録: R46 で `docs/check.mjs` がKB列を出すようになったが、4エリア分の数字が README には無く「なぜ hakone だけ 900KB なのか(収集半径30km)」が外から分からない。`ls` と check.mjs の出力から「エリア / osmRadiusM / elements件数 / wiki件数 / ファイルKB」の5列表を作った。R14 は既に完了しているため、**after 値(現行値)を主として書き、before 値は括弧で併記**した(ROADMAPの旧文言「R14 の before 値として残す」は事実誤認だったため訂正)。文書のみ・コード変更なし・外部API 0回
- [x] 2026-09-16 R76 4エリアの far 件数を並べて R19 の判断材料を作る: 現在 far(車1時間以上)は草津0件・道後0件・箱根10件と分かっているが、`?fixture=<area>&demo=far` を4エリアで開いて件数と最遠距離を1つの表にまとめた記録が無い。`scripts/dump-rank.mjs` の far 出力を4エリア分集め、docs/FIXTURES.md か 09研究ノートに表として貼る。「far は収集半径の関数でしかない(半径15kmでは構造上0件)」という仮説が4エリアで成り立つかを確かめるのが目的。R19 の実装はこの表を見てから決める。文書のみ・コード変更なし・外部API 0回(実測結果は docs/FIXTURES.md「far 実測表」と09研究ノートに記録。仮説は4エリアとも成立)
- [x] 2026-09-16 (R77: 不採用・理由: 「残り30件」は既に `moreHtml()` が出しており30+30=全60は自明。正確な総数は `present()` の戻り値改修=engine変更が要るため出せず不正確な数を出すのはプロジェクトの正直さ方針に反する。`#feed-note` の行数増で375pxが間延びする) R77 状態Bのカードに連番だけでなく「何件中」を出すか検討: 現在カードの番号バッジは `1` `2` … だけで、全部で何件あるのか(30件+もっと見るで60件)がユーザーに見えていない。`#feed-note` 付近に「上位30件を表示中(全◯件)」の淡色1行を出す案と、出さない案(情報量を増やさない方針)を `?fixture=kusatsu` mobile で撮り比べ、どちらを採るか理由とあわせて NIGHTLOG に残す。表示のみ・engine/rank は触らない
- [x] 2026-09-16 R78 `docs/NEXT.md` の履歴が残っていない問題: 毎サイクル上書きしているため、過去にどんな指示で何が実装されたかが辿りにくいという課題があったが、`git log --oneline -- docs/NEXT.md` で全68コミットが既に追え、`git show <hash>:docs/NEXT.md` で当時の全文が読めることを確認した。コピーを置くと同じ内容が2箇所に増え運用事故の元になるため、**`docs/next-archive/` は作らない**と決定し、その旨を `docs/AUTOPILOT.md` に明記した。文書・運用のみ・コード変更なし
- [x] 2026-09-16 R72 `check-all.mjs` を並列化できない理由を docs に明文化: 現在は 23本ではなく **25本**を直列で回して約4分かかっており(旧ROADMAP文言「23本」は事実誤認だったため訂正)、「なぜ速くしないのか」がコード内コメントにしか無かった。実際は「7本以上」ではなく **21本**が各自ポート3000に自前サーバを立てて finally で落とすため同時実行すると EADDRINUSE で落ちる、という構造上の制約がある(旧ROADMAP文言「7本以上」も訂正。サーバを立てる21本は Playwright を使う21本と完全一致)。`docs/CHECKS.md` を新設し、25本それぞれの「何を検査するか・サーバを立てるか」の表と、並列化する場合に必要になる改修(各本のポート番号を環境変数化する等。今回はやらない)を書いた。R55 で測った所要msも転記した。文書のみ・コード変更なし
- [x] 2026-09-16 R73 README の日本語を初心者にも読める語に整える: 現在 README に「geosearch」「fixture」「bbox」「attribution」「デグレ」等が説明なしで出てきて、開発者以外が読むと意味が取れない。初出の箇所に短い言い換えを括弧で添える(例「fixture(固定データ。外部の無料APIを叩かずに同じ画面を再現するための保存済み応答)」)。**用語そのものは置き換えない**(パラメータ名やファイル名と対応が取れなくなるため、あくまで補足を足すだけ)。文書のみ・コード変更なし
- [ ] R81 5エリア目「城崎温泉」の fixture 追加: 兵庫県の城崎(35.6262,134.8055)は温泉街が円山川沿いの1本道に細長く並び、草津(密集)・箱根(広域分散)・道後(市街地隣接)・別府(散在)のどれとも違う「線状」の地形。`node scripts/make-fixture.mjs kinosaki`(Overpass 1回・osmRadiusM=15000)。R80 の救済機構が4エリアでは差分0だったので、観測できるエリアを増やす材料にもなる。`docs/check.mjs` の TARGETS、`app.js` の SAMPLE_LINKS、`check-sample.mjs` の件数アサーション(5本→6本)、README・docs/FIXTURES.md の表(R75 のサイズ表を含む)を全て更新すること
- [x] 2026-09-16 (既に実装済みで、今回足したのは検査だけ) R82 状態Bの「戻る」ボタンに `aria-label` を足す: R13 で地図ピンとチップには `aria-label` が付いたが、`.topbar` の「←」ボタンは文字が矢印1字のみで、読み上げでは「左向き矢印」としか読まれない。`aria-label="地図に戻る"` を1つ足すだけ。`scripts/check-a11y.mjs` に「戻るボタンの aria-label が空でない」検査を追加して機械確認する。表示は変わらないのでデグレ確認1枚のみ
- [x] 2026-09-16 R83 カード要約が無いときの代替文を正直な1行にする: 現在 Wikipedia 記事が紐づかない OSM 単独候補は要約行が空欄のまま詰まって見える。「Wikipediaに記事がありません(地図の情報だけで表示しています)」等の淡色1行を出すか、空欄のままにするかを `?fixture=beppu` mobile で撮り比べる(beppu は OSM 単独候補が多い)。件数は `scripts/dump-rank.mjs` で先に数えて NEXT.md に書くこと。表示のみ・engine/rank は触らない
- [x] 2026-09-16 R84 `?fixture=` 時にランキングの根拠を出す開発者向けフラグ `?debug=1`: 現在カードが何を根拠に何位なのか(source が osm / wiki / both のどれか、距離、カテゴリ多様性の減点後スコア)は `scripts/dump-rank.mjs` を回さないと分からない。`?fixture=` と併用したときだけカードの隅に `osm · 12.3` のような淡色の極小文字を出す。**`?fixture=` が無いときは一切効かないこと**(本番URLで一般の人に見えてはいけない)。`scripts/check-debugflag.mjs` を新設して「fixture 無しでは `.dbg` が0件」を機械検査する
- [x] 2026-09-16 R86 「もっと見る」展開時のスクロール位置維持の確認: 現在 `renderFeed()` が `els.feedList.innerHTML` を丸ごと書き換えているため、「もっと見る」を押した瞬間に読んでいた位置(30枚目付近)から先頭へ飛んでしまう可能性を懸念していたが、**計画役がPlaywrightで実測した結果、実装不要と判明**(mobile 375x812・desktop 1280x900 とも押す前後で scrollY の差0px。理由: innerHTML 差し替えは既存30枚より下にのみ追記するため上部の総高さが不変、「もっと見る」ボタンは feedList 外の別コンテナ #feed-more にあり、ハンドラも renderFeed() を呼ぶだけでスクロール操作を一切行わない)。コード変更なし
- [x] 2026-09-16 R87 状態Bのスケルトン(読み込み中の骨組み)の見た目確認: `renderFeed()` の `loading` 分岐で `skeletonHtml()` が実カードの後ろに積まれるが、この状態を撮影した記録が無かったため、`?fixture=kusatsu&slow=osm3000,wiki9000` を mobile/desktop で撮影し目視した。(a)骨組み高さ312.3px・実カード高さ370.7px(差約58px)で、カード間の白背景の余白により境目が自然に区切られガタつきは軽微(許容範囲)。(b)骨組み(灰色グラデーション)と実カード(白背景+本文)の境目は明確で誤認の恐れなし。(c)prefers-reduced-motion: reduce で getComputedStyle(el).animationName が none になることをPlaywrightで実測、既存のシマー停止実装(style.css:545)は正しく機能している。3点とも崩れなしと判断し、**style.css は無変更**(git diff --stat -- assets 空)。詳細は docs/NEXT.md
- [x] 2026-09-16 R88 埋め込み時の高さ通知の上限値の見直し(R48 の暫定100000px): 計画役が4エリア×2幅×2状態(30枚/60枚展開後)を実測した結果、最大値は kusatsu desktop 60枚展開後の 34075px(ROADMAP旧文言の「例20000px」は事実誤認・R48当時と同じ罠で正規の伸長を弾いてしまう値だった)。実測最大の約1.8倍にあたる **60000px** を新しい上限として採用し、`demo/hotel-page.html` の2箇所(表示用`<pre>`コピーと実スクリプト)を揃えて `100000→60000` に変更、根拠コメントを追加。送信側の `assets/app.js` は無変更(`git diff --stat -- assets` 空)。`node scripts/check-embedheight.mjs` 8件PASS・`node scripts/check-all.mjs` 27本全緑を確認済み
- [x] 2026-09-16 R89 `check-all.mjs` の所要時間短縮 第1歩(重い hotelparam の分割): R55 の実測で `check-hotelparam.mjs` が単独27秒と最遅で、27本合計約4分の1割強を占めている。中身を読んで「何をN回繰り返しているのか」を特定し、(a)同じ context を使い回せる箇所があるか (b)`waitForTimeout` の固定待ちを `waitForSelector` に置き換えられるか を調べる。**既存 check 本の検査内容は減らさない**(速くするために検査を削らない)。5秒でも縮めば成果、縮まないなら「なぜ縮まないか」を `docs/CHECKS.md` に1段落書いて閉じる。計測は同条件で3回取って中央値を使う。実測: 変更前中央値33553ms→変更後中央値8428ms(約75%短縮、41 pass/0 fail一致・5連続無フレーク)
- [x] 2026-09-16 (既に実装済みと実測で確認) R90 `demo/hotel-page.html` の iframe に `loading="lazy"`: `demo/hotel-page.html:213` の iframe タグに既に `loading="lazy"` 属性が付いていることを実測で確認した。コード変更なし
- [ ] R85 `demo/hotel-page.html` の英語版 `demo/hotel-page-en.html`: README には既に英語1段落(R21)があるが、営業用デモページは日本語のみ。海外の宿泊施設・OTA に見せる想定で、同じレイアウトのまま文言だけ英訳した1枚を作る(iframe の中身=やどたび本体は日本語のままでよい。「この埋め込みは日本語UIです」と1行断る)。`docs/check.mjs` の HTML_PAGES と リンク切れ検査の対象に追加する。やどたび本体は無変更
- [x] 2026-09-16 R41 README の「仕組み」節を現状に合わせて更新: 一晩の改修で段階描画・重複マージ・除外ルール・同心円 geosearch・fixture 3エリア・check-all.mjs などが増えたが README の説明が初期のままの箇所がある。コード変更なし・文書のみ
- [x] 2026-09-16 R91 README の `?demo=` 値一覧が実装と2件ズレている: `README.md:120` は「10個すべて」と書き、表に10行しか無いが、`assets/app.js:1453-1490` の実装は **12値**ある。表に無いのは (a)`initpos`(`app.js:1454`・`app.js:1455` で `demoNoSaveView` と `demoStateA` を立てる。`scripts/check-initpos.mjs` が使用中)と (b)`portrait`(`app.js:147` `demoPortrait`・`app.js:797` で400x800の縦長ダミー画像に差し替え、R62 の縦長写真見切れ確認用)の2つ。表に2行足し、本文の「10個すべて」を「12個すべて」に直す。**app.js を grep して実在するものだけ書く**(R52 と同じ方針)。ついでに `?demo=` の各値が現在どの `scripts/check-*.mjs` から使われているかを表の3列目に足すと、値を消してよいか判断できるようになる。文書のみ・コード変更なし・外部API 0回
- [ ] R92 `screenshots/` が 512枚・607MB まで肥大している: `.gitignore:5` で `screenshots/` 全体が除外されているためリポジトリは汚れていない(`git ls-files screenshots/` は0件)が、ローカルのディスクを毎サイクル数十MBずつ食い続けており、古い撮影がどのサイクルのものか判別もできない。`docs/` に「撮影ファイルの命名と保持方針」を明文化する(現在の命名は `<ISO日時>_<ラベル>_<mobile|desktop>.png` で既に統一されている)。そのうえで **削除は自動化しない**(消す判断はみのるんのもの)。代わりに `scripts/` に**一覧を出すだけ**の使い捨てではないコマンド(枚数・合計MB・最も古い/新しい日付・ラベル別の枚数上位10件を表示、削除機能なし)を足すか、`docs/CHECKS.md` に手動での消し方の1行を書くかを実装時に選び、理由を NIGHTLOG に残す。`.gitignore` は変更しない。外部API 0回
- [x] 2026-09-16 R93 `docs/check.mjs` のリンク切れ検査が `demo/hotel-page.html` の iframe の**クエリ文字列付きURL**を検査できているか確認する: `demo/hotel-page.html:213` の iframe は `loading="lazy"` 付きで本番URL(`?hotel=...&embed=1&bg=fff7e6`)を指しているが、R22 のリンク切れ検査は「相対パス参照のみ・外部ドメインは対象外」と明記されており、この iframe が実際に検査対象に入っているのかコードを読まないと分からない。`docs/check.mjs` を読んで (a)入っているなら「入っている」事実を `docs/CHECKS.md` に1行残して閉じる (b)入っていないなら、**本番と同一オリジンなので叩いても外部APIではない**ため、iframe src のパス部分(`/yadotabi/index.html`)だけを200判定に足す(クエリは落とす。`?hotel=` を付けて叩くと Overpass を誘発しかねないため**クエリ付きでは叩かない**こと)。判断の根拠を NIGHTLOG に残す。やどたび本体は無変更(実測で既に検査対象と判明・対処不要。pathnameでクエリが落ちるためindex.htmlとしてHEADされる。CHECKS.mdに記録)
- [x] 2026-09-16 R96 ライトボックスを開いている間に Tab でうしろのページへフォーカスが抜ける(検査の穴): 実測で `assets/app.js:917` は開いた直後に `.lightbox__close` へフォーカスを移し、`app.js:920` の `keydown` ハンドラは `Escape` **だけ**を見ており、**Tab のフォーカストラップが無い**。暗幕が出ている間に Tab を押すと、視覚的に隠れている背後のカードのリンクチップ(1枚あたり5本)へフォーカスが進んでしまい、キーボード利用者は「今どこにいるか見えない」状態になる。`scripts/check-lightbox.mjs` を grep したところ `Tab`/`focus`/`activeElement` の語が**1件も無く**、R66 で足した検査はキーボード挙動を一切見ていない。対処は `lightboxKeyHandler`(`app.js:920`)に `e.key === 'Tab'` の分岐を足し、フォーカス可能要素が閉じるボタン1つだけなので `e.preventDefault()` して閉じるボタンに留める数行のみ(`tabindex` は増やさない)。`scripts/check-lightbox.mjs` に「開いた状態で Tab を5回押しても `document.activeElement` が `.lightbox__close` のまま」の検査を足す。`Escape` と暗幕クリックで閉じる既存挙動・`closeLightbox()` の復帰フォーカス(`app.js:938`)は変更しない。撮影は `?fixture=kusatsu` mobile のデグレ確認1枚。外部API 0回・engine/geo/fixtures 不可・rank 不可
- [x] 2026-09-16 R97 印刷・縮小表示のCSSが存在しない: `grep -rn "@media print" assets/` の実測が**0件**で、状態Bのカード一覧を印刷(またはブラウザのPDF保存)すると、`position: fixed` のトップバー・Leaflet の小地図タイル・暗幕用スタイルがそのまま紙に出る恐れがある。宿の人が「周辺スポット一覧」を紙で客に渡す使い方は営業上ありえるのに未確認。まず**現状を撮る**ところまでを成果とし、崩れていれば `assets/style.css` の末尾に `@media print` を1ブロックだけ足す(地図 `.feedmap` を `display:none`、`.topbar` の固定解除、リンクチップの枠線を薄く、背景色の強制印刷はしない)。判断が要るのは「地図を消すか残すか」だけなので、両方の出力を比べて理由を NIGHTLOG に残す。**画面表示用のCSSは1行も変えないこと**(`@media print` の中だけに閉じる)。`node scripts/check-all.mjs` 27本全緑が条件。外部API 0回・engine/geo/fixtures 不可・rank 不可
- [x] 2026-09-16 R98 埋め込み時に親ページのCSSが `iframe` 越しに干渉しないかの確認と、`sandbox`/`referrerpolicy` の明文化。**訂正**: 本文は当初「`sandbox` は足さない方向で理由だけ残す」としていたが、これは誤りだった。計画役がPlaywrightで実測した結果、`sandbox="allow-scripts allow-same-origin allow-popups allow-popups-to-escape-sandbox"` + `referrerpolicy="no-referrer"` の組み合わせなら無傷で足せることが判明し、`demo/hotel-page.html` の実iframe・`<pre>` タグ例・`README.md` のタグ例の3箇所に反映した。詳細な実測値・理由は `docs/NEXT.md`(R98当時の全文は `git show`)を参照。
- [x] 2026-09-16 R94 状態Aの案内文が3種類あり文言の粒度が揃っていない: 実測で `app.js:407` と `app.js:444` が同一の「この範囲には宿が見つかりませんでした」、`app.js:456` が「宿ピンの取得が混雑中です。検索やエリアチップから選べます。」、`app.js:616` の検索候補0件が「見つかりませんでした」+「別の名前で探してみてください」。**1つ目だけ次にどうすればよいかの案内が無い**(2つ目・3つ目にはある)。R28 で足した文言なので、「この範囲には宿のデータがありません。エリアチップか検索から選べます。」のように次の行動を添える形へ揃える。ただし `app.js:444` は自動ズームアウト(R44)の直後にも出るため、2箇所で同じ文言でよいかを `?demo=nohotels` と `?demo=autozoom` の撮影で確かめてから決める。`scripts/check-nohotels.mjs` が文言に依存していないか**先に grep する**こと(依存していれば検査側も同時に直す)。表示のみ・ロジック変更なし・外部API 0回
- [x] 2026-09-16 R99 `?hotel=` の不正値が黙って通り、地球上に無い座標で状態Bが開く: 実測で `assets/app.js:1330 hotelFromUrl()` は `parseFloat` と `isFinite` しか見ておらず(`app.js:1336-1337`)、**緯度経度の範囲検査が無い**。`?hotel=999,999,テスト` のような値でも `isFinite` を通って `{ id:'url/999,999', ... }` が返り、Leaflet が範囲外の座標で状態Bを開く(提案は当然0件になり、ユーザーには理由が分からない)。対処は `app.js:1337` の `isFinite` 判定に `lat >= -90 && lat <= 90 && lon >= -180 && lon <= 180` を足して**範囲外なら null を返す**(= `?hotel=` 無しと同じ状態Aへ黙ってフォールバックする既存経路に乗る)だけ。`?bg=` が `app.js:1389` で厳格に検証して不正なら黙って無視しているのと同じ方針に揃える。名前部分(`app.js:1338` の `parts.slice(2).join(',')`)は現状どおり何でも受ける(名前は表示するだけで害が無く、空なら `この宿の周辺` にフォールバック済み)。`scripts/check-hotelparam.mjs` に不正値3種(範囲外lat・範囲外lon・NaN)で状態Aのままになる検査を足す。表示のみ・engine/geo/fixtures 不可・rank 不可・外部API 0回
- [x] 2026-09-16 R100 非常に長いスポット名がカード見出しで折り返し放題になっている: 実測で `assets/style.css:450 .feedcard__name` には `overflow-wrap` も `word-break` も**無く**、同じファイル内の `.suggest__name`(`style.css:135-137`)と `.topbar__title`(`style.css:330-336`)は `overflow:hidden; text-overflow:ellipsis; white-space:nowrap` で1行に収めているのに、カード見出しだけ方針が揃っていない。実在する長名の例として fixture には「別府市野口原総合運動場陸上競技場」(R79 で除外済み)級の名前が入りうるので、まず `node scripts/dump-rank.mjs` を4エリアで回して**上位60件の最長スポット名の文字数を実測して NEXT.md に書く**こと。そのうえで (a)カード見出しは省略せず折り返してよい(情報を隠さない方針)ので `overflow-wrap: anywhere` を足して英数字の長い連続でもはみ出さないようにするか、(b)実測で3行を超えないなら「確認した」事実だけ NIGHTLOG に残して閉じるか、を375px の撮影で決める。`.feedcard__times`(`style.css:466`)の `white-space: nowrap` は R54 の判断なので変更しない。style.css のみ・ロジック無変更・engine/geo/fixtures 不可・rank 不可・外部API 0回
- [x] 2026-09-16 R101 地図タイルが読めないときに画面が灰色一色になり理由が出ない: 実測で `assets/app.js:372`(状態A)と `app.js:1146`(状態Bの小地図)はどちらも `L.tileLayer(TILE_URL, { maxZoom:19, attribution: TILE_ATTR })` だけで、**Leaflet の `tileerror` イベントを一切購読していない**(`grep -n "tileerror" assets/` が0件)。OSM タイルサーバが落ちている・社内プロキシで弾かれている等のとき、ユーザーには灰色の矩形とピンだけが見え「壊れた」としか分からない。対処は状態Aの地図に `tileLayer.on('tileerror', ...)` を1つ足し、**同一セッションで1回だけ**(フラグで抑止。タイルは1画面で十数枚失敗するので毎回出すと連打になる)既存の `setMapNote()`(`app.js:379`)に「地図の背景画像を読み込めませんでした。ピンと提案は使えます。」の1行を出す。ピン・提案は座標だけで動くので「使えます」は事実(実装前に Playwright の `route.abort()` でタイルだけ落として確認すること)。状態Bの小地図は `.mapnote` が無いので今回は対象外とし、その理由を NIGHTLOG に残す。R94 で決まる文言の粒度(「状況。次にできること。」の2文)に揃えること。`scripts/check-tileerror.mjs` を新設して `check-all.mjs` に登録(28本目)。engine/geo/fixtures 不可・rank 不可・外部API 0回(タイルは Playwright で遮断して再現)
- [ ] R102 `screenshots/` の保持方針と一覧コマンド(削除は自動化しない): 実測で `screenshots/*.png` は **605枚・696MB**、最古は `2026-09-15T17-51-00_127.0.0.1_3000_hotel_36.6226_138.5960__feed_mobile.png`(命名が未統一な初期の1枚)、最新は `2026-09-16T17-02-29_r96-focustrap_mobile.png`。`.gitignore:5` で除外済みのためリポジトリは汚れていないが、ローカルのディスクを毎サイクル数十MBずつ食い続けている。R92 と方針を揃え、**削除は自動化しない**(消す判断はみのるんのもの)。実装は `scripts/list-shots.mjs` を新設して「総枚数・合計MB・最古/最新の日付・ラベル別の枚数上位10件・30日より古いファイルの枚数とMB」を表示するだけの読み取り専用コマンドにする(`fs.readdirSync` + `statSync` のみ・削除機能なし・移動機能もなし)。あわせて `docs/CHECKS.md` に「撮影ファイルの命名(`<ISO日時>_<ラベル>_<mobile|desktop>.png`)と、古いものを `screenshots/archive/` へ**手動で**移す1行のコマンド例」を書く(移動は可逆なので削除より安全、という理由も添える)。`.gitignore` は変更しない。`check-all.mjs` には登録しない(検査ではないため)。engine/geo/fixtures 不可・rank 不可・外部API 0回
- [x] 2026-09-16 **R114 道後の上位30件に同じ城が2枚出ている(英語名の OSM 要素が重複として潰れない)**: 実測で `node scripts/dump-rank.mjs dogo` の **10位「松山城」(both・公式サイト有)と27位「Matsuyama Castle」(osm単独・公式サイト有)が同一の城**。fixture の生データは `node/611661255`(`historic=castle` / `name=松山城` / `contact:website=https://www.matsuyamajo.jp/` / `wikipedia=ja:松山城 (伊予国)` / `wikidata=Q981357`)と `node/12827570072`(`historic=castle` / `name=Matsuyama Castle` / `website=https://matsuyamajo.jp`)で、**距離79m・主タグ完全一致・公式サイトのホスト名一致**。原因は `assets/engine.js:593 isSamePlace()` が (a)`wikipediaTitle` の一致 (b)正規化名の完全一致 (c)150m以内での名前の包含、の3経路しか持たず、**日本語名と英語名は文字が1つも共通しないため (c) の包含判定に落ちない**点。`wikidataId` は items に載っている(`engine.js:517`)のに `isSamePlace` が一度も読んでおらず、かつ今回は英語側に `wikidata` タグが無いので wikidata 経路を足しても解決しない(4エリアで wikidata を共有する表記違いのペアは**0件**と実測済み)。**対処に使える判定は「公式サイトのホスト名一致 + 主タグ一致 + 150m以内 + 片方が純ASCII名・片方が日本語名」の組み合わせのみ**。この4条件を4エリア全件で当てた実測結果は kusatsu 0 / hakone 0 / dogo 1(松山城ペアのみ) / beppu 0 で**適合1件・誤爆0件**。条件を1つでも緩めると誤爆が出ることも実測済み(ホスト+150mだけだと19ペアで「宮永岳彦記念美術館↔弘法の里湯」「うみたまごの館内施設4件」等を誤併合、主タグを足しても8ペア中5件が誤爆)ので、**ASCII/日本語のペア限定という条件を外さないこと**。rank の重み・閾値・geo.js・fixtures は変更不可。`scripts/check-engine.mjs` にケース追加。詳細は docs/NEXT.md
- [x] 2026-09-16 R115 草津の上位30件に「温泉」カテゴリの誤判定が2件ある: 実測で `dump-rank kusatsu` の4位「草津温泉バスターミナル」と22位「草津温泉スキー場」が、どちらも**カテゴリ「温泉」**として出ている(どちらも source=wiki 単独)。R18 で写真・要約の誤併合は直ったが、カテゴリ判定は名前に「温泉」が含まれることに引きずられたままで、ユーザーには「温泉の候補」として並んで見える。バスターミナルは交通施設、スキー場はレジャー施設。ただし `geo.js` の `CATEGORY_RULES` は変更不可の制約下にあるため、wiki 単独候補のカテゴリをどこで決めているかを先に `engine.js` 側で特定し、engine 側で直せる範囲か(直せないなら朝の相談へ回すか)を判断するところから始める。実ユーザー影響は R114 より小さい(重複表示ではなくラベルの精度)ので R114 の後
- [ ] R116 道後の17位に「商店街」という一般名詞だけの候補が出ている: 実測で `dump-rank dogo` の17位が `osm` 単独・距離30m・カテゴリ「観光名所」の **「商店街」**。固有名を持たず、カードを見ても何の商店街か分からない(28位の「道後ﾊｲｶﾗ通り」が実体としては同じ通りの可能性がある)。同種の一般名詞のみの候補として hakone more に「国登録記念物」(同名が2件)もある。`engine.js` の除外ルールに「固有名を持たない一般名詞のみの名前」を落とす判定を足せるか、落とすと巻き込む観光対象(「湯畑」「筆塚」等の短い固有名)が無いかを4エリアで before/after 全件目視してから決める。**誤爆リスクが高いので R114/R115 より後**
- [x] 2026-09-16 **R117 上位30件に「宿そのもの」(ホテル・旅館)が混ざっており、しかも「温泉」カテゴリで出ている**: やどたびは「宿を選んだあと、その**周り**に何があるか」を出すサイトなのに、実測で `dump-rank` の上位30件に競合の宿が5件並んでいる。hakone **9位「天成園」(温泉)**・**10位「一の湯」(温泉)**・**25位「ヒルトン小田原リゾート&スパ」(スポット)**、beppu **18位「杉乃井ホテル」(温泉)**・**19位「大江戸温泉物語 別府清風」(温泉)**。全件 source=wiki 単独。R115 は「温泉のまま残した5件」のうち4件がこれで、ラベルの問題ではなく**そもそも候補に出してはいけない**種別だったと今回の実測で判明した。原因は `assets/engine.js:200 EXTRACT_KEYWORD_NG` に宿泊施設を落とす語が1つも無いこと(`grep` で「ホテル」「旅館」は0件)。R115 と同じ「定義文(extract 先頭の一文)で判定する」方式がそのまま使え、`isExcludedArticle` の側に寄せれば**カテゴリではなく候補ごと**落とせる。4エリア実測で該当は7件ちょうど・観光対象の誤爆0件(残り2件の 万座プリンスホテル・渋峠ホテルは60件外)。`天成園 屋上浴場`/`天成園足湯` は OSM 側の別要素(`amenity=public_bath`・名前が違うので `hasOsmTagEvidence` に引っかからない)で、日帰り入浴施設として残るのが正しい。rank の重み・閾値・geo.js・fixtures は変更不可。詳細は docs/NEXT.md
- [x] 2026-09-16 **R119 上位30件に「もう存在しない場所」が並んでいる(廃止済み施設が現役のスポットとして出る)**: 計画役の実測で `dump-rank` の cards に**閉鎖・解体済みの施設が3件**出ている。**kusatsu 13位「草津シズカ山スキー場」**(定義文「かつて群馬県吾妻郡草津町に存在していたスキー場。」)・**dogo 12位「愛媛県立道後動物園」**(「愛媛県松山市にかつて存在した愛媛県立の動物園。」)・**beppu 10位「鶴見園」**(「大分県別府市南立石にかつて存在した遊園地。」)。さらに kusatsu more 4位「白根火山ロープウェイ」(「かつて存在した…ロープウェイ」)。全件 source=wiki 単独・カテゴリ「スポット」。宿の客が写真と要約を見て「行こう」と思って向かうと**そこには何も無い**ので、R117(他社の宿)より実害が直接的。原因は `assets/engine.js:463 isExcludedArticle()` が「何であるか」(学校・ホテル)しか見ておらず「**まだ在るのか**」を一度も見ていない点。R115/R117 で整備済みの `definitionScope()`(engine.js:569)+`definitionPredicate()`(engine.js:586)がそのまま使え、述部に**「かつて」と過去存在語(存在した/存在していた/あった)が両方**あるときだけ落とす。4エリア200記事での実測は**該当14件・観光対象の誤爆0件**で、城(湯築城・石垣山城・羽根尾城・長野原城)・石垣山一夜城歴史公園・跡地の公園・現役施設は「かつて」を含まないため全て無傷。rank の重み・閾値・geo.js・fixtures は変更不可。詳細は docs/NEXT.md
- [ ] R103 `docs/check.mjs` の応答時間に「遅い日」を目視で気づける印を付ける(失敗判定は付けない): 実測で `docs/check.mjs:274-280` の集計は `合計 N件 / 総計 Xms / 平均 Yms` と `最遅: <label> <ms>ms` の2行を出すだけで、**各行の ms は `docs/check.mjs:82` `:194` などで個別に出ているものの、どれが異常に遅いのかは人が数字を見比べるしかない**。GitHub Actions のログは毎日流れていくので、遅い日に気づける仕組みが無い。対処は `timings` 配列(`docs/check.mjs:37`)を使って、平均の2倍を超えた項目の行末に ` (遅い)` の印を付けるか、集計行の直後に「平均の2倍を超えた項目」を列挙する1行を足すだけ。**閾値での失敗判定は付けない**(R33/R46 の方針どおり GitHub Pages の揺らぎで赤くしない)ので `hasFailure`(`docs/check.mjs:292`)には一切触れないこと。実装前に `node docs/check.mjs` を1回流して現在の平均と最遅の実値を NEXT.md に書き、2倍という倍率が現状で誤検知しないかを確かめてから決める。`docs/CHECKS.md` にも1行追記。やどたび本体は無変更・engine/geo/fixtures 不可・rank 不可・外部APIは本番URLへのGETのみ(従来どおり)
- [x] 2026-09-16 R104 README の英語段落が実装より古い(fixture のエリア数と `?debug=1` が未反映): 実測で `README.md:3` の英語1段落(R21)は `?fixture=kusatsu` の1エリアしか挙げておらず、日本語側の `README.md:29` と `README.md:113` が既に **`kusatsu` / `hakone` / `dogo` / `beppu` の4エリア + `random`** を列挙しているのとズレている。また `README.md:114` の `?debug=1` や `?embed=1` といった「外から試せる入口」も英語側には1つも無い。海外から本番URLを見た人には日本語表がそのままでは読めないので、英語段落を **3文から4〜5文に拡張**し (a)fixture が4エリア+random であること (b)`?embed=1` で iframe 埋め込みができること (c)日本語の詳細表がこの下にあること、を足す。**用語(`fixture` / `?embed=1` 等のパラメータ名)は翻訳せずそのまま使う**(R73 と同じ方針)。日本語本文・表は1行も変えない。文書のみ・コード変更なし・外部API 0回・engine/geo/fixtures 不可・rank 不可
- [x] 2026-09-16 R105 `?q=` で検索したエリア名が1件もヒットしなかったときに何も起きず、ユーザーには「壊れた」としか見えない: 実測で `assets/app.js:1672` が `params.get('q')` を読み、`app.js:1676` の `YadoGeo.suggestHotels(q).then(...)` の中が **`if (!results.length || state.view !== 'select') return;`** と**黙って return するだけ**で、地図は既定位置(草津)のまま・`setMapNote()` も呼ばれない。つまり `?q=ほげほげ` のような存在しない地名でアクセスすると、草津の地図が出て「なぜ草津なのか」の説明が一切無い。検索欄の候補側は `app.js:641` で「見つかりませんでした / 別の名前で探してみてください」を既に出しているのに、URL 経由の同じ失敗だけ無言という非対称。対処は `app.js:1676` の 0件分岐で `setMapNote()`(`app.js:379`)を使い「『<入力値>』は見つかりませんでした。エリアチップか検索から選べます。」の R94 準拠の2文を出すだけ(入力値は `escapeHtml` を通すこと)。`.catch` 側(`app.js:1679` の「失敗しても初期位置のままでよい」)は通信失敗で意味が違うので今回は触らない。`scripts/check-nohotels.mjs` が `setMapNote` の文言に依存していないか**先に grep する**こと。`?demo=` に0件再現フラグが要るなら追加してよい。表示のみ・engine/geo/fixtures 不可・rank 不可・外部API 0回
- [x] 2026-09-16 R106 `docs/CHECKS.md` を `scripts/check-all.mjs` の実体(26本+docs/check.mjs=27本、サーバを立てる23本+立てない3本+docs/check.mjs)と一対一に合わせた。実測: `docs/CHECKS.md` にあって実体に無い幽霊行は0、実体にあってCHECKS.mdに無かったのは `check-debugflag`(サーバを立てる側)・`check-nosummary`(サーバを立てる側)の2本のみで、これを表に追加。旧文書の「25本/24本/21本/4本」は全て古い数字で、ROADMAP本文の「27行」も `^| ` の数え方が不正確な事実誤認だった(実際は名前一致で差分0)。冒頭に配列と一致させる注記、末尾に本数突き合わせのコマンド例を追加。
- [x] 2026-09-16 R107 状態A→状態Bの体感速度が `?perf=1` で測れているのに、`check-all.mjs` の27本は誰も ms を見ていない: 実測で `assets/app.js:200 perfMarkFirstCard()` と `app.js:1094` の呼び出しにより「最初のカードが描かれるまで」の計測点は既にあり(R5 で 1535ms→834ms を測った実績がある)、`app.js:181 perfMark()` が各段(`app.js:732` の `perfMark('stage:' + stage)`)も刻んでいる。しかし `scripts/check-r5.mjs` を含む既存27本のどれも**この値を数値として記録していない**ため、今後の改修で体感速度が落ちても誰も気づけない(R89 で check 自体は75%速くなったが、それは検査の実行時間であって**アプリの描画速度ではない**)。対処は `?fixture=kusatsu&perf=1` を開いて `first-card-painted` の ms を Playwright で読み取り、**値をログに出すだけ**の記録を既存の `scripts/check-r5.mjs` に足すか、`scripts/check-firstcard.mjs` を新設して `check-all.mjs` の28本目に登録するかを実装時に選ぶ。**閾値での失敗判定は付けない**(R33/R46/R103 と同じ方針。CI の実行環境の揺らぎで赤くしない)。fixture のみを使うので外部API 0回。engine/geo/fixtures 不可・rank 不可・`app.js` の perf 計測ロジックは無変更(読むだけ)
- [x] 2026-09-16 R108 `?q=` の入力値をそのまま案内文に出しており長さ上限が無い: R105 で追加した `assets/app.js:427 noQueryHitText(q)` は `'「' + q + '」は見つかりませんでした。エリアチップか検索から選べます。'` を返すだけで、**入力値の長さを一切切っていない**。呼び出し元の `app.js:1678` は `app.js:1677` の `q.length >= 2` しか見ておらず上限が無いため、`?q=` に100文字入れるとその100文字が丸ごと文言に入る。表示先の `.mapnote`(`assets/style.css:224-244`)は `max-width: calc(100% - var(--sp-6))` + `white-space: normal` + `overflow-wrap: anywhere` なので**はみ出しはしない代わりに何行にでも縦に伸び**、375px では地図の大半を黒い帯が覆う恐れがある(未確認: 実際に何行になるかは撮影していない)。対処は (a)まず `?q=` に20字・50字・100字を入れて mobile 375px で撮影し `.mapnote` の高さと行数を実測して NEXT.md に書き (b)`noQueryHitText` で `q` を **先頭20字+「…」** に切る1行を足す(切る位置は文字数のみ。R42 の句点ロジックは要約用なので流用しない)(c)`app.js:428` の後半「エリアチップか検索から選べます。」と `NO_HOTEL_TEXT`(`app.js:424`)の文言は R94 で揃えた資産なので**変更しない**。`scripts/check-hotelparam.mjs` は R105 で `?q=` 0件ケースを既に持っている(Nominatim を `page.route()` で `[]` に差し替える方式)ので、そこに長い `q` のケースを1件足して `.mapnote` の文字数が上限以内であることを機械検査する。表示のみ・engine/geo/fixtures 不可・rank 不可・外部API 0回
- [x] 2026-09-16 R109 `docs/check.mjs` が R98 で足した iframe の `sandbox` / `referrerpolicy` を一切検査していない: 実測で `grep -n "sandbox\|referrerpolicy" docs/check.mjs` は **0件**で、リンク切れ検査(`docs/check.mjs:110` のコメントどおり src と meta content の抽出のみ)は属性の有無を見ていない。一方 `demo/hotel-page.html:217` の実 iframe と `demo/hotel-page.html:230` の `<pre class="tag-example">` のコピー用タグ、および `README.md` のタグ例の**3箇所**に同じ `sandbox="allow-scripts allow-same-origin allow-popups allow-popups-to-escape-sandbox" referrerpolicy="no-referrer"` が手で書かれており、**どれか1箇所だけ直して他がズレても誰も気づけない**(R106 で CHECKS.md の幽霊行を潰したのと同じ種類の腐り方)。対処は `docs/check.mjs` に「`demo/hotel-page.html` を本番URLから取得し、実 iframe タグと `<pre>` 内のタグ例の `sandbox` トークン集合が一致し、両方に `referrerpolicy="no-referrer"` があること」の検査を足すだけ(本番URLへのGETのみなので従来どおり外部APIではない)。**`hasFailure`(`docs/check.mjs:292`)の既存経路には手を入れてよい**(これは応答時間と違って揺らがない構造検査なので、ズレたら赤くしてよい。R33/R46/R103 の「閾値で赤くしない」方針は**時間の測定値に限った話**であり、属性の有無には適用しない)。`docs/CHECKS.md` にも1行追記。やどたび本体(`assets/`・`index.html`)は無変更・engine/geo/fixtures 不可・rank 不可
- [x] 2026-09-16 R110 受動ログに件数上限はあるが**古いログの掃除が無い**(訂正: 本文中の「R61が何ヶ月も前の宿を初期位置に使う可能性」は事実誤認。R61(`initialView()`)が読むのは`yado.recent.v3`で受動ログ`yado.passive.v1`とは別キーかつ`t`を持たないため今回の対象外だった): 実測で `assets/app.js:293 PASSIVE_MAX = 200` と `app.js:302 if (list.length > PASSIVE_MAX) list = list.slice(-PASSIVE_MAX);` により件数は200件で頭打ちになるが、`app.js:296 entry = { t: Date.now(), type: type }` で**時刻は毎件記録しているのに、古さで捨てる処理が1行も無い**(`grep -n "PASSIVE" assets/app.js` に日数・期限に相当する定数が無い)。結果、たまにしか使わない人の `localStorage` には半年前のタップ記録が残り続け、R61 の「最近見た宿」が**何ヶ月も前の宿を初期位置に使う**可能性がある(未確認: 実際に R61 がどの entry を読んでいるかは `app.js:225` 付近の `lsGet(PASSIVE_KEY)` を読んで確認すること)。対処は `passivePush()`(`app.js:295`)の先頭に「`entry.t` が N 日より古いものを落とす」1行を足すだけ(`PASSIVE_MAX_AGE_DAYS` を定数で置く。N は**90日**を推奨するが、実装時に `docs/passive-log.md` の目的と突き合わせて決め、理由を NIGHTLOG に残す)。**送信しない・端末内のみ**という F3 の方針は変えない。`scripts/check-passive.mjs` は `scripts/check-passive.mjs:94` の `hasText: 'Instagram'` に依存する箇所があるので**先に grep して**壊さないこと。検査は `?demo=passive` で古い `t` を仕込んだ entry を `localStorage` に置いてから開き、表示件数が減ることを機械確認する。engine/geo/fixtures 不可・rank 不可・外部API 0回
- [x] 2026-09-16 R111 カードの外部リンクが新しいタブで開くことを検査している本が1本も無い: 実測で `grep -rn "_blank\|noopener" scripts/` が **0件**、一方 `assets/app.js` 側は `app.js:830`(カードのリンクチップ)・`app.js:995`(状態Bの主ボタン)・`app.js:1018`(帰属表示内のGoogleマップ)・`app.js:1035`(「くわしい仕組み」)の**4箇所**で `target="_blank" rel="noopener"` を手書きしている。埋め込み(`?embed=1`)では iframe の中で同一タブ遷移が起きるとユーザーが宿ページごとやどたびに乗っ取られたように見えるため、`target="_blank"` は埋め込み用途の前提条件そのものなのに、将来 `feedCardHtml()` を書き換えて属性が1箇所だけ落ちても誰も気づけない。対処は `scripts/check-links-target.mjs` を新設し、`?fixture=kusatsu` を開いて `document.querySelectorAll('.feedcard__link')` の**全件**(30枚×5本=150本前後。実装時に実数を数えて NEXT.md に書くこと)について `target === '_blank'` かつ `rel` に `noopener` が含まれることを機械検査する(`?embed=1` でも同様に1回)。`check-all.mjs` に **29本目**として登録する。`rel` に `noreferrer` を足すかは判断が要るので**今回は足さない**(現状の `noopener` のままを正とする)。表示は1ピクセルも変えない・`app.js` は無変更(読むだけ)・engine/geo/fixtures 不可・rank 不可・外部API 0回
- [x] 2026-09-16 R112 `?bg=` の色指定で文字が読めなくなる組み合わせを一切防いでいない(訂正: 本文中の「黒地に黒文字になりカードの本文が完全に読めなくなる」は誇張だった。実測ではカードは `--c-surface`(白)固定で地色の影響を受けず、実害は地色に直接乗る `.feednote`/`.morenote`(`--c-text-faint`)のみ): 実測で `assets/app.js:1428 bgFromUrl()` は6桁HEXの**書式**しか見ておらず(`app.js:1433` の `/^[0-9a-fA-F]{6}$/`)、`app.js:1441` で `document.documentElement.style.setProperty('--c-bg', bg)` と地色だけを差し替える。文字色トークンは変えないため、`?embed=1&bg=000000` のような暗色を指定すると地色の上に直接乗る淡色テキストが読めなくなる。対処: `bgFromUrl()` の書式検査の直後に WCAG 相対輝度(sRGBガンマ補正込み)を計算し、**閾値0.5未満なら黙って null を返す**(`?bg=` 未指定と同じ既定地色にフォールバック)実装を追加。閾値0.5の根拠と適用箇所は `README.md` の `?bg=` 説明行と `demo/hotel-page.html` の該当箇所に明記済み。`scripts/check-embedbg.mjs` に暗色2ケース(`000000`/`333333`)を追加、`check-all.mjs` 28本全緑。
- [x] 2026-09-16 R113 同じエリアチップを連続で押すと毎回 Overpass の再取得が走る(無料APIのマナーに関わる): 実測で `assets/app.js:1815-1823` のチップ clickハンドラは押されたチップの座標で無条件に `flyTo(area.lat, area.lon, DEFAULT_VIEW.zoom)` を呼び、`app.js:516 flyTo()` は `map.setView()`(`app.js:518`)→ `saveMapView()` → `autoZoomArmed = true` → `loadHotelsInView()` を毎回実行する。**既に同じ中心・同じズームにいるかの判定が1行も無い**ため、「草津」を3回押せば Overpass への宿取得も3回走る(Leaflet は移動距離0の `setView` で `moveend` を出さないので debounce では止まらない経路)。fixture モードでは外部APIを叩かないため今まで表に出ていないが、本番では実際にレートを消費している(未確認: 実際に何回 fetch が飛ぶかは Playwright の `page.route()` でカウントして実測すること)。対処は `flyTo()` の冒頭に「現在の `map.getCenter()` が指定座標と概ね一致(例: 緯度経度とも 1e-6 未満の差)かつ `map.getZoom()` が同じなら、`setView` も `loadHotelsInView()` も呼ばずに return する」ガードを足すだけ(`setCurrentChip()` によるチップ強調は従来どおり先に走るので、押した手応えは消えない)。`?q=` からの初回ジャンプ(`app.js:1693`)と最近の宿(`app.js:1810`)も同じ `flyTo` を通るが、初回は必ず位置が変わるので影響しない。`scripts/check-chipcurrent.mjs` が既にチップ強調を検査しているので**先に grep して**壊さないこと。engine/geo/fixtures 不可・rank 不可・外部API 0回(検査は fixture と `page.route()` のカウントで行う)
- [x] 2026-09-16 R95 fixture の鮮度が古くなったときの手順が `docs/FIXTURES.md` に無い: 実測で4 fixture の `meta.generatedAt` は kusatsu 2026-09-15T18:05Z / hakone 2026-09-15T19:15Z / dogo 2026-09-15T22:10Z / beppu 2026-09-16T03:53Z と全て同一日で、R45 でヘッダーに取得日が出るようにはなったが「**何日経ったら古いと見なすか・そのとき何をするか**」が誰にも決まっていない。`docs/FIXTURES.md` に「鮮度の目安」節を足し、(a)判断基準(例: 半年。OSM の観光施設は年単位でしか動かないため短くしすぎない)(b)再取得は Overpass を各エリア1回まででレート事故を避ける(c)再取得後に必須の確認(`node scripts/dump-rank.mjs` の before/after 差分を全件目視、`?fixture=<area>` の撮影、`node scripts/check-all.mjs` 全緑)(d)`scripts/slim-fixtures.mjs` の keep-list 適用が `make-fixture.mjs` 側で自動なので手動実行は不要、の4点を書く。**このサイクルでは再取得しない**(Overpass を叩かない)。文書のみ・コード変更なし・外部API 0回

## 完了(2026-09-16)
- [x] R36 `scripts/check-all.mjs`(全検査を1コマンドで)**: `scripts/check-*.mjs` 10本(a11y/chipcurrent/engine/geo/hotelparam/imgfail/more/passive/pinflash/r5)と `docs/check.mjs` を**直列で**順に実行し、各本の PASS/FAIL と所要msを表で出して、1本でも失敗なら exit 1 にする新規スクリプト。7本が同じポート3000に自前でサーバを立てて finally で落とすため並列は不可(EADDRINUSE)。既存 check スクリプトの中身は一切編集せず外から spawnSync で呼ぶだけ。以後の全サイクルの完了条件を「`node scripts/check-all.mjs` が緑」に一本化して検収を軽くするのが狙い。README に「開発者向け」節を新設して1行。詳細は docs/NEXT.md
- [x] R35 観光対象でない候補の除外漏れ**: R30 の道後観察で「愛媛大学教育学部附属特別支援学校」「松山地方気象台」「松山市青少年センター」等の教育・公共施設が上位30件に混入。rank ではなく除外ルール(`engine.js` の `TITLE_SUFFIX_NG`/`TITLE_KEYWORD_NG`/`EXTRACT_KEYWORD_NG`)の取りこぼし。**`isExcludedArticle` は wiki 側(engine.js:608)でしか呼ばれておらず OSM 側 `buildOsmItems` は素通し**なのが本命の原因。語ベースで除外を拡充し OSM 側にも適用する。誤爆防止に「記念館・資料館・美術館・博物館・道の駅・公園・神社・寺」は保護。rank の重みは触らない。同サイクルの小修正として dogo 1位「伊佐爾波神社」のカテゴリが「記念碑」になる件(geo.js の `CATEGORY_RULES` で `historic=monument` が `place_of_worship` より先)も直す。詳細は docs/NEXT.md
- [x] R18(最優先) 誤併合バグ**: 「草津温泉バスターミナル」「草津温泉スキー場」が engine.js `isSamePlace` の部分文字列一致により「草津温泉」の Wikipedia 要約・写真を誤って継承している(R17 の作業役が発見・未修正)。ユーザーに間違った情報を見せる実害があるため最優先。判定を厳格化し、R17 の妥当な併合(湯畑源泉→湯畑・光泉寺・石垣山)は維持する。rank の重みは触らない。詳細は docs/NEXT.md
- [x] R2-2 提案0件カードの説明文「データが少ないエリアのようです。地図で直接探してみてください。」が mobile で最後の「てみてください。」だけ3行目に落ちる。日本語の禁則・文の分割(2文に切る等)で見た目を整える
- [x] R2-3 `fixtures/hakone.json`(収集半径30km)で `?fixture=hakone&demo=far` を開き、実データで far 10件を確認した
- [x] R1 固定データモード `?fixture=kusatsu`: 草津(36.6226,138.5960)の Overpass/Wikipedia 応答を `fixtures/kusatsu.json` に保存し、このパラメータがあると geo.js が API を呼ばずにそれを返す。撮影・検証をAPI無しで回せるようにする(無料APIのマナーの要)
- [x] R7 状態Bの小地図で番号ピンが中心部で重なって隠れる(草津で1〜5番が見えない)。ピンを小さくする・番号順に上へ・ホバー不要でタップで前面など、ライブラリ追加なしで見える化する
- [x] R8 【R7の再発】混雑モード(Wikipedia単独)で候補が温泉街中心に密集し、番号1〜9が宿ピンや互いの裏に完全に隠れる。nudgeOverlaps が空き枠を見つけられないと元位置のまま置いてしまうのが原因。必ず分離するよう作り替える(詳細は docs/NEXT.md)
- [x] R2 視覚QA第1回: 状態A(地図)・状態B(フィード)・0件時・混雑エラー時 を mobile/desktop で撮影し、文字崩れ・重なり・はみ出しを全部直す
- [x] R3 本番URL(GitHub Pages のサブパス /yadotabi/)で `?hotel=` と `?q=` が動くことを撮影で確認。動かなければ相対パス等を修正
- [x] R4 Overpass 429/504 のとき: 自動で1回だけ再試行(3秒後)、それでも駄目なら Wikipedia だけで提案を出す(画面が空にならない)
- [x] R5 読み込み体感: `?perf=1` で計測できるようにし、collect の "osm" 段が Wikipedia の完了を待ってから発火していた(=段階描画が無効だった)のを、OSM 解決時点で先に発火するよう修正。実API相当の遅延(OSM 800ms/Wiki 1500ms)で最初のカードが 1535ms → 834ms。半径15kmは far の件数維持のため変更せず
- [x] R6 `docs/check.mjs`: 本番URLが200で `<title>やどたび` を含むか、主要JSが読めるかを確認するスクリプト。各サイクルの最後に実行
- [x] F1 埋め込みモード `?embed=1&hotel=...`: 地図と検索を隠しフィードだけ表示。iframe で他サイトに置ける
- [x] F2 営業用デモ `demo/hotel-page.html`: 予約サイト風の宿ページに埋め込みモードを iframe で置いた1枚
- [x] F3 受動ログ: カードのタップ(押したリンク)・スクロール到達位置(何枚目まで見たか)・表示した宿と上位カードのID を localStorage `yado.passive.v1` に記録(送信はしない・上限200件)。`docs/passive-log.md` に形式と目的を書く。`?demo=passive` で記録内容を画面最下部に表示して目視できるようにする。
- [x] F4 エリアチップの拡充(全国の主要温泉地20件)と、チップ表示の横スクロール確認
- [x] F5 カード画像の遅延読み込み(loading=lazy)と、画像なしカードの見栄え改善 — 画像は既に `loading="lazy"`、画像なしカードもカテゴリ絵文字のプレースホルダで空白にならないことを R5 の撮影で確認済み
- [x] S1 09_研究ノートの「実験ログ」に、v3の暫定rankで草津の上位30件を貼り、有名どころ偏りの実態を記録
- [x] R9 固定モードを草津以外にもう1件(箱根)追加し、`?fixture=` の切り替えで地形の違う土地でもピン配置が崩れないか確認できるようにする
- [x] R10 カードの番号バッジをタップすると小地図の該当ピンが一瞬光る(CSSアニメのみ)。「1番ってどれ?」を地図側からも解消する
- [x] R12 OGPメタタグ: index.html に og:title / og:description / og:type / og:url / og:image / og:site_name / og:image:width・height・alt と twitter:card(summary_large_image) + canonical を追加。og:image は kusatsu fixture を1200x630で撮影しJPEG化した `docs/og.jpg`(絶対URL)。LINE/Xに貼ったときにタイトルと説明が出ることを目標にする(実投稿での確認は不要、メタタグの記述と本番URLでの取得可否まで)
- [x] R13 アクセシビリティ最低限: タップ領域44px確保(戻るボタン・チップ・リンクチップ・もっと遠くの各行を `scripts/check-a11y.mjs`(Playwright)で機械検査、全件OK)、フォーカスリング強調、prefers-reduced-motion対応、番号ピン/宿ピンに aria-label、地図コンテナに role="region"、feed-status に role="status"。地図ピンは 24px を意図的に維持(密集分離が壊れるため、カード側の導線で代替)。コントラストは計算上すべて合格だった(faint はプレースホルダ専用のため対象外)
- [x] R15 geo.js に撮影用の遅延パラメータ `?slow=osm800,wiki1500`: R5 の A/B 計測で手で入れた遅延を、URLパラメータで再現できるようにする。段階描画(OSM先出し)が効いていることを毎回撮影で確認できるようになる。fixture モードと併用できること、パラメータ無しなら完全に不変であることが条件
- [x] R17 候補収集の取りこぼし調査と修正(rank ではなく collect のバグ寄り)。09ノートの観察で (a)箱根の大涌谷が候補に入らない (b)彫刻の森美術館が圏外 (c)草津の湯畑が26位(source=osm単独) が判明した。事前調査で判明した事実: 大涌谷は `way/727045804`(center あり・宿から7.6km)、彫刻の森は `node/5182750902`(lat/lon あり・5.2km) で、どちらも OSM_RADIUS_M=15000 の内側かつ座標は取れている(=NIGHTLOG の「座標欠落」は誤り)。また hakone.json の Wikipedia geosearch 50件は最遠 3.7km までしか無く、WIKI_RADIUS_M=10000 を指定しても 50件上限で 3.7km で頭打ちになっている。湯畑も kusatsu.json の Wikipedia 50件に含まれない。取りこぼしの解消のみを行い、rank の重み・閾値は変更しない
- [x] R20 Wikipedia geosearch の50件上限を半径分割で回避: 現状 1回の geosearch が 50件上限で箱根は 3.7km で頭打ち(R17で判明)。中心+周囲数点に分けて呼び、重複を id で除いて結合する。本番のみ効く(fixture は既存データのまま)。外部API呼び出し回数が増えるので、無料APIのマナーに沿って呼び出し上限を決めてから実装する
- [x] R23 カード画像の読み込み失敗時のフォールバック: Wikipedia のサムネイルURLが 404/タイムアウトのとき `<img>` が壊れアイコンや白枠のまま残る。`onerror` で画像枠を畳んで、画像なしカードと同じカテゴリ絵文字プレースホルダに差し替える。撮影は存在しないURLを1枚だけ差し込む `?demo=imgfail` 等で外部APIなしに再現する
- [x] R24 `?hotel=` で名前(`name`)が無いときの見出し: 現在は一律「この宿」。座標しか無い入口でも「このあたり」等、地図から入った文脈に合う語にするか検討し、`?hotel=` の name 有無2パターンを撮影で比較する。文言のみの変更でロジックは触らない
- [x] R25 `docs/check.mjs` を GitHub Actions で毎日1回実行(無料枠内): `.github/workflows/check.yml` を新設し `schedule` + `workflow_dispatch` で `node docs/check.mjs` を走らせる。本番URLへの GET のみで外部APIは叩かない。失敗時に Actions が赤くなることがゴール(通知設定はユーザー判断なので触らない)
- [x] R26 README にスクリーンショットを追加: 既存の `screenshots/` から状態B(mobile)と埋め込みモードの2枚を `docs/` へ複製し README に貼る。新規撮影は fixture で1回まで。コード変更なし
- [x] R27 Leaflet の CDN を unpkg から cdnjs に変更(可用性): index.html の `<script>`/`<link>` を cdnjs の同一バージョン・同一 SRI に差し替える。バージョンは上げない。`node docs/check.mjs` と `?fixture=kusatsu` の撮影で地図が従来どおり描けることを確認する
- [x] R21 README に英語1段落を追加(What this is / How to try / No API keys needed の3文程度)。海外から本番URLを見た人向け。コード変更なし
- [x] R22 `docs/check.mjs` にリンク切れ検査を追加: index.html と demo/*.html 内の相対パス参照(css/js/画像/iframe src)を抽出し、本番URLで 200 が返るかを確認する。外部ドメインへのリンクは対象外(叩かない)
- [x] R29 `?q=<エリア名>` でエリアチップの該当チップを強調: `?q=箱根` や検索で箱根へ飛んだとき、チップ行のどれが今の場所か分からない。`AREAS` のラベルと前方一致したチップに `aria-current="true"` + 枠線強調を付け、チップ行を横スクロールしてそのチップを画面内に入れる(`scrollIntoView({inline:'center'})`)。入力ゼロ原則に反しない(表示だけ)
- [x] R30 fixture 3エリア目の追加(道後温泉): 草津(内陸・密集)・箱根(谷・分散)に続く3つ目として、市街地隣接の温泉地を追加。`node scripts/make-fixture.mjs dogo`(Overpass 1回・osmRadiusM=15000)。カード30枚・ヘッダー「道後温泉(固定データ)」・番号ピン判読可を撮影で確認。市街地特有の教育・公共施設混入とfar 0件をR19の材料として09研究ノートに記録
- [x] R31 小地図の OSM attribution の**復活**と位置調整: **(2026-09-16 計画役の調査で判明 —— 当初「位置確認」としていたが、`app.js:904` の `attributionControl: false` と `app.js:908` の tileLayer に `attribution` 未指定により、状態Bの小地図には帰属表示が最初から存在しない。最新スクリーンショットでも確認済み。OSM タイル利用規約上の必須要件を満たしていないので、これは是正タスク)**。帰属表示を出したうえで番号ピンと重ならない位置に収め、`scripts/check-attrib.mjs`(新規)で機械検査する。詳細は docs/NEXT.md
- [x] R38 カードの「Googleマップ」チップを宿→スポットの**経路**リンクにする: 現在は目的地単体の検索URL。`https://www.google.com/maps/dir/?api=1&origin=<宿lat,lon>&destination=<スポットlat,lon>` に変えれば、既に持っている座標だけで「宿からどう行くか」が1タップで出る(ユーザー入力ゼロ・APIキー不要・コスト0円)。座標が無いスポットは従来の検索URLにフォールバック
- [x] R32 検索候補への「最近見た宿」の統合確認: 現在フォーカス時だけ出る「最近の宿」が、文字を打ち始めた後も関連するものは候補に残るか(消えるなら残す)を `?demo=recent` と `?demo=suggest` の撮影で確認し、必要なら統合する。R2-1(候補とチップの重なり)とは別問題として扱う
- [x] R33 `docs/check.mjs` に応答時間の記録を追加: 各GETの所要ミリ秒を計測して1行に併記し、末尾に合計と最遅項目を出す。閾値での失敗判定は付けない(GitHub Pages の揺らぎで赤くしない)。GitHub Actions のログに毎日の数字が残り、遅くなった日が後から分かることがゴール
- [x] R34 README の画像リンクも `docs/check.mjs` のリンク切れ検査に含める: 現在 `HTML_PAGES` の3つのHTMLしか見ていないため README.md の `<img src>` は未検査。Markdown 内の `<img src="docs/...">` と `![](...)` を抽出して同じ200判定に通す(外部ドメインは対象外のまま)
- [x] R16 カードの「もっと見る」: 現在30件で打ち切っているランキングの31件目以降を、ボタン1つで追加10件ずつ展開する。追加のAPI呼び出しは無し(既に収集済みの配列から出す)。入力ゼロ原則に反しない(タップ1回で増えるだけ)
- [x] 2026-09-16 **R118 帰属表示の退避を「点の集まり」から「矩形」へ作り直す(3サイクル連続再発の根本対処)**: R114/R115/R117 の3サイクルすべてで `scripts/check-attrib.mjs` が落ち、そのたびに作業役が `app.js` の応急処置に時間を取られた。原因は「どのピンが OSM 帰属表示に重なるか」が提案順位に依存する幾何検査であること。実測(2026-09-16・375x812・4エリア)では現状の重なりは0件だが余裕は薄く、帰属表示の矩形(4エリア共通 `left=235.3 top=0 w=139.8 h=14`)と最近接ピンの隙間は hakone 16.0px / beppu 20.0px しかなく、順位が1つ動けば再発する。R117 の点近似(`app.js:1326-1338`、箱幅を間隔23.3pxの7点で代用)自体には幾何上の穴は0件で、真因は `nudgeOverlaps`(`app.js:1211-1269`)が8方向x6リングで空きを見つけられないとき `app.js:1256-1260` で「いちばんマシな候補」をクリアランス負のまま採用するベストエフォート仕様にある(実測でピン同士の矩形重なりが kusatsu 11ペア/hakone 3/dogo 4/beppu 1 残存、縁 clamp 張り付きも kusatsu 3件)。加えて点近似は過剰退避でもあり、箱周辺の格子で「重ならないのに拒否」が 5594/19201(29%)。対処は `nudgeOverlaps` の第2引数を矩形配列に一般化し矩形 vs 矩形で判定、帰属表示だけはベストエフォートの例外(絶対制約)にする。複雑になりすぎる場合は代替案(帰属表示の帯をピン禁止帯にする/`bottomright` + 下端帯)を実測で選んでよい。**どの案でも OSM 帰属表示はライセンス上必須なので必ず表示されたまま**。完了条件は4エリア全部で重なり0かつ順位をシャッフルしても壊れないことを機械検査で示すこと、`node scripts/check-all.mjs` 29本全緑。rank の重み・閾値・engine.js・geo.js・fixtures は変更不可。詳細は docs/NEXT.md

# ROADMAP(自動ループのバックログ。上から順に1つずつ)

## 品質・基盤
- [ ] R2-1 検索候補のドロップダウンがエリアチップ行の上に重なって出る(チップ「草津」が候補の背後から半分はみ出して見える)。候補を開いている間はチップを隠す/暗くする等、どちらを見せるか方針を決めて直す。デザイン判断が要るので朝の相談向き

## 機能(計画書08 v3.1/v3.2)

## 研究(09ノート)— コードではなく文書

## 小さな改善候補(手が空いたとき・コスト0円)
- [x] 2026-09-16 R11 小地図の高さがmobileで固定値なので、密集時だけ少し高くする(または縦横比を調整して詰まりを緩和する)余地がないか検討(kusatsu/hakone/dogoの180px/220px比較の結果、採用。`.feedmap--tall`(220px)を`cards.length>=25`で付与)
- [ ] R14 fixtures/hakone.json の軽量化: 904KB のうち提案生成に使っていない OSM タグ(建物属性・住所細目など)を make-fixture.mjs の保存時に落とす。kusatsu.json も同様。生成し直す際に Overpass を叩くのは各1回まで。軽量化後に `?fixture=hakone` が従来と同じカード30枚・同じ並びになることを撮影で確認する(差分が出たら落としたタグが実は使われている)
- [ ] R19 hakone fixture の far 集中の是正: 収集半径(make-fixture の 30km)と far 判定の閾値(車1時間以上)が噛み合っておらず、far が 30km 境界付近に不自然に固まっている。半径と閾値の整合を取り、`?fixture=hakone&demo=far` で分布が自然になることを dump-rank で確認する。rank の重みは触らない
- [x] 2026-09-16 R28 状態Aで宿ピンが0件のときの案内バナー: 地図を動かしても宿が1件も取れない範囲(山間部・海上など)では現在なにも出ず「壊れている?」と見える。`.mapnote` の既存の仕組みを流用して「この範囲には宿のデータがありません。エリアチップか検索から選べます。」を1行出す(混雑トーストとは文言を分ける)。撮影は `?demo=` に0件状態を再現するフラグを足して外部APIなしで行う。**(2026-09-16 計画役の調査: 案内文自体は既に app.js:396 `setMapNote(hotels.length ? '' : 'この範囲には宿が見つかりませんでした')` で実装済み。残作業は撮影用フラグ(`?demo=nohotels`)と `scripts/check-nohotels.mjs` による機械検査、および混雑トーストとの優先順位の確認のみ)**
- [x] 2026-09-16 R37 状態Bの小地図で宿ピン(♨)をタップすると宿の位置へ `panTo` する: 番号ピンは R10 でカード側から辿れるようになったが、宿ピン自体は押しても何も起きない。`renderFeedMap()` の宿マーカーに `click` ハンドラを足し、`feedMap.panTo([hotel.lat, hotel.lon])` するだけ(ズームは変えない)。入力ゼロ原則に反しない(タップ1回で戻るだけ)
- [x] 2026-09-16 R39 `?fixture=` のときヘッダーに小さな「固定データ」バッジ: 現在は見出し文字列に `(固定データ)` を括弧書きしているだけで、本番URLを見た人には何のことか分からない。`.topbar` に淡色の小さなバッジ要素を出し、見出しからは括弧書きを外す。表示のみでロジック変更なし
- [ ] R40 fixture 4エリア目「別府」の追加: 草津(内陸・密集)・箱根(谷・分散)・道後(市街地隣接)に続く4つ目として、広域に温泉地が散る別府を追加する。`node scripts/make-fixture.mjs beppu`(Overpass 1回)。R19(far 分布)の材料にもなる
- [x] 2026-09-16 R42 カード要約の「…」で文の途中が切れる問題: `engine.js` の `truncate()` が 120字で機械的に切るため「山号は草津山。」の直後など、切れ目が語の途中に落ちることがある。上限の手前にある**最後の句点(。)で切る**(句点が上限の 60% より手前にしか無いときだけ従来どおり字数で切って「…」を付ける)方式に変える。rank の重み・閾値・除外ルールは触らない。`scripts/check-engine.mjs` にケースを追加し、3エリアの1位カードを撮影して切れ方を目視比較する
- [x] 2026-09-16 R43 状態Aの宿ピンに宿名のツールチップ(タップで表示): 状態Aの地図では宿ピンが♨アイコンだけで、押すまでどの宿か分からない。R13 で `aria-label` は付いているので、Leaflet の `bindTooltip`(ライブラリ追加なし)で宿名を出す。ホバーではなく**タップで開く**設定にして入力ゼロ原則を守る(タップ1回で名前が見えるだけ、宿の選択は従来どおり)。密集時にツールチップ同士が重ならないか `?demo=` で撮影確認する
- [x] 2026-09-16 R44 `?q=` ジャンプ後に宿ピンが0件なら自動でズームを1段引く: R28 で「この範囲には宿が見つかりませんでした」は出るようになったが、ユーザーは自分でピンチアウトする必要がある。`?q=` や検索でエリアへ飛んだ直後に限り、宿0件なら**1回だけ**ズームを1段下げて再取得する(無限に引かないよう1回で打ち止め、通常の地図ドラッグでは発動しない)。Overpass 呼び出しが1回増えるので、混雑(429/504)時は再取得せず現状のバナーのままにする
- [x] 2026-09-16 R45 fixture のヘッダーバッジに生成日時を表示: R39 で付いた「固定データ」バッジに、`json.meta` の生成日時(`generatedAt` が無ければ make-fixture.mjs の保存時に追加する)を `title` 属性かバッジ内の小さな文字で出す。いつ取ったデータを見ているのかが分かり、fixture の鮮度が古くなったことに気づける。表示のみでロジック変更なし・Overpass は叩かない
- [x] 2026-09-16 R46 `docs/check.mjs` にレスポンスサイズの記録を追加: R33 で応答時間は記録できるようになったが、バイト数は見ていない。各GETの `content-length`(無ければ本文長)をKB単位で行に併記し、末尾に合計KBと最大項目を出す。閾値での失敗判定は付けない(GitHub Actions のログに毎日の数字が残り、fixture 肥大やアセット追加で重くなった日が後から分かるのがゴール)。R14(fixture 軽量化)の効果測定の物差しにもなる
- [x] 2026-09-16 R47 状態Bのカード一覧の末尾に「この提案の作り方」1行を置く: 現在カードが30枚並んで終わるだけで、順位が何で決まっているのか(暫定の rank・Wikipedia/OSMの2ソース・カテゴリ多様性の減点)が一切書かれていない。「順位は試作中の指標で決めています。有名な場所が下に来ることがあります。」程度の正直な注記1行と、`docs/09_研究ノート` へのリンクを淡色で出す。表示のみ・ロジック変更なし。`?fixture=kusatsu` と `&embed=1` の両方で位置が崩れないか撮影で確認する(埋め込みでは出すか消すかを実装時に決めて NEXT.md に理由を書く)
- [x] 2026-09-16 R48 `?embed=1` で高さを親に通知する postMessage: 現在 `demo/hotel-page.html` の iframe は高さ固定で、中身が長いと iframe 内に二重スクロールが出る。埋め込み時だけ `ResizeObserver` で `document.body.scrollHeight` を監視し、変化したら `parent.postMessage({ type: 'yadotabi:height', height }, '*')` を送る(送信のみ・受信しない・他の情報は載せない)。受信側の数行のサンプルを `demo/hotel-page.html` に実装して、iframe が中身の高さに自動で伸びることを撮影で確認する。埋め込みでないときは一切送らない
- [x] 2026-09-16 R49 README に「判断待ちの設計課題」節を新設: NIGHTLOG の「朝の相談」4件(検索候補とエリアチップの重なり / カテゴリ多様性減点が有名どころを締め出す / 実APIとfixtureのWikipedia件数の食い違い / 小地図ピンのずらし幅)を、外から読んでも分かる形に書き直して README に公開する。「作りかけを隠さない」姿勢そのものが個人開発の見どころになる。文書のみ・コード変更なし
- [x] 2026-09-16 R50 fixtures の再生成手順を `docs/` に明文化: 現在 `scripts/make-fixture.mjs` の使い方がコード内コメントにしかなく、いつ・どの条件で再生成してよいか(Overpass は各エリア1回まで、429/504 が出たら日を改める、再生成後は `?fixture=<area>` の撮影とカード枚数の比較が必須)が残っていない。`docs/fixtures.md` を新設し、対象4エリア・座標・半径・generatedAt の意味・再生成の判断基準を表で書く。R14(軽量化)や R40(別府追加)の前提資料になる。文書のみ・コード変更なし
- [x] 2026-09-16 R51 カードのリンクチップの並び順を根拠のある順にする: 現在「Googleマップ / 公式 / Instagram / TikTok / YouTube」の固定順だが、公式サイトがあるスポットでは公式が先に来る方が役に立つ。`Googleマップ(行き方) → 公式 → SNS3種` の順に並べ替えるだけ(チップの見た目・リンク先・タップ領域44pxは変更しない)。`scripts/check-a11y.mjs` のタップ領域検査が引き続き全件OKであることと、`?fixture=kusatsu` 1位カードの撮影で折り返しが増えていないことを確認する
- [x] 2026-09-16 R52 README に `?fixture=` / `?demo=` / `?simulate=` / `?slow=` / `?embed=1` / `?perf=1` の**全パラメータ一覧表**を追加: 現在これらは app.js のコード内コメントと NIGHTLOG に散らばっており、外から読んだ人がどの画面を再現できるのか分からない。「パラメータ / 値の例 / 何が起きるか / 外部APIを叩くか」の4列の表にまとめ、`docs/FIXTURES.md`(R50)からもリンクする。app.js を grep して**実在するものだけ**書く(実装されていないものを書かない)。文書のみ・コード変更なし
- [x] 2026-09-16 R53 `demo/hotel-page.html` に「埋め込みコードのコピー説明」を1行足す: 現在 `<pre class="tag-example">` にタグは載っているが、コピーして貼る手順(width/height の目安、`?hotel=<lat>,<lon>,<宿名>` の宿名部分は URL エンコードが要ること、R48 の受信スクリプトの要否)が書かれていない。営業資料として読める2〜3行を `.sales-notes` に追記する。表示のみ・やどたび本体は無変更
- [ ] R54 状態Bのカードに「宿から◯km」表記を足すか検討: 現在カードには「徒歩10分 · 車2分」しか出ておらず、距離そのものが見えない。既に `engine.js` が距離を持っているので**表示だけ**で足せる。ただし1行が長くなって mobile で折り返す恐れがあるので、`?fixture=kusatsu`/`hakone`/`dogo` の mobile 撮影で折り返しが増えないことを先に確認し、増えるなら「徒歩/車のどちらか片方を落として距離に差し替える」案も撮り比べて NIGHTLOG に理由を残す。rank・engine のロジックは触らない
- [x] 2026-09-16 R56 リンクチップの見た目調整(mobile 375px で5個を1行に収める): R51 の撮り比べで「ラベル短縮・順序変更では 4個+1個の折り返しが解消せず、チップの**合計幅**が主因」と判明した。残る手段は幅そのものを詰めることなので、`style.css:430` の `.feedcard__link` の `padding: 5px 10px` と `font-size: var(--fs-xs)`(12px)、`.feedcard__links` の `gap: 10px 8px` を1段小さくする3案(A:paddingのみ / B:padding+font-size 11px / C:横gapと横paddingのみ)を撮り比べ、1行に収まる中で最も文字が大きい案を採る。**SNS のラベル文字列は変更不可**(`scripts/check-passive.mjs:94` が `hasText: 'Instagram'` に依存)。`.feedcard__link::after { height:44px }`(R13 のタップ領域)は維持する。完了条件は 375px で全 `.feedcard__link` の `offsetTop` が同値・check-a11y 全OK・check-all 18本全緑。詳細は docs/NEXT.md
- [ ] R55 `scripts/check-all.mjs` の実行時間短縮: 現在17本で約1分、うち Playwright を使う本が毎回ブラウザを起動し直している(サーバは同一ポート3000のため共有不可)。各 check 本の中身は編集しない制約があるので、**`check-all.mjs` 側で `PLAYWRIGHT_*` 系の環境変数や `--only <名前>` 引数(既定は全本)を足す**方向で、開発中に一部だけ回せるようにする。まず各本の所要msを表から読んで**遅い上位3本を特定し NIGHTLOG に記録する**ところまでを1サイクルの成果とする(最適化は測ってから)
- [x] 2026-09-16 R41 README の「仕組み」節を現状に合わせて更新: 一晩の改修で段階描画・重複マージ・除外ルール・同心円 geosearch・fixture 3エリア・check-all.mjs などが増えたが README の説明が初期のままの箇所がある。コード変更なし・文書のみ

## 完了(2026-09-16)
- [x] R36 `scripts/check-all.mjs`(全検査を1コマンドで)**: `scripts/check-*.mjs` 10本(a11y/chipcurrent/engine/geo/hotelparam/imgfail/more/passive/pinflash/r5)と `docs/check.mjs` を**直列で**順に実行し、各本の PASS/FAIL と所要msを表で出して、1本でも失敗なら exit 1 にする新規スクリプト。7本が同じポート3000に自前でサーバを立てて finally で落とすため並列は不可(EADDRINUSE)。既存 check スクリプトの中身は一切編集せず外から spawnSync で呼ぶだけ。以後の全サイクルの完了条件を「`node scripts/check-all.mjs` が緑」に一本化して検収を軽くするのが狙い。README に「開発者向け」節を新設して1行。詳細は docs/NEXT.md
- [x] R35 観光対象でない候補の除外漏れ**: R30 の道後観察で「愛媛大学教育学部附属特別支援学校」「松山地方気象台」「松山市青少年センター」等の教育・公共施設が上位30件に混入。rank ではなく除外ルール(`engine.js` の `TITLE_SUFFIX_NG`/`TITLE_KEYWORD_NG`/`EXTRACT_KEYWORD_NG`)の取りこぼし。**`isExcludedArticle` は wiki 側(engine.js:608)でしか呼ばれておらず OSM 側 `buildOsmItems` は素通し**なのが本命の原因。語ベースで除外を拡充し OSM 側にも適用する。誤爆防止に「記念館・資料館・美術館・博物館・道の駅・公園・神社・寺」は保護。rank の重みは触らない。同サイクルの小修正として dogo 1位「伊佐爾波神社」のカテゴリが「記念碑」になる件(geo.js の `CATEGORY_RULES` で `historic=monument` が `place_of_worship` より先)も直す。詳細は docs/NEXT.md
- [x] R18(最優先) 誤併合バグ**: 「草津温泉バスターミナル」「草津温泉スキー場」が engine.js `isSamePlace` の部分文字列一致により「草津温泉」の Wikipedia 要約・写真を誤って継承している(R17 の作業役が発見・未修正)。ユーザーに間違った情報を見せる実害があるため最優先。判定を厳格化し、R17 の妥当な併合(湯畑源泉→湯畑・光泉寺・石垣山)は維持する。rank の重みは触らない。詳細は docs/NEXT.md
- [x] R2-2 提案0件カードの説明文「データが少ないエリアのようです。地図で直接探してみてください。」が mobile で最後の「てみてください。」だけ3行目に落ちる。日本語の禁則・文の分割(2文に切る等)で見た目を整える
- [x] R2-3 `fixtures/hakone.json`(収集半径30km)で `?fixture=hakone&demo=far` を開き、実データで far 10件を確認した
- [x] R1 固定データモード `?fixture=kusatsu`: 草津(36.6226,138.5960)の Overpass/Wikipedia 応答を `fixtures/kusatsu.json` に保存し、このパラメータがあると geo.js が API を呼ばずにそれを返す。撮影・検証をAPI無しで回せるようにする(無料APIのマナーの要)
- [x] R7 状態Bの小地図で番号ピンが中心部で重なって隠れる(草津で1〜5番が見えない)。ピンを小さくする・番号順に上へ・ホバー不要でタップで前面など、ライブラリ追加なしで見える化する
- [x] R8 【R7の再発】混雑モード(Wikipedia単独)で候補が温泉街中心に密集し、番号1〜9が宿ピンや互いの裏に完全に隠れる。nudgeOverlaps が空き枠を見つけられないと元位置のまま置いてしまうのが原因。必ず分離するよう作り替える(詳細は docs/NEXT.md)
- [x] R2 視覚QA第1回: 状態A(地図)・状態B(フィード)・0件時・混雑エラー時 を mobile/desktop で撮影し、文字崩れ・重なり・はみ出しを全部直す
- [x] R3 本番URL(GitHub Pages のサブパス /yadotabi/)で `?hotel=` と `?q=` が動くことを撮影で確認。動かなければ相対パス等を修正
- [x] R4 Overpass 429/504 のとき: 自動で1回だけ再試行(3秒後)、それでも駄目なら Wikipedia だけで提案を出す(画面が空にならない)
- [x] R5 読み込み体感: `?perf=1` で計測できるようにし、collect の "osm" 段が Wikipedia の完了を待ってから発火していた(=段階描画が無効だった)のを、OSM 解決時点で先に発火するよう修正。実API相当の遅延(OSM 800ms/Wiki 1500ms)で最初のカードが 1535ms → 834ms。半径15kmは far の件数維持のため変更せず
- [x] R6 `docs/check.mjs`: 本番URLが200で `<title>やどたび` を含むか、主要JSが読めるかを確認するスクリプト。各サイクルの最後に実行
- [x] F1 埋め込みモード `?embed=1&hotel=...`: 地図と検索を隠しフィードだけ表示。iframe で他サイトに置ける
- [x] F2 営業用デモ `demo/hotel-page.html`: 予約サイト風の宿ページに埋め込みモードを iframe で置いた1枚
- [x] F3 受動ログ: カードのタップ(押したリンク)・スクロール到達位置(何枚目まで見たか)・表示した宿と上位カードのID を localStorage `yado.passive.v1` に記録(送信はしない・上限200件)。`docs/passive-log.md` に形式と目的を書く。`?demo=passive` で記録内容を画面最下部に表示して目視できるようにする。
- [x] F4 エリアチップの拡充(全国の主要温泉地20件)と、チップ表示の横スクロール確認
- [x] F5 カード画像の遅延読み込み(loading=lazy)と、画像なしカードの見栄え改善 — 画像は既に `loading="lazy"`、画像なしカードもカテゴリ絵文字のプレースホルダで空白にならないことを R5 の撮影で確認済み
- [x] S1 09_研究ノートの「実験ログ」に、v3の暫定rankで草津の上位30件を貼り、有名どころ偏りの実態を記録
- [x] R9 固定モードを草津以外にもう1件(箱根)追加し、`?fixture=` の切り替えで地形の違う土地でもピン配置が崩れないか確認できるようにする
- [x] R10 カードの番号バッジをタップすると小地図の該当ピンが一瞬光る(CSSアニメのみ)。「1番ってどれ?」を地図側からも解消する
- [x] R12 OGPメタタグ: index.html に og:title / og:description / og:type / og:url / og:image / og:site_name / og:image:width・height・alt と twitter:card(summary_large_image) + canonical を追加。og:image は kusatsu fixture を1200x630で撮影しJPEG化した `docs/og.jpg`(絶対URL)。LINE/Xに貼ったときにタイトルと説明が出ることを目標にする(実投稿での確認は不要、メタタグの記述と本番URLでの取得可否まで)
- [x] R13 アクセシビリティ最低限: タップ領域44px確保(戻るボタン・チップ・リンクチップ・もっと遠くの各行を `scripts/check-a11y.mjs`(Playwright)で機械検査、全件OK)、フォーカスリング強調、prefers-reduced-motion対応、番号ピン/宿ピンに aria-label、地図コンテナに role="region"、feed-status に role="status"。地図ピンは 24px を意図的に維持(密集分離が壊れるため、カード側の導線で代替)。コントラストは計算上すべて合格だった(faint はプレースホルダ専用のため対象外)
- [x] R15 geo.js に撮影用の遅延パラメータ `?slow=osm800,wiki1500`: R5 の A/B 計測で手で入れた遅延を、URLパラメータで再現できるようにする。段階描画(OSM先出し)が効いていることを毎回撮影で確認できるようになる。fixture モードと併用できること、パラメータ無しなら完全に不変であることが条件
- [x] R17 候補収集の取りこぼし調査と修正(rank ではなく collect のバグ寄り)。09ノートの観察で (a)箱根の大涌谷が候補に入らない (b)彫刻の森美術館が圏外 (c)草津の湯畑が26位(source=osm単独) が判明した。事前調査で判明した事実: 大涌谷は `way/727045804`(center あり・宿から7.6km)、彫刻の森は `node/5182750902`(lat/lon あり・5.2km) で、どちらも OSM_RADIUS_M=15000 の内側かつ座標は取れている(=NIGHTLOG の「座標欠落」は誤り)。また hakone.json の Wikipedia geosearch 50件は最遠 3.7km までしか無く、WIKI_RADIUS_M=10000 を指定しても 50件上限で 3.7km で頭打ちになっている。湯畑も kusatsu.json の Wikipedia 50件に含まれない。取りこぼしの解消のみを行い、rank の重み・閾値は変更しない
- [x] R20 Wikipedia geosearch の50件上限を半径分割で回避: 現状 1回の geosearch が 50件上限で箱根は 3.7km で頭打ち(R17で判明)。中心+周囲数点に分けて呼び、重複を id で除いて結合する。本番のみ効く(fixture は既存データのまま)。外部API呼び出し回数が増えるので、無料APIのマナーに沿って呼び出し上限を決めてから実装する
- [x] R23 カード画像の読み込み失敗時のフォールバック: Wikipedia のサムネイルURLが 404/タイムアウトのとき `<img>` が壊れアイコンや白枠のまま残る。`onerror` で画像枠を畳んで、画像なしカードと同じカテゴリ絵文字プレースホルダに差し替える。撮影は存在しないURLを1枚だけ差し込む `?demo=imgfail` 等で外部APIなしに再現する
- [x] R24 `?hotel=` で名前(`name`)が無いときの見出し: 現在は一律「この宿」。座標しか無い入口でも「このあたり」等、地図から入った文脈に合う語にするか検討し、`?hotel=` の name 有無2パターンを撮影で比較する。文言のみの変更でロジックは触らない
- [x] R25 `docs/check.mjs` を GitHub Actions で毎日1回実行(無料枠内): `.github/workflows/check.yml` を新設し `schedule` + `workflow_dispatch` で `node docs/check.mjs` を走らせる。本番URLへの GET のみで外部APIは叩かない。失敗時に Actions が赤くなることがゴール(通知設定はユーザー判断なので触らない)
- [x] R26 README にスクリーンショットを追加: 既存の `screenshots/` から状態B(mobile)と埋め込みモードの2枚を `docs/` へ複製し README に貼る。新規撮影は fixture で1回まで。コード変更なし
- [x] R27 Leaflet の CDN を unpkg から cdnjs に変更(可用性): index.html の `<script>`/`<link>` を cdnjs の同一バージョン・同一 SRI に差し替える。バージョンは上げない。`node docs/check.mjs` と `?fixture=kusatsu` の撮影で地図が従来どおり描けることを確認する
- [x] R21 README に英語1段落を追加(What this is / How to try / No API keys needed の3文程度)。海外から本番URLを見た人向け。コード変更なし
- [x] R22 `docs/check.mjs` にリンク切れ検査を追加: index.html と demo/*.html 内の相対パス参照(css/js/画像/iframe src)を抽出し、本番URLで 200 が返るかを確認する。外部ドメインへのリンクは対象外(叩かない)
- [x] R29 `?q=<エリア名>` でエリアチップの該当チップを強調: `?q=箱根` や検索で箱根へ飛んだとき、チップ行のどれが今の場所か分からない。`AREAS` のラベルと前方一致したチップに `aria-current="true"` + 枠線強調を付け、チップ行を横スクロールしてそのチップを画面内に入れる(`scrollIntoView({inline:'center'})`)。入力ゼロ原則に反しない(表示だけ)
- [x] R30 fixture 3エリア目の追加(道後温泉): 草津(内陸・密集)・箱根(谷・分散)に続く3つ目として、市街地隣接の温泉地を追加。`node scripts/make-fixture.mjs dogo`(Overpass 1回・osmRadiusM=15000)。カード30枚・ヘッダー「道後温泉(固定データ)」・番号ピン判読可を撮影で確認。市街地特有の教育・公共施設混入とfar 0件をR19の材料として09研究ノートに記録
- [x] R31 小地図の OSM attribution の**復活**と位置調整: **(2026-09-16 計画役の調査で判明 —— 当初「位置確認」としていたが、`app.js:904` の `attributionControl: false` と `app.js:908` の tileLayer に `attribution` 未指定により、状態Bの小地図には帰属表示が最初から存在しない。最新スクリーンショットでも確認済み。OSM タイル利用規約上の必須要件を満たしていないので、これは是正タスク)**。帰属表示を出したうえで番号ピンと重ならない位置に収め、`scripts/check-attrib.mjs`(新規)で機械検査する。詳細は docs/NEXT.md
- [x] R38 カードの「Googleマップ」チップを宿→スポットの**経路**リンクにする: 現在は目的地単体の検索URL。`https://www.google.com/maps/dir/?api=1&origin=<宿lat,lon>&destination=<スポットlat,lon>` に変えれば、既に持っている座標だけで「宿からどう行くか」が1タップで出る(ユーザー入力ゼロ・APIキー不要・コスト0円)。座標が無いスポットは従来の検索URLにフォールバック
- [x] R32 検索候補への「最近見た宿」の統合確認: 現在フォーカス時だけ出る「最近の宿」が、文字を打ち始めた後も関連するものは候補に残るか(消えるなら残す)を `?demo=recent` と `?demo=suggest` の撮影で確認し、必要なら統合する。R2-1(候補とチップの重なり)とは別問題として扱う
- [x] R33 `docs/check.mjs` に応答時間の記録を追加: 各GETの所要ミリ秒を計測して1行に併記し、末尾に合計と最遅項目を出す。閾値での失敗判定は付けない(GitHub Pages の揺らぎで赤くしない)。GitHub Actions のログに毎日の数字が残り、遅くなった日が後から分かることがゴール
- [x] R34 README の画像リンクも `docs/check.mjs` のリンク切れ検査に含める: 現在 `HTML_PAGES` の3つのHTMLしか見ていないため README.md の `<img src>` は未検査。Markdown 内の `<img src="docs/...">` と `![](...)` を抽出して同じ200判定に通す(外部ドメインは対象外のまま)
- [x] R16 カードの「もっと見る」: 現在30件で打ち切っているランキングの31件目以降を、ボタン1つで追加10件ずつ展開する。追加のAPI呼び出しは無し(既に収集済みの配列から出す)。入力ゼロ原則に反しない(タップ1回で増えるだけ)

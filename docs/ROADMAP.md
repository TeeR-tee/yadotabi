# ROADMAP(自動ループのバックログ。上から順に1つずつ)

## 品質・基盤
- [x] 2026-09-16 R1 固定データモード `?fixture=kusatsu`: 草津(36.6226,138.5960)の Overpass/Wikipedia 応答を `fixtures/kusatsu.json` に保存し、このパラメータがあると geo.js が API を呼ばずにそれを返す。撮影・検証をAPI無しで回せるようにする(無料APIのマナーの要)
- [x] 2026-09-16 R7 状態Bの小地図で番号ピンが中心部で重なって隠れる(草津で1〜5番が見えない)。ピンを小さくする・番号順に上へ・ホバー不要でタップで前面など、ライブラリ追加なしで見える化する
- [ ] R2 視覚QA第1回: 状態A(地図)・状態B(フィード)・0件時・混雑エラー時 を mobile/desktop で撮影し、文字崩れ・重なり・はみ出しを全部直す
- [ ] R3 本番URL(GitHub Pages のサブパス /yadotabi/)で `?hotel=` と `?q=` が動くことを撮影で確認。動かなければ相対パス等を修正
- [x] 2026-09-16 R4 Overpass 429/504 のとき: 自動で1回だけ再試行(3秒後)、それでも駄目なら Wikipedia だけで提案を出す(画面が空にならない)
- [ ] R5 読み込み体感: 状態Bに入ってから最初のカードが出るまでの時間を計測し、3秒超なら fetchSpots の半径縮小や並列化で短縮
- [ ] R6 `docs/check.mjs`: 本番URLが200で `<title>やどたび` を含むか、主要JSが読めるかを確認するスクリプト。各サイクルの最後に実行

## 機能(計画書08 v3.1/v3.2)
- [ ] F1 埋め込みモード `?embed=1&hotel=...`: 地図と検索を隠しフィードだけ表示。iframe で他サイトに置ける
- [ ] F2 営業用デモ `demo/hotel-page.html`: 予約サイト風の宿ページに埋め込みモードを iframe で置いた1枚
- [ ] F3 受動ログ: カードのタップ・スクロール到達位置を localStorage に記録(送信はしない)。`docs/passive-log.md` に形式を書く
- [ ] F4 エリアチップの拡充(全国の主要温泉地20件)と、チップ表示の横スクロール確認
- [ ] F5 カード画像の遅延読み込み(loading=lazy)と、画像なしカードの見栄え改善

## 研究(09ノート)— コードではなく文書
- [ ] S1 09_研究ノートの「実験ログ」に、v3の暫定rankで草津の上位30件を貼り、有名どころ偏りの実態を記録

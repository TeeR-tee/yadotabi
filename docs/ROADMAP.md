# ROADMAP(自動ループのバックログ。上から順に1つずつ)

## 品質・基盤
- [x] 2026-09-16 **R18(最優先) 誤併合バグ**: 「草津温泉バスターミナル」「草津温泉スキー場」が engine.js `isSamePlace` の部分文字列一致により「草津温泉」の Wikipedia 要約・写真を誤って継承している(R17 の作業役が発見・未修正)。ユーザーに間違った情報を見せる実害があるため最優先。判定を厳格化し、R17 の妥当な併合(湯畑源泉→湯畑・光泉寺・石垣山)は維持する。rank の重みは触らない。詳細は docs/NEXT.md
- [ ] R2-1 検索候補のドロップダウンがエリアチップ行の上に重なって出る(チップ「草津」が候補の背後から半分はみ出して見える)。候補を開いている間はチップを隠す/暗くする等、どちらを見せるか方針を決めて直す。デザイン判断が要るので朝の相談向き
- [x] 2026-09-16 R2-2 提案0件カードの説明文「データが少ないエリアのようです。地図で直接探してみてください。」が mobile で最後の「てみてください。」だけ3行目に落ちる。日本語の禁則・文の分割(2文に切る等)で見た目を整える
- [x] 2026-09-16 R2-3 `fixtures/hakone.json`(収集半径30km)で `?fixture=hakone&demo=far` を開き、実データで far 10件を確認した
- [x] 2026-09-16 R1 固定データモード `?fixture=kusatsu`: 草津(36.6226,138.5960)の Overpass/Wikipedia 応答を `fixtures/kusatsu.json` に保存し、このパラメータがあると geo.js が API を呼ばずにそれを返す。撮影・検証をAPI無しで回せるようにする(無料APIのマナーの要)
- [x] 2026-09-16 R7 状態Bの小地図で番号ピンが中心部で重なって隠れる(草津で1〜5番が見えない)。ピンを小さくする・番号順に上へ・ホバー不要でタップで前面など、ライブラリ追加なしで見える化する
- [x] 2026-09-16 R8 【R7の再発】混雑モード(Wikipedia単独)で候補が温泉街中心に密集し、番号1〜9が宿ピンや互いの裏に完全に隠れる。nudgeOverlaps が空き枠を見つけられないと元位置のまま置いてしまうのが原因。必ず分離するよう作り替える(詳細は docs/NEXT.md)
- [x] 2026-09-16 R2 視覚QA第1回: 状態A(地図)・状態B(フィード)・0件時・混雑エラー時 を mobile/desktop で撮影し、文字崩れ・重なり・はみ出しを全部直す
- [x] 2026-09-16 R3 本番URL(GitHub Pages のサブパス /yadotabi/)で `?hotel=` と `?q=` が動くことを撮影で確認。動かなければ相対パス等を修正
- [x] 2026-09-16 R4 Overpass 429/504 のとき: 自動で1回だけ再試行(3秒後)、それでも駄目なら Wikipedia だけで提案を出す(画面が空にならない)
- [x] 2026-09-16 R5 読み込み体感: `?perf=1` で計測できるようにし、collect の "osm" 段が Wikipedia の完了を待ってから発火していた(=段階描画が無効だった)のを、OSM 解決時点で先に発火するよう修正。実API相当の遅延(OSM 800ms/Wiki 1500ms)で最初のカードが 1535ms → 834ms。半径15kmは far の件数維持のため変更せず
- [x] 2026-09-16 R6 `docs/check.mjs`: 本番URLが200で `<title>やどたび` を含むか、主要JSが読めるかを確認するスクリプト。各サイクルの最後に実行

## 機能(計画書08 v3.1/v3.2)
- [x] 2026-09-16 F1 埋め込みモード `?embed=1&hotel=...`: 地図と検索を隠しフィードだけ表示。iframe で他サイトに置ける
- [x] 2026-09-16 F2 営業用デモ `demo/hotel-page.html`: 予約サイト風の宿ページに埋め込みモードを iframe で置いた1枚
- [x] 2026-09-16 F3 受動ログ: カードのタップ(押したリンク)・スクロール到達位置(何枚目まで見たか)・表示した宿と上位カードのID を localStorage `yado.passive.v1` に記録(送信はしない・上限200件)。`docs/passive-log.md` に形式と目的を書く。`?demo=passive` で記録内容を画面最下部に表示して目視できるようにする。
- [x] 2026-09-16 F4 エリアチップの拡充(全国の主要温泉地20件)と、チップ表示の横スクロール確認
- [x] 2026-09-16 F5 カード画像の遅延読み込み(loading=lazy)と、画像なしカードの見栄え改善 — 画像は既に `loading="lazy"`、画像なしカードもカテゴリ絵文字のプレースホルダで空白にならないことを R5 の撮影で確認済み

## 研究(09ノート)— コードではなく文書
- [x] 2026-09-16 S1 09_研究ノートの「実験ログ」に、v3の暫定rankで草津の上位30件を貼り、有名どころ偏りの実態を記録

## 小さな改善候補(手が空いたとき・コスト0円)
- [x] 2026-09-16 R9 固定モードを草津以外にもう1件(箱根)追加し、`?fixture=` の切り替えで地形の違う土地でもピン配置が崩れないか確認できるようにする
- [ ] R10 カードの番号バッジをタップすると小地図の該当ピンが一瞬光る(CSSアニメのみ)。「1番ってどれ?」を地図側からも解消する
- [ ] R11 小地図の高さがmobileで固定値なので、密集時だけ少し高くする(または縦横比を調整して詰まりを緩和する)余地がないか検討
- [x] 2026-09-16 R12 OGPメタタグ: index.html に og:title / og:description / og:type / og:url / og:image / og:site_name / og:image:width・height・alt と twitter:card(summary_large_image) + canonical を追加。og:image は kusatsu fixture を1200x630で撮影しJPEG化した `docs/og.jpg`(絶対URL)。LINE/Xに貼ったときにタイトルと説明が出ることを目標にする(実投稿での確認は不要、メタタグの記述と本番URLでの取得可否まで)
- [x] 2026-09-16 R13 アクセシビリティ最低限: タップ領域44px確保(戻るボタン・チップ・リンクチップ・もっと遠くの各行を `scripts/check-a11y.mjs`(Playwright)で機械検査、全件OK)、フォーカスリング強調、prefers-reduced-motion対応、番号ピン/宿ピンに aria-label、地図コンテナに role="region"、feed-status に role="status"。地図ピンは 24px を意図的に維持(密集分離が壊れるため、カード側の導線で代替)。コントラストは計算上すべて合格だった(faint はプレースホルダ専用のため対象外)
- [ ] R14 fixtures/hakone.json の軽量化: 904KB のうち提案生成に使っていない OSM タグ(建物属性・住所細目など)を make-fixture.mjs の保存時に落とす。kusatsu.json も同様。生成し直す際に Overpass を叩くのは各1回まで。軽量化後に `?fixture=hakone` が従来と同じカード30枚・同じ並びになることを撮影で確認する(差分が出たら落としたタグが実は使われている)
- [ ] R15 geo.js に撮影用の遅延パラメータ `?slow=osm800,wiki1500`: R5 の A/B 計測で手で入れた遅延を、URLパラメータで再現できるようにする。段階描画(OSM先出し)が効いていることを毎回撮影で確認できるようになる。fixture モードと併用できること、パラメータ無しなら完全に不変であることが条件
- [x] 2026-09-16 R17 候補収集の取りこぼし調査と修正(rank ではなく collect のバグ寄り)。09ノートの観察で (a)箱根の大涌谷が候補に入らない (b)彫刻の森美術館が圏外 (c)草津の湯畑が26位(source=osm単独) が判明した。事前調査で判明した事実: 大涌谷は `way/727045804`(center あり・宿から7.6km)、彫刻の森は `node/5182750902`(lat/lon あり・5.2km) で、どちらも OSM_RADIUS_M=15000 の内側かつ座標は取れている(=NIGHTLOG の「座標欠落」は誤り)。また hakone.json の Wikipedia geosearch 50件は最遠 3.7km までしか無く、WIKI_RADIUS_M=10000 を指定しても 50件上限で 3.7km で頭打ちになっている。湯畑も kusatsu.json の Wikipedia 50件に含まれない。取りこぼしの解消のみを行い、rank の重み・閾値は変更しない
- [ ] R19 hakone fixture の far 集中の是正: 収集半径(make-fixture の 30km)と far 判定の閾値(車1時間以上)が噛み合っておらず、far が 30km 境界付近に不自然に固まっている。半径と閾値の整合を取り、`?fixture=hakone&demo=far` で分布が自然になることを dump-rank で確認する。rank の重みは触らない
- [x] 2026-09-16 R20 Wikipedia geosearch の50件上限を半径分割で回避: 現状 1回の geosearch が 50件上限で箱根は 3.7km で頭打ち(R17で判明)。中心+周囲数点に分けて呼び、重複を id で除いて結合する。本番のみ効く(fixture は既存データのまま)。外部API呼び出し回数が増えるので、無料APIのマナーに沿って呼び出し上限を決めてから実装する
- [ ] R21 README に英語1段落を追加(What this is / How to try / No API keys needed の3文程度)。海外から本番URLを見た人向け。コード変更なし
- [x] 2026-09-16 R22 `docs/check.mjs` にリンク切れ検査を追加: index.html と demo/*.html 内の相対パス参照(css/js/画像/iframe src)を抽出し、本番URLで 200 が返るかを確認する。外部ドメインへのリンクは対象外(叩かない)
- [x] 2026-09-16 R16 カードの「もっと見る」: 現在30件で打ち切っているランキングの31件目以降を、ボタン1つで追加10件ずつ展開する。追加のAPI呼び出しは無し(既に収集済みの配列から出す)。入力ゼロ原則に反しない(タップ1回で増えるだけ)

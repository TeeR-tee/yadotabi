# NEXT: 2026-09-24 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にあります。**先頭の目次は `##` 見出しの48件**です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 草津の1位「湯畑」の説明 | **1行も出ていなかった** | **`Wikipediaに記事がありません。写真と地図の情報で表示しています。`**(R234・本番反映済み) |
| 人気バッジが初期5枚で出る枚数 | **8/25枚**(草津 **0/5**) | **10/25枚**(草津 **1/5**・別府 2→3)(R241。**しきい値も文言も1つも変えず**) |
| 「もっと見る」を押す前の手がかり | **`もっと見る` の5文字だけ** | **`この先にあるのは歴史を歩く、景色を見にいくです`**(R236・**件数の数字0文字**・CSS と `index.html` は0バイト変更・本番反映済み) |
| 画面末尾の注記が名乗る「順位の作り方」 | 8つの要素のうち**2つしか名乗っていなかった**(しかも「有名な場所が下に来る」と逆を書いていた) | **4種類に畳んで全部を名乗る**(R246・本番反映済み・順位は1件も動かさず) |
| 注記から飛べる README の順位の説明 | 画面と**正反対**・8つの重みのうち**3つしか名乗らず** | **画面と同じ4種類で説明**(R247)+**`docs/check.mjs` に4軸追加**(41→45 pass) |
| README の数値表 | 「一度測って書いたきり」 | **実測で2件が否定され訂正**(R247) |
| `docs/FIXTURES.md` の行番号による道案内 | **22件中21件が指す先を外していた**(`enrichFame` は約400行ずれ)+`enrichFame` を「本番では動いている」前提で説明 | **全件を実測値に訂正**+**「そもそも呼ばれていない」実態に修正**(R248)+**`docs/check.mjs` に行番号を全件突合する軸**(67→80 report)。**`assets/` は1バイトも変更なし** |
| 直近の通し | — | **35本全PASS・393.7s**・**★FLAKY 0本**(R248。**5サイクル連続0本**) |
## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。


## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R249** — **`docs/CHECKS.md` の行番号引用7件が7件とも外れている(申し送り2の裏取り結果)**

**なぜこれを選んだか(★計画役が実際に全件突合してから決めました)**:

- **実測1(申し送り2の裏取り・対象の絞り込み)**: `grep -rno "[A-Za-z0-9_.-]*\.m\?js:[0-9]\+" docs/` から NIGHTLOG と NEXT を除くと、**`CHECKS.md` 7件・`FIXTURES.md` 6件(R248 で修正済み)・`ROADMAP.md` 152件**でした。**`ROADMAP.md` の152件は「起票当時の記録」で、直すと履歴の意味が変わるため対象にしません**(R232「作らない判断も成果」)。**現役の道案内文書で未修正なのは `CHECKS.md` の7件だけ**です。
- **★実測2(ここが核心。7件を全件突合して7件とも外れ = 100%)**:
  - `docs/check.mjs:131` = `attrRe` の正規表現と書いてあるが、**131行目は `timedFetch(...)` の行**。`attrRe` の実際は **143行目**。
  - `docs/check.mjs:143` = `internalPaths` の `Set` と書いてあるが、**143行目は `attrRe`**(上とちょうど入れ替わっている)。`internalPaths = new Set()` の実際は **170行目**。
  - `docs/check.mjs:165` = `resolved.pathname` と書いてあるが、**165行目は `rel="icon"` の report**。`resolved.pathname` の実際は **181/193行目**(同ファイル内に 346/357 の双子もある)。
  - `docs/check.mjs:151` = `raw.startsWith(BASE)` の分岐と書いてあるが、**151行目は `metaContentRe.exec(m[0])`**。実際は **179行目**。
  - `docs/check.mjs:200` = `checkLink(page, '' || 'index.html')` と書いてあるが、**200行目は `return [...internalPaths];`**。`checkLink` の定義は **203行目**、呼び出しは **228/372行目**。
  - `demo/hotel-page.html:212` = 埋め込み iframe の src と2箇所で書いてあるが、**212行目は `<table class="price">`**。iframe の実際は **232行目**。
  - `demo/hotel-page.html:224` = `<pre class="tag-example">` の見本URLと書いてあるが、**224行目は `<a class="cta">`**。`<pre class="tag-example">` の実際は **247行目**(もう1つ 254行目)。
  - `scripts/check-all.mjs:17` = `SCRIPTS` 配列と**2箇所**(`:3` と `:112`)に書いてあるが、**17行目は R223 のコメント文**。`const SCRIPTS = [` の実際は **43行目**。
  - **7種類9箇所すべて外れ。`FIXTURES.md` の21/22件と同じ型が、同じ深さで再現しました**。
- **★実測3(判断が要るかの切り分け)**: **文書の訂正だけなので、みのるんの判断は不要です**。`docs/check.mjs`・`scripts/check-all.mjs`・`demo/` のコードには一切手を出しません。**`assets/` は1バイトも触りません**。
- **★実測4(守る検査を足せる場所が既にあります。新規ファイル0件・35本のまま)**: **R248 で足した `docs/check.mjs:502-617` のブロックが、まさにこの形の軸の見本**です。ただし**申し送り3のとおり (b) は `FIXTURES.md` 専用**で、対象ファイルも「`L<数字>`+バッククォートの関数名」という名乗り方も `CHECKS.md` とは違います(`CHECKS.md` は **`ファイル名:行番号` 形式**で、指す先は `docs/check.mjs`・`scripts/check-all.mjs`・`demo/hotel-page.html` の**3ファイルにまたがる**)。**同じブロックの直後に `CHECKS.md` 専用の軸を足す**のが素直です。

- **タスクID**: R249 / **目的**: **`docs/CHECKS.md` の行番号引用7種類9箇所を実測値に直し、同じサイクルで `docs/check.mjs` に「`CHECKS.md` の `ファイル名:行番号` 引用が指す先に、その行で名乗っているコードが実在するか」を全件突合する軸を足す**。**画面に出るものは何も変えません**。
- **対象ファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\CHECKS.md`(**主**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\check.mjs`(**R249 軸を `:617` の「ここまで R248」の直後に足す**。**新規ファイルは作らない・35本のまま**。**R248 ブロックは1バイトも変えない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(**R249 を新規起票して `[x] 2026-09-24`**。**R225・R243・R244・R235・R211・R102 は起票のまま残す**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-24 R249 …` 見出し+3行以上。**目次エントリは `##` 見出しの48件のみ**)
- **完了条件(検証可能な形)**:
  1. **★まず自分で9箇所を全件突合すること**(計画役の実測は上記のとおり9箇所すべて外れ。**再現しなければ自分の実測を信じること**)。**「前後数件だけ」は禁止**(R233-3: 部分確認では63%の破損を見逃した実例)。
  2. **★引用の形も直すこと**。`:165` の `resolved.pathname` のように**同じコードが同ファイル内に2組ある**(181/193 と 346/357)箇所は、**どちらを指しているかが分かる書き方**にする(範囲表記でもよい)。**行が複数ある事実を消さないこと**。
  3. **★`CHECKS.md` の本文の主張自体も1つだけ実測で確かめること**: `:3` と `:112` が言う「表と `SCRIPTS` 配列が一対一」は、**実測で表の行 `^| ` が41行・`SCRIPTS` の `check-` 入りエントリが34件**でした(表にはヘッダ行と区切り行と他の表が混ざっている可能性があります)。**数え方を実装と一致させたうえで**(R226)、一致しているなら「確かめた上で変更不要」と NIGHTLOG に書くこと(R232)。
  4. **★コードは1バイトも変えないこと**。`docs/check.mjs` の**検査ロジック本体(`:1`〜`:501`)**・`scripts/check-all.mjs`・`demo/hotel-page.html` は変更禁止範囲です。**行番号が外れているからといってコード側を動かして文書に合わせない**。
  5. **★同じサイクルで守る検査も足す**。**`docs/check.mjs` に CHECKS 照合軸を足す**。軸は **(a) 存在軸** = `CHECKS.md` に `ファイル名:行番号` の引用が1件以上あること(母数の確認)、**(b) ★行番号軸** = **引用が指すファイルの実際の行に、その引用のすぐ近くで名乗っているコード片(バッククォート内の識別子・正規表現・属性名など)が含まれていること**を**全件**照合する(外れた引用を**ファイル名・行番号・名乗り**を名指しで出す)、**(c) 対象実在軸** = 引用されたファイル自体が存在し、引用行番号がそのファイルの総行数以内であること。**★母数も期待値も必ず実ファイルから取ること**(**件数の定数や期待行番号の表を1つも書かないこと** = R240 の教訓「`exp.all.length` が常に104で壊しても発火しない死んだ軸」の再来を防ぐ)。**(b) は `docs/check.mjs` や `demo/hotel-page.html` が編集されるたびに自然に発火する**ので、死んだ軸になりません。
  6. **★名乗りの拾い方が `FIXTURES.md` とは違う点に注意**(申し送り3)。`CHECKS.md` は関数名だけでなく**正規表現リテラルや変数名や HTML の属性**を名乗ります。**拾えない引用を黙って飛ばす実装にしないこと**(飛ばすと母数が減って軸が死にます)。**名乗りが1つも取れない引用は「照合できなかった」として NG 側に数えるか、少なくとも件数を report に出すこと**。
  7. **★足した軸が本当に発火するか、壊して確かめること**。軸ごとに、**それだけを壊して発火する**ことを pass/fail で示す。**R248 では軸の実装バグ(関数の終端判定が甘く、正しい引用を誤検知)を、この手順が捕まえました**。同じことを期待して回すこと。
  8. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail で対比**する。**★その壊し方が本当に旧版を素通りするかを先に確かめること**(**司令塔が推した壊し方は10サイクル中8回外れています**。捕まるなら**自分で作り直すこと**)。候補は **(A) 直した行番号を1件だけ元の古い値に戻す**(`check-all.mjs:43` → `:17`。行番号軸だけが発火。**旧版 `docs/check.mjs` は `CHECKS.md` を1バイトも読んでいないので素通りするはず**)と **(B) 引用先を存在しないファイル名にする**(対象実在軸だけが発火・行番号軸は緑のまま)の2通り。**(A) が旧版で素通りすること**を先に確かめ、**R216・R234・R238・R241・R236・R245・R246・R247・R248 と同じ「有無は緑・中身だけが赤」の形**を再現すること。
  9. **★発火しないときは、軸の不備と決めつける前に壊し方が不完全でないか疑うこと**(R247 で実際に起きました。壊し残した文字列が同じ行の末尾にありました)。
  10. **★撮影は不要です。その理由を NIGHTLOG に明記すること**: **今回変えるのは `docs\` 配下のファイルだけで、`assets\` と `index.html` を1バイトも変えないため、画面に出るものが何も変わりません**。**代わりに、着手時と完了時に `assets\app.js` / `assets\style.css` / `assets\ui.css` / `assets\engine.js` / `assets\geo.js` の md5 を取り、全て一致することで「画面が変わっていない」を証明すること**(**基準値は着手時に自分で `md5sum` を取ること**。**前サイクルの値を鵜呑みにしない**)。**`git diff --stat` に `assets/` と `index.html` と `fixtures/` と `demo/` と `scripts/` が1件も出ないこと**も併せて示す。
  11. `node scripts\check-all.mjs` が **35本中35本PASS または FLAKY(exit 0)**。**本数は35本のまま**(**新規ファイルを作らない**)。
  12. **★FLAKY の本数を必ず NIGHTLOG に記録**(**R244〜R248 は0本で5サイクル連続**。今回も0本を保てるかを書くこと)。
  13. **順位不変の実証**: `git diff assets/geo.js assets/engine.js fixtures/` が**空**であること。**`dump-rank.mjs` は順位ロジックを1バイトも触らないため今回は不要**。
- **検証手順**:
  - `node docs\check.mjs` 単体で pass/fail と秒数を記録(変更前後。**R248 時点は 80 report / 0 fail・約1.8s**)。
  - **壊し方(A)(B)を先に旧版で走らせて素通りを確認**(旧版は `git show HEAD:docs/check.mjs` で取り出す。**`git checkout`/`stash`/`reset` は使わない**) → 新版で落ちることを確認(完了条件7・8)。
  - `assets\` 5ファイルの md5 を着手時と完了時に取り、一致を示す(完了条件10)。
  - `node scripts\check-all.mjs` の通しを1回(秒数と FLAKY 本数を記録。**R248 は 393.7s**)。
- **変更禁止範囲(絶対に触らない)**: **`assets\` の全ファイル(`app.js` / `geo.js` / `engine.js` / `style.css` / `ui.css`)** / **`fixtures\*.json`(読むだけ・再生成しない)** / `index.html` / `404.html` / `README.md` / **`demo\*.html`(`hotel-page.html` は読むだけ)** / `.github\workflows\*` / `docs\AUTOPILOT.md` / `docs\FIXTURES.md`(R248 で直したばかり・読むだけ) / `scripts\dump-rank.mjs` / **`scripts\check-*.mjs` の34本(`check-all.mjs` を含む・読むだけ)** / **`docs\check.mjs` の `:1`〜`:617`(R248 ブロックを含む既存部分。今回足すのは `:617` 以降の新ブロックだけ)** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記のみ許可) / **ROADMAP の既存起票すべて**(R249 の新規追記のみ。**152件の行番号引用は履歴なので直さない**)。
  - **★禁止範囲に矛盾を見つけたら報告した上で最小限の変更で進めてよい**(R228)。**ファイルの場所は自分で grep して確かめること**。
- **所要目安**: 25〜35分(9箇所の全件突合6分・`CHECKS.md` の修正5分・表と `SCRIPTS` の数え方確認3分・検査の軸追加と発火確認9分・破壊実証の旧版素通り確認4分・md5 と diff 2分・35本通し7分・記録とコミット5分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。**R248 で文書側は訂正済み。「呼ぶかどうか」は順位とリクエスト数に影響するため判断待ちのまま**です。
- **R244(判断が要ります)**: `fixtures\kusatsu.json` の `backlinks` が **28件**しかなく、他エリア(箱根73・道後70・別府49・城崎41)の半分以下。**★R241 の申し送り: 城崎 1/5・箱根 3/5 は表示側ではこれ以上動かせません**。初期5枚に **60〜149 の帯**にいるカードが1枚も無いためで、**しきい値を下げない限りどんな条件でも増えません**。**fixture 再生成は順位を動かしうる**ため判断が要る。
- **★R243 は「やらない」と決着しました(7回目を数える前の決着・今回の計画役が判断)**: 地図のピンに人気の手がかりを足す案。**7サイクル分の計画役が全員、毎回「覆す実測根拠が出なかった」として見送っています**。理由は3つ: (1) **色分けは禁止**(人気の基準を色という強い表現に乗せると、★の付かない場所を「劣った場所」に見せる。R227・R231 の「評価の語」問題と同じ)、(2) **ツールチップは `permanent:false` で読む瞬間が無い**という技術的な壁が7サイクル未解決、(3) **R237 でピンは既にカードへ飛ぶようになっており、人気の手がかりはカード側の★で見える**ため、**同じものを2通りで測りにいくことになる**(R245 の (B) と同じ理由)。**作業役は ROADMAP の R243 を `[-] 2026-09-24 やらないと決定(R249 サイクルの計画役)` に変え、理由の3行を添えること。これは唯一許可する既存起票の変更です**。
- 判断不要で残っているもの(**R248 は完了・R249 は今回着手**): **R235**(`check-nosummary.mjs:146` の死んだ照合。**★この起票自身が行番号で書かれています。着手時は必ず実測で確かめること**)、**R211**(dump-rank に日時とコミットID)、**R102**(list-shots.mjs)。**R217 は見送り済み**。**R233 は残件0件で完了**。
- **★R249 からの申し送り(次の計画役へ)**: **行番号引用の調査はこれで一段落です**(`FIXTURES.md` 21/22件・`CHECKS.md` 7/7件・`ROADMAP.md` の152件は履歴として意図的に残す)。**次に疑うべきは「文書が名乗る件数・本数」**(`CHECKS.md` の「35本」「34ファイル」「一対一」など)で、**R212 の教訓「本数・一覧を持つ文書は増減した同じサイクルで直す」が守られているかを実測で確かめること**。判断不要の残りは **R235・R211・R102** の3件のみなので、**尽きたら (B) 本番アプリを見て設計思想と突き合わせる方法に戻ること**(R224・R226・R234・R242 はこの方法で見つかった)。
## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★司令塔が推す壊し方は7サイクル連続で旧版に捕まっている。着手前に必ず旧版で素通りするか実測し、捕まるなら自分で作り直すこと。**
- **★司令塔の指示書そのものにも誤りがある(今夜8回目)**(R241 で2件: 抑制規則の出典を「R231」としたが実際は **R224**・推した壊し方(B)が旧版で既に捕まった)。**実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい**(R228)。
- **★「軸を足した」と書くだけでは不十分。壊して実際に発火するところまで見ること**(R240 の教訓)。作業役が自分で足した母数軸が**死んだ軸**でした(`exp.all.length` が常に104で、壊しても発火しない)。**母数は必ず画面(DOM)から数えること**。
- **★表示側で動かせる限界を見極めること**(R241)。城崎・箱根の初期5枚は **60〜149 の帯にカードが1枚も無い**ため、表示条件をどういじっても増えません。**基準を下げて枚数を増やすのは「人気の基準を甘くする」行為で禁止**。
- **★検査の高速化は R240 で一区切り**(616.5s → **361.1s**)。最遅は `check-pinflash`(41.2s)・次点 `check-attrib`(27.1s)で削る余地は小さい。**みのるんが朝起きて見るのは画面です**。画面側か設計思想に関わるものを選ぶこと。
- **★削減量は「削る対象の秒数」ではなく A/B 実測で確かめる**(R239 の教訓。司令塔の「32.7s縮む」という見込みは実測 **2.1s** = 環境ゆらぎより小さかった)。**★速さのために検査を弱めない**(`force: true` は採らない。アクショナビリティ判定を飛ばすのは検査を弱める行為)。
- **★部分的な確認では見つからない破損がある。何かを全件確認する必要があるときは、機械的に全件突合すること**(R233-3。「前後数件を突合せよ」では**63%の破損を発見できませんでした**)。
- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226)。**本数の下限と中身の照合を別項目で持つ**。**R216・R234・R238・R241 では有無軸が全緑のまま中身軸だけが壊れを検知しました**。
- **★「作らない判断」も成果**(R232。**常に赤い検査は緑の検査より危険**)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231)。**★数え方の定義を実装と一致させる**(R226)。**★件数を引き継ぐときは、引き継いだ側が実物を開いて形を確かめること**(R216)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。**★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228・R237 では起票文の前提自体が外れました)。**★変更の影響は、関係する全ての分岐を列挙してから数える**(R227)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229・R216)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226・R234・**R242** はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225。export されている=使われている、ではない)。**同様に「検査があるから守られている」も疑う**(R234 で `check-nosummary.mjs:146` の `.every()` が死んでいた)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に8件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218)。**新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226・R234・R241 はいずれも新規ファイル0件)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232)。基準値は**毎サイクル自分で `md5sum` を取り直すこと**(2026-09-24 実測: `assets\app.js` = `b994a2fa77332fbaad2c368560ac758c`(**R245 時点の値。着手時に必ず自分で取り直すこと**)、`assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583`、`assets\ui.css` = `c3db293066104848b317b7d04798638d`)。**ピンのCSSは `assets\style.css` にあります**(`ui.css` は該当0件)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。**本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

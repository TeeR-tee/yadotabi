# NEXT: 2026-09-24 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にあります。**先頭の目次は `##` 見出しの48件**です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 草津の1位「湯畑」の説明 | **1行も出ていなかった** | **`Wikipediaに記事がありません。写真と地図の情報で表示しています。`**(R234) |
| 人気バッジが初期5枚で出る枚数 | **8/25枚**(草津 **0/5**) | **10/25枚**(草津 **1/5**・別府 2→3)(R241。**しきい値も文言も1つも変えず**) |
| 「もっと見る」を押す前の手がかり | **`もっと見る` の5文字だけ** | **`この先にあるのは歴史を歩く、景色を見にいくです`**(R236・**件数の数字0文字**) |
| 画面末尾の注記が名乗る「順位の作り方」 | 8つの要素のうち**2つしか名乗らず**(しかも逆を書いていた) | **4種類に畳んで全部を名乗る**(R246・順位は1件も動かさず) |
| 注記から飛べる README の順位の説明 | 画面と**正反対**・8つの重みのうち**3つしか名乗らず** | **画面と同じ4種類で説明**(R247)+**`docs/check.mjs` に4軸追加** |
| `docs/CHECKS.md` の行番号による道案内 | **7種類9箇所が9箇所とも指す先を外していた** | **全件を実測値に訂正**+**双子の行も併記**(R249) |
| 検査本の中の「構造上必ず true の照合」 | **`check-nosummary.mjs` の1本が1度も文言を見ていなかった** | **枚数/母数/中身の3軸に分割**(28→30 pass)+**同じ形を全35本から静的に探す軸**(85→**88 OK**)(R235) |
| 展開後に地図へ増える「・」ピンの説明 | **画面に1文字も無し** | **`地図の小さい点は、この下に続けて並んでいる場所です`**(R250。**`.pin--sub` が実在するときだけ**・件数の数字0文字) |
| 直近の通し | — | **35本全PASS・388.8s**・**★FLAKY 0本**(R250。**8サイクル連続0本**) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。



## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R254** — **展開後の見出しだけが「7件」「5件」と数字を名乗るのに、その数え方の中身は画面に1文字も無い**

**なぜこれを選んだか(★計画役が本番アプリと実データを実測し、R251・R252 を先に潰して選びました)**:

- **★実測1(R251 の前提は外れていました。だから選びません)**: 起票は「展開後のカードに着地の印が無い」と書いていますが、`goToCardFromPin` は `els.feedList.querySelector('.feedcard[data-index="…"]')` で**添字だけでカードを引き**、`feedcard--flash` を付けます。`cardHtml` は `showNo = withNo !== false` で**番号バッジだけを落とし、`data-index` は残す**(コメントも「data-index は残す」と明言)。よって**6件目以降でも着地の縁は出ます**。**R251 は起票の前提が誤りなので、このサイクルでは選びません**(残す理由は積み残しに書き直しました)。
- **★実測2(R252 も前提が外れていました。だから選びません)**: `fixtures\kusatsu.json` の `backlinks` は**配列ではなくキー28件のオブジェクト**で、`null` が **0件**・`0` が **0件**でした(5エリアとも同じ形)。湯畑は `'湯畑' in backlinks === false` = **キーごと存在しません**。つまり「未取得か少ないか」ではなく**そもそも引かれていない**状態で、これを画面で語ると **R245「画面に出ていない印について語らない」に正面から衝突**します。**方針が立たないので選びません**。
- **★実測3(これが今回の1件。画面で見ました)**: `screenshots\2026-09-20T15-54-01_r60-expanded_mobile.png` を Read で開くと、展開後の帯に **`そのほか 5件`** と **`写真と解説がまだ無い場所 7件`** の見出しが並び、**数字が画面に出ています**。コードでも `moreBundledHtml` の3分岐すべてが `'<span class="feedbundle__n">' + escapeHtml(String(b.indices.length)) + '件</span>'` を出しています(`feedbundle` / `feedbundle--rest` / `feedbundle--bare` の3本とも)。
- **★実測4(比べると食い違っています)**: 同じ「もっと見る」まわりで、**押す前**の R236 の行 `この先にあるのは歴史を歩く、景色を見にいくです` は**件数の数字を0文字**にすると決めて作られました。**押した後の見出しだけが数字を出しています**。さらに R250 で足した3行目 `地図の小さい点は、この下に続けて並んでいる場所です` は「この下」を**ひとつながりの並び**として説明していますが、**実際の「この下」は4本の見出しで切り分けられています**。**同じ画面の中で、数字の出し方と「下に何があるか」の説明が揃っていません**。
- **★実測5(設計思想との照合)**: みのるん本人の言葉「**『60何件見つかりました』だと『何をもって60何件なんですか』という話**」がそのまま当たります。`そのほか 5件` の **5** が何を数えた5なのか(テーマが付かなかったカード/1件だけだったテーマから送られてきたカード)は画面のどこにも書かれていません。**数え方を名乗らない数字が、画面でいちばん目立つ見出しに出ています**。
- **★実測6(判断が要るかの切り分け)**: **みのるんの判断は不要です**。変えるのは `moreBundledHtml` が組む**見出しの中の `feedbundle__n` の中身だけ**で、**束分けの結果・カードの枚数・並び順・`data-index`・ピンの位置は1つも変わりません**(`assets\geo.js` / `assets\engine.js` / `fixtures\` は1バイトも触りません)。`.feedbundle__n` の CSS は `assets\ui.css` に既にあり(**変更禁止なので触りません**)、中の文字列だけを替えます。

- **タスクID**: R254 / **目的**: **展開後の束見出しから「◯件」の数字を落とし、R236・R250 と同じ「数字を出さない」言い方にそろえる**。**枚数は画面のカードを数えれば分かるため、情報は失われません**。
- **対象ファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(**主**。`moreBundledHtml` の中の `feedbundle__n` を出している**3分岐すべて**。**★行番号では指しません。`feedbundle__n` と `BARE_BUNDLE_HEAD` を grep して場所を自分で特定すること**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\check.mjs`(**R254 軸を現在の末尾(`// --- ここまで R250 ---` の後ろ)に新ブロックとして足す**。**R235・R248・R249・R250 ブロックは1バイトも変えない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(**R254 を新規起票して `[x] 2026-09-24` に。R251・R252 は「前提が外れていた」ことを1行ずつ添えて `[-]` にする**。**R225・R244・R211・R102 は起票のまま残す**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-24 R254 …` 見出し+3行以上)
- **完了条件(検証可能な形)**:
  1. **★見出しから数字が1文字も出なくなること**。`そのほか 5件` → `そのほか`、`写真と解説がまだ無い場所 7件` → `写真と解説がまだ無い場所`、テーマ名の束も同様。**`feedbundle__n` の span ごと出さないか、中身を数字以外にするかは作業役が決めてよい**が、**「たくさん」「少し」のような量の評価語も禁止**(R227・R231)。**代わりの数字(「5〜」「約5」)も禁止**。
  2. **★見出しそのものは消さないこと**。R226・R158 で「どこから先が写真も解説も無い帯か」を区切るために入れたもので、**束の構造は1つも変えません**。
  3. **★画面に出ていないものを名乗らないこと**。束が0件のエリアでは**従来どおり見出しを作らない**(`kept.filter` と `if (bareIdx.length)` の既存の振る舞いを変えない)。
  4. **★同じサイクルで守る検査も足す**。**`docs\check.mjs` の末尾に R254 軸**を足す。軸は **(a) 母数軸** = `assets\app.js` を `readTarget` で読めていて、`moreBundledHtml` の中に見出しを組む分岐が **2本以上実在すること**(**件数の定数を書かず、実ファイルから数えること**)、**(b) 数字軸** = 見出しを組んでいる式に **`件` と `b.indices.length` が現れないこと**、**(c) 見出し存続軸** = `BARE_BUNDLE_HEAD` と `そのほか` が見出しの文言として**依然として実在すること**(1 の直しで見出しごと消していないことの担保)。
  5. **★(a)母数軸と(b)(c)中身軸はセットで維持すること**。R235 の破壊実証で、**母数軸を壊すと中身の軸が「1本で0件」の偽の緑になる**ことが分かっています。**片方だけ消すと軸が死にます**。
  6. **★足した軸が本当に発火するか、壊して確かめること**。軸ごとに**それだけを壊して発火する**ことを pass/fail で示す。**★軸の母数は「実在する側」から取ること**(空になりうる定数配列や engine の再計算値から取らない)。**★DOM を数える軸を書く場合は、その時点で DOM が最新かを確かめること**(R250 の教訓。凡例が `renderFeedMap` より先に組まれていて**1つ前の描画の地図**を数えていました)。
  7. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail で対比**する。**★その壊し方が本当に旧版を素通りするかを先に確かめること**(旧版は `git show HEAD:docs/check.mjs` で取り出す。**`git checkout`/`stash`/`reset` は使わない**)。**司令塔が推した壊し方は10サイクル中8回外れています**。捕まるなら**自分で作り直すこと**。
  8. **★撮影と目視は必須**(画面に出るものを変えるため)。**5エリア**(草津・箱根・道後・別府・城崎)を `?fixture=<エリア>` で撮影し、**展開後**を撮って **Read で実際に画像を開いて目視**する。画面幅は **mobile(375px)と desktop の両方**。見るのは「見出しから数字が消えているか・見出しが残っているか・文字崩れ・重なり・はみ出し・数字を消した分の余白が不自然でないか」。**展開前も1エリアだけ撮り、R250 の3行が壊れていないことを確認**する。
  9. **★順位不変の実証**: `node scripts\dump-rank.mjs` を変更前後で走らせ、**出力がバイト一致**することを示す(**R250 実測値 `24c9099c4f40da3e1de6a1582d4e86dd`**。**鵜呑みにせず自分で取り直すこと**)。`git diff assets/geo.js assets/engine.js fixtures/` が**空**であること。
  10. **`assets\app.js` 以外の `assets\` を一時的に書き換えたら md5 で完全復元を確認**(**基準値は着手時に自分で `md5sum` を取ること**。下の値は 2026-09-24 の計画役の実測で参考値です)。`assets\style.css` / `assets\ui.css` / `assets\tokens.css` / `assets\engine.js` / `assets\geo.js` は**着手時と完了時で md5 一致**であること。
  11. `node scripts\check-all.mjs` が **35本中35本PASS または FLAKY(exit 0)**。**本数は35本のまま**(**新規ファイルを作らない**)。
  12. **★FLAKY の本数を必ず NIGHTLOG に記録**(**R250 までで8サイクル連続0本**)。
- **検証手順**:
  - 撮影URL: `?fixture=kusatsu` / `?fixture=hakone` / `?fixture=dogo` / `?fixture=beppu` / `?fixture=kinosaki`。**「もっと見る」を押してから**撮る。画面幅は **mobile(375px)と desktop**。
  - `node docs\check.mjs` 単体で OK/NG と秒数を記録(変更前後。**R250 時点は 91 OK / 0 NG**)。
  - `node scripts\dump-rank.mjs` を変更前後で取り、**バイト一致**を示す。
  - `node scripts\check-all.mjs` の通しを1回(秒数と FLAKY 本数を記録。**R250 は 388.8s**)。
  - **着手時の md5 参考値**(2026-09-24 計画役の実測。**必ず自分で取り直すこと**): `assets\app.js` = `21ad239465562870fb8dd3f76e2a1f81` / `assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583` / `assets\ui.css` = `c3db293066104848b317b7d04798638d` / `assets\engine.js` = `9c89f4a949651629f36bda4236e9f4a3` / `assets\geo.js` = `8dd8c3bb205542717b23404c35baa500` / `assets\tokens.css` = `50662697a27e389cc5f1bad50105f35d`。
- **変更禁止範囲(絶対に触らない)**: **`assets\geo.js` / `assets\engine.js` / `assets\style.css` / `assets\ui.css` / `assets\tokens.css`** / **`fixtures\*.json`(読むだけ・再生成しない)** / `index.html` / `404.html` / `README.md` / **`demo\*.html`** / `.github\workflows\*` / `docs\AUTOPILOT.md` / `docs\FIXTURES.md` / `docs\CHECKS.md` / `scripts\dump-rank.mjs` / **`scripts\check-*.mjs` の全34本と `check-all.mjs`(読むだけ)** / **`docs\check.mjs` の既存部分(R235・R248・R249・R250 ブロックを含む。今回足すのは `// --- ここまで R250 ---` より後ろの新ブロックだけ)** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記のみ許可) / **ROADMAP の既存起票すべて**(**R254 の新規起票+完了マークと、R251・R252 の `[-]` だけが例外**)。
  - **★上の禁止範囲は `docs\check.mjs` の末尾追記と矛盾しないことを計画役が確認済みです**(R250 で司令塔が「検査は `scripts\` に足す」と書きながら `docs\check.mjs` を変更禁止にして食い違った事故の再発防止)。**それでも矛盾を見つけたら報告した上で最小限の変更で進めてよい**(R228)。**★ファイルの場所は自分で grep して確かめてから書くこと**。**★行番号でコードを指さないこと**(今夜、行番号引用は30件以上すべてが外れていました。**関数名やコードの断片で指すこと**)。
- **所要目安**: 30〜40分(場所の特定と実測4分・見出しの直し5分・5エリアの撮影と目視10分・検査の軸追加と発火確認8分・破壊実証の旧版素通り確認4分・dump-rank と md5 3分・35本通し7分・記録とコミット5分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。**R248 で文書側は訂正済み。「呼ぶかどうか」は順位とリクエスト数に影響するため判断待ちのまま**です。
- **R244(判断が要ります)**: `fixtures\kusatsu.json` の `backlinks` が **28件**しかなく、他エリア(箱根73・道後70・別府49・城崎41)の半分以下。**初期5枚に 60〜149 の帯にいるカードが1枚も無いため、しきい値を下げない限りどんな条件でも増えません**。**fixture 再生成は順位を動かしうる**ため判断が要る。
- **R243 は「やらない」と R249 で決着済み**。**R217 は見送り済み**。**R233 は残件0件で完了**。
- **★R251 は前提が外れていたので閉じます**(2026-09-24 計画役が実測): `goToCardFromPin` は `data-index` だけでカードを引き、`cardHtml` は `withNo=false` でも `data-index` を残すため、**6件目以降でも `feedcard--flash` の縁は出ます**。**残る差は「`.feedcard--flash` は静止した2px の縁なのに対し `.pin--flash` は 1s のアニメーション」という演出の強さだけ**で、直すには **`assets\style.css`(変更禁止)** を触る必要があります。**着手するなら禁止範囲の見直しが要る**ため、いまは起票を `[-]` にして理由を残します。
- **★R252 も前提が外れていたので閉じます**(2026-09-24 計画役が実測): `fixtures\*.json` の `backlinks` は**オブジェクト**で、5エリアとも **`null` 0件・`0` 0件**。湯畑は**キーごと存在しません**。「未取得か少ないか」ではなく**引かれていない**ので、画面で語ると **R245 と衝突**します。**語らないのが正解**と判断しました(R232「作らない判断も成果」)。
- 判断不要で残っているもの: **R211**(dump-rank に日時とコミットID)、**R102**(list-shots.mjs)。**どちらも開発者用ツールで画面に出ません**。
- **★R254 からの申し送り(次の計画役へ)**: **本番アプリを設計思想に照らして見る方法が今夜も当たりました**。**ただし起票の本文を信じてはいけません。今回 R251・R252 は2件とも前提が実測で覆り、代わりに画面を見て見つけた R254 が残りました**。バックログが尽きたと感じたら**まず画面を開き、次に起票の前提を自分で測ること**。**「押した後・開いた後」に加えて、「同じ画面の中で流儀が揃っていない箇所」(数字を出す/出さないなど)が狙い目です**。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★「実在する側から母数を取る」を実装するとき、タイミングも実在のうち**(R250 の教訓)。凡例は `renderFeed()` の中で `renderFeedMap()` より**先**に組まれていたため、そのまま DOM を数えると**1つ前の描画の地図**を見ていました(組み立てを `renderFeedOrigin()` に切り出し、地図の**後**に呼んで解決)。**DOM から母数を取る軸を書くときは、その時点で DOM が最新かを必ず確かめること。**
- **★司令塔が推す壊し方は7サイクル連続で旧版に捕まっている。着手前に必ず旧版で素通りするか実測し、捕まるなら自分で作り直すこと。**
- **★司令塔の指示書そのものにも誤りがある(今夜8回目)**(R241 で2件: 抑制規則の出典を「R231」としたが実際は **R224**・推した壊し方(B)が旧版で既に捕まった)。**実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい**(R228)。
- **★「軸を足した」と書くだけでは不十分。壊して実際に発火するところまで見ること**(R240 の教訓)。作業役が自分で足した母数軸が**死んだ軸**でした(`exp.all.length` が常に104で、壊しても発火しない)。**母数は必ず画面(DOM)から数えること**。
- **★表示側で動かせる限界を見極めること**(R241)。城崎・箱根の初期5枚は **60〜149 の帯にカードが1枚も無い**ため、表示条件をどういじっても増えません。**基準を下げて枚数を増やすのは「人気の基準を甘くする」行為で禁止**。
- **★検査の高速化は R240 で一区切り**(616.5s → **361.1s**)。最遅は `check-pinflash`(41.2s)・次点 `check-attrib`(27.1s)で削る余地は小さい。**みのるんが朝起きて見るのは画面です**。画面側か設計思想に関わるものを選ぶこと。
- **★削減量は「削る対象の秒数」ではなく A/B 実測で確かめる**(R239 の教訓。司令塔の「32.7s縮む」という見込みは実測 **2.1s** = 環境ゆらぎより小さかった)。**★速さのために検査を弱めない**(`force: true` は採らない。アクショナビリティ判定を飛ばすのは検査を弱める行為)。
- **★部分的な確認では見つからない破損がある。何かを全件確認する必要があるときは、機械的に全件突合すること**(R233-3。「前後数件を突合せよ」では**63%の破損を発見できませんでした**)。
- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226)。**本数の下限と中身の照合を別項目で持つ**。**R216・R234・R238・R241 では有無軸が全緑のまま中身軸だけが壊れを検知しました**。
- **★「作らない判断」も成果**(R232。**常に赤い検査は緑の検査より危険**)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231)。**★数え方の定義を実装と一致させる**(R226)。**★件数を引き継ぐときは、引き継いだ側が実物を開いて形を確かめること**(R216)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。**★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228・R237 では起票文の前提自体が外れました)。**★変更の影響は、関係する全ての分岐を列挙してから数える**(R227)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229・R216)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226・R234・**R242** はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225。export されている=使われている、ではない)。**同様に「検査があるから守られている」も疑う**(R234 で `check-nosummary.mjs:146` の `.every()` が死んでいた)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に8件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218)。**新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226・R234・R241 はいずれも新規ファイル0件)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232)。基準値は**毎サイクル自分で `md5sum` を取り直すこと**(2026-09-24 実測: `assets\app.js` = `b994a2fa77332fbaad2c368560ac758c`(**R245 時点の値。着手時に必ず自分で取り直すこと**)、`assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583`、`assets\ui.css` = `c3db293066104848b317b7d04798638d`)。**ピンのCSSは `assets\style.css` にあります**(`ui.css` は該当0件)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。**本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

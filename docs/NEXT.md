# NEXT: R117 上位30件に「宿そのもの」が混ざっている(ホテル・旅館を候補から落とす)

- タスクID: **R117** / 難易度: **sonnet**(engine.js の配列1つ + 判定数行 + 検査追加。R115 と同型)
- 所要目安: 25〜35分(実装10分 + 4エリア dump-rank 比較10分 + 撮影/check-all)

## 目的

やどたびは「宿を選んだあと、その**周り**に何があるか」を出すサイト。なのに実測で上位30件に
**競合の宿が5件**並び、うち4件は「♨温泉」ラベルで出ている。ユーザーには温泉スポットに見えるが中身は他社の宿。
R115 が「温泉のまま残した5件」と記録したうち4件は、ラベルではなく**そもそも出してはいけない種別**だった。

## 実測で判明した前提

### 1. 原因は `EXTRACT_KEYWORD_NG` に宿泊施設の語が1つも無いこと

`assets/engine.js:200-220` の `EXTRACT_KEYWORD_NG`(冒頭文にこの語があれば記事を落とす)を
全行読んだが、**「ホテル」「旅館」「宿」に類する語は1つも無い**。
政治家・鉄道駅・河川・病院・百貨店などは落ちるのに、宿泊施設だけ素通りする。
`assets/engine.js:437 isExcludedArticle()` は
`isExcludedName(名前)` → `isProtectedName()` で保護なら通す → `EXTRACT_KEYWORD_NG` 走査、の順。
7件の名前を `NAME_PROTECT_SUFFIX`(`engine.js:141`)に当てた実測は**7件とも protected=false**
(「天成園」の「園」は保護語に無く、「一の湯」の「湯」も `engine.js:153` のコメントどおり意図的に外してある)。

### 2. 該当は4エリアで7件ちょうど・観光対象の誤爆は0件

定義文(extract 先頭120字を最初の句点まで、= R115 の `definitionScope()` と同じ範囲)に
`ホテル / 旅館 / 温泉宿 / ペンション / 民宿 / ゲストハウス / 宿泊施設` のいずれかを含む
wiki 記事を4 fixture 全件(50件×4=200記事)に当てた実測:

| エリア | 記事名 | 当たった語 | 定義文(実テキスト) |
|---|---|---|---|
| kusatsu | 万座プリンスホテル | ホテル | 万座プリンスホテル（まんざプリンスホテル）は、群馬県吾妻郡嬬恋村の万座温泉にある**ホテル**。 |
| kusatsu | 渋峠ホテル | ホテル | 渋峠ホテル（しぶとうげホテル）は、長野県下高井郡山ノ内町と群馬県吾妻郡中之条町の境にある渋峠に位置する**ホテル**である。 |
| hakone | 天成園 | ホテル | 天成園（てんせいえん）は、神奈川県足柄下郡箱根町湯本の箱根湯本温泉にある温泉**ホテル**。 |
| hakone | 一の湯 | 旅館 | 一の湯（いちのゆ）は神奈川県箱根町の塔ノ沢温泉にある、株式会社一の湯が経営する、1630年（寛永7年）創業の老舗温泉**旅館**である。 |
| hakone | ヒルトン小田原リゾート&スパ | ホテル | ヒルトン小田原リゾート&スパ（Hilton Odawara Resort & Spa）とは、神奈川県小田原市にあるヒルトングループのリゾート**ホテル**。 |
| beppu | 杉乃井ホテル | ホテル | 杉乃井（すぎのいホテル）は、大分県別府市の別府八湯のひとつ観海寺温泉にある大型リゾート**ホテル**である。 |
| beppu | 大江戸温泉物語 別府清風 | ホテル | 大江戸温泉物語 別府清風（おおえどおんせんものがたり べっぷせいふう）は、大分県別府市北浜にある温泉**ホテル**である。 |

**dogo は該当0件**(差分が出ないエリアの対照になる)。
200記事のうち上記7件以外は1件も当たらない = **観光対象の誤爆0件**。

### 3. OSM 側の救済(R80 `hasOsmTagEvidence`)は効かない = 落ちたものは復活しない

fixture の生データを当たったところ、7件に対応する OSM 要素は
`hakone node/4585443307`(`amenity=public_bath` / `name=天成園 屋上浴場`)と
`hakone node/13779829434`(`amenity=public_bath` / `name=天成園足湯 (天の足湯)`)の**2件だけ**で、
どちらも**名前が記事名「天成園」と完全一致しない**(`hasOsmTagEvidence` は `engine.js:491-494` のとおり
wikipedia タグでの名指しか正規化名の**完全一致**しか認めない)。残り6件の OSM 要素は4エリアとも**0件**。
よって救済経路は発動しない。かつこの2件は**日帰り入浴施設として残るのが正しい**
(宿泊しなくても使える観光対象)。宿の記事だけ消え日帰り入浴施設は残る、という期待どおりの分かれ方になる。

## 対象ファイル(絶対パス)

- `C:\workspace\claude\旅行先用サイト\yadotabi\assets\engine.js` (本体・`EXTRACT_KEYWORD_NG` と周辺コメントのみ)
- `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-engine.mjs` (ケース追加。既存ケースは1件も減らさない)
- `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md` / `docs\NIGHTLOG.md` (記録)

## 実装方針

1. `assets/engine.js:219`(`'研究施設である', '陸上競技場'` の行)の直後に **R117 の1ブロック**を追記する。
   足す語は上の実測で当たった**7件を説明できる最小の集合**にする:
   `'ホテル'` / `'旅館'` / `'温泉宿'` / `'ペンション'` / `'民宿'` / `'ゲストハウス'` / `'宿泊施設'`。
2. **ただし `EXTRACT_KEYWORD_NG` は `extract` 全体を走査する**(`engine.js:444-449`)ので、
   このまま「ホテル」を足すと「〜ホテルに隣接している」「かつてホテルだった建物を活用した美術館」
   のような**二文目以降の言及**で正当な観光対象を巻き込む(R115 が万座プリンスホテルの二文目で
   同じ罠を避けた前例あり)。そこで **`EXTRACT_KEYWORD_NG` の走査範囲は変えず**、R115 の
   `definitionScope()`(`engine.js:535`)を使う**別の配列**として足すこと:
   - 新配列 `DEFINITION_LODGING_NG` を `EXTRACT_KEYWORD_NG` の直後に置く。
   - `isExcludedArticle()`(`engine.js:437`)の `EXTRACT_KEYWORD_NG` ループの**直後**に、
     `definitionScope(t, e)` が `DEFINITION_LODGING_NG` のいずれかを含むなら `return true` する数行を足す。
   - `isProtectedName()` の保護は**先に効かせたまま**順序を変えない(実測では7件とも素通りするが、
     将来「〇〇温泉」名の保護対象を守るため)。
3. コメントに「なぜ定義文の一文目に限るのか」と上の7件の実測結果を短く残す。
4. `scripts/check-engine.mjs` の `(r115)` 節(`check-engine.mjs:344`)の下に **`(r117)` 節**を新設し、
   **実 fixture の冒頭文をそのまま使って**以下を検査する:
   - 落ちる7件(上表の定義文そのまま)がすべて候補から消えること
   - 落ちない対照: 「万座温泉スキー場に隣接している」を二文目に持つ文で**落ちない**こと
   - 落ちない対照: `天成園 屋上浴場` / `天成園足湯 (天の足湯)` の OSM 候補(extract 無し)が残ること
   - 落ちない対照: 「草津温泉」「尻焼温泉」「花敷温泉」(定義文が「温泉である」「温泉。」)が残ること

## 影響を受ける候補の全件リスト

### 直す側(消える。全7件・全て source=wiki 単独)

| エリア | 現在の位置 | 名前 | 現在のカテゴリ |
|---|---|---|---|
| hakone | **cards 9位** | 天成園 | 温泉 |
| hakone | **cards 10位** | 一の湯 | 温泉 |
| hakone | **cards 25位** | ヒルトン小田原リゾート&スパ | スポット |
| beppu | **cards 18位** | 杉乃井ホテル | 温泉 |
| beppu | **cards 19位** | 大江戸温泉物語 別府清風 | 温泉 |
| kusatsu | 60件外(cards にも more にも無し) | 万座プリンスホテル | — |
| kusatsu | 60件外(cards にも more にも無し) | 渋峠ホテル | — |

### 残す側(消えてはいけない。ここが1件でも消えたら失敗)

- **温泉カテゴリの本物(wiki)**: 草津温泉 / 尻焼温泉(kusatsu cards 6位「尻焼温泉 川風呂」) /
  花敷温泉(kusatsu cards 23位) / 応徳温泉 / 道後温泉 / 別府温泉 / 竹瓦温泉(beppu cards 3位) /
  浜脇温泉(beppu cards 14位) — 定義文が「温泉である」「温泉。」「共同温泉である」で宿語を含まない。
- **日帰り入浴施設(OSM 由来・extract が無いので走査対象外)**: 天成園 屋上浴場 / 天成園足湯 (天の足湯) /
  箱根湯寮(hakone 16位) / かっぱ天国(hakone 20位) / 大滝乃湯(kusatsu 7位) / 御座之湯(kusatsu 30位) /
  椿の湯(dogo 28位) / 媛彦温泉(dogo 16位) / やまなみの湯(beppu 24位) / 弥坂湯 / 姫之湯 / 不老泉 ほか。
- **dogo は全30件が無差分であること**(該当0件なので差分が出たら誤爆の証拠)。
- R115 でスポットに直った4件(草津温泉バスターミナル / 草津温泉スキー場 / 冠山 (松山市) / 鶴見園)は
  今回も**スポットのまま残る**こと。

## 完了条件(4エリアの dump-rank 差分が期待どおり)

`node scripts/dump-rank.mjs <area>` を**変更前に4エリア分保存**してから実装し、変更後と全件比較する。

- **kusatsu**: cards・more とも **完全に無差分**(該当2件は元々60件外)
- **dogo**: cards・more とも **完全に無差分**(該当0件)
- **hakone**: cards から 9位「天成園」・10位「一の湯」・25位「ヒルトン小田原リゾート&スパ」の**3件が消え**、
  以降が繰り上がり、more の先頭から3件が cards に入る。
  **カテゴリ多様性減点(`CATEGORY_PENALTY`)で温泉枠が空くため並び順が変わりうる**ので、
  繰り上がった候補を全件目視して観光対象でないものが入っていないか確認すること。
- **beppu**: cards から 18位「杉乃井ホテル」・19位「大江戸温泉物語 別府清風」の**2件が消え**、
  以降が繰り上がり、more の先頭から2件が cards に入る。同じく全件目視。
- 上記以外に消えた候補が**1件もない**こと。

## 検証手順

1. 実装前に `node scripts/dump-rank.mjs kusatsu|hakone|dogo|beppu` を4本とも保存(before)。
2. 実装 → `node --check assets/engine.js`。
3. 4エリア dump-rank(after)を取り、before と全件比較して上の完了条件を満たすか確認。
4. 撮影(`node C:\workspace\tools\shot\shot.mjs <URL> --mobile` と PC幅 **1280**)し、**画像を Read で開いて目視**:
   - `http://127.0.0.1:3000/?fixture=hakone` を **mobile(375) と desktop(1280)** — 宿が消えてカード30枚のまま・文字崩れ/はみ出し無し
   - `http://127.0.0.1:3000/?fixture=beppu` **mobile(375)** — 同上
   - `http://127.0.0.1:3000/?fixture=kusatsu` **mobile(375)** — 無差分エリアのデグレ確認1枚
5. `node scripts/check-engine.mjs` が全 pass / 0 fail(既存245件を1件も減らさず `(r117)` 分が増える)。
6. **`node scripts/check-all.mjs` が 29本すべて緑**(必須)。外部APIは**0回**(すべて fixture 経由)。

## 変更禁止範囲

- **rank の重み・閾値は変更不可**(`engine.js:256-266` の `WEIGHT` / `CATEGORY_FREE_SLOTS` /
  `CATEGORY_PENALTY` に一切触れない)。並び順が変わるのは候補が減った結果で、重みで整えてはいけない。
- `assets/geo.js` 変更不可(`CATEGORY_RULES` / `buildOverpassQuery` 含む)。`fixtures/*.json` 変更不可(再生成もしない)。
- `WIKI_CATEGORY_HINTS`(`engine.js:235`)の `deny` は**触らない**(今回は候補ごと落とすので不要)。
- `git stash` / `git reset` / `git checkout` でファイルを戻す操作は**禁止**。外部API呼び出しは**0回**。
- 既存 `scripts/check-*.mjs` の検査を**減らさない**(追加のみ)。

## 終わったら

1. `docs/ROADMAP.md` の R117 行を **`- [x] 2026-09-16 R117 …`** に更新する。
2. `docs/NIGHTLOG.md` の**ファイル末尾**の「## サイクル記録」節の**末尾**に3行追記
   (やったこと / 見た目の確認結果 / 次)。ファイル先頭に新しい節を作らないこと。
3. **先にコミット**する(1行の日本語メッセージ)。
4. `git push`。
5. 報告は簡潔に(長文の報告書を書かない)。

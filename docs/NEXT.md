# NEXT: R126 朝のまとめを現在地まで更新し、NIGHTLOG の読む順序を整える

- **タスクID**: R126
- **難易度**: sonnet(文書のみ・コード変更なし)
- **所要目安**: 25〜35分
- **種別**: ドキュメント整備(`docs/NIGHTLOG.md` と `docs/AUTOPILOT.md` の2ファイルのみ)

## 目的

みのるんが朝いちばんに読むのは `docs/NIGHTLOG.md` の冒頭「朝のまとめ」だけ。ところがそれが20サイクル以上古く、**直近10サイクルで提案の中身が一番良くなった話が1文字も載っていない**。夜の成果が朝に伝わらないのを直す。過去の記録は消さず、**構成と要約だけを改善する**。

## 実測で判明した前提(2026-09-16 23:10 時点・計画役が計測)

- `docs/NIGHTLOG.md` は **591行**。節構成は以下(行番号は現状値、編集で動くので都度 grep し直すこと):
  - `3: ## 朝のまとめ(2026-09-16 06:10 司令塔が記入)` … 以下 `### 触ってみるURL` `### 夜にやったこと` `### 朝の相談` `### 正直に書いておくこと`
  - `54〜100`: R33/R35/R30/R25/R23 の詳細節(初期の並び。**そのまま残す**)
  - `101: ## サイクル記録` … `103` 以降が本体
  - `483: ### 2026-09-16 R93 …` が **`###` 見出しを持つ最後のサイクル**
  - `487: <!-- 以下は誤ってファイル先頭に積まれていたサイクル記録(R42〜R100)。司令塔が 2026-09-16 17:10 に末尾へ移動 -->`
  - `488〜591`: **R94〜R125 の36サイクル分(109行)が裸の `- ` 箇条書き**。`### ` 見出しが1つも無い
- 朝のまとめの数字は「**103サイクル・145コミット(23:25時点)**」だが、実際は `git log --oneline | wc -l` = **146コミット**、サイクル記録の最終は **R125**(最新3件は R123 / R124、直前が R120+R121)。
- `### 夜にやったこと` は **26項目**で、最後の項目26は「R14 固定データの軽量化 … R83 正直な代替文」。**R84 以降が全て欠落**しており、特に以下の「提案の中身が良くなった10サイクル」が朝に見えない:
  - R115/R119/R120 廃止・消滅した施設(スキー場・ロープウェイ・動物園・鉱山など)を定義文の述部で除外
  - R121 現存するが観光訪問が適切でない施設(国立療養所)を除外。重監房資料館は無傷であることを実測で担保
  - R123 「Wikipediaに記事がありません」の**誤表示26件**(kusatsu2/hakone6/dogo8/beppu10)を是正
  - R124 「記事はあります」と言いながらリンクが1本も無い**行き止まり13件 → 0件**(Wikidata 転送URLでフォールバック)
  - R116/R122 は未実装のまま残っている(誤爆リスクで見送り中)
- `### 朝の相談` は **5項目**。うち R19 だけが「案A/案B/案C+副作用+参照先」という**判断できる形**になっており、他4件は課題文のみで推奨が無い。**R125・R122・R116 は未掲載**。
- `docs/ROADMAP.md` は現在 **146行・完了124件 / 未完了11件**(R126 起票後)。未完了は R126 / R2-1 / R64 / R81 / R85 / R92 / R102 / R116 / R122 / R103 / R125。完了が完全に埋もれているわけではない(`## 完了(2026-09-16)` 節が101行目にある)ため、**今回 ROADMAP の構成は変更しない**。

## 対象ファイル(絶対パス)

- `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(主対象)
- `C:\workspace\claude\旅行先用サイト\yadotabi\docs\AUTOPILOT.md`(末尾「NIGHTLOG の書き方」に1行追記)
- `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R126 を `[x] 2026-09-16` にするだけ)

## 実装方針(3点。順にやる)

### (1) 朝のまとめを現在地まで更新する ← 最重要

- 冒頭の太字1行を実測値に直す。`git log --oneline | wc -l` と NIGHTLOG の最終サイクルID を**自分で数えて**書くこと(計画役の数字を鵜呑みにしない)。書式は現状を踏襲: `**朝まで<N>サイクル・<M>コミット(<時刻>時点、ループ継続中)。…**`
- `### 夜にやったこと` の項目26の**後ろに項目27以降を追記**する。既存26項目は**1文字も消さない・書き換えない**。
- そのうえで `### 夜にやったこと` の**直前**に、新しい小節 `### 直近10サイクルで何が良くなったか(提案の中身)` を1つ足す。ここが今回の本体。箇条書き4〜6行で、**技術用語ではなくみのるんが画面で確かめられる言葉**で書く。例(実測値は NIGHTLOG 末尾の該当サイクルから正確に拾うこと):
  - 「潰れたスキー場・閉園した動物園など、もう行けない場所がカードから消えた(4エリアで◯件)」
  - 「今も入所者が生活している療養所を『見どころ』として出していたのをやめた。学びの場である重監房資料館は残してある」
  - 「『Wikipediaに記事がありません』と嘘をついていたカード26件を是正した」
  - 「『記事はあります』と言いながら押せるリンクが無い行き止まりのカード13件を0件にした」
  - 対応する `?fixture=` のURLを1つ添えて、スマホで確かめられるようにする

### (2) 裸の箇条書きに見出しを補う(内容は消さない)

- `NIGHTLOG.md:488` 以降の36サイクル分について、各サイクルの**1行目の `- R<番号> …` の直前**に `### 2026-09-16 R<番号> <その行の要約を20字以内>` を挿入する。
- 見出しの元になった `- R<番号> …` の行は**そのまま残す**(消して見出しに置き換えない。本文の情報量を減らさないため)。
- 1サイクルの範囲は「`- R<番号>` で始まる行から、次の `- R<番号>` で始まる行の直前まで」。`- 次:` や `- 撮影は…` の継続行は直前のサイクルに属する。
- `487` の HTML コメントは**残す**(経緯の記録なので消さない)。
- 見出しを付けた結果 `### ` の総数が **36 増える**ことを `grep -c "^### " docs/NIGHTLOG.md` の前後比較で確認する。

### (3) 朝の相談を「判断できる形」に揃える

- 既存5項目の**文言は消さず**、各項目に `選択肢:` と `推奨:` の2行をぶら下げる形に整える(R19 が既に良い手本なので**R19 はそのままでよい**)。
- 未掲載の3件を追加する。本文は `docs/ROADMAP.md` の該当行から**事実だけを引き写す**(新しい主張を作らない):
  - **R125**(ROADMAP 最終行): 要約が無いカードのうち32件は OSM の `wikipedia` タグで記事名が判明しているが取りに行っていない。選択肢 = (A)geo.js に `titles=` の2段目を足す+fixtures 再生成 / (B)やらない。推奨はどちらか一方を選び、**geo.js 変更と fixtures 再生成とAPI呼び出し回数の3点でみのるんの承認が要る**ことを明記。
  - **R122**: 場所でない候補が上位に残っている2件(hakone 11位「石橋山の戦い」ほか)。誤爆リスクが高く見送り中である事実を書く。
  - **R116**: dogo 17位に一般名詞だけの「商店街」が出ている。同上。
- 各項目の末尾に `詳細: docs/ROADMAP.md の R<番号>` を付けて辿れるようにする。

### (4) AUTOPILOT.md に1行

`## NIGHTLOG の書き方` の1つ目の箇条書き(「1サイクル3行」の行)に、`### 2026-09-16 R<番号> <一言>` の見出しを**必ず付ける**旨を追記する。今回の裸箇条書きが再発しないようにするのが目的。

## 完了条件

1. `docs/NIGHTLOG.md` の冒頭の数字が実測の最新値(サイクルID・コミット数)になっている。
2. `### 直近10サイクルで何が良くなったか(提案の中身)` が新設され、R115〜R124 の成果が**画面で確かめられる言葉**で4〜6行書かれている。
3. `### 夜にやったこと` の既存26項目が**無傷**で、項目27以降が追記されている。
4. `grep -c "^### " docs/NIGHTLOG.md` が編集前より **36増えて**いる。
5. `### 朝の相談` に R125 / R122 / R116 が追加され、**全項目に `選択肢:` と `推奨:` がある**(R19 は既存の案A/B/Cで条件を満たす)。
6. `git diff --stat` の対象が `docs/NIGHTLOG.md` / `docs/AUTOPILOT.md` / `docs/ROADMAP.md` の**3ファイルのみ**。
7. **削除行が0行に近いこと**: `git diff --numstat docs/NIGHTLOG.md` の削除列を確認し、削除されるのは朝のまとめ冒頭の数字1行と朝の相談の整形分だけ。**サイクル記録の本文は1行も削除されていない**こと。

## 検証手順

1. `cd C:\workspace\claude\旅行先用サイト\yadotabi`
2. 編集前に `grep -c "^### " docs/NIGHTLOG.md` と `wc -l docs/NIGHTLOG.md` を控える。
3. `git log --oneline | wc -l` でコミット数、`grep -n "^- R" docs/NIGHTLOG.md | tail -3` で最終サイクルIDを実測する。
4. 編集後、`grep -n "^## \|^### " docs/NIGHTLOG.md` を出して**目次として読めるか**を自分の目で確認する。
5. `git diff --numstat` で削除行数を確認(完了条件7)。
6. **`node scripts/check-all.mjs` を実行し、29本中29本 PASS(exit 0)であることを必須の合格条件とする。** 文書のみの変更なので落ちるはずがなく、落ちたら環境フレークか誤って別ファイルを触っている。
7. 画面変更が無いため**撮影は省略してよい**(その旨を NIGHTLOG に書く)。外部API 0回。

## 変更禁止範囲

- `assets/` 以下は**1行も触らない**(`git diff --stat -- assets` が空であること)。
- `fixtures/` の変更・再生成は**不可**。
- `scripts/` `index.html` `demo/` `README.md` は今回**対象外**。
- rank の重み・閾値、`engine.js`、`geo.js` は**変更不可**。
- `git stash` / `git reset` / `git checkout` でのファイル復元は**禁止**。
- 外部API呼び出し **0回**。Overpass / Nominatim / Wikipedia を叩かない。
- **過去のサイクル記録は1行も消さない。** 要約に置き換えるのも禁止(見出しは「足す」だけ)。`487` の HTML コメントも残す。
- `## 朝のまとめ` は**常にファイル最初の節**のまま。新しいサイクル記録は**ファイル末尾**に足す。

## 終わったら

1. `docs/ROADMAP.md` の R126 を `- [x] 2026-09-16 **R126 …` に変える。
2. `docs/NIGHTLOG.md` の**ファイル末尾**のサイクル記録に**3行**追記する(やったこと / 確認結果 / 次)。見出し `### 2026-09-16 R126 朝のまとめの更新とNIGHTLOGの見出し整備` を付けること(今回のルールを自分で守る)。
3. **先にコミット**する(コミットメッセージは1行の日本語)。
4. `git push` する。
5. **報告は簡潔に**。長文の報告書は書かない。

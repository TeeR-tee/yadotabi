# NEXT: 2026-09-24 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にあります。**先頭の目次は `##` 見出しの48件**です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| JSが止まったときの画面 | 「🧳」の2文字 | **230文字の案内**(R215)+**検査で保護**(R221) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 「徒歩1分・75m」が何からの距離か | 画面に一度も書かれていない | **`距離は「(宿名)」からの目安です`**(R228) |
| 「もっと見る」の先の同じ形の帯 | 読み飛ばす手がかり無し | **`写真と解説がまだ無い場所 N件` の区切り**(R226・本番反映済み) |
| 草津の1位「湯畑」の説明 | **1行も出ていなかった** | **`Wikipediaに記事がありません。写真と地図の情報で表示しています。`**(R234・本番反映済み) |
| URLを間違えた人が見る画面 | GitHub既定の**英語**404 | **日本語の404ページ**+**リンク軸と中身軸の2軸検査**(R216・本番確認済み) |
| 地図の観光地ピン | 押しても何も起きない死んだピン | **押すと対応カードへ移動して光る+名前のツールチップ**(R237・本番で実タップ確認済み) |
| 展開後のピン104本 | 検査が触れていたのは5本だけ | **104本すべてを照合(3軸41項目)**(R238・破壊実証3通りで旧版は完全素通り) |
| 検査の通し時間 | 616.5s | **361.1s**(R239・R240。**照合は1つも減らさず**、固定待ちを「描けたか」を見る待ち方に置換) |
| 人気バッジが初期5枚で出る枚数 | **8/25枚**(草津 **0/5**) | **10/25枚**(草津 **1/5**・別府 2→3)(R241。**しきい値も文言も1つも変えず**) |
| 人気バッジ「★」が何を根拠にしているか | 画面に1文字も無し | **`★は、百科事典で他の記事からどれだけ触れられているかで付けています`**(R242・数字なし・本番反映済み) |
| 「もっと見る」を押す前の手がかり | **`もっと見る` の5文字だけ** | **`この先にあるのは歴史を歩く、景色を見にいくです`**(R236・**件数の数字0文字**・CSS と `index.html` は0バイト変更・本番反映済み) |
| ★の説明が、画面に★が0枚でも出てしまう問題 | **★0枚でも `★は、…` の行が出ていた** | **画面に★が1枚以上あるときだけ出す**(R245・本番で `page.route()` により★0枚を再現して検証済み・5エリアの表示はバイト一致で不変) |
| 直近の通し | — | **35本全PASS・384.8s**・**★FLAKY 0本**(R245) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。

## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R246** — **画面末尾の注記が、順位の作り方を実態より狭く説明している**

**なぜこれを選んだか(★計画役が実際にコードと画面を開いて実測してから決めました)**:

- **実測1(★(B) 「段の推移を検証できない」は、実測したら穴ではありませんでした)**: 申し送り2に従い、まず読み込み中の段を自分で捕まえにいきました。**`app.js:1622-1623` を読むと `loading` は `stage` が `loading/wikifirst/osm/wiki` のいずれかで、その間 `skeletonHtml()` が実カードの後ろに必ず付きます**。つまり**「読み込み中かどうか」は画面(DOM)に `.skeleton` の有無として出ており、検証できない状態ではありません**。検証できないのは「**★が付く前のカードが画面に出ている瞬間**」という、もっと狭い1点だけです。そしてその1点は **R245 が `page.route()` で作った★0枚の状態で既にカバー済み**(`check-fame.mjs` の★0枚軸)で、**段そのものを再現しなくても同じ画面状態を検査できています**。**申し送り1が警告したとおり再訪を増やすと `check-fame.mjs` がさらに遅くなる**(20.9s→24.6s の実績)ので、**同じものを2通りで測りにいく価値はありません**。**(B) は見送ります**(R232「作らない判断も成果」)。
- **実測2(★(D) R243 は今回も覆りませんでした)**: `app.js:1876` の `bindTooltip(..., { permanent: false })` を再確認し、押すまで出ず押した瞬間にカードへ飛ぶ構造が変わっていないことを確かめました。**覆す実測根拠は今回も無いので4サイクル目の見送り**です。
- **実測3(★(A) 本番アプリを設計思想に照らして見たら、画面が嘘をついている1行が見つかりました)**: **`app.js:1465-1470` の `noteHtml()`** が画面末尾に出している注記は、**`宿からの距離と種類の多様性で並べた暫定版です。有名な場所が下に来ることがあります。`** です。ところが **`assets/engine.js:581-648` の `WEIGHT` を実際に開いて数えると、順位を決めている要素は7つ**あり、注記が名指ししている「距離」(`DISTANCE_PER_KM: 6`)と「種類の多様性」(`CATEGORY_PENALTY: 18`)は**そのうち2つだけ**です。**残る5つのうち最大のものが `PARENT_MENTION: 80`(親記事でどれだけ語られているか)で、これは `WEIGHT` の中で最大の重み**(写真 `WIKI_IMAGE: 25` の3倍以上)。次いで **`BACKLINK_MAX: 18`(被リンク=知られ具合)**、`WIKI_IMAGE: 25`、`SOURCE_BOTH: 20`、`WIKI_SUMMARY: 15`、`OFFICIAL_SITE: 12`。
  - **★ここが今回の核心です**: `PARENT_MENTION` と `BACKLINK_MAX` は**まさに「有名さ」を順位に効かせている重み**です。にもかかわらず注記は「**有名な場所が下に来ることがあります**」と書いており、**「有名さは順位に使っていない」と読める説明**になっています。**同じ画面の上のほうでは R242 が入れた `★は、百科事典で他の記事からどれだけ触れられているかで付けています` が出ていて、被リンクを使っていることを認めています**。**1つの画面の中で、上では「知られ具合を見ている」と言い、下では「有名さは順位に反映されない」と言っている**状態です。
  - **これは R236・R245 と同じ型の3件目です**。R236 は「画面に無いテーマ名を名乗る」、R245 は「画面に出ていない印の説明を先に名乗る」、今回は「**画面で実際にやっていることと違う作り方を名乗る**」。**NIGHTLOG の R245 の申し送り「画面に出ているものと、画面で説明しているものが一致しているか」がそのまま当たった3件目**です。
- **実測4(直す場所と、既にある検査の場所を grep で確かめました)**: 注記の組み立ては **`app.js:1465-1470` の `noteHtml()` の1か所だけ**(`grep -n "feednote" assets/app.js` のヒットは `:1467` 本体と `:1709`・`index.html:85` の器のみ)。守る検査は **`scripts/check-feednote.mjs`(全138行・10項目)が既に35本の中にあり、同じファイルに足せます**(新規ファイル0件・**35本のまま**)。ただし**現状の `check-feednote.mjs` は「暫定版」「OpenStreetMap」「Wikipedia」の3語が入っているかと、位置(最下部にあるか)しか見ておらず、注記が名乗っている作り方が実態と合っているかは1項目も見ていません**。

- **タスクID**: R246 / **目的**: **注記の1文を、実際に順位を決めている要素と食い違わない表現に直す**。**具体的には「有名な場所が下に来ることがあります」という、有名さを順位に使っていないと読める一文を実態に合わせる**。**`WEIGHT` の値・順位ロジック・★バッジのしきい値と文言は1バイトも変えない**(画面の文字だけを直す)。
- **対象ファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(**`noteHtml()`(`:1465-1470`)の文言のみ**。`FAME_ORIGIN_TEXT`(`:1146`)・`badgeTextFor` 系(`:1160-1182`)・`renderFeed()` の `#feed-origin` ブロック(`:1676-1690`)は**触らない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-feednote.mjs`(**守る軸を足す**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(**R246 を新規起票して `[x] 2026-09-24`**。**R243・R244・R235・R211・R102 は起票のまま残す**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-24 R246 …` 見出し+3行以上。**目次エントリは `##` 見出しの48件のみ**)
  - `assets\style.css` / `assets\ui.css` / `index.html` は**触りません**(**新しい色もクラスも作らない**。既存 `.feednote` にそのまま相乗り)
- **完了条件(検証可能な形)**:
  1. **★まず現状を自分で実測する**。**`assets\engine.js` の `WEIGHT` を実際に開いて、順位に効いている重みを全件列挙し、それぞれが注記の文で説明されているかを表にする**(計画役の実測: `PARENT_MENTION: 80` / `WIKI_IMAGE: 25` / `SOURCE_BOTH: 20` / `CATEGORY_PENALTY: 18` / `BACKLINK_MAX: 18` / `WIKI_SUMMARY: 15` / `OFFICIAL_SITE: 12` / `DISTANCE_PER_KM: 6`。**再現しなければそちらを信じること**)。**注記が名乗っているのは距離と多様性の2つだけ**であることを確かめる。
  2. **★新しい文は、実際に効いている要素だけを名乗ること**。**画面に出ていない要素を名乗らないこと**(R236・R245 で2回続けてこの型の不具合が見つかっています)。**逆に、`PARENT_MENTION` と `BACKLINK_MAX` が効いている以上「有名さは順位に使っていない」と読める表現は残さない**こと。
  3. **★文言に評価の語・数字・専門用語を使わないこと**(R242・R245 と同じ制約)。**「被リンク」「スコア」「重み」「80点」のような語は禁止**。**しきい値・点数を画面に一切出さない**。**既に画面にある `★は、百科事典で他の記事からどれだけ触れられているかで付けています` と言い方をそろえる**のが安全(同じ概念を2通りの言葉で呼ばない)。
  4. **★注記の既存の3語(`暫定版`・`OpenStreetMap`・`Wikipedia`)と、`くわしい仕組み` のリンク(`https://github.com/TeeR-tee/yadotabi#仕組みかんたん解説`)は残すこと**。`check-feednote.mjs` の既存10項目が**1つも赤くならない**こと。
  5. **★5エリアの撮影と Read での目視**(検証手順を参照)。**375px で `#feed-note` が `scrollWidth <= clientWidth`(はみ出しなし)**であることを画像で確認する。**文が長くなるので折り返しを必ず目で見ること**。
  6. **★順位不変の実証**: `node scripts\dump-rank.mjs <エリア>` を**5エリア分**、変更前後で取って**バイト一致**(`md5sum`)。`git diff assets/geo.js assets/engine.js fixtures/` が**空**であること。
  7. **★同じサイクルで守る検査も足す**。`check-feednote.mjs` に **(a) 整合軸** = 「**注記の文に『有名な場所が下に来る』という、有名さを順位に使っていないと読める表現が無いこと**」、**(b) 禁止語軸** = 「**注記に数字・評価の語・専門用語(被リンク/スコア/重み 等)が出ていないこと**」を足す。**母数は必ず画面(DOM)の `#feed-note` の `textContent` から取ること**(`app.js` の定数を import して突き合わせると、定数を書き換えたときに検査も一緒にずれて死んだ軸になる。R240 の教訓)。
  8. **★足した軸が本当に発火するか、壊して確かめること**。**engine の再計算値ではなく画面(DOM)の値を見ること**。足した軸ごとに、**それだけを壊して発火する**ことを pass/fail で示す。
  9. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail で対比**する。**★その壊し方が本当に旧版を素通りするかを先に確かめること**(**司令塔が推した壊し方は連続で外れています**。捕まるなら**自分で作り直すこと**)。候補は **(A) 注記を R245 時点の文に戻す**(整合軸が発火。**旧版 `check-feednote.mjs` は3語と位置しか見ていないので素通りするはず**)と **(B) 新しい文に点数や「被リンク」の語を入れる**(禁止語軸が発火・整合軸は緑のまま)の2通り。**(A) が旧版で素通りすること**を先に確かめ、**R216・R234・R238・R241・R236・R245 と同じ「有無は緑・中身だけが赤」の形**を再現すること。
  10. **★`assets\` の一時変更は md5 で完全復元を確認**。**基準値は着手時に自分で `md5sum` を取ること**。**今回は `app.js` を本当に変えるので最終 diff は空になりません**。**`style.css` と `ui.css` は最終 md5 が着手時の基準値と一致する**こと(= 1バイトも変えない)。
  11. `node scripts\check-all.mjs` が **35本中35本PASS または FLAKY(exit 0)**。**本数は35本のまま**(**新規ファイルを作らない**)。
  12. **★FLAKY の本数を必ず NIGHTLOG に記録**(**R245 は0本**。今回も0本を保てるかを書くこと)。
  13. **本番へ push 後、実ブラウザで草津を1回開いて、画面末尾の注記を実際に読む**(AUTOPILOT ルール9)。
- **検証手順**:
  - 撮影は `node C:\workspace\tools\shot\shot.mjs <URL> --mobile`(375px)と PC幅。**URL は固定データで `http://localhost:<port>/?fixture=kusatsu`(および hakone / dogo / beppu / kinosaki)**。**注記は画面末尾なので、撮影は必ずページ最下部まで入る形にすること**(入らなければ `?demo=` 等ではなくスクロール後に撮る)。**5エリアすべて撮り、Read で画像を開いて目視**すること。**外部APIを叩く撮影は最大2回まで**(AUTOPILOT ルール4)。
  - `node scripts\check-feednote.mjs` 単体で pass/fail と秒数を記録(変更前後。**計画役実測: 現状10項目**)。
  - **壊し方(A)(B)を先に旧版で走らせて素通りを確認**(旧版は `git show HEAD:scripts/check-feednote.mjs` で取り出す。**`git checkout`/`stash`/`reset` は使わない**) → 新版で落ちることを確認(完了条件9)。
  - `node scripts\dump-rank.mjs` 5エリア分の変更前後バイト一致(完了条件6)。
  - `node scripts\check-all.mjs` の通しを1回(秒数と FLAKY 本数を記録)。
- **変更禁止範囲(絶対に触らない)**: **`assets\geo.js` / `assets\engine.js` / `WEIGHT` 定数(順位ロジック) / `fixtures\*.json`(読むだけ・再生成しない)** / `assets\style.css` / `assets\ui.css` / `index.html` / `404.html` / `demo\*.html` / `.github\workflows\*` / `docs\AUTOPILOT.md` / `scripts\dump-rank.mjs` / **`scripts\check-*.mjs` のうち `check-feednote.mjs` 以外の34本** / **`app.js` の `FAME_ORIGIN_TEXT`・バッジのしきい値と文言(`:1146`・`:1160-1182`)・`renderFeed()` の `#feed-origin` ブロック・`moreHtml()`・`moreThemesHtml()`・`moreBundledHtml()`・`farHtml()`** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記のみ許可) / **ROADMAP の R243・R244・R235・R211・R102**(起票のまま残す)。
  - **★禁止範囲に矛盾を見つけたら報告した上で最小限の変更で進めてよい**(R228)。**ファイルの場所は自分で grep して確かめること**。
- **所要目安**: 30〜40分(WEIGHT の全件列挙と現状実測6分・文言の検討と実装5分・5エリア撮影と目視7分・検査の軸追加と発火確認7分・破壊実証の旧版素通り確認6分・dump-rank 3分・35本通し7分・記録とコミット6分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- **R244(判断が要ります)**: `fixtures\kusatsu.json` の `backlinks` が **28件**しかなく、他エリア(箱根73・道後70・別府49・城崎41)の半分以下。**★R241 の申し送り: 城崎 1/5・箱根 3/5 は表示側ではこれ以上動かせません**。初期5枚に **60〜149 の帯**にいるカードが1枚も無いためで、**しきい値を下げない限りどんな条件でも増えません**。**fixture 再生成は順位を動かしうる**ため判断が要る。
- **★(B) 「段の推移を検証できない」の申し送り(今回見送った理由の記録)**: **読み込み中かどうかは `.skeleton` の有無として画面に出ており、検証不能ではありません**(`app.js:1622-1623`・`:1669`)。検証できないのは「★が付く前のカードが出ている一瞬」だけで、**同じ画面状態は R245 の `page.route()` による★0枚軸が既にカバー済み**です。**再訪を増やすと `check-fame.mjs` がさらに遅くなる**(R245 の申し送り1: 20.9s→24.6s)ため、**同じものを2通りで測りにいかないこと**。
- 判断不要で残っているもの(**R246 は今回着手**): **R243**(地図のピンに人気の手がかりが無い。**色分けは禁止**。ツールチップが `permanent:false` で読む瞬間が無い問題は未解決。**4サイクル連続で見送り**)、**R235**(`check-nosummary.mjs:146` の死んだ照合)、**R211**(dump-rank に日時とコミットID)、**R102**(list-shots.mjs)。**R217 は見送り済み**。**R233 は残件0件で完了**。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★司令塔が推す壊し方は7サイクル連続で旧版に捕まっている。着手前に必ず旧版で素通りするか実測し、捕まるなら自分で作り直すこと。**
- **★司令塔の指示書そのものにも誤りがある(今夜8回目)**(R241 で2件: 抑制規則の出典を「R231」としたが実際は **R224**・推した壊し方(B)が旧版で既に捕まった)。**実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい**(R228)。
- **★「軸を足した」と書くだけでは不十分。壊して実際に発火するところまで見ること**(R240 の教訓)。作業役が自分で足した母数軸が**死んだ軸**でした(`exp.all.length` が常に104で、壊しても発火しない)。**母数は必ず画面(DOM)から数えること**。
- **★表示側で動かせる限界を見極めること**(R241)。城崎・箱根の初期5枚は **60〜149 の帯にカードが1枚も無い**ため、表示条件をどういじっても増えません。**基準を下げて枚数を増やすのは「人気の基準を甘くする」行為で禁止**。
- **★検査の高速化は R240 で一区切り**(616.5s → **361.1s**)。最遅は `check-pinflash`(41.2s)・次点 `check-attrib`(27.1s)で削る余地は小さい。**みのるんが朝起きて見るのは画面です**。画面側か設計思想に関わるものを選ぶこと。
- **★削減量は「削る対象の秒数」ではなく A/B 実測で確かめる**(R239 の教訓。司令塔の「32.7s縮む」という見込みは実測 **2.1s** = 環境ゆらぎより小さかった)。**★速さのために検査を弱めない**(`force: true` は採らない。アクショナビリティ判定を飛ばすのは検査を弱める行為)。
- **★部分的な確認では見つからない破損がある。何かを全件確認する必要があるときは、機械的に全件突合すること**(R233-3。「前後数件を突合せよ」では**63%の破損を発見できませんでした**)。
- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226)。**本数の下限と中身の照合を別項目で持つ**。**R216・R234・R238・R241 では有無軸が全緑のまま中身軸だけが壊れを検知しました**。
- **★「作らない判断」も成果**(R232。**常に赤い検査は緑の検査より危険**)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231)。**★数え方の定義を実装と一致させる**(R226)。**★件数を引き継ぐときは、引き継いだ側が実物を開いて形を確かめること**(R216)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。**★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228・R237 では起票文の前提自体が外れました)。**★変更の影響は、関係する全ての分岐を列挙してから数える**(R227)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229・R216)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226・R234・**R242** はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225。export されている=使われている、ではない)。**同様に「検査があるから守られている」も疑う**(R234 で `check-nosummary.mjs:146` の `.every()` が死んでいた)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に8件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218)。**新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226・R234・R241 はいずれも新規ファイル0件)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232)。基準値は**毎サイクル自分で `md5sum` を取り直すこと**(2026-09-24 実測: `assets\app.js` = `b994a2fa77332fbaad2c368560ac758c`(**R245 時点の値。着手時に必ず自分で取り直すこと**)、`assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583`、`assets\ui.css` = `c3db293066104848b317b7d04798638d`)。**ピンのCSSは `assets\style.css` にあります**(`ui.css` は該当0件)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。**本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

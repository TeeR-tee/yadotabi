# NEXT: 2026-09-21 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、**先頭に261件の目次が付いた**ので「Rいくつの話がどこにあるか」は探せるようになりました。R220 完了)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**34本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**(R85・R207・R208)+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の案内**(R215)+**検査で保護**(R221) |
| サイトアイコン | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214) |
| 検査がコメントを本物と誤判定 | 4か所が素通り | **解消**(R222・「壊しても緑のまま」を先に実証) |
| 検査がときどき理由なく赤くなる | 1回落ちたら赤が確定 | **PASS / FLAKY / FAIL の3値+自動1回再実行**(R223) |
| NIGHTLOG(2123行) | 朝に読めない | **先頭に261件の目次**(R220) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。


## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R224** — 「どれだけ人気か」を、すでに手元にあるデータでカードに出す

**なぜ R224 なのか(バックログではなく、本番アプリを実際に見て決めました)。**

残っていたバックログ5件(R216/R217/R219/R211/R102)は**どれも小粒で、みのるんが画面で気付けるものが1件もありません**。そこで固定データの画面(`?fixture=kusatsu`)と `dump-rank.mjs` の出力、`app.js`/`geo.js`/`engine.js` を実測し、**設計思想に照らして何が足りないか**を調べました。結果を `docs/ROADMAP.md` に **R224〜R228 の5件**として追記しています。

設計思想は3本柱です。「宿を選ぶと周辺の見どころが出る」「入力ゼロ」「**その周りの観光地と、どれだけ人気かを示す**」。**このうち3本目が、画面に1文字も出ていません。**

| 測ったこと | 結果 |
|---|---|
| `人気` の語が画面側ファイルにあるか | `index.html` **0件** / `app.js` **0件** / CSS3本すべて **0件** |
| 人気の生データ `backlinks` は手元にあるか | **ある**。`fixtures/kusatsu.json` に含まれ、`engine.js` が `_debug.backlinks` としてカードに載せている |
| それは今どう使われているか | `app.js` L1014 `representativeReason` が **「30件以上か」の内部判定にだけ使い、数値は捨てている** |

つまり**データは既にカードまで届いているのに、利用者には強弱がまったく見えない**状態です。表示を足すだけで解決でき、**順位ロジックには一切触れません**。

**★着手前に必ず知っておくこと(ここが今回いちばん間違えやすい所)。**

`backlinks` は「その記事が日本語版Wikipedia内で何件から張られているか」です。**生の数字をそのまま「人気 312」と出してはいけません。**(1)単位のない数字は利用者に意味が伝わらず、(2)Wikipedia記事が無い場所(草津の more 19件の多くがこれ)は 0 になり、**「人気0」と表示されてしまう**からです。0 の場所は**行ごと出さない**のが正解です(`reasonText` が理由なしで行を出さないのと同じ考え方)。

- **タスクID**: R224
- **目的**: カードに「どれだけ知られている場所か」が**一目で分かる短い表示**を足す。**順位は1件も変えない。**
- **変更するファイル(絶対パス・この4件のみ)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(**表示の生成のみ**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\ui.css`(見た目のみ。既存のチップ/バッジの型を流用し、**新しいCSSファイルは作らない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R224 を `[x] 2026-09-21` に)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-21 R224 …` 見出し+3行。**先頭の目次にもこの1行を足す**=R220 で作った目次を同じサイクルで更新する)
- **作り方(注意点)**:
  - **新しいファイル・新しいページを増やさない**こと。既存カードの中に収める。
  - `_debug.backlinks` が **0 または欠落のカードには何も出さない**(空欄も出さない)。「人気0」と読める表示を絶対に作らないこと。
  - **生の数値を出すか、段階の言葉(例: よく知られた場所)にするかは作業役が決めてよい**が、決めた根拠を NIGHTLOG に書くこと。段階にする場合、**しきい値は5エリアの実データを見てから決める**(`node scripts/dump-rank.mjs <area>` を5エリア分)。既存の `REASON_BACKLINKS_MIN = 30` を**流用してよいが、流用するなら「30で妥当か」を5エリアの実数で確かめた結果を書くこと**。数字を写すだけは不可。
  - 既存の 💡理由行(`reasonText`)と**同じことを二重に言わない**こと。理由行が「このあたりの代表的な観光名所」と出ているカードに「よく知られた場所」を並べると重複です。**どちらを出すかの規則を決めて、5エリアで重複0件を確認する**こと。
- **完了条件(検証可能な形)**:
  1. **順位が1件も変わっていないこと**: `node scripts/dump-rank.mjs` の5エリア分の出力が、変更前の HEAD の出力と**バイト単位で完全一致**すること。両方の出力をファイルに落として `diff` し、**差分なし**の実行結果を NIGHTLOG に貼る。(表示だけの変更であることの唯一の証明です)
  2. **`backlinks` 0 のカードに何も出ていないこと**: 固定データ5エリアで、新表示が付いたカードの件数と、`_debug.backlinks >= しきい値` のカードの件数が**一致**すること。数え方(コマンドと出力)を NIGHTLOG に残す。
  3. **理由行との重複が0件**であること。5エリアで数えた結果を NIGHTLOG に貼る。
  4. **検査**: `node scripts/check-all.mjs` が **34本中34本 PASS または FLAKY(exit 0)**。**FLAKY が出たら本数を NIGHTLOG に記録すること**(下記の申し送り)。
  5. **新しい検査を1本足す**(`scripts/` 配下。**34本→35本**): 「`backlinks` 0 のカードに人気表示が付いたら赤くなる」検査。**わざと 0 のカードに表示を出すコードに書き換えて、その検査が実際に赤くなることを確認した実行結果を NIGHTLOG に貼る**(確認後は必ず元に戻すこと)。緑になったことだけの報告は不可。
  6. **本数を持つ文書を同じサイクルで直す**: `docs/CHECKS.md` と `README.md` の検査本数を **35本**に更新(R212 の教訓)。どこに本数が書かれているかを `grep -n "34"` で2方向から探して漏らさないこと。
  7. `git diff --numstat assets/geo.js assets/engine.js` が**空**。
- **検証手順(撮影)**:
  - `node C:\workspace\tools\shot\shot.mjs "http://127.0.0.1:3000/?fixture=kusatsu" --mobile` と **PC幅**の2枚。加えて **`?fixture=beppu` のmobile 1枚**(草津だけで判断しない)。
  - 撮った画像を **Read で開いて目視**し、(a)新表示が既存のチップと重なっていないか、(b)下半分の写真なしカードで表示が浮いていないか、(c)1行に収まっているか、を確認して結果を NIGHTLOG に書く。
  - **本物APIは叩かない**(固定データで足ります。人気データは fixture に入っています)。
- **変更禁止範囲(絶対に触らない)**: `assets/geo.js` / `assets/engine.js` / `WEIGHT` 定数 / `fixtures/*.json` / `index.html` / `demo/*.html` / `.github/workflows/*` / `docs/AUTOPILOT.md` / **NIGHTLOG の既存本文**(末尾追記と目次への1行追加のみ許可)。
- **所要目安**: 35〜45分(5エリアの実数調査としきい値決め10分・実装10分・順位不変の証明8分・新検査と「わざと壊して赤」の実証8分・撮影と目視6分・記録とコミット5分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(新規・判断が要ります)**: `geo.js` の `enrichFame`(Wikidata の他言語版リンク数と月間ページビューを取る関数)が、**定義と export だけで、どこからも呼ばれていません**。つまり本番でも `fame` は常に空です。`docs/FIXTURES.md:177` は動いている前提で書かれています。**呼ぶとリクエストが増え、順位にも影響しうるため、みのるんの判断が要ります**。判断不要なのは文書の訂正だけです。
- 判断不要で残っているもの: **R226(more側が全件同じ見た目)・R227(1位の理由行が常に「歩いて行ける」)・R228(何からの距離か書いていない)**・R217(embed-check の OGカード)・R219(README照合)・R216(404)・R211・R102。
  R224 の次は **R228(凡例1行・最小)→ R227(理由行の優先順・要 before/after 比較)→ R226** の順を推奨。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。R223 の仕上げ実行で**本物のフレークが1本出ました**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE` で1回目に落ち、再実行で通過)。**この環境ではフレークが1回の通しで1本出る頻度**です。**FLAKY の本数が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224 の教訓。バックログ5件のどれにも「人気が1文字も出ていない」は入っていませんでした。**起票は、起票した時点で気付いていたことしか含みません**)。
- **「動いている前提で書かれた文書」を疑う**(R225 の教訓。`FIXTURES.md` は `enrichFame` が本番で動く前提でしたが、呼び出しが1件も無かった。**export されている=使われている、ではありません**)。
- **起票の中に書かれた「こうすれば簡単」も疑う**(R220 の教訓。実測すると見出しは2形式・62個が別形式でした)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる。
- **「今回だけの偶然」と片付ける前に、過去に何回起きたかを数える**(R223 の教訓)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立し、R222 で実証まで進めた手順)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220 はいずれも新規ファイル0件で済ませました)。
- **自動でうまく処理する仕組みは「問題を隠す方向」に働かないか考える**(R223 の教訓)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

# NEXT: 2026-09-25 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、**先頭に目次が付いた**ので「Rいくつの話がどこにあるか」は探せます。R220 完了)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 | **230文字の案内**(R215)+**検査で保護**(R221) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 「徒歩1分・75m」が何からの距離か | 画面に一度も書かれていない | **`距離は「(宿名)」からの目安です`**(R228) |
| 検査が「空集合を検査して常に緑」 | 未点検 | **35本を2方向で全数点検(R232)**・**28件を発見 → R233 で13件消化(残り15件)** |
| 「もっと見る」の先の同じ形の帯 | 読み飛ばす手がかり無し | **`写真と解説がまだ無い場所 N件` の区切り**(R226・本番反映済み)。順位は5エリアでバイト一致 |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。

## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R233-2** — **`check-fame.mjs`(4件)と `check-distance.mjs`(2件)の「母数を見ていない判定」を消化する**

**なぜこれを選んだか(選択肢A。計画役が実測して判断しました)**:
- 前サイクル R226 で**画面側の1件を消化**し、画面に出る改善は一区切りしました。残る判断不要の項目は R233 の残り15件・R216・R211・R102 のみです。
- **R226 の教訓がそのままこの2本に当てはまることを実測で確かめました。** `scripts\check-fame.mjs:243-274` の判定は**ほぼ全てが `badged` という1つの配列から作られています**(`badged.every(...)` が3件・`lowBadged.length === 0` が1件)。**`badged` が空になる壊し方をすると、この4件は全部『正しく緑』になります**。これは R226 で「`isBareCard` を偽にすると class も同時に消えるので噛み合わせの検査が全部緑になった」のと**同じ形**です。`:237` に `badged.length >= 1` の下限はありますが、**エリア単位の下限しか無く、5エリア合計の別軸がありません**。
- `scripts\check-distance.mjs:97` / `:122` の `kusatsuWalkMismatch.length === 0` / `hakoneWalkMismatch.length === 0` も、`kusatsuTexts` が空なら常に緑です(`:88` の `length > 0` は別項目なので、**mismatch 判定自体は母数を見ていません**)。
- 合計6件で、R233 の残り15件のうち**4割**を1サイクルで消化できます。手順(母数を検査項目に格上げ→わざと壊して赤を実証)は R232・R233 で確立済みです。

**★ただし R226 の教訓を必ず適用すること**: 母数の下限を足すだけでは足りません。**`badged` を軸にした項目と、`badged` を通らない別軸(全カードから期待値を組み直す `mismatch`(`:271`)や、5エリア合計)を必ず両方持たせる**こと。「軸が1つだけ」の状態で終わらせない。

- **タスクID**: R233-2 / **目的**: `check-fame` 4件・`check-distance` 2件を「母数が0でも緑」から「母数が0なら赤」に格上げし、**かつ軸が1つに寄らないようにする**。**画面と順位は1バイトも変えない**。
- **変更するファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-fame.mjs`
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-distance.mjs`
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R233 の残数を **15件 → 9件** に更新。内訳 `check-engine` 5・`check-reason` 3・`check-nosummary` 1 を明記)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-25 R233-2 …` 見出し+3行以上・**先頭の目次にも1行**)
- **完了条件(検証可能な形)**:
  1. **上記6件それぞれについて、母数を検査項目に格上げ**したこと(何を母数としたかを NIGHTLOG に1件ずつ列挙する)。**6件より多く直してもよいが、その場合は ROADMAP の残数を実際の数に合わせること。**
  2. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail の数字で対比**する(旧版は `git show HEAD:scripts/check-fame.mjs` で取り出す。**`git checkout` / `git stash` / `git reset` は使わない**)。**2通り以上**行い、うち**最低1つは「数字だけ見る検査では素通りする壊し方」**にすること:
     - **壊し方A(母数を消す)**: `assets\app.js` でバッジの描画をやめる(`fame` バッジを1枚も出さない)。→ `badged` が空になる。**旧版が何 pass / 0 fail で素通りしたか**と、**新版が何 fail になるか**を数字で並べる。
     - **★壊し方B(数字が1つも変わらない壊し方)**: **バッジの枚数も本数も1つも変えずに**、`★ 全国的によく知られた場所` と `★ この地方でよく知られた場所` の**2種類を入れ替える**(全国級のカードに地方級の文言を出す)。バッジ枚数・カード枚数・文言の種類数は**全て不変**だが、**意味は完全に壊れている**。`:271` の `mismatch` 項目がこれを捕まえるはずなので、**捕まえられるか実際に確かめる**こと。捕まえられなければ中身照合の項目を足す。
  3. **★検査の軸が1つだけになっていないか確認する**(R226 の教訓)。NIGHTLOG に「この6件は `badged` 軸と ◯◯ 軸の2つで支えている」と**軸の名前を書く**こと。壊し方Aで**全項目が同時に緑になってしまう**なら軸が1本です。その場合は**5エリア合計の下限**など別軸を足すこと。
  4. `assets\` を一時的に書き換えて実証したら、**手で書き戻し**、`md5sum` が実証の**前後で同一**であることを示す(現在値: `assets\app.js` = `fa5caf9f12f1b2fb0e4fd7868c33a4ee`、`assets\ui.css` = `c3db293066104848b317b7d04798638d`。**今回は `assets\` を正式には変更しないので、最終的にこの値と一致すること**が完了条件)。
  5. `node scripts\check-all.mjs` が **35本中35本 PASS または FLAKY(exit 0)**。**検査の本数は35本のまま**(既存2本への項目追加のみ)なので `docs\CHECKS.md` は変更不要。
  6. **★FLAKY の本数を必ず NIGHTLOG に記録**(R227〜R226 は**8サイクル連続0本**)。
- **検証手順(撮影は不要。その理由も明記)**: **画面に出るものを1ドットも変えないタスク**(検査スクリプトのみの変更で `assets\` は最終的に md5 一致)なので、**撮影と5エリア目視は不要**です。代わりに:
  - `node scripts\check-fame.mjs` / `node scripts\check-distance.mjs` の単体実行で pass/fail の数字を記録。
  - `node scripts\dump-rank.mjs` は**順位に触らないので不要**ですが、`git diff assets/` が**空**であることは必ず示すこと(完了条件4の裏付け)。
  - `node scripts\check-all.mjs` の通しを1回。
- **変更禁止範囲(絶対に触らない)**: **`assets\geo.js` / `assets\engine.js` / `WEIGHT` 定数(順位ロジック) / `fixtures\*.json`(読むだけ・再生成しない)** / `index.html` / `assets\style.css` / `assets\ui.css` / `demo\*.html` / `.github\workflows\*` / `docs\AUTOPILOT.md` / `scripts\dump-rank.mjs` / **`scripts\check-fame.mjs` と `scripts\check-distance.mjs` 以外の検査スクリプト** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記と目次1行のみ許可)。
  - **`assets\app.js` は「壊して実証するための一時変更」のためだけに触ってよい**(必ず手で書き戻して md5 一致を示すこと)。実装対象ではありません。
  - **★この禁止範囲に矛盾を見つけたら、報告した上で最小限の変更で進めてよい**(R228 の教訓。司令塔の指示書そのものにも誤りがあります)。
- **所要目安**: 30〜40分(現状把握と6件の特定5分・実装10分・破壊実証2通り+旧版対比10分・35本通し7分・記録とコミット8分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- 判断不要で残っているもの: **R233 の残り9件**(このサイクル後)→ **R216(404・検査追加とセット)** の順を推奨。ほかに R211・R102。**R217 は見送り済み**。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226 の教訓。**判定と表示が同じ条件から作られていると、その条件を壊しても噛み合わせは崩れず、検査は全部『正しく緑』になる**。別軸=合計の下限・中身の照合を必ず持つこと)。
- **★「作らない判断」も成果**(R232 の教訓。誤検知53%の自動検査を見送った。**常に赤い検査は緑の検査より危険**)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231 の教訓)。**★数え方の定義を実装と一致させる**(R226 で計画役の数え方と `isBare` がずれ、該当枚数が草津11→7・別府1→0 と変わった)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230 の教訓)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。
- **★司令塔の指示書そのものにも誤りがある**(R228。実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい)。
- **★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228。起票が正しいのは「問題があること」までで、その理由づけは別)。
- **★変更の影響は、関係する全ての分岐を列挙してから数える**(R227 の数え漏れ)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229 の教訓)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226 の教訓)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226 はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225。export されている=使われている、ではありません)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に7件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立)。**数字だけ見る検査では素通りする壊れ方がある**(R229・R232・R233)。**本数の下限と中身の照合を別項目で持つ**。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226 はいずれも新規ファイル0件)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232 で確立)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

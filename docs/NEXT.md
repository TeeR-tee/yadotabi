# NEXT: 2026-09-24 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にあります。**先頭の目次は `##` 見出しの48件**です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 草津の1位「湯畑」の説明 | **1行も出ていなかった** | **`Wikipediaに記事がありません。写真と地図の情報で表示しています。`**(R234・本番反映済み) |
| 人気バッジが初期5枚で出る枚数 | **8/25枚**(草津 **0/5**) | **10/25枚**(草津 **1/5**・別府 2→3)(R241。**しきい値も文言も1つも変えず**) |
| 「もっと見る」を押す前の手がかり | **`もっと見る` の5文字だけ** | **`この先にあるのは歴史を歩く、景色を見にいくです`**(R236・**件数の数字0文字**・CSS と `index.html` は0バイト変更・本番反映済み) |
| 画面末尾の注記が名乗る「順位の作り方」 | 8つの要素のうち**2つしか名乗っていなかった**(しかも「有名な場所が下に来る」と逆を書いていた) | **4種類に畳んで全部を名乗る**(R246・本番反映済み・順位は1件も動かさず) |
| 注記から飛べる README の順位の説明 | 画面と**正反対**・8つの重みのうち**3つしか名乗らず** | **画面と同じ4種類で説明**(R247)+**`docs/check.mjs` に4軸追加**(41→45 pass) |
| README の数値表 | 「一度測って書いたきり」 | **実測で2件が否定され訂正**(R247) |
| `docs/CHECKS.md` の行番号による道案内 | **7種類9箇所が9箇所とも指す先を外していた**(`check-all.mjs:17` は実際は `:43` 等) | **全件を実測値に訂正**+**双子の行も併記**(R249)+**`docs/check.mjs` に5軸追加**(80→85 report)。**`assets/` は1バイトも変更なし** |
| 直近の通し | — | **35本全PASS・382.2s**・**★FLAKY 0本**(R249。**6サイクル連続0本**) |
## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。



## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R235** — **`scripts/check-nosummary.mjs` の「6a.」の照合が、構造上1度も実行されていない**

**なぜこれを選んだか(★計画役が実測してから決めました。起票の前提が1つ外れていました)**:

- **★実測1(起票の行番号は外れていました。申し送りどおり実測して正解でした)**: 起票は `check-nosummary.mjs:146` と書いていますが、**146行目は `page.on('pageerror', …)` の行**でした(ファイルは全665行)。**死んだ照合の実体は `:193`**(`ok(hasArticleRows.length === 0 && hasArticleRows.every(…), '6a. …')`)です。**起票文の前提が誤っていた実例は今夜これで9件目**なので、`docs/ROADMAP.md:309` の起票本文の行番号も **`:146` → `:193` に訂正**してから着手すること。
- **★実測2(死んでいることの裏取り)**: `DOGO_HAS_ARTICLE_NAMES` は **`:63` で空配列 `[]`**、`hasArticleRows` は **`:182` でその配列による `filter`** なので**常に空**です。よって `&&` の左が真のとき右の `.every()` は空配列への呼び出しで**常に true**。検査名は「文言が `HAS_ARTICLE_NO_SUMMARY_TEXT` になっている」と名乗っているのに、**その文言照合は1度も走っていません**(R225「export されている=使われている、ではない」と同型)。
- **★実測3(同じ型が他に無いことを全34本で確認)**: `grep -rn "length === 0 && .*\.every("` を `scripts/` と `docs/check.mjs` 全体にかけた結果、**ヒットは `:193` の1件だけ**でした。逆順(`.every(...) && ...length === 0`)も**0件**。**つまり直す対象は1箇所で確定**しており、範囲が膨らみません。
- **★実測4(守る検査を足せる場所が既にあります。新規ファイル0件・35本のまま)**: `docs/check.mjs` には **`:755` で `readTarget('scripts/check-all.mjs')` を使って検査本のソースを読む仕組みが既にあります**(R249 の一対一軸)。同じ `readTarget` で `scripts/check-nosummary.mjs` を読み、**「`length === 0 && …every(…)` の形が1件も無いこと」を静的に照合する軸**を足せます。**`scripts/` は1バイトも触らずに済む側の検査**なので、`scripts/check-nosummary.mjs` は**今回だけ変更許可**にします(下記)。
- **★実測5(判断が要るかの切り分け)**: **みのるんの判断は不要です**。`assets/` は1バイトも触らず、**順位も画面も1ミリも変わりません**。ただし `check-nosummary.mjs` は検査本なので、**直した結果が緑のままであること**を実際に走らせて示す必要があります。

- **タスクID**: R235 / **目的**: **`scripts/check-nosummary.mjs:193` の「常に true の `.every()`」を、実際に文言を照合する生きた形に直し、同じサイクルで `docs/check.mjs` に「この死んだ形が検査本の中に1件も無いこと」を全34本+`docs/check.mjs` 自身から静的に照合する軸を足す**。**画面に出るものは何も変えません**。
- **対象ファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-nosummary.mjs`(**主。`:193` の `ok(...)` 1箇所と、その直前のコメントだけ**。**★この1本だけは「`scripts\check-*.mjs` は読むだけ」の禁止範囲から今回に限り外します**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\check.mjs`(**R235 軸を `:791`(現在の末尾)の後ろに新ブロックとして足す**。**R248/R249 ブロックは1バイトも変えない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(**R235 の起票本文の `:146` を `:193` に訂正**したうえで `[x] 2026-09-24` に。**R225・R244・R211・R102 は起票のまま残す**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-24 R235 …` 見出し+3行以上。**目次エントリは `##` 見出しの48件のみ**)
- **完了条件(検証可能な形)**:
  1. **★まず起票の行番号を自分で実測し直すこと**(計画役の実測では `:146` は `pageerror` の行、実体は `:193`)。**再現しなければ自分の実測を信じること**。訂正してから進めること。
  2. **★直し方は「枚数と文言を別々の検査項目に分ける」こと**。今の1本は枚数判定(`length === 0`)と文言判定(`.every(...)`)が `&&` で繋がっているため、**枚数が0になると文言側が自動的に緑になります**。**枚数の項目と文言の項目を `ok()` 2本に割る**か、**文言照合の母数を `hasArticleRows` ではなく「実際に `HAS_ARTICLE_NO_SUMMARY_TEXT` で始まる `--none` が存在する側」から取る**かのどちらかにすること(`:294`・`:338` に既に同じ文言を母数に使っている実例があります)。**R226 の「本数の下限と中身の照合を別項目で持つ」がそのまま当てはまります**。
  3. **★検査名を実態に合わせること**。「記事が実在する8枚が…」という名乗りは、**実測0枚の今の状態と食い違っています**(R246 と同じ「名乗りと実態の食い違い」)。**0枚であることを名乗る文**に直すこと。
  4. **★`check-nosummary.mjs` 全体の pass/fail 本数を変更前後で記録**し、**直した結果も緑であること**を示す。**枚数の期待値(`=== 2` など他の項目)は1つも変えないこと**。緩めて緑にするのは禁止です。
  5. **★同じサイクルで守る検査も足す**。**`docs/check.mjs` に「死んだ照合の形」軸**を足す。軸は **(a) 母数軸** = 走査対象の検査本が **35本**(`scripts/check-*.mjs` 34本 + `docs/check.mjs` 自身)読めていること(**★ここも件数の定数を書かず、`readdirSync` など実ファイルから数えること** = R240 の「死んだ軸」再来防止)、**(b) ★死んだ形軸** = `length === 0 && ….every(` および逆順 `.every(… ) && ….length === 0` が**全本で0件**であること(見つかったら**ファイル名・行番号・その行**を名指しで出す)、**(c) 生存確認軸** = `check-nosummary.mjs` の中に `HAS_ARTICLE_NO_SUMMARY_TEXT` を**実際に照合している `ok()` が1本以上ある**こと(直した本人が消してしまう事故を防ぐ)。
  6. **★足した軸が本当に発火するか、壊して確かめること**。軸ごとに、**それだけを壊して発火する**ことを pass/fail で示す。**R248・R249 では、この手順が軸自身の実装バグを計3件捕まえました**。同じことを期待して回すこと。
  7. **★軸が誤検知したら、軸を緩めるのではなく対象側の書き方を直すこと**(R249 の判断)。**例外リストを作って黙って飛ばす実装にしないこと**(飛ばすと母数が減って軸が死にます)。
  8. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail で対比**する。**★その壊し方が本当に旧版を素通りするかを先に確かめること**(**司令塔が推した壊し方は10サイクル中8回外れています**。捕まるなら**自分で作り直すこと**)。候補は **(A) 直した `ok()` を元の `length === 0 && ….every(` の形に戻す**(死んだ形軸だけが発火。**旧版 `docs/check.mjs` は `scripts/check-nosummary.mjs` のソースを1バイトも読んでいないので素通りするはず**)と **(B) `HAS_ARTICLE_NO_SUMMARY_TEXT` の照合行を消す**(生存確認軸だけが発火・死んだ形軸は緑のまま)の2通り。**(A) が旧版で素通りすること**を先に確かめ、**R216・R234・R238・R241・R245〜R249 と同じ「有無は緑・中身だけが赤」の形**を再現すること。
  9. **★発火しないときは、軸の不備と決めつける前に壊し方が不完全でないか疑うこと**(R247 で実際に起きました。壊し残した文字列が同じ行の末尾にありました)。
  10. **★撮影は不要です。その理由を NIGHTLOG に明記すること**: **今回変えるのは `docs\` 配下と `scripts\check-nosummary.mjs`(検査本)だけで、`assets\` と `index.html` を1バイトも変えないため、画面に出るものが何も変わりません**。**代わりに、着手時と完了時に `assets\app.js` / `assets\style.css` / `assets\ui.css` / `assets\engine.js` / `assets\geo.js` の md5 を取り、全て一致することで「画面が変わっていない」を証明すること**(**基準値は着手時に自分で `md5sum` を取ること**。**前サイクルの値を鵜呑みにしない**)。**`git diff --stat` に `assets/` と `index.html` と `fixtures/` と `demo/` が1件も出ないこと**、**`scripts/` は `check-nosummary.mjs` の1本だけであること**も併せて示す。
  11. `node scripts\check-all.mjs` が **35本中35本PASS または FLAKY(exit 0)**。**本数は35本のまま**(**新規ファイルを作らない**)。
  12. **★FLAKY の本数を必ず NIGHTLOG に記録**(**R244〜R249 は0本で6サイクル連続**。今回も0本を保てるかを書くこと)。
  13. **順位不変の実証**: `git diff assets/geo.js assets/engine.js fixtures/` が**空**であること。**`dump-rank.mjs` は順位ロジックを1バイトも触らないため今回は不要**。
- **検証手順**:
  - `node scripts\check-nosummary.mjs` 単体で pass/fail と秒数を記録(変更前後)。
  - `node docs\check.mjs` 単体で pass/fail と秒数を記録(変更前後。**R249 時点は 85 report / 0 fail・約1.0s**)。
  - **壊し方(A)(B)を先に旧版で走らせて素通りを確認**(旧版は `git show HEAD:docs/check.mjs` で取り出す。**`git checkout`/`stash`/`reset` は使わない**) → 新版で落ちることを確認(完了条件6・8)。
  - `assets\` 5ファイルの md5 を着手時と完了時に取り、一致を示す(完了条件10)。
  - `node scripts\check-all.mjs` の通しを1回(秒数と FLAKY 本数を記録。**R249 は 382.2s**)。
- **変更禁止範囲(絶対に触らない)**: **`assets\` の全ファイル(`app.js` / `geo.js` / `engine.js` / `style.css` / `ui.css`)** / **`fixtures\*.json`(読むだけ・再生成しない)** / `index.html` / `404.html` / `README.md` / **`demo\*.html`** / `.github\workflows\*` / `docs\AUTOPILOT.md` / `docs\FIXTURES.md` / `docs\CHECKS.md`(R249 で直したばかり・読むだけ) / `scripts\dump-rank.mjs` / **`scripts\check-*.mjs` のうち `check-nosummary.mjs` を除く33本と `check-all.mjs`(読むだけ)** / **`docs\check.mjs` の `:1`〜`:791`(R248・R249 ブロックを含む既存部分。今回足すのは `:791` 以降の新ブロックだけ)** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記のみ許可) / **ROADMAP の既存起票すべて**(**R235 の行番号訂正と完了マークだけが例外**。**152件の行番号引用は履歴なので直さない**)。
  - **★禁止範囲に矛盾を見つけたら報告した上で最小限の変更で進めてよい**(R228)。**ファイルの場所は自分で grep して確かめること**。
- **所要目安**: 25〜35分(起票の行番号の実測と訂正3分・`:193` の作り直し6分・`check-nosummary` 単体の緑確認4分・検査の軸追加と発火確認9分・破壊実証の旧版素通り確認4分・md5 と diff 2分・35本通し7分・記録とコミット5分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。**R248 で文書側は訂正済み。「呼ぶかどうか」は順位とリクエスト数に影響するため判断待ちのまま**です。
- **R244(判断が要ります)**: `fixtures\kusatsu.json` の `backlinks` が **28件**しかなく、他エリア(箱根73・道後70・別府49・城崎41)の半分以下。**★R241 の申し送り: 城崎 1/5・箱根 3/5 は表示側ではこれ以上動かせません**。初期5枚に **60〜149 の帯**にいるカードが1枚も無いためで、**しきい値を下げない限りどんな条件でも増えません**。**fixture 再生成は順位を動かしうる**ため判断が要る。
- **R243 は「やらない」と R249 で決着済み**(`ROADMAP.md` に `[-] 2026-09-24` と理由3点を記録済み)。**R217 は見送り済み**。**R233 は残件0件で完了**。
- 判断不要で残っているもの(**R249 は完了・R235 は今回着手**): **R211**(dump-rank に日時とコミットID)、**R102**(list-shots.mjs)の**2件のみ**。**どちらも開発者用ツールで画面に出ません**。
- **★R235 からの申し送り(次の計画役へ)**: **判断不要のバックログはこれで実質尽きます**(残る R211・R102 は画面に出ない開発者用ツールで、優先度は低い)。**次は (B) 本番アプリを実際に見て、設計思想に照らして足りないものを探す方法に戻ること**。**今夜この方法が最も成果を出しています**(R224・R226・R234・R237・R241・R242・R245・R246 がこの方法)。**行番号引用の調査は R248・R249 で一段落**、**「文書が名乗る件数・本数」の調査も R249 で1件出て一段落**しています。
## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★司令塔が推す壊し方は7サイクル連続で旧版に捕まっている。着手前に必ず旧版で素通りするか実測し、捕まるなら自分で作り直すこと。**
- **★司令塔の指示書そのものにも誤りがある(今夜8回目)**(R241 で2件: 抑制規則の出典を「R231」としたが実際は **R224**・推した壊し方(B)が旧版で既に捕まった)。**実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい**(R228)。
- **★「軸を足した」と書くだけでは不十分。壊して実際に発火するところまで見ること**(R240 の教訓)。作業役が自分で足した母数軸が**死んだ軸**でした(`exp.all.length` が常に104で、壊しても発火しない)。**母数は必ず画面(DOM)から数えること**。
- **★表示側で動かせる限界を見極めること**(R241)。城崎・箱根の初期5枚は **60〜149 の帯にカードが1枚も無い**ため、表示条件をどういじっても増えません。**基準を下げて枚数を増やすのは「人気の基準を甘くする」行為で禁止**。
- **★検査の高速化は R240 で一区切り**(616.5s → **361.1s**)。最遅は `check-pinflash`(41.2s)・次点 `check-attrib`(27.1s)で削る余地は小さい。**みのるんが朝起きて見るのは画面です**。画面側か設計思想に関わるものを選ぶこと。
- **★削減量は「削る対象の秒数」ではなく A/B 実測で確かめる**(R239 の教訓。司令塔の「32.7s縮む」という見込みは実測 **2.1s** = 環境ゆらぎより小さかった)。**★速さのために検査を弱めない**(`force: true` は採らない。アクショナビリティ判定を飛ばすのは検査を弱める行為)。
- **★部分的な確認では見つからない破損がある。何かを全件確認する必要があるときは、機械的に全件突合すること**(R233-3。「前後数件を突合せよ」では**63%の破損を発見できませんでした**)。
- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226)。**本数の下限と中身の照合を別項目で持つ**。**R216・R234・R238・R241 では有無軸が全緑のまま中身軸だけが壊れを検知しました**。
- **★「作らない判断」も成果**(R232。**常に赤い検査は緑の検査より危険**)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231)。**★数え方の定義を実装と一致させる**(R226)。**★件数を引き継ぐときは、引き継いだ側が実物を開いて形を確かめること**(R216)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。**★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228・R237 では起票文の前提自体が外れました)。**★変更の影響は、関係する全ての分岐を列挙してから数える**(R227)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229・R216)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226・R234・**R242** はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225。export されている=使われている、ではない)。**同様に「検査があるから守られている」も疑う**(R234 で `check-nosummary.mjs:146` の `.every()` が死んでいた)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に8件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218)。**新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226・R234・R241 はいずれも新規ファイル0件)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232)。基準値は**毎サイクル自分で `md5sum` を取り直すこと**(2026-09-24 実測: `assets\app.js` = `b994a2fa77332fbaad2c368560ac758c`(**R245 時点の値。着手時に必ず自分で取り直すこと**)、`assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583`、`assets\ui.css` = `c3db293066104848b317b7d04798638d`)。**ピンのCSSは `assets\style.css` にあります**(`ui.css` は該当0件)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。**本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

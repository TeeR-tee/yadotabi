# NEXT: 2026-09-20 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、2123行あるので朝は読まなくて大丈夫です。**この「読めない」状態を直すのが今夜の次のタスク R220 です**)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ(Windows手動) | **GitHub Actions で全PASS**(R64) |
| 営業用デモ | 日本語1枚のみ | **英語版 `demo/hotel-page-en.html` 追加**(R85)+ README冒頭に入口(R207)+ OGカード(R208) |
| 営業デモの検査 | 2枚がノーガード | **`scripts/check-demo.mjs` 新設**(R218) |
| JS無効時の案内を守る検査 | 1本も無かった | **`scripts/check-noscript.mjs` 新設で検査 34本に**(R221) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の日本語の案内**(R215・本番反映確認済み) |
| サイトアイコン(favicon) | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214・新規ファイル0件のデータURI) |
| 検査がコメントを本物と誤判定する穴 | `check-bundle.mjs` の4か所が素通り | **コメント除去を入れて解消**(R222・「壊しても緑のまま」を先に実証) |
| 検査がときどき理由なく赤くなる | 1回落ちたら赤が確定(4日で5回発生) | **落ちた1本だけ自動で1回やり直し、結果は PASS / FLAKY / FAIL の3値に**(R223) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。



## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R220** — `docs/NIGHTLOG.md`(2123行)の先頭に目次を置いて、朝に読める状態にする

**なぜ R220 なのか(すべて実測して決めました)。**

未着手6件(R216/R217/R219/R220/R211/R102)を実際に測って比べました。

| 候補 | 実測 | 判断 |
|---|---|---|
| **R220** | NIGHTLOG は **2123行**、`### ` 見出しは **213個**。NEXT.md 自身が「2100行あるので朝は読まなくて大丈夫」と読者に諦めさせている | **採用**。みのるんが朝いちばんに触れる2つの文書のうち片方が読めない状態 |
| R216(404) | `404.html` は確かに存在しない | 見送り。**新しいページを1枚増やす**案なので、「ファイルを増やさずに済む方法を先に探す」教訓に照らすと今夜の優先度は下 |
| R217(embed-check の og) | `demo/embed-check.html` の `og:` は **0行**、`name="description"` も **0行**(他3枚は og: 10/10/9行) | 見送り。差は小さく、いつでもできる |
| R219(README照合) | 文書のみ | 見送り。R220 と同種の文書仕事で、読者への影響は R220 のほうが大きい |
| R211 / R102 | どちらも開発者向けの道具 | 見送り。みのるんが直接読むものではない |

**★R220 の起票内容には、実測で分かった誤りがあります(必ず読んでから着手すること)。**

起票時は「AUTOPILOT の R126 規約のおかげで `grep -n "^### "` で機械的に拾えるので、拾った見出しを表に流すだけでよい」と書かれています。**これは実態と合っていません。**2方向から数えました。

| 測ったこと | 結果 |
|---|---|
| `### ` 見出しの総数 | **213個** |
| うち規約どおりの `### 2026-` 形式 | **151個**(=**62個は日付なしの別形式**) |
| `## サイクル記録` の開始行 | **346行目** |
| 346行目より前にある `## ` 見出し | **24個**(R205・R204・R203…と、サイクル記録の外に古い節が散らばっている) |
| 346行目より後の `### ` 見出し | **205個** |

つまり**見出しの形式が2種類あり、しかも「サイクル記録」節の外側にも24個の節がある**ので、機械的に1パターンで拾うと**62個の見出しが目次から抜け落ちます**。そこが今回いちばん間違えやすい所です。

- **タスクID**: R220
- **目的**: NIGHTLOG の先頭に**目次だけ**を置き、「Rいくつの話がどこにあるか」を朝に探せるようにする。**本文は1行も消さない・書き換えない。**
- **変更するファイル(絶対パス・この3件のみ)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(**先頭への目次の挿入のみ**。挿入位置は `# 夜間ログ` の直後)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R220 を `[x] 2026-09-20` に)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md` 末尾「## サイクル記録」に `### 2026-09-20 R220 …` 見出し+3行(上と同じファイル)
- **作り方(注意点)**:
  - 目次は**新しいファイルを作らずに NIGHTLOG の中に置く**こと(分割ファイルを作るかどうかは**みのるんの判断**なので今回はしない、と起票に明記されています)。
  - 拾う対象は **`^## ` と `^### ` の両方**。片方だけだと上表のとおり抜けます。**拾った件数を目次を作る前と後で数え、一致することを確かめる**こと。
  - **新しい見出しを増やしたり、既存の見出しの文言を揃えたりしない**こと。見出しの表記ゆれを直したくなりますが、それは本文の改変にあたり、完了条件4に違反します。
  - 目次の各行は **R番号 → 1行要約 → 行番号**。ただし**行番号は目次を挿入すると全部ずれます**。挿入後の行番号で書くこと(挿入する目次の行数をあらかじめ数えて足す、または挿入後に再採番する)。**ずれたまま出すと目次として役に立ちません。**
- **完了条件(検証可能な形)**:
  1. **目次の網羅性**: 目次の項目数が、本文の `^## `(目次自身の見出しを除く)と `^### ` の合計と**一致**すること。その数え方(コマンドと出力)を NIGHTLOG に残すこと。
  2. **行番号が正しいこと**: 目次から**無作為に5行**選び、書かれた行番号を `sed -n '<N>p' docs/NIGHTLOG.md` で開いて、**その見出しが実際にそこにある**ことを5件とも確認する。5件の実行結果を NIGHTLOG に貼ること。(目次の総数だけ合っていても行番号が全部ずれていれば意味が無いため、この2つは別々に確かめます)
  3. **本文が1行も消えていないこと**: `git diff --numstat docs/NIGHTLOG.md` の**削除行が0**であること。
  4. **本文が書き換わっていないこと**: 目次の挿入範囲を除いた残り全部が変更前とバイト一致すること(`git show HEAD:docs/NIGHTLOG.md` と、目次部分を取り除いた新ファイルを `diff` して**差分なし**を確認)。**削除行0だけでは「既存行を書き換えて同じ行数にした」場合に気付けない**ので、この2つは両方やること。
  5. `node scripts/check-all.mjs` が **34本中34本 PASS または FLAKY(exit 0)**。**FLAKY が出たら本数を NIGHTLOG に記録すること**(下記の申し送り)。
  6. `git diff --numstat assets/ fixtures/ index.html demo/ scripts/` が**空**(本番に配信されるファイルと検査スクリプトは1バイトも変わっていない)。
- **検証手順(撮影)**:
  - **撮影は不要です。** 変更するのは文書2本のみで、本番に配信されるファイル(`index.html` `assets/` `demo/` `fixtures/`)を1バイトも変更しないためです。画面は変わりようがありません。
  - 代わりに、**完了条件1・2・4の実行結果をそのまま NIGHTLOG に貼る**こと。
- **変更禁止範囲(絶対に触らない)**: `assets/geo.js` / `assets/engine.js` / `WEIGHT` 定数 / `assets/app.js` / `assets/*.css` / `index.html` / `demo/*.html` / `fixtures/*.json` / `docs/check.mjs` / `scripts/` 配下の全ファイル(検査34本・`check-all.mjs` 含む) / `.github/workflows/*` / `README.md` / `docs/AUTOPILOT.md` / `docs/CHECKS.md` / **NIGHTLOG の既存本文(目次の挿入と末尾への追記のみ許可)**。
- **所要目安**: 25〜35分(見出しの採取と形式2種の確認8分・目次の生成と行番号の再採番10分・完了条件1〜4の検証10分・`check-all.mjs` 通し6分・記録とコミット7分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- R64 の残り — CI を `schedule` で1日1回回すかの判断のみ(本体は完了)。
- 判断不要で残っているもの: **R217(embed-check の OGカード・実測で `og:` 0行/`description` 0行)・R216(404)・R219(README照合)**・R211(dump-rank に日時とコミットID)・R102(`scripts/list-shots.mjs`)。
  R220 の次は **R217(1枚の取り残しを揃えるだけ)→ R219(README照合・文書のみ)→ R216(404。ただし新設ページを同じサイクルで `docs/check.mjs` の `HTML_PAGES` に足すこと。現在は4ページのみが対象)** の順を推奨。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。R223 の仕上げ実行で**本物のフレークが1本出ました**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE` で1回目に落ち、再実行で通過)。**この環境ではフレークが1回の通しで1本出る頻度**です。**FLAKY の本数が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **起票の中に書かれた「こうすれば簡単」も疑う**(R220 の教訓。起票は「規約どおり `^### ` で拾えばよい」と書いていましたが、実測すると見出しは2形式・62個が別形式で、そのまま従うと目次が抜け落ちるところでした)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる。
- **「今回だけの偶然」と片付ける前に、過去に何回起きたかを数える**(R223 の教訓。1回の報告では偶然に見えたものが、数えたら4日で5回・9種類の検査にまたがっていました)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立し、R222 で「直す前は緑のまま通る」の実証まで進めた手順)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214 はデータURIで新規ファイル0件、R223 は既存スクリプト内で完結、R220 は NIGHTLOG の中に目次を置く)。
- **自動でうまく処理する仕組みは「問題を隠す方向」に働かないか考える**(R223 の教訓。だから FLAKY を PASS と別表示にし、本数を数えられるようにしました)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

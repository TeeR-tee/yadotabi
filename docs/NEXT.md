# NEXT: 2026-09-20 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、2100行あるので朝は読まなくて大丈夫です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ(Windows手動) | **GitHub Actions で全PASS**(R64) |
| 営業用デモ | 日本語1枚のみ | **英語版 `demo/hotel-page-en.html` 追加**(R85)+ README冒頭に入口(R207)+ OGカード(R208) |
| 営業デモの検査 | 2枚がノーガード | **`scripts/check-demo.mjs` 新設**(R218) |
| JS無効時の案内を守る検査 | 1本も無かった | **`scripts/check-noscript.mjs` 新設で検査 34本に**(R221) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の日本語の案内**(R215・本番反映確認済み) |
| サイトアイコン(favicon) | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214・新規ファイル0件のデータURI) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。


## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R222** — 検査が「コメントアウトされたコード」を本物と誤判定する穴の一斉点検

**なぜ R222 なのか(すべて実測して決めました)。**

R214 で favicon の検査を作ったとき、**検査そのもののバグ**が見つかりました。`rel="icon"` をコメントアウトした状態でも「ある」と誤判定していたのです(正規表現がHTMLコメントの中のタグを拾っていた)。同じ穴が他の34本にも無いかを、2方向から実測しました。

| 測ったこと | コマンド | 結果 |
|---|---|---|
| コメント除去を実装している検査は何本か | `grep -lE "replace\(/<!--" scripts/*.mjs docs/check.mjs` | **`docs/check.mjs` の1本だけ**(R214 で入れた `156行目` の1行)。`scripts/` の34本は **0本** |
| ファイルの中身を文字列として読んで正規表現で判定する検査 | `grep -l readFileSync scripts/check-*.mjs` | **5本**(`check-bundle` `check-engine` `check-geo` `check-osmfallback` `check-r5`) |
| そのうち「読んだ文字列に直接 match/includes する」= 穴の候補 | `grep -cE "(geoSrc\|engSrc\|src)\.(includes\|match\|…)"` | **`check-bundle.mjs` の4か所のみ**(58・61・65・67行目)。残り4本は `vm.runInContext` で**実行してから**検証しているので、コメントは無視され**穴ではない** |
| 判定対象ファイルに実際にコメントがどれだけあるか | `grep -cE "^\s*//" assets/geo.js assets/engine.js` | `geo.js` **491行**・`engine.js` **374行**。誤判定の土壌は十分にある |
| もう一方の方向: ブラウザで取ったHTML文字列を判定する検査 | `grep -lE "innerHTML\|outerHTML\|page.content\(\)" scripts/check-*.mjs` | **0本**。ブラウザ側は DOM を触るので**この穴は無い** |

**結論として、穴は「34本のうち `check-bundle.mjs` の4か所」に絞り込めました。** 「危なそうだから全部直す」のではなく、**実測で4か所と分かったものを直す**のが今回のタスクです。とくに 58・67行目の `var CATEGORY_LABELS = {…}` / `var THEME_OF = {…}` を拾う正規表現は、**その定義がコメントアウトされたり、コメント内に古い定義が残っていたりすると、消えた定義を「ある」と誤報告して緑のまま通ります**。これは検査の存在意義そのものを損ないます。

このタスクは**順位ロジックを1行も変えません**(`geo.js` `engine.js` は読むだけで、書き換えません)。判断待ちの2件にも依存しません。

- **タスクID**: R222
- **目的**: `check-bundle.mjs` が JavaScript のコメントを本物のコードと取り違えないようにし、「わざとコメントアウトすると赤くなる」ことを実証する。あわせて、残り33本は**実測の結果この穴が無いこと**を文書に記録して、将来の同じ調査を繰り返さないようにする。
- **変更するファイル(絶対パス・この4件のみ)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-bundle.mjs`(43・44行目で読んだ `geoSrc` / `engSrc` から**JSコメントを除去した別の変数**を作り、58・61・65・67行目の判定をそちらに向ける。**元の `geoSrc` / `engSrc` を使っている他の箇所は一切変えない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\CHECKS.md`(`check-bundle.mjs` の説明に「コメントを除いてから判定する」を追記。**34本という現在値は変わらない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R222 を追記して `[x] 2026-09-20` に)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-20 R222 …` 見出し+3行)
- **コメント除去のやり方(注意点)**:
  - `//` から行末まで と `/* … */` の2種類を除去する。**文字列リテラルの中の `//`(例: `'https://…'`)を消さないこと**。単純な正規表現だとURLを壊して逆に誤判定を生むので、**除去したあとの文字列でも 58・61・65・67行目の4つの判定がすべて従来どおり通ること**を先に確認してから進める。
  - 迷ったら「行頭が空白+`//` の行を丸ごと落とす」までに留めてよい(実測で `geo.js` 491行・`engine.js` 374行がこの形)。**安全側に倒すこと**。
- **完了条件(検証可能な形)**:
  1. `node scripts/check-bundle.mjs` が **exit 0**(除去処理を入れても従来の判定結果が変わらない)。
  2. **落ちる検査であることの検証**: `assets/engine.js` の `var FALLBACK_THEME = '…'` の行を**一時的にコメントアウト**して `node scripts/check-bundle.mjs` を実行し、**FAIL して exit 1 になること**を確認する。確認後は**必ず元に戻し**、`git diff assets/engine.js` が**空**であることを目で確かめる(R218・R221・R214 と同じ手順)。
     - ※ 修正前の状態で同じことをすると**緑のまま通ってしまう**はずです。**先に修正前で1回試して「緑のまま通る」ことを実測し**、その値を NIGHTLOG に書くこと(これがバグの存在証明になります)。
  3. `node scripts/check-all.mjs` が **34本中34本PASS**(exit 0)。合計時間を NIGHTLOG に記録。
  4. `git diff --numstat assets/` が **空**(順位ロジックは1行も変わっていない)。
  5. `docs/CHECKS.md` の**現在値を述べている箇所がすべて34本であること**(検査ファイルは増減しません。**過去の経緯として32本・33本と書いてある歴史的な記述は正しいので消さないこと**)。
  6. NIGHTLOG に、**「残り33本にこの穴が無い」と判断した根拠のコマンドと実測値**(上の表の5行)をそのまま残すこと。将来同じ調査を再実行しなくて済むようにするため。
- **検証手順(撮影)**:
  - **撮影は不要です。** 変更するのは検査スクリプト1本と文書3本のみで、**本番に配信されるファイル(`index.html` `assets/` `demo/`)を1バイトも変更しない**ためです。画面は変わりようがありません。
  - 代わりに、`git diff --numstat` の出力(変更が `scripts/check-bundle.mjs` と `docs/` の3本に限られていること)を NIGHTLOG に貼ること。
- **変更禁止範囲(絶対に触らない)**: `assets/geo.js` / `assets/engine.js` / `WEIGHT` 定数 / `assets/app.js` / `assets/*.css` / `index.html` / `demo/*.html` / `fixtures/*.json` / `docs/check.mjs` / `scripts/check-*.mjs` のうち `check-bundle.mjs` **以外の33本** / `scripts/check-all.mjs`(本数は増やさない) / `.github/workflows/*` / `README.md`。
- **所要目安**: 25〜35分(修正前に「緑のまま通る」ことの実測5分・コメント除去の実装8分・4つの判定が従来どおり通ることの確認5分・わざと落とす検証と復元5分・`check-all.mjs` 7分・記録とコミット8分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- R64 の残り — CI を `schedule` で1日1回回すかの判断のみ(本体は完了)。
- 判断不要で残っているもの: **R217(embed-check のOGカード・実測で `og:` 0行)・R216(404)・R219(README照合)・R220(NIGHTLOG目次・2106行/210見出し)**・R211(dump-rank に日時とコミットID)・R102(`scripts/list-shots.mjs`)。
  R222 の次は **R217(1枚の取り残しを揃えるだけ・検査は `docs/check.mjs` に相乗り可)→ R219(README照合・文書のみ)→ R216(404。ただし新設ページを同じサイクルで `HTML_PAGES` に足すこと)** の順を推奨。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(今回の R222 も、「ファイルを読む側」と「ブラウザでHTMLを取る側」の2方向で数え、後者は0本と確認しました)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけです。
- **新しく作った検査は「わざと壊して赤くなること」まで確認する**(R218 で確立した手順)。今回はさらに一歩進めて、**修正前に「壊しても緑のまま」を実測してからバグを直す**手順を取ります。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214 はデータURIで新規ファイル0件にできました)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

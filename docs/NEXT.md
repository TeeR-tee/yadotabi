# NEXT: 2026-09-21 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、**先頭に261件の目次が付いた**ので「Rいくつの話がどこにあるか」は探せるようになりました。R220 完了)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**34本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**(R85・R207・R208)+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の案内**(R215)+**検査で保護**(R221) |
| サイトアイコン | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214) |
| 検査がコメントを本物と誤判定 | 4か所が素通り | **解消**(R222・「壊しても緑のまま」を先に実証) |
| 検査がときどき理由なく赤くなる | 1回落ちたら赤が確定 | **PASS / FLAKY / FAIL の3値+自動1回再実行**(R223) |
| NIGHTLOG(2123行) | 朝に読めない | **先頭に261件の目次**(R220) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224・順位は5エリアで差分なしを実証) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。


## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R229** — R224 で出した人気バッジを、検査1本で守る(34本→35本)

**なぜ R229 なのか。** R224 で新機能を入れたのに、**それを守る検査が1本も無い状態**です。本来 R224 の完了条件に「検査を1本足して35本化」が入っていましたが、司令塔の指示要約がその条件を落とし、かつ `scripts/` を変更禁止範囲に書いてしまったため、作業役が(正しく)禁止側を優先して申し送りにしました。**その後始末を最優先でやります。** 新機能は、次の誰かが `fameText()` をうっかり触った瞬間に静かに壊れます。

**守る対象(`assets/app.js` L1043〜L1080 の `fameText()` を実際に読んで確定した規則)。**

| 規則 | 実装上の根拠 | 壊れたときの害 |
|---|---|---|
| `backlinks >= 150` → `★ 全国的によく知られた場所` | `FAME_NATIONAL_MIN = 150`(L1061・L1073) | 全カードに出て情報量が消える |
| `60 <= backlinks < 150` → `★ この地方でよく知られた場所` | `FAME_LOCAL_MIN = 60`(L1062・L1074) | 同上 |
| `backlinks` が 0・欠落・非有限 → **何も出さない** | L1072・L1074 | **「人気0」という存在しない評価を出す**(R224 で一番避けたかった事故) |
| 理由行が `このあたりの代表的な` で始まるカードには **地方級を出さない**(全国級は出す) | L1078 | 同じ範囲の話を2回言う重複が5エリアで10枚復活する |
| 表示は `.badge.feedcard__fame` 1個・文言の頭は `★ ` | L1158〜L1160 | — |

- **タスクID**: R229
- **目的**: 上の5行を**機械が守る**状態にする。**`assets/` は1バイトも変えない**(表示は既に正しい。今回は検査を足すだけ)。
- **変更するファイル(絶対パス・この4件のみ)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-fame.mjs`(**新規1本だけ**。`scripts/check-reason.mjs` の作りをそのまま踏襲する。`PLAYWRIGHT_IMPORT` 環境変数・`ensureServer()`・`AREAS = ['kusatsu','hakone','beppu','dogo','kinosaki']`・`ok()` の形を変えない=CIでも動く)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-all.mjs`(`SCRIPTS` 配列に1行足すだけ。**アルファベット順の位置=`check-embedbg` の前**に入れる)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\CHECKS.md`(本数・表・所要目安。下の完了条件6)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md` と `docs\NIGHTLOG.md`(R229 を `[x] 2026-09-21` に/末尾「## サイクル記録」へ `### 2026-09-21 R229 …` 見出し+3行。**先頭の目次にも1行足す**)
- **検査の中身(固定データ5エリアのみ。外部APIは1回も叩かない)**: 各エリアで `?fixture=<area>` を開き、`check-reason.mjs` と同じやり方でカード配列と DOM を取り、以下5点を判定する。
  1. **出る/出ないが両方ある**: 5エリアの合計で、バッジ付きカードが**1枚以上**かつ**全カードの枚数未満**。さらに**エリア単位でも各エリアが1枚以上**(R224 実測: 草津3/24・箱根5/22・道後4/18・別府6/17・城崎2/23)。「全部に出る」「1枚も出ない」のどちらでも落ちること。
  2. **0と欠落に出ていない**: バッジ付きカードの `_debug.backlinks` が**全て 60 以上**。言い換えると「バッジが付いた枚数」と「`backlinks >= 60` の枚数」が**エリアごとに一致**すること。
  3. **文言としきい値が対応**: 150以上のカードは必ず `全国的に`、60〜149 のカードは `この地方で`(ただし規則4の除外を先に適用)。**しきい値の数字 150/60 は検査側にも定数として書き、`assets/app.js` から正規表現で読んだ値と一致するか**も見る(片方だけ動いたら落ちる。`check-bundle` の R222 と同じで、**コメントを除去してから読むこと**)。
  4. **重複0件**: 同じカードに `💡 このあたりの代表的な…` と `★ この地方でよく知られた場所` が**同時に出たら落ちる**。5エリア合計で0件であること(全国級との同居は正常なので落とさない)。
  5. **バッジは1カードに最大1個**・文言は上の2種類以外を出さない。
- **完了条件(検証可能な形)**:
  1. `node scripts/check-fame.mjs` 単体が緑。5エリアの内訳(エリア名・バッジ枚数/全枚数・重複件数)を標準出力に**数字で出す**こと(緑だけの報告は不可)。
  2. `node scripts/check-all.mjs` が **35本中35本 PASS または FLAKY(exit 0)**。**FLAKY の本数を必ず NIGHTLOG に記録**(R223 の申し送り)。
  3. **★わざと壊して赤くなることを4通り実証する**(実行結果を NIGHTLOG に貼る。確認後は必ず元に戻し、戻した後に再度緑を確認する):
     (a) `FAME_LOCAL_MIN` を `0` にする → 規則1か2で赤。
     (b) L1072 の 0 除外を外して `backlinks` 0 にも出す → 規則2で赤。
     (c) L1078 の重複除外の1行を消す → 規則4で赤。
     (d) `FAME_NATIONAL_MIN` を `150` から別の数に変える → 規則3で赤。
     **4つとも「赤くなった」と確認できなければ、その規則は守られていません。**
  4. `git diff --numstat assets/` が**空**(`app.js` も `ui.css` も最終的に無変更)。実証で一時的に触るのは構わないが、コミット前に `git diff assets/` が空であることを確認する(**`git checkout` や `stash` は禁止なので、手で書き戻すこと**)。
  5. `git diff --numstat` の対象が上の4ファイル(新規1+既存3)だけであること。既存34本の検査スクリプトは1本も変更しない。
  6. **本数を持つ文書を同じサイクルで直す**: `docs/CHECKS.md` の **(i) 1行目の見出し「34本」→35本**、**(ii) 7行目「計33ファイル…合計34本」→34ファイル/35本**、**(iii) 23行目「34本の検査スクリプト」**、**(iv) 25行目「サーバを立てる29本」→30本**、**(v) 表に `| check-fame | …` の行を追加**(`grep -c "^| check" docs/CHECKS.md` が 33→34 になる。`docs/check.mjs` の行は別枠)、**(vi) 63行目の所要目安を今回の実測値で更新**(R223 実測は34本350.1s。今回の合計秒と最遅の本名を書く)。加えて `grep -rn "34本" docs/ README.md` を流して**書き漏れ0**を確認する(R212 の教訓)。
- **検証手順(撮影)**: 今回は表示を変えないため撮影は**不要**。代わりに **`node scripts/check-fame.mjs` の出力全文**と、完了条件3の(a)〜(d)の赤ログを NIGHTLOG の証拠にする。**本物APIは1回も叩かない。**
- **変更禁止範囲(絶対に触らない)**: **`assets/` 配下すべて**(`app.js`/`ui.css` 含む。実証で一時的に書き換えるのは可、最終差分は空) / `fixtures/*.json` / `index.html` / `demo/*.html` / `.github/workflows/*` / `docs/AUTOPILOT.md` / **既存34本の検査スクリプト**(`check-all.mjs` の `SCRIPTS` 配列への1行追加のみ許可) / **NIGHTLOG の既存本文**(末尾追記と目次への1行追加のみ許可)。
- **所要目安**: 40〜50分(既存 `check-reason.mjs` の読み込みと5エリアの実数確認8分・検査の実装15分・4通りの「わざと壊して赤」の実証12分・35本通し6分・CHECKS.md の6か所修正と記録・コミット8分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- 判断不要で残っているもの: **R228(何からの距離か書いていない・凡例1行)→ R227(1位の理由行が常に「歩いて行ける」・要 before/after)→ R226(more側が全件同じ見た目)** の順を推奨。ほかに R217(embed-check の OGカード)・R219(README照合)・R216(404)・R211・R102。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229 の教訓。R224 は表示を入れたのに守る検査が0本のまま1サイクル空いた。**司令塔の指示要約が元の完了条件を落とすと、作業役は落ちた条件を復元できない**。要約は元文書の完了条件の件数と突き合わせること)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。R223 の仕上げ実行で**本物のフレークが1本出ました**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE` で1回目に落ち、再実行で通過)。**この環境ではフレークが1回の通しで1本出る頻度**です。**FLAKY の本数が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224 の教訓。**起票は、起票した時点で気付いていたことしか含みません**)。
- **「動いている前提で書かれた文書」を疑う**(R225 の教訓。**export されている=使われている、ではありません**)。
- **起票の中に書かれた「こうすれば簡単」も疑う**(R220 の教訓)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる。
- **「今回だけの偶然」と片付ける前に、過去に何回起きたかを数える**(R223 の教訓)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立し、R222 で実証まで進めた手順)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220 はいずれも新規ファイル0件)。
- **自動でうまく処理する仕組みは「問題を隠す方向」に働かないか考える**(R223 の教訓)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

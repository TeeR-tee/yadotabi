# NEXT: 2026-09-24 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にあります。**先頭の目次は `##` 見出しの47件**です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 | **230文字の案内**(R215)+**検査で保護**(R221) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 「徒歩1分・75m」が何からの距離か | 画面に一度も書かれていない | **`距離は「(宿名)」からの目安です`**(R228) |
| 「もっと見る」の先の同じ形の帯 | 読み飛ばす手がかり無し | **`写真と解説がまだ無い場所 N件` の区切り**(R226・本番反映済み) |
| 草津の1位「湯畑」の説明 | **1行も出ていなかった** | **`Wikipediaに記事がありません。写真と地図の情報で表示しています。`**(R234・本番反映済み) |
| URLを間違えた人が見る画面 | GitHub既定の**英語**404 | **日本語の404ページ**+**リンク軸と中身軸の2軸検査**(R216・本番確認済み) |
| 地図の観光地ピン | 押しても何も起きない死んだピン | **押すと対応カードへ移動して光る+名前のツールチップ**(R237・本番で実タップ確認済み) |
| 展開後のピン104本 | 検査が触れていたのは5本だけ | **104本すべてを照合(3軸41項目)**(R238・破壊実証3通りで旧版は完全素通り) |
| 検査の通し時間 | 616.5s | **361.1s**(R239 で 465.2s → R240 で 361.8s。**照合は1つも減らさず**、固定待ちを「描けたか」を見る待ち方に置換) |
| 人気バッジが初期5枚で出る枚数 | **8/25枚**(草津 **0/5**) | **10/25枚**(草津 **1/5**・別府 2→3)(R241。**しきい値も文言も1つも変えず**、抑制規則を「同じカテゴリが2枚以上あるときだけ」に狭めた) |
| 人気バッジ「★」が何を根拠にしているか | 画面に1文字も無し | **`★は、百科事典で他の記事からどれだけ触れられているかで付けています`**(R242・数字なし・本番反映済み) |
| 直近の通し | — | **35本全PASS・382.5s**・**★FLAKY 1本**(`check-attrib`。17サイクル連続0本が途切れた。原因は通信エラー `ERR_NO_BUFFER_SPACE` で照合の失敗ではない。**計画役が3回再実行して再現せず**) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。

## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R236** — **「もっと見る」を押す前に、その先にどんなテーマの場所があるかが見えない問題を直す**

**なぜこれを選んだか(★計画役が5エリアを実際に開いて展開し、DOM から見出しを数えてから決めました)**:
- **実測1(A=FLAKY調査は落選。再現しませんでした)**: 申し送りに従い**まず再現を試しました**。`node scripts\check-attrib.mjs` を**3回連続実行 → 3回とも exit 0・222 pass / 0 fail**で `ERR_NO_BUFFER_SPACE` は**1度も出ませんでした**。さらに **`check-all.mjs:145` は `for (const script of SCRIPTS)` + `spawnSync` の完全な逐次実行**で、**同時に起動する Chromium は常に1つ**。司令塔の「同時起動数を疑う」という前提は**実装上そもそも成立しません**。動的ポート **16384本**に対し `TIME_WAIT` は **184本**(約1%)で枯渇の兆候なし。NIGHTLOG の grep でも実フレークは **R223 と R242 の2回のみ**。**再現しないものは追わず**、推移の記録だけ続けます(完了条件13)。
- **実測2(R236 の起票文にあった「1サイクルに収まらない」は誤りでした)**: 前サイクルの NEXT.md は「テーマ名の文言設計を5エリア分実測する必要があり1サイクルに収まらない」としていましたが、**`assets\engine.js` の `THEME_OF`/`FALLBACK_THEME` が既にテーマ名を持ち、`bundle()` が返すラベルがそのまま `h3.feedbundle` として画面に出ています**。**文言設計は1文字も要りません**。5エリアを展開して DOM から実測した見出し:

| エリア | 展開後カード | 見出し本数 | 実際の見出し(DOM実測) |
|---|---|---|---|
| 草津 | 24枚 | **4本** | 歴史を歩く3 / 景色を見にいく5 / そのほか4 / 写真と解説がまだ無い場所7 |
| 箱根 | 22枚 | **6本** | この土地の名物2 / 景色を見にいく4 / 緑をゆっくり4 / 歴史を歩く3 / 湯を楽しむ3 / そのほか1 |
| 道後 | 18枚 | **6本** | 屋内でじっくり2 / 景色を見にいく2 / 緑をゆっくり4 / 歴史を歩く2 / 湯を楽しむ2 / 写真と解説がまだ無い場所1 |
| 別府 | 17枚 | **5本** | 歴史を歩く3 / 景色を見にいく2 / この土地の名物2 / 屋内でじっくり3 / そのほか2 |
| 城崎 | 23枚 | **7本** | 景色を見にいく3 / 湯を楽しむ4 / 歴史を歩く2 / 屋内でじっくり3 / この土地の名物2 / そのほか1 / 写真と解説がまだ無い場所3 |

  **「歴史が好きだった人は歴史の関連のところに行きたい」というみのるんの思想に対し、`歴史を歩く` は5エリア全部に実在します。**押した先には既にあるのに、押す前は `もっと見る` の5文字しか無い(`app.js:1397`)のが現状です。
- **実測3(★B=R243 の後回し判断は覆りませんでした)**: `app.js:1876` の `bindTooltip(..., { permanent: false })` と `app.js:1877` の `m.on('click', goToCardFromPin)` を実際に読んで確認しました。**スマホではツールチップは押すまで出ず、押した瞬間にカードへ飛ぶ**ので読む瞬間がほぼ無い、という前サイクルの判断は**コード上そのとおり**です。**覆す根拠は見つかりませんでした**ので R243 は今回も見送ります。
- **実測4(★実装を阻む既存検査を先に見つけました。ボタンの中には足せません)**: **`scripts\check-more.mjs:70` が `moreBtnText.trim() === 'もっと見る'` という完全一致**を課しています(R152 で件数表記を廃止したときの歯止め)。したがって**テーマ名をボタンの中に入れると既存検査が赤くなります**。**ボタンの外に兄弟要素として1行出す**形にすること(R228 が `#feed-origin` でやったのと同じ形)。**この制約は着手時に自分でも `grep -n "もっと見る" scripts\check-more.mjs` で再確認すること**。

- **タスクID**: R236 / **目的**: **「もっと見る」を押す前に、その先にあるテーマ名が画面で読めるようにする**。テーマ名・束分けロジック・カードの枚数や並びは**1つも変えない**(`bundle()` が既に返しているラベルをそのまま出すだけ)。
- **対象ファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(**`moreHtml()`(`:1394-1398`)のみ**。`moreBundledHtml()`(`:1477-`)と `BARE_BUNDLE_HEAD`(`:916`)は**1バイトも触らない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-more.mjs`(**守る軸を足す**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R236 を `[x] 2026-09-24`。**R243・R244・R235・R211・R102 は起票のまま残す**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-24 R236 …` 見出し+3行以上。**目次エントリは `##` 見出しの47件のみ**)
  - `assets\style.css` / `assets\ui.css` は**触りません**(**新しい色もクラスも作らない**。既存 `.feedorigin`(`style.css:412`。`overflow-wrap:anywhere`+`line-height:1.4` を持つので375pxで崩れません)か `.morebtn` 周辺の既存余白に相乗りすること)
- **完了条件(検証可能な形・★互いに矛盾しないことを確認済み)**:
  1. **★まず現状を自分で実測する**。5エリアを展開して `h3.feedbundle` の本数と文言を DOM から記録(計画役の実測: **草津4・箱根6・道後6・別府5・城崎7**)。**再現しなければそちらを信じること**。
  2. **★件数の数字を出さない**。「あと19件」「7件」のような**件数・数字を押す前の行に出す案は禁止**(みのるん本人の設計思想。R152 が一度廃止済み)。**出してよいのはテーマ名そのものだけ**。なお展開**後**の `h3.feedbundle` には既に `◯件` が付いていますが、**これは既存の表示なので1バイトも変えないこと**(今回足すのは押す**前**の行だけ)。
  3. **★テーマ名を作らない・言い換えない**。`bundle()` が返すラベルを**そのまま**使う(`歴史を歩く` 等)。**`engine.js` の `THEME_OF`・`FALLBACK_THEME` は変更禁止範囲**です。
  4. **★表示の文言に評価の語を使わない**(R227・R231・R226・**R242**)。**「おすすめ」「人気」「必見」「最高」等は禁止**。既存の禁止語リストは `check-fame.mjs:73` の `BANNED_WORDS` と `check-bundle.mjs:202` の `JUDGING_WORDS` で、**どちらかを流用してよいか実物で確かめること**。また**★ボタンの中に入れないこと**(実測4)=**`check-more.mjs:70` の `=== 'もっと見る'` 完全一致を壊さない**こと(壊さざるを得ないなら**報告した上で**進める)。
  5. **★「もっと見る」が無い状態で行だけが浮かないこと**。`moreHtml()` は `if (!more.length) return '';` と `if (open) return '';` の2分岐を持つ(`:1395-1396`)ので、**(a) more が0件のとき (b) 展開後 (c) 宿未選択のトップ(状態A)** の3状態で**この行も一緒に消えること**を検査で押さえる。
  6. **★5エリアの撮影と Read での目視**(検証手順を参照)。**375px で新しい行が `scrollWidth <= clientWidth`(はみ出しなし)**・**テーマ名が7本ある城崎で行が折り返して縦に伸びすぎていないか**を必ず画像で確認する(**城崎が最悪ケース**)。
  7. **★順位不変の実証**: `node scripts\dump-rank.mjs <エリア>` を**5エリア分**、変更前後で取って**バイト一致**(`md5sum`)。`git diff assets/geo.js assets/engine.js fixtures/` が**空**であること。
  8. **★同じサイクルで守る検査も足す**。`check-more.mjs` に **(a) 有無軸** = 「5エリアの展開前にその行が存在し、**テーマ名を1つ以上含む**こと」+「**more が0件・展開後・状態Aでは出ないこと**」、**(b) 中身軸** = 「その行に出ているテーマ名が、**展開後に実際に出る `h3.feedbundle` の文言の部分集合**であること(**画面に無いテーマ名を名乗っていないこと**)」+「**数字(`/[0-9]/`)が1文字も含まれない**こと」+「**評価の語を1つも含まない**こと」を足す。**母数は必ず画面(DOM)から数えること**(展開後の見出しも DOM から取り、engine の再計算値と突き合わせないこと)。
  9. **★足した軸が本当に発火するか、壊して確かめること(R240 の教訓)**。**engine の再計算値ではなく画面(DOM)の値を見ること**。足した軸ごとに、**それだけを壊して発火する**ことを pass/fail で示す。
  10. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail で対比**する。**★その壊し方が本当に旧版を素通りするかを先に確かめること**(**司令塔が推した壊し方は8サイクル連続で外れています**。捕まるなら**自分で作り直すこと**)。候補は **(A) 行そのものを出すのをやめる**(有無軸が発火)と **(B) ★行は出したまま中身だけ壊す**(例: 実際には出ないテーマ名 `グルメを楽しむ` を1つ混ぜる = **有無軸は緑のまま中身軸だけが赤**)の2通り。**(B) が旧版で素通りすること**を先に確かめ、**R216・R234・R238・R241 と同じ「有無は緑・中身だけが赤」の形**を再現すること。
  11. **★`assets\` の一時変更は md5 で完全復元を確認**。**基準値は着手時に自分で `md5sum` を取ること**(2026-09-24 の計画役実測: `assets\app.js` = `2730d54a7c72ca8726d9c2ba04626551`(**R242 で変わりました。旧値 `f9029fd7…` は無効**)・`assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583`・`assets\ui.css` = `c3db293066104848b317b7d04798638d`)。**今回は `app.js` を本当に変えるので最終 diff は空になりません**。**`style.css` と `ui.css` は最終 md5 が基準値と一致する**こと(= 1バイトも変えない)。
  12. `node scripts\check-all.mjs` が **35本中35本PASS または FLAKY(exit 0)**。**本数は35本のまま**(**新規ファイルを作らない**)。
  13. **★FLAKY の本数を必ず NIGHTLOG に記録**(**R242 で1本出て、17サイクル連続0本が途切れました**。今回が0本に戻るか、また出るかを書くこと)。
  14. **本番へ push 後、実ブラウザで草津を1回開いて「もっと見る」を押す前後を確認**(AUTOPILOT ルール9)。
- **検証手順**:
  - 撮影は `node C:\workspace\tools\shot\shot.mjs <URL> --mobile`(375px)と PC幅。**URL は固定データで `http://localhost:<port>/?fixture=kusatsu`(および hakone / dogo / beppu / kinosaki)**。**5エリアすべて撮り、Read で画像を開いて目視**すること。**城崎(テーマ7本=最悪ケース)は必ず含める**。**外部APIを叩く撮影は最大2回まで**(AUTOPILOT ルール4)。
  - `node scripts\check-more.mjs` と `node scripts\check-bundle.mjs` 単体で pass/fail と秒数を記録(変更前後)。
  - **壊し方(A)(B)を先に旧版で走らせて素通りを確認**(旧版は `git show HEAD:scripts/check-more.mjs` で取り出す。**`git checkout`/`stash`/`reset` は使わない**) → 新版で落ちることを確認(完了条件10)。
  - `node scripts\dump-rank.mjs` 5エリア分の変更前後バイト一致(完了条件7)。
  - `node scripts\check-all.mjs` の通しを1回(秒数と FLAKY 本数を記録)。
- **変更禁止範囲(絶対に触らない)**: **`assets\geo.js` / `assets\engine.js`(`THEME_OF`・`FALLBACK_THEME`・`bundle()` を含む) / `WEIGHT` 定数(順位ロジック) / `fixtures\*.json`(読むだけ・再生成しない)** / `assets\style.css` / `assets\ui.css` / `404.html` / `demo\*.html` / `.github\workflows\*` / `docs\AUTOPILOT.md` / `scripts\dump-rank.mjs` / **`scripts\check-*.mjs` のうち `check-more.mjs` 以外の34本** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記のみ許可) / **ROADMAP の R243・R244・R235・R211・R102**(起票のまま残す)。
  - `index.html` は**原則触らない**(今回は `moreHtml()` が文字列を返すだけなので不要のはず)。**★禁止範囲に矛盾を見つけたら報告した上で最小限の変更で進めてよい**(R228)。**ファイルの場所は自分で grep して確かめること**。
- **所要目安**: 30〜40分(現状実測5分・実装6分・5エリア撮影と目視7分・検査の軸追加と発火確認7分・破壊実証の旧版素通り確認6分・dump-rank 3分・35本通し7分・記録とコミット6分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- **R244(判断が要ります)**: `fixtures\kusatsu.json` の `backlinks` が **28件**しかなく、他エリア(箱根73・道後70・別府49・城崎41)の半分以下。**★R241 の申し送り: 城崎 1/5・箱根 3/5 は表示側ではこれ以上動かせません**。初期5枚に **60〜149 の帯**にいるカードが1枚も無いためで、**しきい値を下げない限りどんな条件でも増えません**(計画役の実測: 60〜149 の帯は 草津5・箱根11・道後13・別府4・城崎3)。R244 と同じ根で、**fixture 再生成は順位を動かしうる**ため判断が要る。
- 判断不要で残っているもの(**R236 は今回着手**): **R243**(地図のピンに人気の手がかりが無い。**色分けは禁止**。ツールチップが `permanent:false` で読む瞬間が無い問題は未解決)、**R235**(`check-nosummary.mjs:146` の死んだ照合)、**R211**(dump-rank に日時とコミットID)、**R102**(list-shots.mjs)。**R217 は見送り済み**。**R233 は残件0件で完了**。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★司令塔が推す壊し方は7サイクル連続で旧版に捕まっている。着手前に必ず旧版で素通りするか実測し、捕まるなら自分で作り直すこと。**
- **★司令塔の指示書そのものにも誤りがある(今夜8回目)**(R241 で2件: 抑制規則の出典を「R231」としたが実際は **R224**・推した壊し方(B)が旧版で既に捕まった)。**実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい**(R228)。
- **★「軸を足した」と書くだけでは不十分。壊して実際に発火するところまで見ること**(R240 の教訓)。作業役が自分で足した母数軸が**死んだ軸**でした(`exp.all.length` が常に104で、壊しても発火しない)。**母数は必ず画面(DOM)から数えること**。
- **★表示側で動かせる限界を見極めること**(R241)。城崎・箱根の初期5枚は **60〜149 の帯にカードが1枚も無い**ため、表示条件をどういじっても増えません。**基準を下げて枚数を増やすのは「人気の基準を甘くする」行為で禁止**。
- **★検査の高速化は R240 で一区切り**(616.5s → **361.1s**)。最遅は `check-pinflash`(41.2s)・次点 `check-attrib`(27.1s)で削る余地は小さい。**みのるんが朝起きて見るのは画面です**。画面側か設計思想に関わるものを選ぶこと。
- **★削減量は「削る対象の秒数」ではなく A/B 実測で確かめる**(R239 の教訓。司令塔の「32.7s縮む」という見込みは実測 **2.1s** = 環境ゆらぎより小さかった)。**★速さのために検査を弱めない**(`force: true` は採らない。アクショナビリティ判定を飛ばすのは検査を弱める行為)。
- **★部分的な確認では見つからない破損がある。何かを全件確認する必要があるときは、機械的に全件突合すること**(R233-3。「前後数件を突合せよ」では**63%の破損を発見できませんでした**)。
- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226)。**本数の下限と中身の照合を別項目で持つ**。**R216・R234・R238・R241 では有無軸が全緑のまま中身軸だけが壊れを検知しました**。
- **★「作らない判断」も成果**(R232。**常に赤い検査は緑の検査より危険**)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231)。**★数え方の定義を実装と一致させる**(R226)。**★件数を引き継ぐときは、引き継いだ側が実物を開いて形を確かめること**(R216)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。**★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228・R237 では起票文の前提自体が外れました)。**★変更の影響は、関係する全ての分岐を列挙してから数える**(R227)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229・R216)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226・R234・**R242** はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225。export されている=使われている、ではない)。**同様に「検査があるから守られている」も疑う**(R234 で `check-nosummary.mjs:146` の `.every()` が死んでいた)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に8件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218)。**新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226・R234・R241 はいずれも新規ファイル0件)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232)。基準値は**毎サイクル自分で `md5sum` を取り直すこと**(2026-09-24 実測: `assets\app.js` = `f9029fd7a07be3454c403526929e57c7`(**R241 で変わりました。旧値 `4b401a3…` は無効**)、`assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583`、`assets\ui.css` = `c3db293066104848b317b7d04798638d`)。**ピンのCSSは `assets\style.css` にあります**(`ui.css` は該当0件)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。**本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

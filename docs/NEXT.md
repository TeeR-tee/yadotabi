# NEXT: R152 提案を「初期5件＋もっと見る10件＝合計15件」に絞り、件数表記を全廃する

- タスクID: **R152** / 難易度: 中〜大(`engine.js` の定数と `present()` の選別 + `app.js` 4箇所 + **既存 check 8本の30決め打ち追従 + `check-more.mjs` 全面書き換え**)
- 所要目安: 120〜150分(うち `check-all` 1回が約5分)

## 目的

市場調査(`計画書一式/10_市場調査_競合分析と提案軸.md`「何件出すべきか(2026-09-19 調査)」)の結論を実装する。
競合は**提案枠3〜5件・一覧枠10〜30件の二層**が標準で、**現在の30件は一覧の数字**。やどたびは「提案する」を名乗りながら一覧の件数を出しており、オーナー指摘「**69件の69に意味がない**」の構造的な原因がここにある。**初期5件＋もっと見る+10＝合計15件で打ち切り**、**件数を数字で言う文言を全廃**し、**理由の有無を選別基準に昇格**させる。

## 実測で判明した前提(計画役が5エリア fixture に engine を実ブラウザ上で直接走らせて測った)

### (1) 理由付き候補の残存件数 — 推奨15件とほぼ一致するが、5エリア中3エリアは15に届かない

| エリア | collect | cards | **理由あり** | 初期5件のカテゴリ多様性 | 展開後の合計 |
|---|---|---|---|---|---|
| 草津 | 154 | 30 | **14** | **5種**(寺院/動物園/公園/共同浴場/山頂) | 14 |
| 箱根 | 2770 | 30 | **14** | **3種**(寺院x2/山頂x2/観光名所) | 14 |
| 別府 | 447 | 30 | **14** | **5種** | 14 |
| 道後 | 380 | 30 | **16** | 4種 | 15 |
| 城崎 | 310 | 30 | **15** | **3種**(寺院x2/博物館x2/洞窟) | 15 |

**15件に届くのは道後・城崎だけ**。他3エリアは14件で自然に尽きる。これは欠陥ではなく設計どおり(「15件に理由を付けられた」が成果)だが、**「残りN件」を数字で出せない理由**でもある。また **`more` 側には reason が1件も付いていない**(`engine.js:1587-1590` が cards にしか reasonFor を掛けない)。

### (2) ★最重要: **落ちる有名どころが実在する**。理由生成ルールの不足であり、表示側で救ってはいけない

| 落ちる有名どころ | 順位 | 理由が付かない原因 |
|---|---|---|
| **湯畑**(草津) | cards 20位 | `categoryIndex=1`(観光名所の2番目。1位は熱乃湯) / proof=0 |
| **松山城**(道後) | cards 10位 | `castle` の2番目(1位は湯築城) / proof=2 |
| **別府地獄めぐり・龍巻地獄**(別府) | 15位/28位 | 観光名所の2・3番目 / proof=2 |
| **御所の湯・一の湯**(城崎) | 30位/more19位 | 共同浴場の2番目以降 / proof=0 |
| 大滝乃湯・御座之湯(草津) / 小田原城址公園・強羅公園(箱根) / 坂の上の雲ミュージアム・萬翠荘(道後) | 6〜29位 | 同カテゴリ2番目以降 / proof≤2 |

**救われている有名どころ**: 光泉寺・熱乃湯・本白根山・草津白根山 / 小田原城天守閣・早雲寺 / 竹瓦温泉・別府タワー・高崎山・うみたまご / 湯築城・石手寺・子規記念博物館 / 玄武洞・城崎マリンワールド・鴻の湯。

**fixture に元々存在しない**(geo/fixtures 変更不可のため R152 の範囲外 → **R154 として起票済み**): 箱根の**大涌谷・彫刻の森・芦ノ湖・箱根神社**、城崎の**城崎温泉(外湯の総称)**。箱根 fixture は実体が小田原寄りで、そもそも箱根の有名どころを持っていない。

**どう足せば救えるか**: 落ちる例すべてに共通するのは **`categoryIndex >= 1` かつ proof < 3**。現行 `reasonFor`(`engine.js:1440-1462`)は `categoryIndex===0` か proof>=3 しか拾わない。そこで:
- **規則6(新・今回入れる)**: `d.categoryIndex === 1` かつ 同カテゴリが cards 内に**3件以上** → `「<label>ならここも外せない」`。**湯畑・松山城・別府地獄めぐり・御所の湯・小田原城址公園が拾える**。挿入位置は既存の `categoryIndex===0` ブロックの**直後・proof 判定の前**(優先順を守る)
- **規則7(段階2送り)**: `d.official && d.both` → `「公式サイトがあり、2つの情報源で一致している」`。大滝乃湯・龍巻地獄・萬翠荘が拾えるが、**R151 で決めた「裏付け単独は理由にしない」方針に反する**ため、**規則6を入れて再実測し、それでも有名どころが残った場合のみ**検討する

### (3) 件数表記が今どこに出ているか — **全廃対象は実測2箇所だけ**

| # | 場所 | 現在 | 対処 |
|---|---|---|---|
| 1 | `app.js:1138` | `もっと見る（残り' + more.length + '件）` | **「もっと見る」だけにする**(数字を消す) |
| 2 | `app.js:1151` | `もっと遠く（車1時間以上）' + far.length + '件` | **「もっと遠く（車1時間以上）」だけにする** |
| 3 | `app.js:1136` | `<startNo>番以降は地図に表示していません。` | 「番」は順位であって件数。**段階1では現状維持**(R153 のピン方針で注記ごと不要になる) |

**触らないもの**: `app.js:239`(`[passive] 総件数` は `?passive=1` のデバッグ出力で本番UIに出ない)、`app.js:441/672`(「見つかりませんでした」は0件の案内)、`feedcard__no`(順位バッジ)。**「N件見つかりました」という文言は現コードに存在しない**(オーナーが見た「69件」は過去版か番号バッジ最大値の読み替え)。

### (4) 「もっと見る」の現在の作り(行番号つき)

| 行 | 内容 | 5+10 にするとどうなるか |
|---|---|---|
| `engine.js:29` `MAX_CARDS = 30` | 初期件数 | **`5` に変更**(意味が「切り出し後の件数」に変わるのでコメント更新) |
| `engine.js:30` `MAX_MORE = 30` | 展開分 | **`10` に変更** |
| `engine.js:1585` `slice(0, MAX_CARDS)` | 母数確定 | 選別を挟む(実装方針2) |
| `engine.js:1587-1590` `reasonFor(card, topCards)` | 理由付与 | **母数は上位30件のまま固定**(罠1) |
| `engine.js:1594` `slice(MAX_CARDS, +MAX_MORE)` | more | 理由付きのみから10件 |
| `app.js:108` コメント`// 31〜60件目` | — | 実態に合わせて更新 |
| `app.js:1133-1139` `moreHtml()` | ボタン | 件数表記削除(#1) |
| `app.js:1227-1228` / `:1239` | 連結描画・呼び出し | **変更不要**(`cards.length` 基準で自動追従し 6 が入る) |
| `app.js:1446` `feedmap--tall` を `cards.length >= 25` | 地図の高さ | **15件では永久に false = 背の高い地図が二度と出ないデグレ。`>= 5` に緩める** |
| `app.js:1464-1475` ピン生成 | 地図 | **段階2(R153)。段階1では触らない** |

### (5) ★地図のピンをどうするか — 設計判断

ピンは **`state.cards` だけ**から打たれる(`app.js:1464`)。`state.more` は展開してもピンが増えないので `app.js:1136` の注記が存在する。5+10 にすると素直には**初期ピン5本**になる。

- **A. cards(5件)だけ**: 現状ロジックのまま。**地図が寂しくなり「この一帯」の広がりが伝わらない**が、変更ゼロで安全
- **B. 展開後は15本**(`moreOpen` のとき `cards.concat(more)`): **カードと地図が完全一致**し注記#3 が不要になる。`renderFeedMap` の入力1行 ← **推奨**
- C. 常に15本: カード5枚と地図15本がズレ「押したらカードが無い」が起きる → **却下**
- D. 30本据え置き: cards が5件になると出所が無い → **却下**

→ **推奨は B。ただし段階2(R153)に回す**。段階1でピンまで動かすと `check-pinflash`/`check-lightbox`/`check-distance` が同時に落ちて原因を切り分けられない。**段階1は A のまま通し、注記#3 も現状維持。**

## 却下した案とその理由

- **チャンク化**(「湯上がりに歩ける3件」等の名前付きの束): 調査でも代替案とされ「**理由文の型が固まってから**」と明記。段階1でまさにその規則(規則6)を足すので、束ね方はその後。次サイクル以降
- **理由なし候補を下に置く / 別セクションに畳む**: 調査どおり**69問題が場所を変えて残るだけ**
- **rank の重みで有名どころを上げる**: **変更不可制約**。かつ順位を動かすと `check-debugflag` の「debug 有無で並び一致」が意味を失う
- **`feedmap--tall` の閾値を触らず放置**: 15件で永久に false になるデグレ。必ず直す

## 実装方針(段階1 = 今サイクルの R152 のみ。段階2は R153 として起票済み)

1. `engine.js:1440-1462` の `reasonFor` に**規則6**を追加
2. `engine.js:1585-1595` の `present()` を**「理由で選別してから切る」**順に変える — まず `cards` 上位30件を母数に `reasonFor` を掛け、`reason` が非 null のものだけを抜き、**先頭5件を `cards`・次の10件を `more`** にする
3. `app.js:1138` / `:1151` から**件数の数字を削除**
4. `app.js:1446` の `>= 25` を `>= 5` に緩める / `app.js:108` のコメント更新
5. **既存 check の30決め打ちを追従修正**(罠3)

## 実装上の罠

1. **★`reasonFor` の母数を5件にしてはいけない**。母数を5にすると「この一帯で唯一のX」が**5件中で唯一という意味に変わり事実として嘘になる**。母数は**上位30件のまま固定**し、切り出しだけ 5/10 にする。`check-reason.mjs` の項目(b)がまさにこれを見ている
2. **選別後は more にも reason が入る**ので、`check-reason.mjs` の項目**(c)「more/far に reason が付いていない」が必ず落ちる** → 仕様変更として **「far に reason が付いていない」に書き換える**
3. **★30決め打ちの check が8本**。すべて直す: `check-nosummary:88` / `check-passive:193` / `check-lightbox:232` / `check-debugflag:57,86,115-116` / `check-sample:122,187` / `check-imgfail:79,110` / `check-embedbg:103` / `check-engine:206`。**`check-more.mjs` は全面書き換え**(`:54` 初期30枚→5枚 / `:68` >30→>5 / `:75` morenote「31」→「6」 / `:84` 31番バッジ→6番)。`check-feednote:58` は位置比較なので**コメント修正のみ**
4. **理由付きが0件のエリア**では `cards` が空になり `emptyHtml`(`app.js:1232`)に落ちる。実測では最小14件なので起きないが、**理由付き0件のときだけ従来どおり上位5件を出す**フォールバックを入れる
5. 規則6で理由文の重複が増えうる。**初期5件に同じ文が3件以上並んでいないか目視する**(R151 の教訓)

## 対象ファイル(絶対パス)

- `C:\workspace\claude\旅行先用サイト\yadotabi\assets\engine.js`(29,30 / 1440-1462 / 1585-1595)
- `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(108 / 1133-1139 / 1151 / 1446)
- `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-more.mjs`(全面書き換え)+ 罠3 の check 8本

## 変更禁止範囲

**`rank()` の重み・閾値・並び順**(`WEIGHT` / `CATEGORY_FREE_SLOTS` / `scoreBreakdown` / `rank` 本体) / `assets/geo.js` / `fixtures/*.json` / 入力UIの追加(チップ・検索窓・フィルタの新設) / 外部API呼び出し(0回を維持)・コスト0円維持。

## 完了条件

1. 5エリアすべてで**初期カード5枚**、`もっと見る` 押下後が**14〜15枚**
2. `もっと見る`・`もっと遠く` から**数字が消えている**
3. **湯畑(草津)・松山城(道後)・別府地獄めぐり(別府)・御所の湯(城崎)が理由付きに入っている**(規則6の効果を実測確認)
4. 初期5件に**同じ理由文が3件以上並んでいない**
5. `?debug=1` の有無でカードの並びが完全一致(rank 無改変の証明)
6. `node scripts/check-all.mjs` が **30本全緑**

## 検証手順

1. `node scripts/check-all.mjs` → **30本すべて PASS**(必須)
2. 撮影(`screenshots/` に保存): `?fixture=kusatsu` 幅 **390**(初期5枚＋ボタン) / 同 `#more-btn` クリック後 幅 **390**(14枚展開) / `?fixture=dogo` 幅 **1280**(松山城に理由が付いていること) / `?fixture=hakone` 幅 **390**(初期5件のカテゴリ偏りを目視)
3. 撮影画像を**自分で開いて目視**し、理由文の重複・カード高さの暴れ・地図の高さを確認する

## 終わったら

`docs/ROADMAP.md` の R152 を **[x] 2026-09-19** にする → `docs/NIGHTLOG.md` の**ファイル末尾**のサイクル記録に**3行**追記 → **先にコミット** → `git push` → **報告前に `git log --oneline -1` で実際のハッシュを確認する**(推測で書かない)。

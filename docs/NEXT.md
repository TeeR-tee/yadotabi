# NEXT: 2026-09-30 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にあります。**先頭の目次は `##` 見出しの47件**です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 | **230文字の案内**(R215)+**検査で保護**(R221) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 「徒歩1分・75m」が何からの距離か | 画面に一度も書かれていない | **`距離は「(宿名)」からの目安です`**(R228) |
| 「もっと見る」の先の同じ形の帯 | 読み飛ばす手がかり無し | **`写真と解説がまだ無い場所 N件` の区切り**(R226・本番反映済み) |
| 草津の1位「湯畑」の説明 | **1行も出ていなかった** | **`Wikipediaに記事がありません。写真と地図の情報で表示しています。`**(R234・本番反映済み) |
| URLを間違えた人が見る画面 | GitHub既定の**英語**404 | **日本語の404ページ**+**リンク軸と中身軸の2軸検査**(R216・本番確認済み) |
| 地図の観光地ピン | 押しても何も起きない死んだピン | **押すと対応カードへ移動して光る+名前のツールチップ**(R237・本番で実タップ確認済み) |
| 展開後のピン104本 | 検査が触れていたのは5本だけ | **104本すべてを照合(3軸41項目)**(R238・破壊実証3通りで旧版は完全素通り) |
| 検査の通し時間 | 616.5s(`check-pinflash` が 168.6s で最遅) | **465.2s**(`check-pinflash` を **44.6s** へ。**ピン104本は1本も減らさず**、破壊実証3通りも短縮前と同じ pass/fail で落ちる)(R239) |
| 検査の通し時間(続き) | 465.2s(R239) | **361.8s**(R240。`check-reason` 79.7s→14.6s・`check-nosummary` 51.9s→13.2s。**照合は1つも減らさず**、固定待ちが実測の5〜25倍だったのを「描けたか」を見る待ち方に置換) |
| 直近の通し | — | **35本全PASS・FLAKY 0本(16サイクル連続)・361.8s** |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。

## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R241** — **「どれだけ人気なのか」が、いちばん見られる初期5枚でほとんど出ていない問題を直す**

**なぜこれを選んだか(★計画役が本番アプリを5エリア分ブラウザで実測してから決めました。R224・R226・R234・R237 と同じ見つけ方です)**:
- **実測1(出現率)**: `★ …よく知られた場所` バッジは、**展開後の全104枚では19枚**に出るのに、**みのるんが最初に見る初期5枚では25枚中8枚**しか出ていません(内訳: 草津 **0/5**・箱根 3/5・道後 2/5・別府 2/5・城崎 1/5)。**看板エリアの草津が初期5枚で0枚**で、README・営業デモ・スクリーンショットに載るのはまさにこの画面です。
- **実測2(原因は2つに分かれます)**: **(原因1)** `assets\app.js:1157-1158` の抑制規則「理由行が『このあたりの代表的な』で始まるカードには地方級を出さない」が効きすぎています。**草津2位の草津熱帯圏は backlinks 129(地方級の基準60以上)なのに、この規則だけで消えています**。初期5枚25枚のうち**11枚が『このあたりの代表的な…』の理由行**で、抑制が初期画面に集中していました。**(原因2)** **草津1位の湯畑は、`fixtures\kusatsu.json` の `backlinks`(28件)にも `wiki.query.pages`(50件)にも1件も存在しません**。R234 で「Wikipediaに記事がありません」と出ている通りで、**測る材料がそもそも無い**ので現状維持が正解です(0を「人気が低い」と出すのが R224 が一番避けたかった事故)。
- **実測3(順位には一切関係しません)**: `fameText()` は**表示専用**で、`WEIGHT` にも `engine.js` にも `geo.js` にも触れていません。**このサイクルで順位は1件も動きません**(`dump-rank.mjs` のバイト一致で実証すること)。
- **実測4(なぜ R236 より先か)**: R236 は「押す前にテーマを見せる」案ですが、**テーマ名は `bundle()` が展開時に計算するもので、押す前に出すには文言設計(375px での折り返し・テーマ数の上限)を先に実測する必要があり、1サイクルには収まりません**。R241 は**既に画面にある部品の出し分け条件1箇所**を直す作業で、対象が `fameText()` の中に閉じています。R235 は検査の内部品質で、**3サイクル続いた検査作業から画面側に戻す**今回の方針に合いません。

- **タスクID**: R241 / **目的**: **初期5枚で人気バッジが出る枚数を増やす**。ただし**増やすために基準を下げない**(150/60 のしきい値は動かさない)。直すのは**抑制規則の条件だけ**。
- **対象ファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(**`fameText()`(`:1148-1160`)の抑制条件のみ**。しきい値 `FAME_NATIONAL_MIN`=150 / `FAME_LOCAL_MIN`=60 は**動かさない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-fame.mjs`(**守る軸を足す**。現状は「もっと見る」を押した後しか見ていません=`:161-163`)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R241 を `[x]`。**R242〜R244 は今回は触らない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-30 R241 …` 見出し+3行以上。**目次エントリは `##` 見出しの47件のみ**)
  - `assets\style.css` / `assets\ui.css` は**触りません**(**新しい色もクラスも作らない**。既存 `.feedcard__fame`(`style.css:542`)をそのまま使う)
- **完了条件(検証可能な形・★互いに矛盾しないことを確認済み)**:
  1. **★まず現状を自分で実測する**。5エリアの**初期5枚**と**展開後の全枚数**で、バッジの枚数・文言・そのカードの理由行を記録(計画役の実測は 初期 8/25・展開後 19/104。**再現しなければそちらを信じること**)。`fixtures\*.json` の `backlinks` 件数も自分で数える(計画役の実測: 草津28・箱根73・道後70・別府49・城崎41)。
  2. **★抑制規則の条件を「同じ語が重なるときだけ」に狭める**。現行は「理由行が『このあたりの代表的な』で始まれば**地方級を一律で出さない**」。これを、**理由行の分類名(『動物園』『美術館・博物館』等)と人気の文言が同じ範囲の話になるときだけ抑制する**形にする。**具体的な条件は作業役が実測して決めてよい**が、**R224 が避けたかった「同じ範囲の話を2回する」状態を復活させないこと**を、実際のカード文言を並べて確かめること。**緩めた結果どのカードが増えるかを1枚ずつ列挙**し、**増えた各枚が『重なって見えないか』を自分の目で確かめる**(撮影した画像で)。
  3. **★backlinks が無い/0 のカードには今までどおり何も出さない**(湯畑型)。**0 を「人気が低い」と出さない**。これは R224 の中心的な判断なので、**緩める方向の変更でここが崩れていないことを検査で明示すること**。
  4. **★増やした枚数を数字で示す**。初期5枚での枚数が **8枚から何枚になったか**を5エリア別に記録。**増えていなければ取り下げてよい**(「作らない判断も成果」R232)。**★増やすためにしきい値(150/60)を下げる案は禁止**(それは「人気の基準を甘くする」行為で、表示の誠実さが崩れます)。
  5. **★5エリアの撮影と Read での目視**(検証手順を参照)。**バッジが理由行と並んで折り返していないか・375px で崩れていないか**を必ず画像で確認する。
  6. **★順位不変の実証**: `node scripts\dump-rank.mjs <エリア>` を**5エリア分**、変更前後で取って**バイト一致**(`md5sum`)。`git diff assets/geo.js assets/engine.js fixtures/` が**空**であること。
  7. **★同じサイクルで守る検査も足す**。`check-fame.mjs` に**(a) 母数軸** = 「**初期5枚(= もっと見るを押す前)**を5エリアで読み、合計25枚あること」+「その25枚のうちバッジが出ている枚数が**期待値と一致**すること」(**現状の検査は展開後しか見ておらず、初期画面の出現率を1つも守っていません**)、**(b) 中身軸** = 「バッジが出た各カードの backlinks がしきい値を満たすこと」+「**backlinks が無い/0 のカードにバッジが1枚も付いていないこと**」を足すこと。
  8. **★足した軸が本当に発火するか、壊して確かめること(R240 の教訓)**。母数軸の数え方に、**画面ではなく engine の再計算値を使わないこと**(R240 では `exp.all.length` が常に104で、壊しても発火しない「死んだ軸」になっていました)。**必ず画面(DOM)から数えること**。足した軸ごとに、**それだけを壊して発火する**ことを pass/fail で示す。
  9. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail で対比**する。**★その壊し方が本当に旧版を素通りするかを先に確かめること**(R233-2・R233-3・R216・R234・R238・R240 と**6サイクル連続**で、推された壊し方が旧版で既に捕まり作業役が作り直しています)。推奨は **(A) 抑制規則を元に戻す**(枚数だけが動く)と **(B) ★枚数を1枚も変えずに中身を壊す**(例: backlinks が 0 のカードにもバッジを出しつつ、別のカードから1枚減らして合計を保つ)の2通り。**(B) が旧版で素通りすること**を先に確かめ、**中身軸だけが検知する**形(R216・R234・R238 と同じ形)を再現すること。
  10. **★表示の文言に評価の語を使わない**。**今回は新しい文言を1つも作らない**(既存の2種類の文言をそのまま使い、出す条件だけを変える)。**新しい文言を足したくなったら、それは別タスクです**(R242 として起票済み)。**既存の文言や `aria-label` をそのまま流用してよいかは、実際の対象カードで嘘にならないか確かめること**(R234・R237 で2回実例)。
  11. **★`assets\` の一時変更は md5 で完全復元を確認**。**基準値は着手時に自分で `md5sum` を取ること**(2026-09-30 の計画役実測: `assets\app.js` = `4b401a30d73d86c816ac56f6fad9af47`・`assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583`・`assets\ui.css` = `c3db293066104848b317b7d04798638d`)。**今回は `app.js` を本当に変えるので、最終 diff は空になりません**。**`style.css` と `ui.css` は最終 md5 が基準値と一致する**こと(= 1バイトも変えない)。
  12. `node scripts\check-all.mjs` が **35本中35本PASS または FLAKY(exit 0)**。**本数は35本のまま**(**新規ファイルを作らない**)。
  13. **★FLAKY の本数を必ず NIGHTLOG に記録**(**16サイクル連続0本**)。
  14. **本番へ push 後、実ブラウザで草津を1回開いて確認**(AUTOPILOT ルール9。**固定データでは検証できない領域がある**)。
- **検証手順**:
  - 撮影は `node C:\workspace\tools\shot\shot.mjs <URL> --mobile`(375px)と PC幅。**URL は固定データで `http://localhost:<port>/?fixture=kusatsu`(および hakone / dogo / beppu / kinosaki)**。**5エリアすべて撮り、Read で画像を開いて目視**すること(バッジの位置・折り返し・理由行との重なり)。**外部APIを叩く撮影は最大2回まで**(AUTOPILOT ルール4)。
  - `node scripts\check-fame.mjs` 単体で pass/fail と秒数を記録(変更前後)。
  - **壊し方(A)(B)を先に旧版で走らせて素通りを確認** → 新版で落ちることを確認(完了条件9)。
  - `node scripts\dump-rank.mjs` 5エリア分の変更前後バイト一致(完了条件6)。
  - `node scripts\check-all.mjs` の通しを1回(秒数と FLAKY 本数を記録)。
- **変更禁止範囲(絶対に触らない)**: **`assets\geo.js` / `assets\engine.js` / `WEIGHT` 定数(順位ロジック) / `FAME_NATIONAL_MIN`=150 と `FAME_LOCAL_MIN`=60 の値 / `fixtures\*.json`(読むだけ・再生成しない)** / `assets\style.css` / `assets\ui.css` / `index.html` / `404.html` / `demo\*.html` / `.github\workflows\*` / `docs\AUTOPILOT.md` / `scripts\dump-rank.mjs` / **`scripts\check-*.mjs` のうち `check-fame.mjs` 以外の34本** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記のみ許可) / **ROADMAP の R242・R243・R244**(今回は起票のまま残す)。
  - **★この禁止範囲に矛盾を見つけたら、報告した上で最小限の変更で進めてよい**(R228 の教訓)。**ファイルの場所は自分で grep して確かめること**。
- **所要目安**: 30〜40分(現状の実測8分・抑制条件の設計と実装8分・5エリア撮影と目視6分・検査の2軸追加と発火確認6分・破壊実証の旧版素通り確認6分・dump-rank 突合3分・35本通し7分・記録とコミット6分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- **R244(判断が要ります)**: `fixtures\kusatsu.json` の `backlinks` が **28件**しかなく、他エリア(箱根73・道後70・別府49・城崎41)の半分以下。看板エリアで人気表示が出にくい根の原因だが、**fixture 再生成は順位を動かしうる**ため判断が要る。
- 判断不要で残っているもの: **R242**(人気バッジの根拠が画面に無い。**数字を出す案は禁止**)、**R243**(地図のピンに人気の手がかりが無い。**色分けは禁止**)、**R236**(「もっと見る」の先のテーマが押す前に見えない。**件数を出す案は選ばないこと**)、**R235**(`check-nosummary.mjs:146` の死んだ照合)、**R211**(dump-rank に日時とコミットID)、**R102**(list-shots.mjs)。**R217 は見送り済み**。**R233 は残件0件で完了**。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★「軸を足した」と書くだけでは不十分。壊して実際に発火するところまで見ること**(R240 の教訓)。作業役が自分で足した母数軸が**死んだ軸**でした。展開後の枚数を engine の再計算値(`exp.all.length`)で数えており、**展開の有無に関わらず常に104**で、壊しても発火しませんでした。**画面側(DOM)の値に直して発火を確認**しています。**母数は必ず画面から数えること**。
- **★検査の高速化は R240 で一区切り**(616.5s → 465.2s → **361.8s**)。最遅は `check-pinflash`(41.0s)・次点 `check-attrib`(28.1s)で削る余地は小さく、**3サイクル続けて検査の内部作業でした。みのるんが朝起きて見るのは画面です**。次は画面側か設計思想に関わるものを選ぶこと。
- **★削減量は「削る対象の秒数」ではなく A/B 実測で確かめる**(R239 の教訓)。司令塔の「350ms×109回を削れば32.7s縮む」という見込みは**実測では 2.1s = 環境ゆらぎより小さく**、真因は `scrollIntoView({behavior:'smooth'})` で**待ちを削った分だけ click が伸びる押し付け合い**でした。**原因を計測で特定したことで124s削れています**。
- **★速さのために検査を弱めない**(R239)。作業役は **`force: true`(1本5msまで速くなる)を採用しませんでした**。動いているピンを誤爆して不一致7件が出たためで、**アクショナビリティ判定を飛ばすのは検査を弱める行為**です。**後続サイクルで「もっと速く」と言われてもこの手は採らないこと**。
- **★部分的な確認では見つからない破損がある。何かを全件確認する必要があるときは、機械的に全件突合すること**（R233-3 の教訓。司令塔の指示「前後数件を突合せよ」では**63%の破損を発見できませんでした**。前後4件のうち3件がたまたま正しかったためです）。
- **★「この壊し方なら捕まえられるはず」は実測で確かめる**（R233-2・R233-3・R216 と**3サイクル連続**で、指示書が推した壊し方はどれも旧版で既に捕まり、作業役が作り直しました）。**推された案が効かないと分かったら、作り直すのが作業役の仕事**。
- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226 の教訓。別軸=合計の下限・中身の照合を必ず持つこと)。**R216 ではリンク軸が全緑のまま中身軸だけが壊れを検知しました**。
- **★「作らない判断」も成果**(R232 の教訓。**常に赤い検査は緑の検査より危険**)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231)。**★数え方の定義を実装と一致させる**(R226)。**★件数を引き継ぐときは、引き継いだ側が実物を開いて形を確かめること**(R216 で R233 の「残り6件」が実際は1〜3件だったと判明)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。
- **★司令塔の指示書そのものにも誤りがある**(R228。実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい)。
- **★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228)。**★変更の影響は、関係する全ての分岐を列挙してから数える**(R227)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229・R216)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226・**R234** はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225。export されている=使われている、ではありません)。**同様に「検査があるから守られている」も疑う**(R234 で `check-nosummary.mjs:146` の `.every()` が死んでいることが判明)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に8件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218)。**本数の下限と中身の照合を別項目で持つ**。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226 はいずれも新規ファイル0件。**R234 も新規ファイル0件**)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232)。基準値は**毎サイクル自分で `md5sum` を取り直すこと**(2026-09-29 実測: `assets\app.js` = `4b401a30d73d86c816ac56f6fad9af47`(R237 で変わりました。旧値 `fa5caf…` は無効)、`assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583`、`assets\ui.css` = `c3db293066104848b317b7d04798638d`)。**ピンのCSSは `assets\style.css` にあります**(`ui.css` は該当0件)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。
- **★司令塔が渡す数字も、起票文に書かれた前提そのものも、着手前に自分で実測して確かめる**(今夜だけで司令塔の数字の誤りが4回: R230 の実測コマンドのキー名・R233 の残件数・R233-3 の目次の突合範囲・R234 の目次件数273→実際は47。**R237 では起票文の「sticky にすべきか」という前提自体が実測で外れました**)。
- **★司令塔が渡す数字が誤っていた実例が今夜6回になりました**(直近は R238 の「未照合は最大15本中10本」。**実測は104本中79本で約8倍**)。**教訓: 司令塔の数字も含め、着手前に自分で実測して確かめる。母数の上限は仕様ではなくデータで決まることがある**。

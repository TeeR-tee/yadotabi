# NEXT: 2026-09-27 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、**先頭に目次が付いた**ので「Rいくつの話がどこにあるか」は探せます。**R216 で目次273件を全件振り直しました**)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 | **230文字の案内**(R215)+**検査で保護**(R221) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 「徒歩1分・75m」が何からの距離か | 画面に一度も書かれていない | **`距離は「(宿名)」からの目安です`**(R228) |
| 「もっと見る」の先の同じ形の帯 | 読み飛ばす手がかり無し | **`写真と解説がまだ無い場所 N件` の区切り**(R226・本番反映済み) |
| URLを間違えた人が見る画面 | GitHub既定の**英語**404 | **日本語の404ページ**+**リンク軸と中身軸の2軸検査**(R216・本番確認済み) |
| 直近の通し | — | **35本全PASS・FLAKY 0本(11サイクル連続)・431.5s** |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。

## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R234** — **草津の1位「湯畑」のカードだけ、説明が1行も出ていない**

**なぜこれを選んだか(計画役が本番を実際に開いて見つけました)**:
- **実測1**: 本番 `https://teer-tee.github.io/yadotabi/?fixture=kusatsu` をモバイル(375px)とPC幅で撮って目視したところ、**1位「湯畑」のカードは、名前→理由(`歩いて行ける`)→`観光名所 徒歩1分・車1分・75m`→ボタンと並び、説明文が1行もありません**。同じ位置で別府の1位「竹瓦温泉」は `竹瓦温泉（たけがわらおんせん）は、大分県別府市元町にある市営の共同温泉である。…` の2行が出ています。**同じ1位なのに、片方だけ無言**です。
- **実測2(原因)**: `assets/app.js:1213-1223` の `summary` の三項分岐で、**「写真はあるが Wikipedia 記事が紐づかない」カードだけが `''`(空文字)に落ちます**。`card.summary` 無し → `hasArticle` 偽(wikipediaTitle も wikidataId も無い) → `isPlaceholderMedia` 偽(写真がある) → **どの案内文にも当たらない**。R174 が「写真があるカードで『記事がありません』と出すのは矛盾して見える」として行ごと消した結果、**代わりの文が用意されないまま空白だけが残った**分岐です。
- **実測3(母数を数えました)**: `node scripts/dump-rank.mjs` を5エリア分回し、`Wikipedia要約有=×` かつ `画像有=○` のカードを数えると **kusatsu 1件 / hakone 0 / beppu 0 / dogo 0 / kinosaki 0 = 全104カード中1件**。**件数は1件ですが、その1件が「草津の1位」**で、READMEにも営業デモにも載る**やどたびで最も人が見るカード**です。**「宿を選ぶと、まわりの見どころが並ぶ」の『見どころ』を最初に説明する場所が空白**という点で、設計思想に直接当たります。
- **実測4(なぜ順位を触らずに直せるか)**: 直すのは `summary` の**最後の分岐(現在 `''`)に1行を足すだけ**で、`card` の中身も並び順も読みません。**`assets/geo.js` / `assets/engine.js` / `WEIGHT` / `fixtures/` には一切触りません**。

- **タスクID**: R234 / **目的**: **写真はあるが Wikipedia 記事が紐づかないカード**(現在5エリアで1件=草津の1位「湯畑」)に、**事実だけを述べる1行**を出す。**順位・並び・既存103カードの見た目は1バイトも変えない**。
- **変更するファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(**`summary` の最終分岐のみ**。`assets/ui.css` は**既存の `.feedcard__summary--none` を再利用して触らない**。新しいクラスを足したくなったら、まず既存クラスで足りないか確かめること)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-nosummary.mjs`(下記の2軸を追加。**本数は35本のまま**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R234 を起票して `[x]`+**R233 の残件を実測で数え直した結果**(完了条件7))
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-27 R234 …` 見出し+3行以上・**先頭の目次にも1行**。**目次に1行足すと下の全見出しが1行ずれるので、触ったら273件+1件を機械で全件突合すること**)
- **完了条件(検証可能な形)**:
  1. 草津の1位「湯畑」のカードに**説明の1行が出る**。**文言に評価の語・情緒語を使わない**(「残念ながら」「有名な」「人気の」「見逃せない」等は禁止。R227・R231・R226 の教訓)。**書いてよいのは「いま手元に Wikipedia の解説が無い」という事実と、地図の情報で出していることだけ**。**推測で場所の説明を作らない**。**入力欄・選択UIは置かない**(入力ゼロ原則)。
  2. **既存の103カードの表示が1バイトも変わらない**。`.feedcard__summary` を持つカード数と、`.feedcard__summary--none` を持つカード数の**変更前後の内訳を5エリア分の数字で示す**(増えるのは湯畑の1枚だけ、のはず。**増えた枚数が1でなかったら、その時点で分岐の理解が間違っているので止めて報告すること**)。
  3. **★検査の軸を2本にする。** (a)**有無軸**=「写真があり Wikipedia 記事が無いカードに、説明の行が存在する」(`.feedcard__summary` または `--none` が1つある)。(b)**中身軸**=その行の**文言が定数と完全一致する**ことと、**禁止語(`有名`/`人気`/`おすすめ`/`残念`/`申し訳`)が1つも含まれない**ことの照合。**本数の下限だけにしない**(R226 の教訓)。**母数が0件だと必ず緑になる書き方(`xs.filter(...).length === 0` だけ)にしない**こと(R232・R233 の教訓)。NIGHTLOG に「◯◯軸と◯◯軸の2本で支えている」と**軸の名前を書く**こと。
  4. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail の数字で対比**する(旧版は `git show HEAD:scripts/check-nosummary.mjs`。**`git checkout` / `git stash` / `git reset` は使わない**)。**2通り以上**行い、うち**最低1つは「枚数が1枚も変わらない壊し方」**にすること:
     - **壊し方A(消す)**: 足した1行をまた `''` に戻す。→ 旧版が何 pass / 何 fail かを先に数字で出すこと。
     - **★壊し方B(枚数が1つも変わらない壊し方)**: **行の数を1つも変えずに**、文言だけを**評価の語入り**(例:「有名な観光名所です」)に差し替える。枚数は不変だが中身は壊れている。**着手前に旧版で1回走らせて、本当に素通りするか(何 pass / 0 fail か)を先に確かめること。旧版が既に捕まえるなら、別の壊し方を自分で作り直すこと**(R233-2・R233-3・R216 と3サイクル連続で、計画役が推した壊し方が旧版で既に捕まっています)。
  5. **★順位不変の実証**: `node scripts\dump-rank.mjs <エリア>` を**5エリア分**、変更前後で取って**バイト一致**を示す(`md5sum` で可)。`git diff assets/geo.js assets/engine.js fixtures/` が**空**であること。
  6. **★`assets\` の一時変更は md5 で完全復元を確認**。最終状態で `assets\ui.css` = `c3db293066104848b317b7d04798638d`(**このタスクでは ui.css を触らないので基準値と一致するはず**)。`assets\app.js` は**意図した変更があるので md5 は変わります**が、`git diff assets/app.js` が**`summary` の分岐まわりだけ**であることを diff で示すこと。
  7. **★ROADMAP の R233 の残件を実測で数え直す**。計画役の実測では `scripts\check-nosummary.mjs` の `length === 0` は **4箇所(`:146` / `:181` / `:255` / `:474`)**。うち **`:181` はコンソールエラー0件の検査で母数の概念が無く対象外**。**`:146` は `hasArticleRows.length === 0 && hasArticleRows.every(...)` という形で、`&&` の左が真のとき `hasArticleRows` は必ず空配列なので、右の `.every()` は常に true を返す死んだ条件**です(検査名は「文言が HAS_ARTICLE_NO_SUMMARY_TEXT になっている」と言っているのに、その照合は1度も実行されていません)。**この3箇所(`:146` / `:255` / `:474`)の母数を実際に数えて、R233 の残件が何件なのかを行番号を添えて ROADMAP に書くこと**。**今回のサイクルで直すのは R234 だけでよく、R233 は数え直して記録するだけ**でよい(欲張らない)。
  8. `node scripts\check-all.mjs` が **35本中35本 PASS または FLAKY(exit 0)**。**本数は35本のまま**(既存 `check-nosummary.mjs` への項目追加のみ)。
  9. **★FLAKY の本数を必ず NIGHTLOG に記録**(R216 まで**11サイクル連続0本**)。
- **検証手順**:
  - **★5エリアの撮影と Read での目視が必要です**(カードの表示が変わるため)。撮影URLは `https://teer-tee.github.io/yadotabi/?fixture=<kusatsu|hakone|beppu|dogo|kinosaki>`(push 前はローカルサーバ。**push 後に本番でも草津を1枚撮り直す**)。画面幅は**モバイル375px と PC幅の2通り**(最低でも草津は両方、他4エリアはモバイル1枚ずつで可)。**Read で開いて**文字崩れ・はみ出し・行の重なり・カード高さの異常が無いことを確認する。
  - `node scripts\dump-rank.mjs` 5エリア分の変更前後バイト一致(完了条件5)。
  - `node scripts\check-nosummary.mjs` の単体実行で pass/fail を記録(**旧版・新版・壊した状態B・壊した状態A の4通り**)。
  - `node scripts\check-all.mjs` の通しを1回。
  - **本番反映の確認**: push 後、本番の草津で湯畑のカードに説明の行が出ることを**実ブラウザで**確認する(curl は不可。GitHub Pages の反映には数分かかる)。
- **変更禁止範囲(絶対に触らない)**: **`assets\geo.js` / `assets\engine.js` / `WEIGHT` 定数(順位ロジック) / `fixtures\*.json`(読むだけ・再生成しない)** / `assets\ui.css` / `index.html` / `404.html` / `demo\*.html` / `.github\workflows\*` / `docs\AUTOPILOT.md` / `docs\check.mjs` / `scripts\dump-rank.mjs` / **`scripts\check-*.mjs` のうち `check-nosummary.mjs` 以外の34本** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記と目次1行のみ許可)。
  - **`assets\app.js` は今回の実装対象なので禁止範囲ではありません**(触ってよいのは `summary` の分岐まわりだけ)。
  - **★この禁止範囲に矛盾を見つけたら、報告した上で最小限の変更で進めてよい**(R228 の教訓)。
- **所要目安**: 30〜40分(文言の決定と実装6分・検査2軸の追加10分・破壊実証2通り+旧版対比10分・5エリア撮影と目視6分・dump-rank 突合4分・35本通し7分・記録とコミット8分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。**関連する実測(R234 の計画時)**: `assets/app.js:1092` のコメント通り**湯畑は `backlinks` が null** のため、`★ 全国的によく知られた場所` のバッジも出ていません。つまり草津の1位は**「どれだけ人気か」も「何の場所か」も画面に出ていない**状態です。R234 は後者だけを直します(前者は順位に関わるので判断待ち)。
- 判断不要で残っているもの: **R233 の残り**(R234 で数え直した後の実数)、**R211**(dump-rank に日時とコミットID)、**R102**(list-shots.mjs)。**R217 は見送り済み**。
- **計画役が本番を見て見つけた改善候補(ROADMAP に追記済み・R234 以外)**: (a) 草津の1位に「人気度」の手がかりが無い(R225 待ち)、(b) 「もっと見る」を押す前に、その先に何の種類の場所があるか分からない、(c) 地図のピン番号とカード番号の対応が、スクロールすると見失う。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★部分的な確認では見つからない破損がある。何かを全件確認する必要があるときは、機械的に全件突合すること**（R233-3 の教訓。司令塔の指示「前後数件を突合せよ」では**63%の破損を発見できませんでした**。前後4件のうち3件がたまたま正しかったためです）。
- **★「この壊し方なら捕まえられるはず」は実測で確かめる**（R233-2・R233-3・R216 と**3サイクル連続**で、指示書が推した壊し方はどれも旧版で既に捕まり、作業役が作り直しました）。**推された案が効かないと分かったら、作り直すのが作業役の仕事**。
- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226 の教訓。別軸=合計の下限・中身の照合を必ず持つこと)。**R216 ではリンク軸が全緑のまま中身軸だけが壊れを検知しました**。
- **★「作らない判断」も成果**(R232 の教訓。**常に赤い検査は緑の検査より危険**)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231)。**★数え方の定義を実装と一致させる**(R226)。**★件数を引き継ぐときは、引き継いだ側が実物を開いて形を確かめること**(R216 で R233 の「残り6件」が実際は1〜3件だったと判明)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。
- **★司令塔の指示書そのものにも誤りがある**(R228。実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい)。
- **★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228)。**★変更の影響は、関係する全ての分岐を列挙してから数える**(R227)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229・R216)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226・**R234** はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225。export されている=使われている、ではありません)。**同様に「検査があるから守られている」も疑う**(R234 で `check-nosummary.mjs:146` の `.every()` が死んでいることが判明)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に8件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218)。**本数の下限と中身の照合を別項目で持つ**。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226 はいずれも新規ファイル0件。**R234 も新規ファイル0件**)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232)。基準値: `assets\app.js` = `fa5caf9f12f1b2fb0e4fd7868c33a4ee`、`assets\ui.css` = `c3db293066104848b317b7d04798638d`。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

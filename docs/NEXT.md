# NEXT: 2026-09-20 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、2100行あるので朝は読まなくて大丈夫です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ(Windows手動) | **GitHub Actions で全PASS**(R64) |
| 営業用デモ | 日本語1枚のみ | **英語版 `demo/hotel-page-en.html` 追加**(R85)+ README冒頭に入口(R207)+ OGカード(R208) |
| 営業デモの検査 | 2枚がノーガード | **`scripts/check-demo.mjs` 新設**(R218) |
| JS無効時の案内を守る検査 | 1本も無かった | **`scripts/check-noscript.mjs` 新設で検査 34本に**(R221) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の日本語の案内**(R215・本番反映確認済み) |
| サイトアイコン(favicon) | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214・新規ファイル0件のデータURI) |
| 検査がコメントを本物と誤判定する穴 | `check-bundle.mjs` の4か所が素通り | **コメント除去を入れて解消**(R222・「壊しても緑のまま」を先に実証) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。


## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R223** — 検査が「ときどき理由なく赤くなる」状態をなくす(`check-all.mjs` に1回だけの自動再実行を入れる)

**なぜ R223 なのか(すべて実測して決めました)。**

前サイクルで報告された `check-attrib` の一過性FAILを「再現するか」から調べたところ、**これは今回だけの偶然ではなく、4日間で5回起きている慢性的な現象**だと分かりました。

| 測ったこと | コマンド | 結果 |
|---|---|---|
| `ERR_NO_BUFFER_SPACE` が NIGHTLOG に出てくる回数 | `grep -c "ERR_NO_BUFFER_SPACE" docs/NIGHTLOG.md` | **5回** |
| それが起きた日付 | 各出現行より上の直近の日付を採取 | **09-16・09-16・09-18・09-19・09-20 の4日間**(今日も含む=現役の問題) |
| 巻き込まれた検査の種類 | 同じ行から `check-[a-z]+` を抽出 | **9種類**(attrib / bundle / embedbg / history / initpos / nohotels / nosummary / reason / all)。**特定の1本の欠陥ではない** |
| `check-all.mjs` に再実行の仕組みはあるか | `grep -cE "retry\|リトライ\|再実行\|再試行\|attempt" scripts/check-all.mjs` | **0件**(1回落ちたらそこで赤が確定する) |
| ブラウザを立てる検査の本数 | `grep -l "chromium.launch" scripts/check-*.mjs \| wc -l` | **29本**。34本を直列で回すと約345秒かかり、その間ソケットを使い続ける |
| Windowsの使える一時ポート数 | `netsh int ipv4 show dynamicport tcp` | **49152番から16384個**。29回のブラウザ起動で使い切りうる量 |

**この現象は過去に2度、別の角度から対処されています**(`check-all.mjs` の冒頭コメント)。R130 で「各検査が自分でサーバを立ててポート3000を奪い合う」のをやめて共有サーバ方式にし、R172 で「放置されたサーバの検出警告」を足しました。**それでもなお今日も起きている**ので、原因をこれ以上つぶすのではなく、**落ちたときに1回だけ自動でやり直す**という別の手で受け止めます。

**なぜこれが最優先なのか。** 毎サイクルの作業役は、赤を見るたびに手で単体再実行して「フレークでした」と確かめる作業に時間を使っています(NIGHTLOG に5回分その記録が残っています)。それ以上に危ないのは、**「どうせフレークだろう」で本物の不具合を見逃すこと**です。R214・R222 で「緑は正しいという意味ではない」ことを学んだばかりですが、**「赤も赤という意味ではない」状態は、それと同じくらい検査の値打ちを下げます**。

このタスクは**順位ロジックを1行も変えません**。判断待ちの2件にも依存しません。**新しいファイルも1つも作りません**(既存の `check-all.mjs` の中だけで完結します)。

- **タスクID**: R223
- **目的**: 検査が落ちたとき、**その1本だけを自動でもう1回走らせ**、2回目も落ちたときだけ赤にする。あわせて「1回目は落ちたが2回目で通った」ことを**表に必ず残して**、フレークを見えなくするのではなく**数えられるようにする**。
- **変更するファイル(絶対パス・この4件のみ)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-all.mjs`(118〜134行目の実行ループ。**実行するスクリプトの一覧 `SCRIPTS` は1本も増減させない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\CHECKS.md`(再実行の仕様と、表の見方に「FLAKY」の説明を追記。**34本という現在値は変わらない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R223 を追記して `[x] 2026-09-20` に)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-20 R223 …` 見出し+3行)
- **作り方(注意点)**:
  - 再実行は**1本につき最大1回**まで(合計2回)。**回数を増やさないこと**。何度でもやり直す作りにすると、**本物の不具合を永久に隠す**道具になります。
  - 2回目に入る前に**数秒待つ**こと(ソケットが片付く時間を与えるため。待たずに即やり直すと同じ理由で落ちるだけです)。待ち時間は作業役が実測して決め、**選んだ根拠を NIGHTLOG に書く**こと。
  - 最後の表の結果欄は、**PASS / FAIL に加えて「FLAKY(2回目で通った)」の3つ**にする。**FLAKY は緑(exit 0)扱いでよい**が、**表と最終行の両方で目立つように出す**こと。ここを「PASS」と同じ表示にしてしまうと、今回の目的である「数えられるようにする」が消えます。
  - 失敗ログ(`saveFailLog`)は**1回目の失敗分も必ず残す**こと。原因を後から追えなくなるため、2回目が通ったからといって捨てないこと。
- **完了条件(検証可能な形)**:
  1. **「本当に落ちる検査はちゃんと赤いまま」の実証**(これが一番大事)。検査スクリプト1本を一時的に必ず失敗するよう書き換え(例: `check-noscript.mjs` の判定値を存在しない語にする)、`node scripts/check-all.mjs` が**2回実行したうえで FAIL・exit 1 になること**を確認する。確認後は**必ず元に戻し**、`git diff scripts/check-noscript.mjs` が**空**であることを目で確かめる(R218・R221・R222 と同じ手順)。
  2. **「フレークは緑になる」の実証**。**1回目だけ落ちて2回目は通る**検査を一時的に作り(例: 一時ファイルの有無で1回目だけ `process.exit(1)` する行を検査1本に足す)、`check-all.mjs` が **FLAKY と表示して exit 0 になること**、かつ**1回目の失敗ログが `screenshots/fail-*.txt` に残っていること**を確認する。確認後は**必ず元に戻し**、`git diff` が空であることを確かめる。
  3. `node scripts/check-all.mjs` が **34本中34本PASS(exit 0)**。合計時間を NIGHTLOG に記録し、**R222 時点の344.1秒からどれだけ増えたか**を書く(再実行が1度も発生しなければ、ほぼ増えないはずです)。
  4. `git diff --numstat assets/ fixtures/ index.html demo/` が **空**(本番に配信されるファイルは1バイトも変わっていない)。
  5. `docs/CHECKS.md` の**現在値を述べている箇所がすべて34本であること**(**過去の経緯として32本・33本と書いてある歴史的な記述は正しいので消さないこと**)。
  6. NIGHTLOG に、上の表の**6行の実測値をそのまま残す**こと(この問題が4日間で5回・9種類の検査にまたがって起きていた証拠。将来また「今回だけの偶然では?」と調べ直さなくて済むようにするため)。
- **検証手順(撮影)**:
  - **撮影は不要です。** 変更するのは検査の実行スクリプト1本と文書3本のみで、**本番に配信されるファイル(`index.html` `assets/` `demo/` `fixtures/`)を1バイトも変更しない**ためです。画面は変わりようがありません。
  - 代わりに、**完了条件1と2の実行結果(表の該当行と最終行)をそのまま NIGHTLOG に貼る**こと。
- **変更禁止範囲(絶対に触らない)**: `assets/geo.js` / `assets/engine.js` / `WEIGHT` 定数 / `assets/app.js` / `assets/*.css` / `index.html` / `demo/*.html` / `fixtures/*.json` / `docs/check.mjs` / `scripts/check-*.mjs` の**34本すべて**(完了条件1・2の一時改変を除き、**必ず元に戻すこと**) / `scripts/lib/server.mjs`(R130 の共有サーバ方式は今回いじらない) / `.github/workflows/*` / `README.md`。
- **所要目安**: 25〜35分(再実行ループの実装10分・完了条件1の実証5分・完了条件2の実証7分・`check-all.mjs` 通し6分・記録とコミット7分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- R64 の残り — CI を `schedule` で1日1回回すかの判断のみ(本体は完了)。
- 判断不要で残っているもの: **R217(embed-check のOGカード・実測で `og:` 0行)・R216(404)・R219(README照合)・R220(NIGHTLOG目次・2100行超)**・R211(dump-rank に日時とコミットID)・R102(`scripts/list-shots.mjs`)。
  R223 の次は **R217(1枚の取り残しを揃えるだけ・検査は `docs/check.mjs` に相乗り可)→ R219(README照合・文書のみ)→ R216(404。ただし新設ページを同じサイクルで `HTML_PAGES` に足すこと)** の順を推奨。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる。
- **「今回だけの偶然」と片付ける前に、過去に何回起きたかを数える**(R223 の教訓。1回の報告では偶然に見えたものが、数えたら4日で5回・9種類の検査にまたがっていました)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立し、R222 で「直す前は緑のまま通る」の実証まで進めた手順)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214 はデータURIで新規ファイル0件、R223 は既存スクリプト内で完結)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

# NEXT: 2026-09-22 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、**先頭に261件の目次が付いた**ので「Rいくつの話がどこにあるか」は探せるようになりました。R220 完了)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**(R85・R207・R208)+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の案内**(R215)+**検査で保護**(R221) |
| サイトアイコン | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214) |
| 検査がコメントを本物と誤判定 | 4か所が素通り | **解消**(R222・「壊しても緑のまま」を先に実証) |
| 検査がときどき理由なく赤くなる | 1回落ちたら赤が確定 | **PASS / FLAKY / FAIL の3値+自動1回再実行**(R223) |
| NIGHTLOG(2123行) | 朝に読めない | **先頭に261件の目次**(R220) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224・順位は5エリアで差分なしを実証) |
| 人気バッジを守る検査 | 0本 | **`check-fame.mjs` を新設(R229)・4通りの壊し方で赤を実証**・検査は**35本**に |
| README の本数・fixture表・因果の説明 | 実態とズレ(7本違い・表は全セル誤り) | **実測で是正(R230)**・R219 も同時に消化 |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。


## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R228** — 「徒歩1分・75m」が、**何からの距離なのか画面に一度も書かれていません**

**なぜ R228 なのか(すべて今回の計画役が自分で実測した数字です)。**

未着手は R216(404)・R217(embed-check の OG)・R211・R102・R226〜R228。実際に測って比べました。

| 候補 | 実測して分かったこと | 判断 |
|---|---|---|
| **R228** | `grep -n "選んだ宿\|お泊まり\|泊まる宿\|起点" index.html assets/*.js` の結果、**利用者に見える文字列としては0件**(7件ヒットしたが**全てJSのコメント行**)。距離の起点を説明する文は画面に存在しない | **採用** |
| R217 | 起票は正しい(`og:` が hotel-page.html は10行・embed-check.html は**0行**)。だが `embed-check.html` は自分の `<h1>` で「埋め込み確認用(**ローカル**)」、本文で「**ローカル**で目視確認するためのページ」と名乗る**開発者用の道具**。起票の前提「チャットに貼ったときリンクカードが出ない」は、**誰も貼らないページ**には当てはまらない | 見送り(起票の前提が誤り。ROADMAP に理由を追記) |
| R216 | `ls 404.html` = 存在しないのは事実。ただし新規ファイル1枚+検査追加で、今回より重い | 次点 |
| R226/R227 | 表示の改善だが、R228(起点の明示)が入らないと「何分」の意味自体が伝わらない。**R228 が前提** | R228 の後 |

**★この起票には誤りが1つ混ざっており、それを訂正した上で採用します。**

起票は「**ヘッダは『草津温泉』=エリア名**、宿名はヘッダに無く」と書いています。**これは誤りです。** `index.html:69` に `<h1 class="topbar__title" id="feed-title">` が実在し、`assets/app.js:1382` が `els.feedTitle.textContent = hotel.name` を代入しています。つまり**ヘッダには宿名が出ています**。

ではなぜスクリーンショットが「草津温泉」なのか。**固定データモードだからです。** `fixtures/kusatsu.json` を実際にパースすると top-level に `hotel` キーは無く(`Object.keys` = `meta, overpass, wiki, wikidataTitles, parentExtract, parentImage, wikiByTitle, backlinks`)、宿名は `meta.label` = `"草津温泉"` から作られています。**本番では実在の宿名が出ます。**

**それでも R228 の指摘そのものは生きています。** ヘッダに宿名が出ていても、カードの「🚶徒歩1分・🚗車1分・75m」が**その宿からの距離だとはどこにも書かれていない**からです。ヘッダの宿名と、カードの数字を結びつける文が0件、というのが正しい問題の形です。

**2方向から確かめたこと**(「◯件も無い」を1回の grep で決めない、の教訓)。
- 方向A: `grep -n "選んだ宿\|お泊まり\|泊まる宿\|起点" index.html assets/*.js` の7件を**1件ずつ目で読み**、全て `// ` 始まりのJSコメントで画面に出ないことを確認。
- 方向B: 最新の撮影 `screenshots/2026-09-20T03-26-12_r10-flash_mobile.png` を実際に開いて目視。ヘッダ「草津温泉」・1位カード「🚶徒歩1分・🚗車1分・75m」の間に、起点を示す文言が**画像上でも無い**ことを確認。

**★なお、司令塔が渡した実測コマンドの `j.osm.elements` は今回も誤りでした**(正しくは `j.overpass.elements`。上記の `Object.keys` で確認済み)。R230 の教訓のとおりです。

- **タスクID**: R228
- **目的**: カードの所要時間・距離が**いま選んだ宿を起点にした値である**ことを、凡例1行で明示する。**順位・並び順・距離の計算値は1バイトも変えない**(表示のみ)。
- **変更するファイル(絶対パス・この5件のみ)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(凡例の描画。**距離計算・並び替えには触れない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\index.html`(**新しい要素を足さずに済むなら足さない**。既存の `#feed-status`(`index.html:75`)の直下など、**既にある枠の中に収める方法を先に検討**し、どうしても必要な場合のみ1要素追加)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\style.css`(凡例の体裁のみ)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R228 を `[x] 2026-09-22` に・**R217 に「起票の前提が誤り。embed-check はローカル開発用ページのため見送り」を追記**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-22 R228 …` 見出し+3行・**先頭の目次にも1行**)
- **完了条件(検証可能な形)**:
  1. **凡例が出る**: `?fixture=kusatsu` の状態Bで、カード一覧より**上**に、起点が選んだ宿であると分かる1行が表示される。**宿名を実際に差し込むこと**(固定文言だけでは、ヘッダの宿名と数字が結びつかない)。文面は作業役が決めてよいが、**「宿」と「ここから」に当たる意味が両方含まれること**。
  2. **状態Aでは出ない**: 宿を選ぶ前(トップ)には凡例が出ない(`hidden`)。宿が無いのに起点を語らないため。
  3. **0件のときに嘘をつかない**: `?fixture=kusatsu&simulate=empty` で、カードが0件のとき凡例が**出ない**こと(`check-feednote.mjs` の項目7が同じ考え方を既に採っている)。
  4. **★守る検査を同じサイクルで足す**(R229 の教訓。**新機能を入れたら守る検査も入れる**)。**新規ファイルを作らず、既存の `scripts/check-distance.mjs` に項目を追記する**(この検査は既に「距離表記」を担当しており、凡例は同じ主題)。追記する項目は上の1〜3と、**375px 幅で凡例が `scrollWidth <= clientWidth`(はみ出しなし)**。**検査本数は 35本のまま変わらない**ので、本数を書いた文書の更新は不要。
  5. **★「壊れた状態で落ちること」を実証する**(R218・R222・R229 の手順)。**最低3通り**わざと壊して、`node scripts/check-distance.mjs` が**赤くなること**を確かめ、結果を NIGHTLOG に残す。**数字だけ見る検査では素通りする壊れ方を必ず1つ入れること**(R229 の実例)。例: (a) 凡例要素を消す、(b) 宿名の差し込みをやめて固定文言だけにする、(c) `simulate=empty` でも凡例を出すようにする。**実証後は `git diff` が空であること**を確認してから本実装に戻る(R229 が実践した「完全に元通り」の確認)。
  6. **順位が変わっていない**: `node scripts/dump-rank.mjs kusatsu` と `hakone` の出力を**変更前に保存**し、変更後と **`diff` でバイト一致**すること。一致しなければ表示だけの変更になっていない。
  7. `node scripts/check-all.mjs` が **35本中35本 PASS または FLAKY(exit 0)**。**FLAKY の本数を必ず NIGHTLOG に記録**(R223 の申し送り・直近は FLAKY 0本)。
- **検証手順(撮影)**: **撮影は必須**(画面の見た目を変えるため)。`node C:\workspace\tools\shot\shot.mjs <URL> --mobile` と PC幅の両方。
  - 撮影URL①: `http://127.0.0.1:<port>/index.html?fixture=kusatsu`(**mobile 375px と desktop の2枚**)— 凡例の文字崩れ・はみ出し・カードとの重なりを目視。
  - 撮影URL②: `http://127.0.0.1:<port>/index.html?fixture=kusatsu&simulate=empty`(**mobile 1枚**)— 凡例が出ていないことを目視。
  - **撮影は全て固定データモードで行い、外部APIは0回**(AUTOPILOT 絶対ルール4)。**画像は必ず Read で開いて目視する**(緑の検査だけで済ませない)。
- **変更禁止範囲(絶対に触らない)**: **`assets/geo.js` / `assets/engine.js` / `WEIGHT` 定数(順位ロジック)** / `app.js` の距離計算・並び替え・`walkMin`/`carMin` の算出部分 / `fixtures/*.json`(**読むだけ。再生成しない**) / `demo/*.html` / `404.html`(作らない) / `.github/workflows/*` / `docs/AUTOPILOT.md` / `docs/CHECKS.md`(本数が変わらないため) / **`check-distance.mjs` 以外の検査34本** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記と目次1行のみ許可)。
- **所要目安**: 30〜40分(凡例の実装10分・検査の追記8分・3通りの破壊実証8分・dump-rank 突合3分・撮影と目視5分・35本通し7分・記録とコミット8分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- 判断不要で残っているもの: **R227(1位の理由行が常に「歩いて行ける」・要 before/after)→ R226(more側が全件同じ見た目)→ R216(404・検査追加とセット)** の順を推奨。ほかに R211・R102。**R217 は今回見送り**(起票の前提が誤り・上表の理由)。


## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★もっともらしい因果ほど、書く前に測ると外れる**(R230 の教訓。作業役が書きかけた説明3つが実測で全て否定された)。**司令塔が渡した数字やコマンドも鵜呑みにしない**(R230・R228 と2回続けて司令塔の実測コマンドのキー名が誤っていた。正しくは `overpass.elements`)。
- **★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228 の教訓。「宿名はヘッダに無い」という起票の記述が誤りで、実際は `index.html:69` に存在した。**起票が正しいのは「問題があること」までで、その理由づけは別**)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229 の教訓。R224 は表示を入れたのに守る検査が0本のまま1サイクル空いた。**司令塔の指示要約が元の完了条件を落とすと、作業役は落ちた条件を復元できない**。要約は元文書の完了条件の件数と突き合わせること)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。R223 の仕上げ実行で**本物のフレークが1本出ました**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE` で1回目に落ち、再実行で通過)。**この環境ではフレークが1回の通しで1本出る頻度**です。**FLAKY の本数が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224 の教訓。**起票は、起票した時点で気付いていたことしか含みません**)。
- **「動いている前提で書かれた文書」を疑う**(R225 の教訓。**export されている=使われている、ではありません**)。
- **起票の中に書かれた「こうすれば簡単」も疑う**(R220 の教訓)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる。
- **「今回だけの偶然」と片付ける前に、過去に何回起きたかを数える**(R223 の教訓)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立し、R222 で実証まで進めた手順)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220 はいずれも新規ファイル0件)。
- **自動でうまく処理する仕組みは「問題を隠す方向」に働かないか考える**(R223 の教訓)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

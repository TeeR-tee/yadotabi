# NEXT: 2026-09-21 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、**先頭に261件の目次が付いた**ので「Rいくつの話がどこにあるか」は探せるようになりました。R220 完了)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**(R85・R207・R208)+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の案内**(R215)+**検査で保護**(R221) |
| サイトアイコン | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214) |
| 検査がコメントを本物と誤判定 | 4か所が素通り | **解消**(R222・「壊しても緑のまま」を先に実証) |
| 検査がときどき理由なく赤くなる | 1回落ちたら赤が確定 | **PASS / FLAKY / FAIL の3値+自動1回再実行**(R223) |
| NIGHTLOG(2123行) | 朝に読めない | **先頭に261件の目次**(R220) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224・順位は5エリアで差分なしを実証) |
| 人気バッジを守る検査 | 0本 | **`check-fame.mjs` を新設(R229)・4通りの壊し方で赤を実証**・検査は**35本**に |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。


## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R230** — README.md が実態とズレています(申し送りの FIXTURES.md より深刻でした)

**なぜ R230 なのか(すべて今回の計画役が自分で実測した数字です)。**

申し送りは「`docs/FIXTURES.md:86` に『29本全緑』という古い記述がある(現在は35本)」でした。**実在は確認しました。ただし調べたら、これは同種のズレの中で一番小さいものでした。**

| 場所 | 書いてあること | 実測 | ズレ |
|---|---|---|---|
| `README.md:207` | 「`check-*.mjs` の**28本**と `docs/check.mjs` の1本、計**29本**」 | 34本+1本=**35本** | **7本ちがう** |
| `README.md:211` | 「**29本**の検査それぞれが何を検査するか」 | **35本** | 6本ちがう |
| `docs/FIXTURES.md:86` | 「`check-all.mjs` が**29本全緑**」 | **35本** | 6本ちがう(申し送り分) |
| `README.md:220`の表 | hakone の `osmRadiusM` = **30,000** | **15,000**(R176で変更済) | **説明の中身ごと間違い** |
| `README.md:220`の表 | OSM件数 kusatsu 189 / hakone 4,186 / dogo 463 / beppu 667 / kinosaki 455 | 189 / **1,135** / 463 / 667 / 455 | hakoneが**3.7倍ちがう** |
| `README.md:220`の表 | ファイルKB 55.5 / 585.7 / 93.7 / 122.7 / 86.2 | **112.4 / 320.9 / 231.3 / 206.8 / 131.5** | **5行とも全部ちがう** |
| `README.md:225` | 「**hakone だけ10倍近く大きい理由は収集半径**。hakone のみ 30000 で他4エリアの4倍の面積」 | 5エリアとも15,000で**半径は同じ** | **理由の説明そのものが成立しない** |
| `docs/FIXTURES.md:76` | 「現在の**4** fixture の `generatedAt` はいずれも **2026-09-15〜16** 生成」 | **5**エリア・全て **2026-09-19** 生成 | 件数も日付も違う |

**★とくに重い2つ。**
1. **README は、このリポジトリを初めて見る人が最初に読む唯一の入口です。** そこに書かれた「29本」は、**みのるんが営業デモを見せる相手(予約サイト運営者)も読む数字**です。ズレた文書の中で、これが一番人目に触れます。
2. **`README.md:225` は数字ではなく「理由の説明」が壊れています。** 「hakone だけ半径が30000だから10倍大きい」という因果の説明が、R176 で半径を揃えた今は**まるごと事実に反します**。**数字だけを直すと、この段落は「全エリア同じ半径なのに hakone だけ大きい理由」を説明しない文章として残ります**(実際の理由は OSM 要素数が 1,135 対 189〜667 と多いこと)。**機械的な数字置換では直らないズレ**なので、今回のタスクの本体はここです。

**2方向から確かめたこと**(「◯件も無い」を1回の grep で決めない、の教訓)。
- 方向A: `ls scripts/check-*.mjs` = 34本、`check-all.mjs` の `SCRIPTS` 配列 = 34本+`docs/check.mjs` = **35本**。
- 方向B: `fixtures/*.json` を `node` で実際にパースして `meta.osmRadiusM` と要素数と `statSync` のサイズを読んだ(上表の実測列)。README の表の記憶や FIXTURES.md の記述には一切頼っていません。
- **なお `docs/CHECKS.md` は R229 が直した直後で、完全に正しい状態です**(35本・表34行・サーバ30本を突合して確認。`grep -l ensureServer` = 30、`grep -l playwright` = 30 で CHECKS.md:25・76 の記述と一致)。**CHECKS.md は今回**変更しません**。

**この件は ROADMAP の R219(README「ファイル構成」の照合)と重なります。** R219 は起票時に「`scripts/` は check系32本」と書いており、**起票そのものが既に古い**(現在34本)。今回 R230 として R219 を吸収し、ROADMAP では R219 を「R230 で消化」と記録します(R209 を R212 で消化としたのと同じ扱い)。

- **タスクID**: R230
- **目的**: **本番配信物・コードは1バイトも変えず**、`README.md` と `docs/FIXTURES.md` の「現在値を述べている箇所」を実測値に揃える。あわせて **R219 の「ファイル構成の照合」を同時に済ませる**。
- **変更するファイル(絶対パス・この4件のみ)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\README.md`
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\FIXTURES.md`
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R230 を `[x] 2026-09-21` に・**R219 に「R230で消化」を追記**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-21 R230 …` 見出し+3行・**先頭の目次にも1行**)
- **完了条件(検証可能な形)**:
  1. **本数**: `README.md` と `docs/FIXTURES.md` で**検査の現在の本数を述べている箇所がすべて 35本**であること(README:207 は「`check-*.mjs` の**34本**と `docs/check.mjs` の1本、計**35本**」)。**「29本という文字列が無いこと」を条件にしない** — `fixtures` の件数や過去記録として 29 が出てよい。確認は `grep -n "本" README.md docs/FIXTURES.md` を**全件目で読み**、1件ずつ「現在値か/過去記録か」を判定した結果を NIGHTLOG に残す。
  2. **fixture の表**: `README.md:220` の表5行(osmRadiusM・OSM elements・ファイルKB)が**実測と一致**。検証は**その場で測り直す**こと:
     `node -e "for(const a of ['kusatsu','hakone','dogo','beppu','kinosaki']){const fs=require('fs');const j=JSON.parse(fs.readFileSync('fixtures/'+a+'.json','utf8'));console.log(a,j.meta.osmRadiusM,(j.osm&&j.osm.elements||[]).length,(fs.statSync('fixtures/'+a+'.json').size/1024).toFixed(1)+'KB',j.meta.generatedAt)}"`
     この出力を NIGHTLOG に貼り、表の全セルと突き合わせた結果を書く。**「R14前」列は過去の記録なのでそのまま残す**(列の意味が変わるので消さない)。
  3. **★理由の説明を書き直す**: `README.md:225` の「hakone だけ10倍近く大きい理由は収集半径」の段落を、**現在成立する説明に書き換える**(5エリアとも半径15,000で同じ。hakone が大きいのは OSM 要素が 1,135件と他の1.7〜6倍あるため。**R176 で 30,000→15,000 に変えた経緯は1行残す** — 消すと「なぜ昔の README にそう書いてあったか」が追えなくなる)。**数字だけ差し替えて段落の論旨を放置したら不合格**。
  4. **FIXTURES.md:76**: 「4 fixture」→**5**、「2026-09-15〜16 生成」→**2026-09-19 生成**、見直し目安の年月も生成日から半年後に整合させる。
  5. **R219 の照合(ファイル構成)**: `README.md:147〜159` の各行と、実際の `ls assets/ demo/ scripts/ docs/ fixtures/` を1対1で突き合わせる。**(a)** README に書かれたパスが**全て実在**すること、**(b)** 実在するトップレベルの成果物で README に1度も出てこないものが**無い**こと。**両方向を `ls` の生出力とともに NIGHTLOG に残す**(片方向だけでは R219 の起票と同じ間違いをします)。差分があれば同じサイクルで直す。無ければ「照合したが差分0件だった」と明記する(**黙って何もしないのは不可**)。
  6. **コードが無変更**: `git diff --numstat assets/ scripts/ index.html demo/ fixtures/ docs/CHECKS.md .github/` が**空**。`git diff --numstat` の対象が上の4ファイルだけであること。
  7. `node scripts/check-all.mjs` が **35本中35本 PASS または FLAKY(exit 0)**。**FLAKY の本数を必ず NIGHTLOG に記録**(R223 の申し送り・直近は FLAKY 0本)。`node docs/check.mjs` も exit 0。
- **検証手順(撮影)**: **撮影は不要**。理由: 変更するのは `README.md` と `docs/*.md` の4ファイルのみで、**本番に配信される画面(`index.html` / `assets/` / `demo/`)を1バイトも変えないため、見た目が変わりようがない**(完了条件6でそれを機械的に保証する)。代わりに**完了条件2のコマンド出力全文**と**完了条件5の `ls` 両方向の突合結果**を NIGHTLOG の証拠にする。**外部APIは1回も叩かない。**
- **変更禁止範囲(絶対に触らない)**: **`assets/` 配下すべて**(とくに `geo.js` / `engine.js` / `WEIGHT` 定数=順位ロジック) / `scripts/` 配下すべて(**35本の検査は1本も変更しない**) / `fixtures/*.json`(**読むだけ。再生成しない**) / `index.html` / `demo/*.html` / `.github/workflows/*` / `docs/AUTOPILOT.md` / **`docs/CHECKS.md`**(R229 が直した直後で正しい。触らない) / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記と目次1行のみ許可)。
- **所要目安**: 30〜40分(実測コマンドと両方向 `ls` の突合10分・README の表と理由段落の書き直し10分・FIXTURES.md と本数の全件判定5分・35本通し7分・記録とコミット8分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- 判断不要で残っているもの: **R228(何からの距離か書いていない・凡例1行)→ R227(1位の理由行が常に「歩いて行ける」・要 before/after)→ R226(more側が全件同じ見た目)** の順を推奨。ほかに R217(embed-check の OGカード)・R219(README照合)・R216(404)・R211・R102。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229 の教訓。R224 は表示を入れたのに守る検査が0本のまま1サイクル空いた。**司令塔の指示要約が元の完了条件を落とすと、作業役は落ちた条件を復元できない**。要約は元文書の完了条件の件数と突き合わせること)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。R223 の仕上げ実行で**本物のフレークが1本出ました**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE` で1回目に落ち、再実行で通過)。**この環境ではフレークが1回の通しで1本出る頻度**です。**FLAKY の本数が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224 の教訓。**起票は、起票した時点で気付いていたことしか含みません**)。
- **「動いている前提で書かれた文書」を疑う**(R225 の教訓。**export されている=使われている、ではありません**)。
- **起票の中に書かれた「こうすれば簡単」も疑う**(R220 の教訓)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる。
- **「今回だけの偶然」と片付ける前に、過去に何回起きたかを数える**(R223 の教訓)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立し、R222 で実証まで進めた手順)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220 はいずれも新規ファイル0件)。
- **自動でうまく処理する仕組みは「問題を隠す方向」に働かないか考える**(R223 の教訓)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

# NEXT: 2026-09-24 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にあります。**先頭の目次は `##` 見出しの48件**です)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224)+**`check-fame.mjs`**(R229) |
| 草津の1位「湯畑」の説明 | **1行も出ていなかった** | **`Wikipediaに記事がありません。写真と地図の情報で表示しています。`**(R234) |
| 人気バッジが初期5枚で出る枚数 | **8/25枚**(草津 **0/5**) | **10/25枚**(草津 **1/5**・別府 2→3)(R241。**しきい値も文言も1つも変えず**) |
| 「もっと見る」を押す前の手がかり | **`もっと見る` の5文字だけ** | **`この先にあるのは歴史を歩く、景色を見にいくです`**(R236・**件数の数字0文字**) |
| 画面末尾の注記が名乗る「順位の作り方」 | 8つの要素のうち**2つしか名乗らず**(しかも逆を書いていた) | **4種類に畳んで全部を名乗る**(R246・順位は1件も動かさず) |
| 注記から飛べる README の順位の説明 | 画面と**正反対**・8つの重みのうち**3つしか名乗らず** | **画面と同じ4種類で説明**(R247)+**`docs/check.mjs` に4軸追加** |
| `docs/CHECKS.md` の行番号による道案内 | **7種類9箇所が9箇所とも指す先を外していた** | **全件を実測値に訂正**+**双子の行も併記**(R249) |
| 検査本の中の「構造上必ず true の照合」 | **`check-nosummary.mjs` の1本が1度も文言を見ていなかった** | **枚数/母数/中身の3軸に分割**(28→30 pass)+**同じ形を全35本から静的に探す軸**(85→**88 OK**)(R235) |
| 展開後に地図へ増える「・」ピンの説明 | **画面に1文字も無し** | **`地図の小さい点は、この下に続けて並んでいる場所です`**(R250。**`.pin--sub` が実在するときだけ**・件数の数字0文字) |
| 展開後の束見出しが名乗る件数 | **`そのほか 5件` / `写真と解説がまだ無い場所 7件`** | **数字を span ごと削除**(R254)。これで「もっと見る」まわりの**3箇所すべてが数字0文字**にそろった |
| 直近の通し | — | **35本全PASS・380.0s**・**★FLAKY 0本**(R254。**9サイクル連続0本**) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。



## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R255** — **カードの「徒歩◯分・車◯分」は道を歩いた時間ではなく直線距離の割り算なのに、画面はそれを1文字も断っていない**

**なぜこれを選んだか(★計画役が本番アプリのコードと画面を実測して選びました)**:

- **★実測1(画面で見ました)**: `screenshots\2026-09-20T16-26-09_r66-regression-kusatsu_mobile.png` を Read で開くと、草津1位「湯畑」のメタ行に **`🚶徒歩1分 · 🚗車1分 · 75m`** が出ています。**全カードに必ず出る行**です(`cardHtml` が `feedcard__times` を無条件で組んでいます)。
- **★実測2(この数字の作り方を追いました)**: `assets\engine.js` の `toCard` は `walkMin: minutesFor(distanceM, WALK_M_PER_MIN)` / `driveMin: minutesFor(distanceM, DRIVE_M_PER_MIN)` で作っており、`minutesFor` は **`Math.max(1, Math.ceil(d / metersPerMin))`** = **割り算だけ**です。その `distanceM` は `distanceBetween` = **`YadoGeo.haversineM`(地球上の2点の直線距離)**。つまり**道も坂も信号も川も一切見ていない、直線距離÷固定の速さ**です(`WALK_M_PER_MIN` と `DRIVE_M_PER_MIN` は `engine.js` の先頭に固定値で置かれています)。
- **★実測3(画面に断り書きが無いことを2方向から確かめました)**: `assets\app.js` を `徒歩` で grep すると**ヒットは2件だけ**で、**どちらもコメントと `timesText` の組み立て**です(画面に出る説明文は0件)。`#feed-origin` の凡例を組む `renderFeedOrigin` が出す行は**3行**で、**距離の起点**(`距離は「◯◯」からの目安です`)・**★の出どころ**(`FAME_ORIGIN_TEXT`)・**「・」ピン**(`SUBPIN_ORIGIN_TEXT`)。**分数(徒歩・車)について語る行は1本もありません**。
- **★実測4(同じ画面の中で流儀が揃っていません。R254 の申し送りどおりの狙い目です)**: `#feed-origin` は R228・R242・R250 と3回かけて「**画面に出ているものは、その出どころを名乗る**」という形に育ててきました。**距離(`75m`)には起点の説明が付いたのに、すぐ隣に並ぶ `徒歩1分`・`車1分` には何も付いていません**。同じ1本のメタ行の中で、**説明のある数字と無い数字が隣り合っています**。
- **★実測5(設計思想との照合)**: みのるん本人の言葉「**『60何件見つかりました』だと『何をもって60何件なんですか』という話**」がそのまま当たります。**「徒歩1分」は「何をもって1分なんですか」に画面が答えられません**。しかも距離と違い、**分数は「その通りに歩ける」と読まれる数字**で、直線距離由来だと**川や崖を挟んだ場所で実際より短く出ます**(箱根・別府のような起伏のある土地ほどズレます)。**嘘になりうる数字が、全カードに必ず出ています**。
- **★実測6(判断が要るかの切り分け)**: **みのるんの判断は不要です**。**数字そのものは1つも変えません**(`timesText` も `engine.js` も触りません)。**`renderFeedOrigin` に4行目を足すだけ**で、**順位・枚数・並び順・ピン・束分けは1つも動きません**。
- **★実測7(R254 の教訓どおり、この表示を見ている既存の検査を先に洗いました)**: `#feed-origin` を見ている検査は **`check-distance.mjs`**(表示される/宿名を含む/状態Aと0件では出ない/375pxではみ出し0)・**`check-fame.mjs`**(表示される/★の根拠の行を含む/距離の凡例と同じ要素に並ぶ/1280pxではみ出し0)・**`check-feednote.mjs`**(★の説明と注記の用語統一)の**3本**。**いずれも「含む」「はみ出し0」だけを見ており、行数や `br` の本数を数えている軸は全35本と `docs\check.mjs` に0件**(`grep` で確認済み)。したがって**末尾に1行足す分には、向きを裏返す必要のある検査はありません**。**ただしはみ出し軸(375px・1280px)は4行目でも守る必要があるため、文言を短くすること**が条件です。**この3本は変更禁止のままで進めます**。

- **タスクID**: R255 / **目的**: **`#feed-origin` に4行目を足し、「徒歩◯分・車◯分」が直線距離をもとにした目安であることを画面で断る**。**数字は1つも変えない**。
- **対象ファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(**主**。**★行番号では指しません**。`renderFeedOrigin` と `SUBPIN_ORIGIN_TEXT` を grep して場所を自分で特定すること。文言は `FAME_ORIGIN_TEXT` / `SUBPIN_ORIGIN_TEXT` と**同じ形の `var` 定数**として隣に置くこと)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\check.mjs`(**R255 軸を現在の末尾(`// --- ここまで R254 ---` の後ろ)に新ブロックとして足す**。**R235・R248・R249・R250・R254 ブロックは1バイトも変えない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(**R255 を新規起票して `[x] 2026-09-24` に**。**R225・R244・R211・R102 は起票のまま残す**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-24 R255 …` 見出し+3行以上)
- **完了条件(検証可能な形)**:
  1. **★4行目が出ること**。既存3行(距離の起点・★の出どころ・「・」ピン)は**1文字も変えない**。文言は**「◯◯は、…です」の形にそろえる**(他の3行と同じ)。
  2. **★文言の制約**(既存3行と同じ・R224/R227/R231/R245):
     - **数字を1文字も入れない**(「80m/分」「時速4km」は禁止)。
     - **評価の語を入れない**(おすすめ・人気・必見など)。
     - **専門用語を入れない**(「ハーバサイン」「直線距離」も**噛み砕くこと**。例: `分数は、道のりではなく地図の上のまっすぐな長さから出しています` のように、**中学生が読んで分かる言い方**にする。文言そのものは作業役が決めてよい)。
     - **謝罪や言い訳にしない**(「正確ではありません」「ご了承ください」は禁止。**事実だけを言う**)。
  3. **★画面に出ていないものを名乗らないこと(R245 の型)**。**`#feed-origin` に `.feedcard__times` が1つも無いのにこの行を出してはいけません**。出す条件は `state.cards.length` や定数からではなく、**`els.feedList.querySelectorAll('.feedcard__times').length > 0`(=描き終えた DOM)から決めること**。**★そのとき DOM が最新かを確かめること**(R250 の教訓。`renderFeedOrigin` は `renderFeedMap` の**後**に呼ばれているので条件は満たせるはずですが、**自分で確かめること**)。「分数はありません」のような代わりの文言は出さない。
  4. **★同じサイクルで守る検査も足す**。**`docs\check.mjs` の末尾に R255 軸**を足す。軸は **(a) 母数軸** = `assets\app.js` を `readTarget` で読めていて、`renderFeedOrigin` の本体に**行を足す分岐が3本以上実在すること**(**件数の定数を書かず、実ファイルから数えること**)、**(b) 中身軸1(禁止語)** = 新しい文言の定数を**実ファイルから取り出し**、数字・評価の語・専門用語が現れないこと(`SUBPIN_BANNED_WORDS` と同じ基準を持つ。**定数を書き写さない**)、**(c) 中身軸2(条件軸)** = その行を出す分岐が **`.feedcard__times` を DOM から数えている**こと(`state.cards` や定数から決めていないこと)。
  5. **★(a)母数軸と(b)(c)中身軸はセットで維持すること**。R235・R254 の破壊実証で、**母数軸を壊すと中身の軸が「0件で偽の緑」になる**ことが分かっています。**中身軸には必ず母数ガードを入れ、母数0のとき落ちることを実証すること**。
  6. **★足した軸が本当に発火するか、壊して確かめること**。軸ごとに**それだけを壊して発火する**ことを pass/fail で示す。**★軸の母数は「実在する側」(実ファイル・DOM)から取ること**。
  7. **★わざと壊して落ちることを実証**し、**直す前は素通りしていたことを pass/fail で対比**する。**★その壊し方が本当に旧版を素通りするかを先に確かめること**(旧版は `git show HEAD:docs/check.mjs` で取り出す。**`git checkout`/`stash`/`reset` は使わない**)。**司令塔が推した壊し方は11サイクル中8回外れています**。捕まるなら**自分で作り直すこと**。
  8. **★撮影と目視は必須**(画面に出るものを変えるため)。**5エリア**(草津・箱根・道後・別府・城崎)を `?fixture=<エリア>` で撮影し、**Read で実際に画像を開いて目視**する。画面幅は **mobile(375px)と desktop(1280px)の両方**。見るのは「4行目が出ているか・既存3行が無傷か・**375px と 1280px で凡例がはみ出していないか**(既存検査の軸)・文字崩れ・重なり・4行になって地図とカードの間が詰まりすぎていないか」。**展開後も1エリアだけ撮り、R250 の3行目と R254 の見出しが壊れていないことを確認**する。**★カードが0件の画面(`?fixture=kusatsu&simulate=empty`)も確かめ、凡例ごと出ないこと**(`check-distance.mjs` の既存軸)を目視する。
  9. **★順位不変の実証**: `node scripts\dump-rank.mjs <エリア>` を変更前後・**5エリア個別**で走らせ、**出力がバイト一致**することを示す(**★`dump-rank.mjs` は引数必須です。引数なしだと使い方を出すだけで空になります**。**2026-09-24 計画役の実測値**: kusatsu `a4e3a1d0ad784b93c9f92f2a1f8604c4` / hakone `c1d505b7b6729f961ee2a5c32782f4d0` / dogo `3137fde0a152003bc761517428d2a638` / beppu `be5958e8454d1abecd8a4ee8d19f86be` / kinosaki `8c7df397f0f4f60f4e9ad1b4df8a16df`。**鵜呑みにせず自分で取り直すこと**)。`git diff assets/geo.js assets/engine.js fixtures/` が**空**であること。
  10. **`assets\app.js` 以外の `assets\` を一時的に書き換えたら md5 で完全復元を確認**(**基準値は着手時に自分で `md5sum` を取ること**。下は 2026-09-24 計画役の実測で参考値)。`assets\style.css` / `assets\ui.css` / `assets\tokens.css` / `assets\engine.js` / `assets\geo.js` は**着手時と完了時で md5 一致**であること。
  11. `node scripts\check-all.mjs` が **35本中35本PASS または FLAKY(exit 0)**。**本数は35本のまま**(**新規ファイルを作らない**)。
  12. **★FLAKY の本数を必ず NIGHTLOG に記録**(**R254 までで9サイクル連続0本**)。
- **検証手順**:
  - 撮影URL: `?fixture=kusatsu` / `?fixture=hakone` / `?fixture=dogo` / `?fixture=beppu` / `?fixture=kinosaki`、および `?fixture=kusatsu&simulate=empty`。画面幅は **mobile(375px)と desktop(1280px)**。
  - `node docs\check.mjs` 単体で OK/NG と秒数を記録(変更前後。**R254 時点は 94 OK / 0 NG**)。
  - `node scripts\check-distance.mjs` と `node scripts\check-fame.mjs` を**変更後に単体で**走らせ、**はみ出し軸が緑のまま**であることを確認(4行目で凡例が伸びるため)。
  - `node scripts\dump-rank.mjs <エリア>` を変更前後・5エリアで取り、**バイト一致**を示す。
  - `node scripts\check-all.mjs` の通しを1回(秒数と FLAKY 本数を記録。**R254 は 380.0s**)。
  - **着手時の md5 参考値**(2026-09-24 計画役の実測。**必ず自分で取り直すこと**): `assets\app.js` = `ff428698bccd16e82b2b3f6c2d4c0082` / `assets\style.css` = `9da5e11ad2550a6181b02c7f0e176583` / `assets\ui.css` = `c3db293066104848b317b7d04798638d` / `assets\engine.js` = `9c89f4a949651629f36bda4236e9f4a3` / `assets\geo.js` = `8dd8c3bb205542717b23404c35baa500` / `assets\tokens.css` = `50662697a27e389cc5f1bad50105f35d`。
- **変更禁止範囲(絶対に触らない)**: **`assets\geo.js` / `assets\engine.js` / `assets\style.css` / `assets\ui.css` / `assets\tokens.css`** / **`fixtures\*.json`(読むだけ・再生成しない)** / `index.html` / `404.html` / `README.md` / **`demo\*.html`** / `.github\workflows\*` / `docs\AUTOPILOT.md` / `docs\FIXTURES.md` / `docs\CHECKS.md` / `scripts\dump-rank.mjs` / **`scripts\check-*.mjs` の全34本と `check-all.mjs`(読むだけ)** / **`docs\check.mjs` の既存部分(R235・R248・R249・R250・R254 ブロックを含む。今回足すのは `// --- ここまで R254 ---` より後ろの新ブロックだけ)** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記のみ許可) / **ROADMAP の既存起票すべて**(**R255 の新規起票+完了マークだけが例外**)。
  - **★計画役が確認済み**: `#feed-origin` を見ている既存検査3本(`check-distance` / `check-fame` / `check-feednote`)は**行数を数えていない**ため、**4行目の追加で向きを裏返す必要はありません**。**それでも矛盾を見つけたら報告した上で最小限の変更で進めてよい**(R228・R254 では `check-bundle.mjs` が実際にこれに当たりました)。**★ファイルの場所は自分で grep して確かめてから書くこと**。**★行番号でコードを指さないこと**(今夜、行番号引用は30件以上すべてが外れていました。**関数名やコードの断片で指すこと**)。
- **所要目安**: 30〜40分(場所の特定と実測4分・4行目の追加5分・5エリア+0件画面の撮影と目視10分・検査の軸追加と発火確認8分・破壊実証の旧版素通り確認4分・dump-rank と md5 3分・35本通し7分・記録とコミット5分)。

### 積み残し
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。**R248 で文書側は訂正済み。「呼ぶかどうか」は順位とリクエスト数に影響するため判断待ちのまま**です。
- **R244(判断が要ります)**: `fixtures\kusatsu.json` の `backlinks` が **28件**しかなく、他エリア(箱根73・道後70・別府49・城崎41)の半分以下。**初期5枚に 60〜149 の帯にいるカードが1枚も無いため、しきい値を下げない限りどんな条件でも増えません**。**fixture 再生成は順位を動かしうる**ため判断が要る。
- **R243 は「やらない」と R249 で決着済み**。**R217 は見送り済み**。**R233 は残件0件で完了**。**R251・R252 は R254 で前提が覆り `[-]` で閉鎖済み**。
- 判断不要で残っているもの: **R211**(dump-rank に日時とコミットID)、**R102**(list-shots.mjs)。**どちらも開発者用ツールで画面に出ません**。
- **★R255 からの申し送り(次の計画役へ)**: **R254 の申し送り「同じ画面の中で流儀が揃っていない箇所」が今回も当たりました**。今回見つけたのは**「説明のある数字と、説明の無い数字が同じ1行に隣り合っている」**という形です。**次も同じ探し方が使えます: 画面に出ている数字・記号・色を1つずつ挙げ、「これは画面のどこで説明されているか」を DOM で確かめること**。**まだ説明の無いものの候補**: カード左上の**番号バッジ(1〜5)**が何の順番なのか、**カテゴリの絵文字**(🏛 など)が何を表すのか、**`もっと遠く（車1時間以上）`** の「1時間」という数字(ただし `?demo=far` でしか出ないため画面に出る頻度は低い)。**★ただし起票の本文を信じてはいけません。必ず自分で画面とコードを測ってから選ぶこと**(R251・R252 は2件とも前提が覆りました)。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★表示を変えると、その表示を前提にした検査が「直しそのものを禁止する検査」になることがある**(R254 の教訓)。`check-bundle.mjs` の「見出しが名乗る件数と実数が一致」は、件数を外した瞬間に**5エリアでFAIL**し、しかも緑にしても `claimed` が常に0で成立しえない**死んだ照合**になりました(照合の向きを裏返して2本に割って解決)。**表示を変えるタスクでは、その表示を見ている既存の検査を先に洗い、向きを裏返す必要がないか確かめること。変更禁止範囲に入れてしまうと作業役が身動きできなくなります。**
- **★「実在する側から母数を取る」を実装するとき、タイミングも実在のうち**(R250 の教訓)。凡例は `renderFeed()` の中で `renderFeedMap()` より**先**に組まれていたため、そのまま DOM を数えると**1つ前の描画の地図**を見ていました(組み立てを `renderFeedOrigin()` に切り出し、地図の**後**に呼んで解決)。**DOM から母数を取る軸を書くときは、その時点で DOM が最新かを必ず確かめること。**
- **★司令塔が推す壊し方は8サイクル連続で旧版に捕まっている。着手前に必ず旧版で素通りするか実測し、捕まるなら自分で作り直すこと。**
- **★司令塔の指示書そのものにも誤りがある(今夜9回目)**(R254 で `dump-rank.mjs` の「引数なしの通し」ハッシュが実際には**引数必須で空出力**だったことが判明)。**実装に必要なファイルが変更禁止範囲に入っていたら報告した上で最小限の変更で進めてよい**(R228)。
- **★「軸を足した」と書くだけでは不十分。壊して実際に発火するところまで見ること**(R240 の教訓)。作業役が自分で足した母数軸が**死んだ軸**でした(`exp.all.length` が常に104で、壊しても発火しない)。**母数は必ず画面(DOM)か実ファイルから数えること**。
- **★表示側で動かせる限界を見極めること**(R241)。城崎・箱根の初期5枚は **60〜149 の帯にカードが1枚も無い**ため、表示条件をどういじっても増えません。**基準を下げて枚数を増やすのは「人気の基準を甘くする」行為で禁止**。
- **★検査の高速化は R240 で一区切り**(616.5s → **361.1s**)。**みのるんが朝起きて見るのは画面です**。画面側か設計思想に関わるものを選ぶこと。
- **★削減量は A/B 実測で確かめる**(R239)。**★速さのために検査を弱めない**(`force: true` は採らない)。**★全件確認が要るときは機械的に全件突合する**(R233-3。「前後数件」では**63%の破損を見逃しました**)。
- **★検査の軸が1つだと、その軸ごと壊す変更は捕まえられない**(R226)。**本数の下限と中身の照合を別項目で持つ**。**R216・R234・R238・R241・R254 では有無軸が全緑のまま中身軸だけが壊れを検知しました**。
- **★「作らない判断」も成果**(R232。**常に赤い検査は緑の検査より危険**)。
- **★申し送りの「実測しました」も再現を確かめる**(R231)。**★数え方の定義を実装と一致させる**(R226)。**★件数を引き継ぐときは実物を開いて形を確かめる**(R216)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230)。**司令塔が渡した数字やコマンドも鵜呑みにしない**。**★起票の本文の「現状の説明」も実測で確かめる**(R228・R237・**R251・R252** では起票文の前提自体が外れました)。**★変更の影響は全分岐を列挙してから数える**(R227)。**★新機能を入れたサイクルで、それを守る検査も入れる**(R229・R216)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。**R223 で本物のフレークが1本出た**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE`)。**この環境ではフレークが1回の通しで1本出る頻度**。**FLAKY が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231・R226)。**★数字も同じ。数え方を名乗れない数字は出さない**(R254)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224・R226・R234・R242・**R254** はこの方法で見つかった)。
- **「動いている前提で書かれた文書」を疑う**(R225)。**同様に「検査があるから守られている」も疑う**(R234・R235)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に10件)。
- **検査を疑うこと自体が仕事になる**(R214)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**(R254 の `check-bundle.mjs` の赤は、直しが正しかったことの証拠でした)。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218)。**新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232・R226・R234・R241・R254 はいずれも新規ファイル0件)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232)。基準値は**毎サイクル自分で `md5sum` を取り直すこと**。**ピンのCSSは `assets\style.css` にあります**(`ui.css` は該当0件)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。**本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

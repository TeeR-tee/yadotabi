# NEXT: 2026-09-23 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、**先頭に目次が付いた**ので「Rいくつの話がどこにあるか」は探せるようになりました。R220 完了)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**(R85・R207・R208)+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の案内**(R215)+**検査で保護**(R221) |
| サイトアイコン | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214) |
| 検査がコメントを本物と誤判定 | 4か所が素通り | **解消**(R222・「壊しても緑のまま」を先に実証) |
| 検査がときどき理由なく赤くなる | 1回落ちたら赤が確定 | **PASS / FLAKY / FAIL の3値+自動1回再実行**(R223) |
| NIGHTLOG(2100行超) | 朝に読めない | **先頭に目次**(R220) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224・順位は5エリアで差分なしを実証) |
| 人気バッジを守る検査 | 0本 | **`check-fame.mjs` を新設(R229)・4通りの壊し方で赤を実証**・検査は**35本**に |
| README の本数・fixture表・因果の説明 | 実態とズレ(7本違い・表は全セル誤り) | **実測で是正(R230)**・R219 も同時に消化 |
| 「徒歩1分・75m」が何からの距離か | 画面に一度も書かれていない | **地図とカードの間に `距離は「(宿名)」からの目安です`**(R228・順位は5エリアで差分なし・本番反映済み) |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。



## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R227** — 1位の理由行が、**その土地の代表でも「歩いて行ける」で終わる**

**なぜ R227 なのか(数字はすべて今回の計画役が実際に engine を動かして測ったものです)。**

未着手は R216(404)・R217・R211・R102・R226・R227。5エリア全25枚の理由行を、現在の優先順と入れ替えた場合の両方で実際に出力して比べました。

| 候補 | 実測して分かったこと | 判断 |
|---|---|---|
| **R227** | 上位5枚のうち「歩いて行ける」が **草津5/5・城崎4/5・箱根2/5・道後2/5・別府1/5 = 25枚中14枚(56%)**。優先順を入れ替えると **5エリアで4枚が別の文言に変わる**(草津2位 草津熱帯圏→「代表的な動物園」/箱根2位 早雲寺→「代表的な神社・寺院」/道後5位 子規記念博物館→「代表的な美術館・博物館」/別府3位 別府市美術館→「代表的な美術館・博物館」)。**表示のみで順位は1件も動かない** | **採用** |
| R226 | 起票どおり more 側は情報の薄いカードが並ぶが、**R227 で理由行の情報量が上がった後のほうが差が測りやすい** | R227 の後 |
| R216 | `ls 404.html` = 存在しないのは事実。新規ファイル1枚+検査追加で今回より重い | 次点 |
| R211/R102 | どちらも開発者向けの道具の改善。利用者の画面は1文字も変わらない | 後回し |
| R217 | 前サイクルで見送り決定。**ただし ROADMAP 273行目を確認したところ、見送りの理由が1文字も書かれていません**(まだ `[ ]` のまま) | **今回ついでに理由を追記する** |

**★この起票には誤りが1つ混ざっており、それを訂正した上で採用します。**

起票は「**湯畑は草津の代表そのものだが `representativeReason` が後ろにあるため出ない**」と書いています。**これは誤りです。** 実測すると湯畑は `_debug.backlinks` が **`null`**(Wikipedia記事に紐づいていない OSM 単独のカード)で、`REASON_BACKLINKS_MIN = 30` の判定に**そもそも到達しません**。つまり**優先順を入れ替えても湯畑は「歩いて行ける」のまま**です。

**それでも R227 の指摘そのものは生きています。** 変わるのは上表の4枚で、いずれも「歩いて行ける」より**その場所が何者かを伝える文言**になります。**「1位が変わる」のではなく「25枚中4枚の説明が濃くなる」というのが正しい効果の大きさ**です。作業役はこの4枚を期待値として持ち、**それ以外が変わったら実装を疑うこと**。

**2方向から確かめたこと**(「◯件も無い」を1回の grep で決めない、の教訓)。
- 方向A: `app.js` の `reasonText()` が `walkableReason → representativeReason → rareReason` の順で最初に当たったものを返すことをコードで確認。`walkableReason` の条件は距離800m以下のみ。
- 方向B: 5エリアで `YadoEngine.collect/rank/present` を実際に走らせ、25枚の `now` と `swapped` を1枚ずつ出力して差分4枚を特定(湯畑が変わらないことも同時に判明)。

- **タスクID**: R227
- **目的**: 💡理由行の優先順を `representativeReason → walkableReason → rareReason` に入れ替え、代表格の場所に「歩いて行ける」より情報量のある文言を出す。**順位・並び順・距離は1バイトも変えない**(表示のみ)。
- **変更するファイル(絶対パス・この5件のみ)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\assets\app.js`(`reasonText()` の**呼び出し順だけ**。各 Reason 関数の中身・しきい値 `REASON_BACKLINKS_MIN` は変えない)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-reason.mjs`(守る検査の追記。**新規ファイルは作らない**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R227 を `[x] 2026-09-23` に・**273行目の R217 に「見送り: embed-check はローカル開発用ページでチャットに貼る想定が無いため。起票の前提が誤り」を追記**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-23 R227 …` 見出し+3行・**先頭の目次にも1行**)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\09_研究ノート.md`(理由行の優先順を書いた箇所があれば同じサイクルで直す。**無ければ触らない**)
- **完了条件(検証可能な形)**:
  1. **before/after を5エリア全件で出す**: 実装前に5エリアの上位5枚の理由行を**ファイルに保存**し、実装後と `diff` を取る。**差分が上表の4枚ちょうど**であること(草津2位・箱根2位・道後5位・別府3位)。**それ以外が1枚でも変わったら実装が間違っている**。湯畑が「歩いて行ける」のままであることも確認する。
  2. **理由行が消えたカードが0枚**: 入れ替えで `null` になるカードが出ていないこと(25枚のうち理由行を持つ枚数が before と同じ)。
  3. **★守る検査を同じサイクルで足す**(R229 の教訓)。**既存の `scripts/check-reason.mjs` に項目を追記**する(この検査は既に理由行を担当している)。追記する項目は、**「上位5枚のうち『歩いて行ける』だけが並ぶエリアが草津1つだけであること」ではなく、`representativeReason` が当たる条件を満たすカード(backlinks が `REASON_BACKLINKS_MIN` 以上かつ同カテゴリ内で最大)には必ず『代表的な』が出ていること**。**検査本数は 35本のまま**なので本数を書いた文書の更新は不要。
  4. **★「壊れた状態で落ちること」を実証する**(R218・R222・R229 の手順)。**最低3通り**わざと壊して `node scripts/check-reason.mjs` が**赤くなること**を確かめ、結果を NIGHTLOG に残す。**枚数だけ数える検査では素通りする壊れ方を必ず1つ入れること**(R229 の実例)。例: (a) 優先順を元に戻す、(b) `representativeReason` を常に `null` を返すようにする、(c) **文言を「このあたりの代表的な」から別の文字列に変える**(枚数は変わらないので数える検査では通ってしまう)。**実証後は `git diff` が空であること**を確認してから本実装に戻る(R229・R228 が実践した「完全に元通り」の確認)。
  5. **順位が変わっていない**: `node scripts/dump-rank.mjs` を5エリア分、変更前に保存し、変更後と **`diff` でバイト一致**すること。
  6. `node scripts/check-all.mjs` が **35本中35本 PASS または FLAKY(exit 0)**。**FLAKY の本数を必ず NIGHTLOG に記録**(R223 の申し送り・直近は FLAKY 0本・35本388.6s)。
- **検証手順(撮影)**: **撮影は必須**(理由行の文字数が変わり折り返しが起きうるため)。`node C:\workspace\tools\shot\shot.mjs <URL> --mobile` と PC幅。
  - 撮影URL①: `http://127.0.0.1:<port>/index.html?fixture=beppu`(**mobile 375px と desktop の2枚**)— 3位「別府市美術館」の理由行が「このあたりの代表的な美術館・博物館」になり、**1行に収まるか・カードからはみ出さないか**を目視。5エリアで最も長い文言になる。
  - 撮影URL②: `http://127.0.0.1:<port>/index.html?fixture=kusatsu`(**mobile 1枚**)— 2位「草津熱帯圏」の変化と、1位「湯畑」が**変わっていない**ことを目視。
  - **撮影は全て固定データモードで行い、外部APIは0回**(AUTOPILOT 絶対ルール4)。**画像は必ず Read で開いて目視する**(緑の検査だけで済ませない)。
- **変更禁止範囲(絶対に触らない)**: **`assets/geo.js` / `assets/engine.js` / `WEIGHT` 定数(順位ロジック)** / `engine.js` の `reasonFor()` / `app.js` の距離計算・並び替え・`walkMin`/`carMin`・`fameText` / `REASON_BACKLINKS_MIN` の値 / `index.html` / `assets/style.css`(**文言が1行に収まらなかった場合のみ、その旨を報告した上で最小限の調整を可とする**) / `fixtures/*.json`(**読むだけ・再生成しない**) / `demo/*.html` / `404.html`(作らない) / `.github/workflows/*` / `docs/AUTOPILOT.md` / `docs/CHECKS.md`(本数が変わらないため) / **`check-reason.mjs` 以外の検査34本** / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記と目次1行のみ許可)。
- **所要目安**: 30〜40分(before/after の採取5分・入れ替え実装5分・検査の追記10分・3通りの破壊実証8分・dump-rank 突合3分・撮影と目視5分・35本通し7分・記録とコミット8分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- 判断不要で残っているもの: **R226(more側が全件同じ見た目)→ R216(404・検査追加とセット)** の順を推奨。ほかに R211・R102。**R217 は見送り**(起票の前提が誤り・上表の理由。今回 ROADMAP に理由を追記する)。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★もっともらしい因果ほど、書く前に測ると外れる**(R230 の教訓。作業役が書きかけた説明3つが実測で全て否定された)。**司令塔が渡した数字やコマンドも鵜呑みにしない**(R230・R228 と2回続けて司令塔の実測コマンドのキー名が誤っていた。正しくは `overpass.elements`)。
- **★司令塔の指示書そのものにも誤りがある**(R228 の教訓。**実装に必要なファイルが変更禁止範囲に入っていたら、その矛盾を報告した上で最小限の変更で進めてよい**。R228 では凡例の置き場所 `index.html` が禁止範囲に入っており、作業役は1行追加のみで正しく対応した)。
- **★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228 の教訓。「宿名はヘッダに無い」という起票の記述が誤りで、実際は `index.html:69` に存在した。**起票が正しいのは「問題があること」までで、その理由づけは別**)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229 の教訓。R224 は表示を入れたのに守る検査が0本のまま1サイクル空いた。**司令塔の指示要約が元の完了条件を落とすと、作業役は落ちた条件を復元できない**。要約は元文書の完了条件の件数と突き合わせること)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。R223 の仕上げ実行で**本物のフレークが1本出ました**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE` で1回目に落ち、再実行で通過)。**この環境ではフレークが1回の通しで1本出る頻度**です。**FLAKY の本数が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224 の教訓。**起票は、起票した時点で気付いていたことしか含みません**)。
- **「動いている前提で書かれた文書」を疑う**(R225 の教訓。**export されている=使われている、ではありません**)。
- **起票の中に書かれた「こうすれば簡単」も疑う**(R220 の教訓)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる。
- **「今回だけの偶然」と片付ける前に、過去に何回起きたかを数える**(R223 の教訓)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立し、R222 で実証まで進めた手順)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220 はいずれも新規ファイル0件)。
- **自動でうまく処理する仕組みは「問題を隠す方向」に働かないか考える**(R223 の教訓)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。

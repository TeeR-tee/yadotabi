# NEXT: 2026-09-24 夜間サイクルの結果と、次にやること

みのるんへ。まずここだけ読めばOKです。
(詳しい経緯は `docs/NIGHTLOG.md` にありますが、**先頭に目次が付いた**ので「Rいくつの話がどこにあるか」は探せるようになりました。R220 完了)

## 1. 今日までに変わったこと(本番で確認済みのみ)

| 項目 | 変更前 | 変更後 |
|---|---|---|
| 草津の1位 | 草津熱帯圏(742m先の動物園) | **湯畑(徒歩4分)** |
| 別府の1位 | 別府市美術館 | **竹瓦温泉** |
| 最初のカードが出るまで | 22秒前後 | **1.9〜4.5秒** |
| 全国での親記事の解決率 | 6% | **67%** |
| 検査の自動化 | ローカルのみ | **GitHub Actions で全PASS**(R64)・**35本** |
| 営業用デモ | 日本語1枚のみ | **英語版+README入口+OGカード**(R85・R207・R208)+**専用検査**(R218) |
| JSが止まったときの画面 | 「🧳」の2文字 = 実質まっ白 | **230文字の案内**(R215)+**検査で保護**(R221) |
| サイトアイコン | 4ページすべて白紙 | **4ページに設置+検査で保護**(R214) |
| 検査がコメントを本物と誤判定 | 4か所が素通り | **解消**(R222・「壊しても緑のまま」を先に実証) |
| 検査がときどき理由なく赤くなる | 1回落ちたら赤が確定 | **PASS / FLAKY / FAIL の3値+自動1回再実行**(R223) |
| NIGHTLOG(2100行超) | 朝に読めない | **先頭に目次**(R220) |
| 「どれだけ人気か」の表示 | 画面に1文字も無し | **`★ 全国的によく知られた場所` / `★ この地方でよく知られた場所`**(R224) |
| 人気バッジを守る検査 | 0本 | **`check-fame.mjs` を新設(R229)**・検査は**35本**に |
| README の本数・fixture表 | 実態とズレ(7本違い・表は全セル誤り) | **実測で是正(R230)**・R219 も同時に消化 |
| 「徒歩1分・75m」が何からの距離か | 画面に一度も書かれていない | **`距離は「(宿名)」からの目安です`**(R228・本番反映済み) |
| 検査が「空集合を検査して常に緑」 | 未点検 | **35本を2方向で全数点検(R232)**・文字列の空振りは**0件**・**母数を見ていない判定28件**を発見し3件(+派生2項目)を修正 |

## 2. ★みのるんの判断を待っている2件

### (1) 箱根の問題設定そのものが間違っていました(市場調査役 第16回で判明)

**これまで「小田原市の施設が混ざっている」ことを問題にしていましたが、この見方自体が誤りでした。**

現在の1位「神奈川県立生命の星・地球博物館」は小田原市にありますが、最寄り駅は箱根湯本から登山電車で1駅の「入生田駅」で、**箱根町の公式観光サイト自身がこの施設に「#箱根湯本」タグを付けています**。つまり「小田原市だから箱根じゃない」という批判は的が外れていました。

**本当の問題は別にあります。** 箱根に泊まった人が選ぶ観光地ランキング(小田急トラベル調べ・やどたびと同じ「宿泊者」が対象の調査)と、やどたびの現状を比べると、上位の顔ぶれがまったく違います。

| 順位 | 宿泊者ランキング(実際に人気の場所) | やどたびの現状 |
|---|---|---|
| 1位 | 大涌谷 | 生命の星・地球博物館(8位まで下げると出てくる) |
| 2位 | **箱根神社** | **圏外(表示22件に0件)** |
| 3位 | 彫刻の森美術館 | 4位 |
| 4位 | **芦ノ湖** | **圏外(表示22件に0件)** |

つまり、箱根に泊まった人が本当に行きたい場所トップ4のうち、2つ(箱根神社・芦ノ湖)がそもそも画面に出てこない状態です。

**原因は箱根という土地の構造にあります。** 草津・別府・道後は「泊まる場所=見る場所」ですが、箱根湯本は箱根町公式サイト自身が「玄関口・交通の拠点」と説明しており、目的地そのものではありません。観光のモデルコースはどれも「湯本に泊まって、大涌谷や芦ノ湖まで足を延ばす」という組み方です。つまり**やどたびの「宿から近い順」という仕組みが、箱根では機能しないタイプの土地**だということです。

**★みのるんに判断してほしいこと:**
「その土地は歩いて回れる土地か、それとも広い範囲を移動して回る土地か」を見分ける仕組みを、やどたびに追加するかどうか。これは**やどたびの設計の考え方そのものに関わる変更**なので、司令塔(みのるん)の判断が必要です。同じ構造を持つ土地は箱根以外にも、鬼怒川・下呂・黒川などが挙げられています。

**追記(市場調査役 第17回・4a8470e7):見分ける方法が見つかりました。**

判定条件は2つで、**両方**当てはまると「広い範囲を移動して回る土地」と判定できます。
1. Wikipediaで、その土地専用の記事が見つからず、代わりに市区町村の記事に行き着いてしまう
2. その市区町村の記事の中に、「温泉街」「外湯」「温泉郷」という言葉が1回も出てこない

| 土地 | 専用記事が見つからない | 「温泉街」等の言葉の回数 | 判定 |
|---|---|---|---|
| 草津 | いいえ | 24回 | 歩いて回れる土地 |
| 箱根 | **はい** | **0回** | **広い範囲を移動して回る土地** |
| 道後 | いいえ | 8回 | 歩いて回れる土地 |
| 別府 | いいえ | 21回 | 歩いて回れる土地 |
| 城崎 | いいえ | 28回 | 歩いて回れる土地 |

箱根だけが2つの条件を両方満たし、他の4つとは回数が一度も重なりませんでした(今日の草津・別府の改善結果は壊れません)。司令塔が最初に考えた3つの見分け方(「玄関口」の語/交通機関の記事の多さ/距離の分布)は**すべて上記5エリアでは通用しない**ことが分かっています。**この方法の限界**: 鬼怒川は誤判定しますが、誤判定しても現状維持になるだけで実害はありません。温泉地以外には使えません。

### (2) Wikipediaへのリクエスト数をどう見るか

宿1件あたりの外部リクエストは実測で**18〜26回**(ふなや26回・箱根23回)。**429(レート制限)は0件**で、規約違反や実害は今のところ出ていません。Wikipediaへの逐次リクエストが待ち時間の大半を占めていますが、**直列化自体はWikipedia側のマナーで明記されたやり方**なので、むやみに並列化して速度を上げることは規約違反になります。

判断ポイント: **このリクエスト数のまま様子見でよいか、それとも上限を設けて減らす対処を次サイクルで検討すべきか**。実害が出ていないため急ぎではありません。

## 3. ★次のサイクルでやる1件(計画役が選定・判断待ちに依存しません)

### 今回の1件: **R233** — **「母数が0でも緑になる判定」の残り25件のうち、影響の大きい10件を直す**

**なぜ**: R232 で 35本を点検し、「ここに1枚も無ければ何も検査していないのと同じ」判定が **28件**見つかりました。直したのは3件で、**残り25件**が R233 として起票済みです。ROADMAP:305 の推奨に従い、**`check-bundle.mjs`(6件)と `check-attrib.mjs`(4件)の計10件**を今回で消化します(残りは15件)。

**計画役が実際に開いて確かめた現状(作業役はこれを鵜呑みにせず自分で読み直すこと)**:
- `scripts/check-attrib.mjs`: 4件はすべて **`info.pinRects` を母集団にした `filter(...).length === 0`**(`:130` 重なり / `:134` 地図内 / `:160` 帰属と重なる / `:173` 地図内)。**同ファイル内に `pinRects.length > 0` を確かめる項目は1つも無い** → **地図にピンが1本も描かれなくなっても4項目とも緑**。`:117` の `ok(count > 0, …)` はカード枚数の母数で、ピンの母数ではない(別物)。
- `scripts/check-bundle.mjs`: 6件は `heads`(束見出し)と `cards` を母集団にする `:240 thin` / `:278 mismatched` / `:280 countBad` / `:286 idx` / `:292 restWithNo` / `:296 noIdxBad`。**`cards.length > 0` を確かめる項目が無い**(`:235` の `heads.length >= 1` は見出しの母数のみ)→ **展開後にカードが1枚も無くても6項目中5件が緑**。

- **タスクID**: R233 / **目的**: 上記10件の**母数そのものを検査項目に格上げ**する(R232 と同じ手法)。**順位・表示・文言は1バイトも変えない**(検査側だけの作業)。
- **変更するファイル(絶対パス)**:
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-bundle.mjs`
  - `C:\workspace\claude\旅行先用サイト\yadotabi\scripts\check-attrib.mjs`
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\ROADMAP.md`(R233 の**残り件数を25→15に更新**し、消化済みの内訳2ファイルを消す。全部消えないので `[x]` にはしない)
  - `C:\workspace\claude\旅行先用サイト\yadotabi\docs\NIGHTLOG.md`(末尾「## サイクル記録」に `### 2026-09-22 R233 …` 見出し+3行以上・**先頭の目次にも1行**)
  - **壊して実証するための一時変更**: `assets\app.js` / `assets\ui.css`(**完了条件5の md5 突合を必須とする**)
- **完了条件(検証可能な形)**:
  1. **10件すべてについて母数の項目を足したこと**を、項目ラベルの一覧で NIGHTLOG に示す。しきい値は**推測せず `?fixture=kusatsu`・375x812 の実測値から決め、その実測値も書く**(fixture の微増減で赤くならない値まで下げること。R232 の `LINK_CHIP_MIN = 5` が手本)。**ピンの本数とカードの枚数は別の母数なので別項目にする**。
  2. **★わざと壊して落ちることを実証**し、**直す前は同じ壊し方で素通りしていた**ことを **pass/fail の数字で対比**して示す(旧版は `git show HEAD:scripts/check-attrib.mjs` で取り出す。`git checkout` は使わない)。**2通り以上**行い、うち**最低1つは「数字だけ見る検査では素通りする壊し方」**にすること。候補(そのまま使わず自分で選んでよい):
     - **壊し方A(母数を消す)**: ピンの描画を止める、または展開後のカードを0枚にする。→ 直す前は緑のまま、が実証の核。
     - **★壊し方B(数字は変わらない壊し方)**: 例えば**ピンの本数は変えずに位置だけを地図の外へ出さない微小なずらし**や、**束見出しのテーマ名だけを別の名前に書き換える**(件数も枚数も1つも変わらない)。R232 の「id を改名するだけ」が好例。
  3. `assets\` を一時的に書き換えた場合、**md5 を実証の前後で突き合わせて完全一致**すること、**`git diff --numstat assets/` が空**であることを示す(手で書き戻す。`git checkout` / `stash` / `reset` は不使用)。
  4. `node scripts/check-all.mjs` が **35本中35本 PASS または FLAKY(exit 0)**。**検査の本数は35本のまま**(既存ファイルへの項目追加のみなので `docs/CHECKS.md` の本数は触らない。もし本数を増やす判断をしたなら CHECKS.md の本数を持つ全箇所を同じサイクルで直すこと=R212 の教訓)。
  5. **★FLAKY の本数を必ず NIGHTLOG に記録**(R227〜R232 は**6サイクル連続0本**)。
- **検証手順(撮影)**: **今回は撮影不要**。理由: **画面に出るものを1バイトも変えないタスク**(検査スクリプトと文書のみの変更で、`assets/` への変更は実証用の一時変更を md5 で書き戻す)だから。ただし**検査を直した結果として画面の実際の不具合が見つかった場合は話が別**で、その場合は同サイクルで直さず **ROADMAP に起票して報告**すること(1サイクル=1タスク)。
- **変更禁止範囲(絶対に触らない)**: **`assets\geo.js` / `assets\engine.js` / `WEIGHT` 定数(順位ロジック) / `fixtures\*.json`(読むだけ・再生成しない)** / `index.html` / `assets\style.css` / `demo\*.html` / `.github\workflows\*` / `docs\AUTOPILOT.md` / **NEXT.md の「2. みのるんの判断を待っている2件」と「4. 必ず守ること」**(次の計画役が保持する) / **NIGHTLOG の既存本文**(末尾追記と目次1行のみ許可)。
  - **`assets\app.js` と `assets\ui.css` は「壊して実証するための一時変更のみ可」**(完了条件3の md5 突合が必須)。
  - **★この禁止範囲に矛盾を見つけたら、報告した上で最小限の変更で進めてよい**(R228 の教訓。司令塔の指示書そのものにも誤りがあります)。
- **所要目安**: 30〜40分(2ファイルの読み直しと母集団の特定8分・実測値の採取5分・10件の項目追加10分・破壊実証(2通り+旧版対比)10分・md5 突合2分・35本通し7分・記録とコミット8分)。

### 積み残し(判断待ちが解けてから)
- R177 と「箱根神社が圏外」/ R138(座標欠落40件)— 判断待ち(1)(2)が決まり次第。
- **R225(判断が要ります)**: `geo.js` の `enrichFame` が定義と export だけで、どこからも呼ばれていません(本番でも `fame` は常に空)。`docs/FIXTURES.md:177` は動いている前提。呼ぶとリクエストが増え順位にも影響しうるため判断が要る。判断不要なのは文書の訂正だけ。
- 判断不要で残っているもの: **R233 の残り15件** → **R226(more側が全件同じ見た目)→ R216(404・検査追加とセット)** の順を推奨。ほかに R211・R102。**R217 は見送り済み**(ROADMAP:273 に理由を追記済み)。

## 4. ★次のサイクルで必ず守ること(今日までの教訓)

- **★「作らない判断」も成果**(R232 の教訓。文字列突合の自動検査は**誤検知53%**のため見送った。**常に赤い検査は緑の検査より危険**で、赤が常態化するとやがて赤を見なくなる)。
- **★申し送りに書かれた「実測しました」も再現を確かめる**(R231 の教訓。前サイクルの作業役が実測した「草津で珍しい◯◯が3枚」は、計画役が同じ HEAD で数えると**0枚**だった。観察した時点のコードが違うと数字はそのまま持ち越せない)。
- **★もっともらしい因果ほど、書く前に測ると外れる**(R230 の教訓。作業役が書きかけた説明3つが実測で全て否定された)。**司令塔が渡した数字やコマンドも鵜呑みにしない**(R230・R228 と2回続けて司令塔の実測コマンドのキー名が誤っていた。正しくは `overpass.elements`)。
- **★司令塔の指示書そのものにも誤りがある**(R228 の教訓。**実装に必要なファイルが変更禁止範囲に入っていたら、その矛盾を報告した上で最小限の変更で進めてよい**)。
- **★起票の本文に書かれた「現状の説明」も実測で確かめる**(R228 の教訓。**起票が正しいのは「問題があること」までで、その理由づけは別**)。
- **★変更の影響は、関係する全ての分岐を列挙してから数える**(R227 の数え漏れ)。
- **★新機能を入れたサイクルで、それを守る検査も入れること**(R229 の教訓。**司令塔の指示要約が元の完了条件を落とすと、作業役は落ちた条件を復元できない**。要約は元文書の完了条件の件数と突き合わせること)。
- **★FLAKY の本数を毎サイクル NIGHTLOG に記録し、推移を見ること**(R223 の申し送り)。R223 の仕上げ実行で**本物のフレークが1本出ました**(`check-chipcurrent` が `ERR_NO_BUFFER_SPACE` で1回目に落ち、再実行で通過)。**この環境ではフレークが1回の通しで1本出る頻度**です。**FLAKY の本数が増えていくようなら、再実行で隠しているだけで本物の不具合が育っている可能性があります。**
- **表示の文言に評価の語を使うと、対象によっては嘘になる**(R227・R231 の教訓)。
- **バックログが小粒になったら、本番アプリを実際に見て設計思想と突き合わせる**(R224 の教訓。**起票は、起票した時点で気付いていたことしか含みません**)。
- **「動いている前提で書かれた文書」を疑う**(R225 の教訓。**export されている=使われている、ではありません**)。
- **「◯件も無い」は grep 1回で決めない**。必ず**2方向以上**から確かめる(前提が誤っていた実例が過去に6件)。
- **検査を疑うこと自体が仕事になる**(R214 の教訓)。緑は「正しい」ではなく「その検査が赤くならなかった」だけ。**そして赤も、赤とは限りません**。
- **新しい検査・検査の変更は「わざと壊して赤くなること」まで確認する**(R218 で確立し、R222・R232 で実証まで進めた手順)。**数字だけ見る検査では素通りする壊れ方がある**(R229・R232)。
- **本数・一覧を持つ文書は、対象を増減した同じサイクルで必ず直す**(R212 の教訓)。
- **新しいものを作る前に、ファイルを増やさずに済む方法を先に探す**(R214・R223・R220・R232 はいずれも新規ファイル0件)。
- **本番配信ファイルを一時的に書き換えたら md5 で完全復元を確認する**(R232 で確立)。
- **固定データでは検証できない領域がある**(速度・APIの失敗・住所の解析・宿名に依存する処理)。大きな変更のあとは本番で実際に宿を選んで確認する。
- **本番確認は必ず実ブラウザで**(curl や素の fetch は Overpass に406で弾かれ誤診断になる)。**`geo.js` は10分間キャッシュされる**。
- **数字を報告するときは必ず測定条件を添える**(どのエリアか/固定データか本番か/初見かキャッシュありか)。
